import { defineConfig } from 'vitest/config';

/**
 * Тесты конвейера агентской разработки.
 *
 * Отдельным корнем, а не проектом внутри общего прогона, ровно по той же
 * причине, что и `scripts/`: в корне репозитория лежит `vitest.workspace.ts`,
 * и Vitest подхватывает его раньше любого `--config`. Прогон, запущенный
 * отсюда (`vitest run --root plugins/pipeline`), рабочего пространства
 * над собой не видит и гоняет ровно эти файлы.
 *
 * Пакетом `plugins/pipeline` не является намеренно. Плагин обязан
 * переноситься в другой проект копированием каталога, а запись
 * в `pnpm-workspace.yaml` привязала бы его к этому репозиторию и заодно
 * потребовала бы правки файла блокировки — то есть установки на каждой
 * машине ради трёх файлов без единой зависимости.
 *
 * Проверяется только счёт: годность записи бэклога, таблица переходов,
 * решения сканера. Ничего из того, что заводит деревья, порождает сессии
 * и ходит в сеть, здесь не проверяется: это работа живого конвейера,
 * а не быстрого набора.
 */
export default defineConfig({
  test: {
    name: 'pipeline',
    environment: 'node',
    include: ['**/*.test.mjs'],
  },
});
