## 1. Доставить контрольный профиль

- [x] 1.1 Добавить и зарегистрировать `ADVANCE_CONTROL_PROFILE` (`advance-control-2026-08`) с `movement.advanceFraction = 2`, глубоким замораживанием и комментарием о двух эффектах порога; добавить быстрый `advance-control.test.ts` с проверками точного отличия от baseline, прежнего default, регистрации, неизменяемости, границы рубежей, переключения режима обеих сторон и положительного контроля baseline на рубеже 0,75 по design. Оценка: до 2 часов, один самостоятельно проверяемый коммит.

  Разрешённые рабочие пути: `packages/ai/src/profile.ts`, `packages/ai/src/advance-control.test.ts` и обязательная отметка выполнения этого пункта в `openspec/changes/add-advance-threshold-control/tasks.md`. Алгоритмы ИИ, sim, эталоны, workflow и чужие профили не править. Определение и регистрация едут вместе с тестом, поэтому этот же первый шаг достаточен для открытия чернового PR после отправки.

  Проверки в назначенном рабочем дереве, отдельными вызовами:

  ```powershell
  npx vitest run packages/ai/src/advance-control.test.ts
  npx eslint packages/ai/src/profile.ts packages/ai/src/advance-control.test.ts
  npx prettier --check packages/ai/src/profile.ts packages/ai/src/advance-control.test.ts
  ```

  Наблюдаемый результат: сценарии нового профиля и исключение MODIFIED-дельты покрыты; тест положительного контроля действительно видит команду Breakthrough и `escorting: true` у baseline, в то время как контроль остаётся в Engage и не включает сопровождение. В том же коротком решении контроля при достаточной энергии проверить сохранение пределов: не более одного заказа юнита и одного улучшения. Пять сохранённых сценариев основы проверяются существующими регрессиями ИИ в CI; их не дублировать и не ослаблять. Полную сборку и матчевые регрессии исполняет CI PR; локально их не запускать, эталоны не переснимать. Ожидания CI в этом списке нет.

  После выполнения отметить пункт. Добавить только три перечисленных пути явным списком. В командах ниже `<дерево>` означает назначенный абсолютный путь; подставить его перед выполнением. Проверять и пути, и содержание индекса до коммита, затем содержание созданного коммита:

  ```powershell
  git -C <дерево> add -- packages/ai/src/profile.ts packages/ai/src/advance-control.test.ts openspec/changes/add-advance-threshold-control/tasks.md
  git -C <дерево> diff --cached --name-only
  git -C <дерево> diff --cached
  ```

  В `tasks.md` допустима только отметка выполненного пункта; прочие несвязанные правки в разрешённых файлах также исключены. Создать атомарный коммит по правилам этапа реализации и немедленно отправить:

  ```powershell
  git -C <дерево> push -u origin HEAD
  git -C <дерево> show --format=fuller --stat HEAD
  git -C <дерево> show --format= --patch HEAD
  ```

  Открыть черновой PR штатным порядком этапа implement. Файлы временных проверок и журналы матчей в коммит не включать. Зеркальный прогон отсутствует среди шагов реализации: он уже заказан карточкой 0285 с зависимостью от 0049 и выполняется после появления профиля.
