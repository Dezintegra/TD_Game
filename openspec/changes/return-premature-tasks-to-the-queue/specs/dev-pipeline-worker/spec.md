## ADDED Requirements

### Requirement: Проработка и имплементация возвращают в очередь задачу, чей предмет ещё не появился

Требование заводит **второй случай-исключение** из требования «Проработка
ведётся через OpenSpec» (изменение `agent-backlog-pipeline`, та же
способность), где сказано без единой оговорки: «На этапе `design` сессия SHALL
завести изменение OpenSpec... а имя изменения SHALL записать в отчёт». Первое
исключение — снятый предмет — заводит требование «Проработка объявляет предмет
снятым, а не проектирует пустоту» (изменение
`close-tasks-that-lost-their-point`, та же способность). Здесь заводится
обратный ему случай: предмет жив, но его ещё нет в дереве.

Читать все три текста SHALL так: изменение OpenSpec заводится всегда, **кроме**
задачи, чей предмет проверен делом и оказался либо снятым, либо ещё
не появившимся. Во всём прочем — в составе артефактов, в разделе не-целей,
в слове о влиянии на отзывчивость интерфейса и сетевой трафик, в запрете
приступать к коду — «Проработка ведётся через OpenSpec» действует неизменно.
Третьего перечня обязанностей проработки здесь не заводится.

Перечень исходов сессии при этом **расширяется, а не переписывается**: к пяти
исходам, названным требованиями «Сессия выполняет ровно один переход состояния»
(изменение `supervise-the-pipeline`) и «Исход `moot` закрывает задачу,
потерявшую предмет» (изменение `close-tasks-that-lost-their-point`),
добавляется шестой — `premature`. Нового поля отчёта здесь не заводится вовсе:
доказательство едет в том же поле `evidence`, что и у `moot`. Прочий состав
отчёта, место его возврата и терпимость разбора к пояснительному тексту
остаются нетронутыми.

Сессия проработки, установившая, что предмета у задачи ещё нет, SHALL
завершиться с исходом `premature`, не заводя изменения OpenSpec.

Сессия имплементации, установившая то же самое, SHALL завершиться тем же
исходом, не написав ни строки кода и не открыв pull request. Случай этот
не выдуман: 04.09.2026 задача, чей предмет лежал в чужом черновом
pull request, упала на имплементации исходом `failed`, хотя предусловие было
названо заранее и в описании задачи, и первым шагом её списка задач. Ход
поэтому SHALL быть открыт обоим этапам, где предмет встречается делом
впервые, а не одной проработке: исход, доступный только ей, такой задаче
не помогает вовсе — из «Ошибки» её поднимают в тот же `implement`.

Аудит исхода `premature` не получает и получать SHALL NOT: увидев ту же
беду, он возвращает задачу в проработку исходом `rejected`, а решение
о возврате в очередь принимается в одном месте — там, где предмет проверяют
делом.

Предмет считается не появившимся, когда описанное в задаче существует, но
не в той базе, на которой ведётся работа: правка лежит в чужой невлитой ветке,
в открытом pull request либо в задаче, не дошедшей до своего этапа. Случай этот
**противоположен** снятому предмету, и путать их MUST NOT: снятый предмет
закрывает задачу навсегда, не появившийся возвращает её в очередь.

Установлено это SHALL быть **делом, а не рассуждением**: сессия SHALL назвать
в поле `evidence` своего отчёта, чем именно проверено, — файл со строкой, где
нужного нет; номер невлитого pull request; идентификатор задачи, которая
предмет принесёт; либо вывод команды. Отчёт без такого доказательства применён
быть MUST NOT, и задача уйдёт в разбор.

Сессия SHALL назвать в поле `summary`, **чего именно ждут**. Запись перехода
уносит `summary` вместе с `evidence` в журнал задачи, и следующая сессия читает
оттуда, чего ждали, вместо того чтобы разбираться заново.

Проверять SHALL на свежей главной ветке, подтянутой в своё дерево. Вывод
о ещё не появившемся предмете, сделанный на устаревшей базе, ложен ровно в том
случае, ради которого он делается: предмет приносит чужая работа, а её
в старом дереве нет.

Сессия проработки исходом `premature` завершиться MUST NOT, если изменение
OpenSpec по задаче уже заведено, **а в очередь задача ещё не возвращалась**:
у такой задачи предмет как раз нашёлся. Вернувшись из аудита с замечаниями,
сессия правит заведённое изменение, а спорит с постановкой задачи отчётом
`question`.

Задача, **вернувшаяся в очередь** и взятая в проработку заново, случай
противоположный, и правило у неё своё. Изменение OpenSpec ей завёл прошлый
заход — сама проработка либо возврат с имплементации, — и заведённое изменение
исход `premature` у неё отменять SHALL NOT. Установив, что предмета всё ещё
нет, сессия SHALL завершиться исходом `premature` снова: правки, которую ждут,
по-прежнему нет, а круги ожидания ограничены пределом возвратов. Отличается
такая задача проверяемым признаком — счётом возвратов больше нуля при
не открытом pull request, — а не памятью сессии.

Отчётом `question` такая задача завершаться SHALL NOT: владельцу продукта
здесь нечего решать, а вопрос поставил бы её в ожидание ответа, то есть
потребовал бы того самого ручного вмешательства, ради отмены которого ход
и заводится. Отчётом `done` — тоже SHALL NOT, пока предмета нет: он погнал бы
задачу через аудит в имплементацию, где она упёрлась бы в то же отсутствие,
заплатив за круг тремя сессиями.

Предмет тем временем приехал — сессия работает обычным порядком: сверяет
артефакты заведённого изменения со свежей базой, правит разошедшееся
и завершается исходом `done`.

Сессия имплементации исходом `premature` завершиться MUST NOT, если
pull request по задаче уже открыт: работа начата, и выбрасывать её вправе
человек, а не сессия. Заведённое проработкой изменение OpenSpec ей при этом
не помеха — у всякой дошедшей до имплементации задачи оно есть по определению
маршрута, и мерка «работа не начата» SHALL читаться у каждого этапа по его
собственному следу.

#### Scenario: Предмет лежит в чужой невлитой ветке

- **WHEN** сессия проработки, подтянув свежую главную ветку, находит, что
  файла или правила, которые задача просит поправить, в дереве ещё нет,
  и убеждается, что они приедут открытым pull request другой задачи
- **THEN** сессия завершается с исходом `premature`, называет в `summary`
  ожидаемую работу, в `evidence` — номер невлитого pull request, и изменения
  OpenSpec не заводит

#### Scenario: Имплементации нечего править

- **WHEN** сессия имплементации, подтянув свежую главную ветку, находит, что
  файла или правила, которые предписывает править её список задач, в дереве
  ещё нет, и убеждается, что они приедут открытым pull request другой задачи
- **THEN** сессия завершается с исходом `premature`, называет в `summary`
  ожидаемую работу, в `evidence` — номер невлитого pull request, и ни строки
  кода не пишет

#### Scenario: Предмета нет, но pull request уже открыт

- **WHEN** сессия имплементации, успевшая открыть черновой pull request,
  находит, что предмета оставшихся шагов в дереве ещё нет
- **THEN** она не завершается исходом `premature`, а объявляет исход `failed`:
  работа начата, и распоряжается ею человек

#### Scenario: Предмет ещё не появился, но изменение уже заведено

- **WHEN** сессия проработки возвращается по замечанию аудита к задаче, под
  которую изменение уже заведено, а в очередь задача ещё не возвращалась,
  и считает предмет ещё не появившимся
- **THEN** она не завершается исходом `premature`, а выносит вопрос владельцу
  продукта исходом `question`, назвав заведённое изменение

#### Scenario: Проработка взяла задачу после возврата в очередь

- **WHEN** сессия проработки берёт задачу, вернувшуюся в очередь (счёт
  возвратов больше нуля, pull request не открыт), под которую изменение
  OpenSpec заведено прошлым заходом, и, подтянув свежую главную ветку,
  находит предмет всё ещё не появившимся
- **THEN** заведённое изменение возврату не мешает: сессия завершается
  исходом `premature` снова, счёт возвратов растёт, и задача возвращается
  в очередь либо останавливается пределом — но не встаёт в ожидание ответа
  владельца продукта

#### Scenario: Предмет приехал к возвращённой задаче

- **WHEN** сессия проработки берёт вернувшуюся в очередь задачу и находит,
  что предмет уже влит в главную ветку
- **THEN** она работает обычным порядком: сверяет артефакты заведённого
  изменения со свежей базой, правит разошедшееся и завершается исходом `done`

#### Scenario: Подозрение вместо проверки

- **WHEN** сессия проработки предполагает, что предмет ещё не появился,
  но проверить это делом не может
- **THEN** она не завершается исходом `premature`, а работает дальше либо
  выносит вопрос владельцу продукта

#### Scenario: Предмет снят, а не отсутствует

- **WHEN** сессия проработки находит, что описанное в задаче не появится
  вовсе: правило уже действует, дефект уже починен влитым изменением
- **THEN** она завершается исходом `moot`, а не `premature`: задача
  закрывается, а не возвращается в очередь
