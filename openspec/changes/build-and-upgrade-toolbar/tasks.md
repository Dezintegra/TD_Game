## 1. Подготовка: цель прокачки базы

- [x] 1.1 Переименовать `UpgradeTarget.Economy` в `UpgradeTarget.Base`
      в `packages/shared/src/balance.ts`, сохранив значение 7; поправить
      комментарий про число веток — их двадцать девять, а не двадцать семь.
      Проверка: `pnpm --filter @td/shared test` зелёный, значение
      не изменилось.
- [x] 1.2 Поправить места использования: `packages/sim/src/stats.ts`,
      `packages/ai/src/{opponent,value,profile}.ts` и тесты.
      Проверка: `pnpm typecheck` проходит.
- [x] 1.3 Оставить в `sim/stats.ts` комментарий о раннем выходе
      `structureBaseline` для базы: появится ветка её прочности —
      выход надо снять, иначе ветка будет продаваться и не работать.
      Проверка: комментарий на месте, поведение не изменилось.
- [x] 1.4 Прогнать оба эталона и убедиться, что они НЕ изменились.
      Проверка: `pnpm --filter @td/sim test` и `pnpm --filter @td/ai test`
      зелёные без правки контрольных сумм.

## 2. Управление: одна таблица раскладки

- [x] 2.1 Завести в `apps/client/src/game/controls.ts` единую таблицу
      раскладки: клавиша, действие, подпись для игрока.
      Проверка: `HOTKEYS` в `hud/labels.ts` строится из неё, а не своим
      списком.
- [x] 2.2 Переписать разбор нажатий: цифры заказывают юнитов вне режима
      строительства и выбирают постройку внутри него; `Q` включает
      и выключает режим; `E` и `Esc` выходят из него.
      Проверка: юнит-тесты управления на каждый сценарий спеки
      `match-controls`.
- [x] 2.3 Освободить `R` и завести на нём переключение показа
      характеристик; сохранять выбранное состояние в браузере.
      Проверка: юнит-тест — `R` не выбирает снайперскую башню; состояние
      читается обратно после перезаписи.
- [x] 2.4 Показывать круг радиуса строительства по включению режима,
      а не только по выбранному виду постройки; до выбора вида нажатие
      по полю выделяет объект.
      Проверка: юнит-тест управления плюс осмотр — круг появляется
      по `Q`.

## 3. Снимок: характеристики и цены

- [x] 3.1 Завести в `apps/client/src/game/store.ts` тип строки
      характеристики: подпись, действующее значение, цена следующего
      уровня, доступность, индекс ветки.
      Проверка: проверка типов проходит.
- [x] 3.2 Собирать эти строки в `bootstrap.ts` из `playerStats` и
      `upgradeCosts`, переводя величины к виду, в котором их читают:
      перезарядку — в выстрелы в секунду, внутренние единицы — в клетки,
      тики — в секунды.
      Проверка: юнит-тест — прокачка скорострельности УВЕЛИЧИВАЕТ
      показанное число; дальность показана в клетках.
- [x] 3.3 Добавить в снимок цену покупки каждого вида с учётом
      подорожания от прокачки.
      Проверка: юнит-тест — после прокачки штурмовика его цена покупки
      выросла.

## 4. Тулбар

- [x] 4.1 Нарисовать пиктограммы генерала и базы в
      `apps/client/src/hud/icons.tsx`, теми же правилами, что остальные.
      Проверка: линт не находит литеральных цветов.
- [x] 4.2 Собрать плитку: пиктограмма, название, цена покупки, горячая
      клавиша.
      Проверка: тест компонента с подставленными данными.
- [x] 4.3 Собрать столбец характеристик со стрелками покупки: акцентная
      при достатке энергии, приглушённая при нехватке, рядом цена уровня.
      Каждая строка подписана на своё значение отдельно.
      Проверка: тест компонента; корневой тулбар не перерисовывается
      при изменении одной строки.
- [x] 4.4 Переписать `ActionBar.tsx`: две группы с разделителем —
      заказываемое и свои объекты; нажатие по генералу и базе переносит
      камеру.
      Проверка: e2e проверяет, что нажатие по плиткам генерала и базы
      ничего не заказывает, не тратит энергию и не даёт отказа.
      Сам перенос камеры сквозной проверкой НЕ покрыт: камера живёт
      в сцене и в разметку не выводится, а заводить ради этого
      диагностический атрибут — отдельная работа, которой это изменение
      не касается. Проводка вызова проверена типами и чтением кода.
- [x] 4.5 Удалить `apps/client/src/hud/UpgradePanel.tsx` и его
      подключение.
      Проверка: файла нет, ссылок на него нет, линт зелёный.
- [x] 4.6 Перенести режим атаки в середину верхней полосы, рядом с целью.
      Проверка: e2e — переключатель режима виден в верхней полосе.

## 5. Раскладка полосы

- [x] 5.1 Завести в `packages/ui/src/tokens.css` две высоты нижней
      полосы — свёрнутую и развёрнутую — и переключать их классом
      на корне.
      Проверка: сумма верхней и развёрнутой нижней не превышает 340 точек.
- [x] 5.2 Убедиться, что смена высоты доходит до сцены наблюдателем
      за контейнером, и что анимации высоты нет.
      Измерено: `R` ставит класс на корень документа, полоса идёт
      177 → 75, контейнер поля растёт с 484 до 584 точек.
      `transition-duration` у полосы и у контейнера сцены — `0s`,
      то есть переход мгновенный и сцена перестраивается один раз,
      а не шестьдесят раз в секунду.
      Класс пришлось ставить на КОРЕНЬ ДОКУМЕНТА, а не на `#hud`:
      контейнер сцены живёт вне дерева React, и переопределения
      на `#hud` он бы не увидел.
- [x] 5.3 Проверить, что тулбар умещается по ширине в окно 1366 точек
      в развёрнутом виде.
      Измерено в браузере: развёрнутый тулбар 1174 × 138 в полосе высотой
      177, свёрнутый 1042 × 48 в полосе 75. Переполнения нет ни по ширине,
      ни по высоте ни при 1600, ни при 1366; страница не прокручивается.
      Девять плиток и все двадцать девять строк характеристик на месте.

      Подписи ради этого пришлось укоротить дважды: названия плиток взяты
      из `UNIT_SHORT` и `STRUCTURE_SHORT` («Снайп. башня» вместо
      «Снайперская башня»), а характеристики получили короткие формы
      («темп» вместо «Скорострельность»). С полными названиями расчёт
      давал около 1400 точек — то есть за край экрана ноутбука.

## 6. Проверки

- [x] 6.1 Поправить `e2e/smoke.spec.ts`: прокачка проверяется в тулбаре,
      а не в упразднённой панели; режим строительства включается `Q`.
      Проверка: сквозные проверки проходят.
- [x] 6.2 Прогнать `pnpm verify` целиком.
      Проверка: линт, типы, тесты и сборка зелёные.
- [x] 6.3 Посмотреть матч глазами: заказ юнита одним нажатием, `Q`
      показывает круг, цифры в режиме выбирают постройку, `R` сворачивает
      столбцы, стрелка покупает уровень и число растёт.
      Живой матч в этой машине не поднимается (скрытая панель браузера,
      Pixi на двух кадрах в секунду), поэтому клавиши проверены
      юнит-тестами управления и сквозными проверками, а тулбар измерен
      в странице с подставленным снимком: девять плиток, двадцать девять
      строк, переполнения нет.
