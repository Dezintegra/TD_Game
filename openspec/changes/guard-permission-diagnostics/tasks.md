## 1. Различение свидетельств

- [x] 1.1 За отдельный коммит (до 90 минут) добавить чистый классификатор `supervisor/lib/permission-evidence.mjs` и `supervisor/lib/permission-evidence.test.mjs` по таблице design: applied/not-applied/unknown отдельно от enforced/not-enforced/unknown и verified. Проверить годную пару, оба прошедших контроля, достоверно отсутствующий источник, неизвестный источник, отсутствующий native-инструмент, тайм-аут, рассказ модели вместо события, program-result с ошибкой Git, несовпадающие session/tool/digest и отказ positive. Отрицательные фикстуры должны менять verified с true на false при удалении независимого подтверждения или замене policy-denied на program-result. Классификатор не запускает CLI и не импортирует matcher. Проверки: `npx vitest run --root supervisor lib/permission-evidence.test.mjs`, `npx eslint supervisor/lib/permission-evidence.mjs supervisor/lib/permission-evidence.test.mjs`, `npx prettier --check supervisor/lib/permission-evidence.mjs supervisor/lib/permission-evidence.test.mjs`. Отметить собственный пункт, отправить коммит сразу и открыть собственный draft PR; не использовать PR 227 как PR этой карточки.

## 2. Единая доставка протокола и правил продолжения

- [x] 2.1 За отдельный атомарный коммит (до 2 часов) добавить `supervisor/permission-diagnostics.md`, согласовать `supervisor/skills/implement.md` и `supervisor/skills/revise.md`, обновить исходные proposal/design/tasks `verify-lease-push-permissions` по разделу доставки design и добавить `supervisor/lib/permission-diagnostics-contract.test.mjs`. Сначала сверить свежую main и отправленный PR 227; при отсутствии исходного каталога разрешено получить только четыре плановых файла из закреплённого SHA, отдельными `git -C <дерево> restore --source=48808202e8168a51c70e16af64fab15631b296d7 --worktree -- <точный путь>`, без слияния ветки. Сохранить исходное предложение, Non-goals, открытый 1.1, все критерии force/lease и условие правки deny; подключить использование классификатора в плане одноразового сценария, безопасный отбор свидетельств и ограниченную новую ветвь. Сторож проверяет ссылки обоих скиллов, согласованный допуск и запреты, опись без секретов, независимый контроль, различение unknown/not-applied, лимит и сохранение исходной приёмки 0078. Проверить сторожа изменяемыми текстовыми фикстурами: удаление каждой существенной границы должно уронить соответствующую проверку; реальный журнал и успешный живой запуск не подделывать. Проверки: `npx vitest run --root supervisor lib/permission-evidence.test.mjs lib/permission-diagnostics-contract.test.mjs`, `npx eslint supervisor/lib/permission-diagnostics-contract.test.mjs`, `npx prettier --check supervisor/permission-diagnostics.md supervisor/skills/implement.md supervisor/skills/revise.md supervisor/lib/permission-diagnostics-contract.test.mjs`, `openspec validate guard-permission-diagnostics --strict`, `openspec status --change guard-permission-diagnostics`, `openspec validate verify-lease-push-permissions --strict`, `openspec status --change verify-lease-push-permissions`. Отметить только свой 2.1, сразу отправить коммит. Наблюдаемый результат: исходный протокол продолжим в прежнем implement с PR 227, но исходная проверка остаётся незавершённой.

## Проверки и состав коммитов

Все команды выполняются по одной из назначенного дерева; `<дерево>` — его абсолютный путь. Формы git -C, npx vitest/eslint/prettier и openspec сверены с `supervisor/config/stage-settings.json`. Полные тесты supervisor выполняет CI этого PR, локально — перечисленные узкие проверки. Ни полного verify, ни арены, ни живых CLI-проб 0078, ни ожидания CI в этих пунктах нет.

Для исходного бездельтового изменения допускается только единственная ошибка validate, сообщение которой после служебного оформления начинается с `Change must have at least one delta. No deltas found.`. Прочитать весь вывод, проверить отсутствие каталога specs и содержательное обоснование proposal. Status исходного изменения ожидается 3/4; это не открытые задачи 0296. Собственное изменение обязано пройти validate без исключения и иметь status 4/4.

Пути коммита 1: `supervisor/lib/permission-evidence.mjs`, `supervisor/lib/permission-evidence.test.mjs`, обязательная отметка в `openspec/changes/guard-permission-diagnostics/tasks.md`.

Пути коммита 2: `supervisor/permission-diagnostics.md`, `supervisor/skills/implement.md`, `supervisor/skills/revise.md`, `supervisor/lib/permission-diagnostics-contract.test.mjs`, `openspec/changes/verify-lease-push-permissions/.openspec.yaml`, `openspec/changes/verify-lease-push-permissions/proposal.md`, `openspec/changes/verify-lease-push-permissions/design.md`, `openspec/changes/verify-lease-push-permissions/tasks.md`, обязательная отметка в `openspec/changes/guard-permission-diagnostics/tasks.md`. Метаданные исходного изменения переносятся без изменения. Исходный tasks разрешён только для заказанного уточнения плана, не для отметки работы 0078. При необходимости технически уточнить собственный план действует согласованный раздел design, без изменения требований и приёмки.

Каждый фактически изменённый путь добавлять явно. До коммита выполнить отдельными вызовами `git -C <дерево> diff --check`, `git -C <дерево> diff --cached --name-only`, `git -C <дерево> diff --cached`; после — `git -C <дерево> show --format=fuller --stat HEAD` и `git -C <дерево> show --format= --patch HEAD`. Проверить не только имена, но и содержание хунков: посторонние изменения в допустимом файле также запрещены. Отправить `git -C <дерево> push -u origin HEAD`; проверить пустой хвост `git -C <дерево> log --oneline '@{u}..HEAD'`.

В индексе и коммите не должно быть `supervisor/config/stage-settings.json`, кода/теста исходной реализации `supervisor/config/lease-push-permissions.test.mjs`, `openspec/changes/verify-lease-push-permissions/probe-results.md` или временных `.matchlog/0078-lease-probe.mjs` и `.matchlog/0078-lease-probe/`. Ссылки на ранее сохранённые доказательства допустимы, их перенос в наш коммит не нужен. Изменения основных спецификаций, архивов, чужих веток и доски запрещены.
