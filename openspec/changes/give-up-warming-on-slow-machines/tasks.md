# Задачи

Каждая — отдельный коммит, отправляемый сразу. Порядок выбран так, чтобы
любая остановка оставляла работоспособное состояние.

## 1. Прогрев отчитывается о сделанном

- [x] 1.1 `scene.ts`: `RendererHost.warm(budgetMs)` возвращает
      `{ baked, rest }` вместо `boolean`. `session.ts` читает `rest > 0`
      там, где читал `true`. Поведение не меняется ничем.

## 2. Мерка скорости прогрева

- [x] 2.1 Завести `apps/client/src/session/warm-pace.ts`: смотрит среднюю
      длину кадра в полусекундном окне и отвечает на вопрос «дёргается
      ли меню». Константы с обоснованием: `WARM_FRAME_LIMIT_MS = 40`,
      `WARM_WINDOW_MS = 500`, `WARM_WINDOW_FRAMES_MIN = 4`.
- [x] 2.2 Модульные тесты `warm-pace.test.ts`: ровные кадры прогрев
      не трогают, дёрганые прекращают, первое окно не судит, одна
      заминка исхода не решает, матч окно не портит.

## 3. Прогрев отступает

- [x] 3.1 `session.ts`: `startWarming` заводит кадровые часы на время
      прогрева и при первом дёрганом окне прекращает прогрев до конца
      сессии, сказав об этом в консоль с цифрами. `rewarmInMenu` отступ
      уважает.

## 4. Проверка делом

- [x] 4.1 Прогнать настоящие проверки лобби в режиме runner'а без
      `VITE_E2E_CHEAP_TEXTURES`: временный конфиг Playwright
      (`--disable-gpu --use-gl=swiftshader --disable-gpu-compositing`,
      `workers: 2`, свои порты, `reuseExistingServer: false`).
      Записать времена рядом с таблицей из `proposal.md`.
- [x] 4.2 Проверить, что на здоровой машине (видеокарта, один работник)
      прогрев проходит очередь целиком и не отступает зря.
- [ ] 4.3 Дождаться зелёного CI на pull request.
