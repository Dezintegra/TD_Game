# Задачи

## 1. Ядро: ракета и разворот постройки

- [x] 1.1 Завести `ShotWeapon.Missile` в `balance.ts`, отдать его генералу
      (`GENERAL_WEAPON`), задать срок жизни следа больше трассерного.
      Проверка: `balance.test.ts` требует положительный срок у каждого
      вида оружия и знает, что ракета живёт дольше трассера.
- [x] 1.2 Добавить `facing` в `StructureState` и в рабочее состояние;
      начальное направление — от базы владельца наружу.
      Проверка: новая постройка имеет направление, не равное «стоять».
- [x] 1.3 Разворачивать постройку на цель тем же `aimFacing`, каким
      разворачивается юнит.
      Проверка: сценарии в `combat.test.ts` — выстрелила и повернулась,
      промолчала и сохранила направление.
- [x] 1.4 Включить `facing` постройки в контрольную сумму, обновить
      эталон детерминизма тем же коммитом.
      Проверка: `determinism.golden.test.ts` зелёный с новым числом,
      `profile.golden.test.ts` — с прежним поведением противника.

## 2. Клиент: модели построек

- [x] 2.1 Написать `towers.ts`: модели стены, базовой и снайперской
      башни из тел с фактурой; поворотная часть отделена от неподвижной.
      Модуль самодостаточен — со своей сборкой деталей в силуэт.
      Проверка: `towers.test.ts` — тел больше одного, силуэты видов
      различаются, основание не выходит за клетку.
- [x] 2.2 Кеш силуэтов по (вид, разворот, ступень готовности, сторона).
      Проверка: повторный запрос возвращает ту же запись; возведение
      от начала до конца не даёт больше записей, чем ступеней.
- [x] 2.3 Перевести `entities.ts` на модели построек, полосу здоровья
      поднять на высоту модели.
      Проверка: `entities.test.ts` зелёный, полоса не висит в воздухе.

## 3. Клиент: облик выстрела

- [x] 3.1 Завести `shots.ts` и перенести туда трассер, луч и разряд
      без изменений; сцена отдаёт модулю два слоя — дым и свет.
      Проверка: перенесённые сценарии из `entities.test.ts` живут
      в `shots.test.ts` и зелёные.
- [x] 3.2 Вспышка у ствола: ядро, направленный ореол, лучи вдоль
      выстрела, клуб дыма.
      Проверка: свежий выстрел даёт фигуры в слое света у стрелка,
      через половину срока — не даёт.
- [x] 3.3 Попадание: вспышка в точке цели и разлетающиеся искры,
      разлёт выводится из места выстрела.
      Проверка: две отрисовки одного момента совпадают точка в точку.
- [x] 3.4 Высота дульного среза по стрелку: постройка, генерал, юнит.
      Проверка: начало луча снайперской башни выше половины её модели;
      выстрел юнита выходит с прежней высоты.
- [x] 3.5 Бюджет подробностей на кадр, подробности — свежим.
      Проверка: полсотни выстрелов дают меньше фигур, чем пятьдесят
      подробных.

## 4. Клиент: ракета генерала

- [x] 4.1 Выкладка полёта отдельной функцией: положение и высота
      по доле срока.
      Проверка: высота в конце меньше высоты в начале, тело приходит
      в точку цели раньше конца срока.
- [x] 4.2 Корпус с факелом и клубы дыма, рождающиеся по доле пути
      и рассеивающиеся после попадания.
      Проверка: после прилёта клубы ещё рисуются, ракеты уже нет.

## 5. Сцена и палитра

- [x] 5.1 Слой света выстрелов со сложением цвета, порядок слоёв
      по замыслу из `design.md`.
      Проверка: дым выстрела ниже следа, свет выстрела выше следа
      и ниже света взрывов.
- [x] 5.2 Цвета выстрела читаются из тех же дизайн-токенов, что цвета
      взрыва; литералов в модулях нет.

## 6. Замысел и завершение

- [x] 6.1 Обновить `docs/game-design.md`, раздел 7.2: таблица обликов
      выстрела с ракетой, вспышки у всех оружий, башня как сооружение,
      фактура построек. Абзацы про юнита и истребителя не трогать —
      их правит параллельное изменение `turn-boxes-into-armour`.
- [x] 6.2 `pnpm verify` — линт, типы, тесты и сборка зелёные.
- [x] 6.3 Глазами: прогнать матч в браузере и убедиться, что
      перестрелка читается перестрелкой, а не схемой связей.
      Отдельным пунктом потому, что «стало красиво» тестом
      не проверяется.

## 7. Долг, оставленный сознательно

- [ ] 7.1 Вынести общую сборку силуэта из `models.ts` и `towers.ts`
      в один модуль — ПОСЛЕ того, как `turn-boxes-into-armour`
      закончит переписывать модели техники. Разбор в `design.md`,
      решение 8.
