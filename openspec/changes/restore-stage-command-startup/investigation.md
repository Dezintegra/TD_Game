# Диагностика 0366, 14.09.2026

## Проверенная база

Назначенное дерево подтверждено `git -C . worktree list`; ветка `worktree-0366-vosstanovit-zapusk-komand-posle-neudachn`. После `git -C . fetch origin` команда `git -C . merge origin/main` ответила `Already up to date.`. HEAD: `98a6ea0b11dcae7abda344d7a80da8fb3995cdd5`. Начальный status чистый. Чтения и Git в 0366 успешны; к контексту 0011 этот результат не переносится.

## Первичные факты

1. `C:/src/dezintegra/TD_Game/.pipeline/logs/0011-post-match-statistics-audit-2026-09-14T12-04-26.724Z-74c8f0fa-1ce8-4be1-9a67-d6c20b676fbc.log`: запуск audit `74c8f0fa-1ce8-4be1-9a67-d6c20b676fbc`, PID 21564, сессия `01a09fcd-ebcf-7532-a29d-e336938dd785`. В stderr в 12:04:38.194440 и 12:04:43.267068 UTC — `exec_command failed: CreateProcess ... helper_unknown_error: setup refresh had errors`. stdout содержит сообщения агента и terminal usage, но не `command_execution` с результатами этих команд. Код сессии 0 и отсутствие permission denials не означают успех инструментов.
2. `C:/Users/dmitry.ivanov/.codex/.sandbox/sandbox.2026-09-14.log:24534–24537` и `24569–24572`: в 12:04:38.177337 и 12:04:43.257113 UTC зафиксировано `deny ACE failed on C:\src\dezintegra\TD_Game\.claude\worktrees\0011-post-match-statistics\.agents: SetNamedSecurityInfoW failed ...: 5`, затем `setup refresh completed with errors` и `setup error: setup refresh had errors`. Совпадают точный путь и оба времени отказа. В 12:09:56.548357 тот же путь появляется снова, строки 25195–25198, во время последующего postmortem. Журнал общий: соседние успешные `errors=[]` нельзя приписывать 0011 без идентичности вызова.
3. Первая запись `C:/Users/dmitry.ivanov/.codex/sessions/2026/09/14/rollout-2026-09-14T15-04-27-01a09fcd-ebcf-7532-a29d-e336938dd785.jsonl`: `cli_version=0.153.4`, `source=exec`, cwd дерева 0011, commit `845982a1a9043076afb238ee19df6c9a463bedf2`. Это версия CLI сессии и SHA дерева задачи, **не версия загруженных модулей супервизора**.
4. Read-only `Get-Acl C:/src/dezintegra/TD_Game/.claude/worktrees/0011-post-match-statistics/.agents` вернул владельца `NB3391\CodexSandboxOnline`. Read-only `icacls` успешно обработал один каталог: видны наследуемые Modify для sandbox-пользователей, FullControl Administrators/SYSTEM и Modify Authenticated Users. Разрешение части имён SID сопровождалось `The trust relationship between this workstation and the primary domain failed.`. Эта побочная диагностика разрешения имён не доказывает причину отказа setup. Состояние ACL прочитано позже сбоя; историческая идентичность ACL не установлена.
5. `C:/src/dezintegra/TD_Game/.pipeline/cycle.log:277538`: в 11:56:41.061 UTC запущен супервизор PID 24252, root основного дерева. Строка 277689 подтверждает запуск audit PID 21564 в 12:04:26.999; 277839 — завершение failed в 12:05:08.918. Прочитанный `supervisor.lock` содержит PID 24252 и takenAt 11:56:40.717, но не SHA/argv. Это доказательство зарегистрированного PID, не фактической версии процесса.
6. `Get-CimInstance Win32_Process -Filter 'ProcessId = 24252'` завершился кодом 1: `Access denied`. Фактическая командная строка, пользователь/токен и источник загруженного кода этим способом недоступны. Обход ограничений, повышение прав и повтор через другую оболочку не выполнялись.

## Сопоставление с существующим механизмом

`supervisor/lib/provider.mjs:37–105` строит именованный профиль `td-pipeline`, `extends=":workspace"`, точные служебные записи Git/perf, `approval_policy="never"`, Windows sandbox elevated по умолчанию. Это код изученной ревизии, не доказанный argv PID 24252 или уже закончившегося ребёнка. Нельзя считать текущую main версией работающего процесса.

`tool-diagnostics.mjs` строит независимые контроли через тот же `stageCommand`, сравнивает provider/cwd/environmentId/permissionsId и принимает структурированные tool events. `stage-tool-health.mjs` различает healthy/confirmed/inconclusive. Отсутствие events исторической сессии не превращается в confirmed разбором stderr. Связанные setup-логи дают материал причинному расследованию, а не готовый автоматический вердикт. `tool-controls.mjs` уже предоставляет shell, child, Git, GitHub, transport и dry-run push. Проверка фактического чтения правил и непрерывности одной сессии должна быть учтена при дальнейшем проектировании, без подмены одиночными успехами разных сессий.

`pipeline-incidents.mjs` отдельно учитывает fixedBy, единственную пробу и `incidentVerification`. Инцидент из исторического audit — `93a4b8112848838f60f84cc9`; актуальный ID после нового диагноза следует брать из нового назначения. Ремонт не сбрасывает счёт пробы и не отмечает инцидент verified. Формат evidence — отдельная 0368; в текущем `verifyIncident` требуется непустая строка.

В `sum-up-the-match/tasks.md:94` пункт 3.4 остаётся открытым и касается замера пути догона в `e2e/catchup.spec.ts`. Здесь он только прочитан для сохранения адреса приёмки: содержательный аудит и любые отметки принадлежат исходному audit 0011.

`prepare-windows-diagnostic-host/design.md` прочитан через `origin/worktree-0302-podgotovit-razreshennyy-windows-marshrut`. Внешний хозяин создаёт фиксированный fixture для 0083, разделяет hostReady/cliStart/permissionAcceptance и исключает ACL-ремонт, runas/UAC и смену пользователя исполнителем. Поэтому 0307 не объявляется обязательным предшественником 0366 по одному слову Windows.

Официальная [Windows sandbox](https://learn.chatgpt.com/docs/windows/windows-sandbox), прочитанная 14.09.2026, описывает отдельного ограниченного пользователя elevated sandbox и рекомендует журнал подготовки при расследовании. Она не устанавливает причину этого SetNamedSecurityInfoW/5 и не даёт оснований автоматически менять профиль или ACL проекта.

## Диагноз и граница знания

**Установлен непосредственный блокирующий шаг:** обновление защиты `.agents` дерева 0011, которому отказано в изменении ACL ещё до создания запрошенного процесса. Простое разрешение Get-Content/Git или повтор команды не чинит этот шаг.

**Не установлен причинный ремонт:** неизвестны фактические права процесса refresh, историческое владение/наследование ACL, применённые параметры запуска и версия загруженного супервизора. Владелец каталога sandbox-пользователь согласуется с гипотезой несовместимого владения, но сам по себе её не доказывает. Не доказаны гонка setup, дефект CLI, необходимость 0307 или необходимость расширять разрешения. Нельзя выбирать между исправлением подготовки каталога, жизненного цикла helper и актуализацией запуска до получения этих фактов.

## Обязательная внешняя предпосылка

Нужен ограниченный **read-only сбор свидетельств штатным владельцем Windows-хозяина NB3391**, вне ограниченного контекста этой сессии. Никакого ремонта или повторного audit в рамках предпосылки.

- Для зарегистрированного супервизора 24252 сверить PID вместе с временем рождения, исполняемым файлом, полной структурой argv, home/root/config и идентичностью пользователя. При смене PID сохранить обе идентичности и явно указать, какие исторические факты невосстановимы. Секреты в argv/env не публиковать.
- Установить источник реально загруженного кода в 11:56:40–12:04:44 UTC: launch evidence, SHA и хеши модулей provider/run-stage/tool-diagnostics/supervisor при старте, либо проверяемый эквивалент. Текущий HEAD без подтверждения неизменности файлов с запуска недостаточен. Если прежнего снимка нет — явно назвать пробел; не объявлять версию доказанной.
- Получить фактический профиль, режим sandbox, путь/версию CLI и helper, argv/cwd для запуска 0011; соотнести с метаданными сессии и обоими моментами setup refresh. Определить контекст владельца refresh и его доступ к WRITE_DAC/владению `.agents`, read-only снять owner/DACL/наследование и reparse/realpath конкретной цели и родителей. Сравнить с назначенным успешно работающим деревом без внесения правок. Не делать выводы по одному имени пользователя или общей строке ошибки.
- Вернуть причинный вывод с доказательствами или явные оставшиеся неизвестные; сохранить очищенные результаты и первичные ссылки в отдельном новом файле под `.matchlog/0366-host-diagnostics/` сохранённого дерева 0366. Передать абсолютный путь, SHA-256, время, источник и ограничения через журнал карточки предпосылки. Исходные логи и материалы не перезаписывать. Не читать `.sandbox-secrets`, ключи, токены или полное окружение; требуются только разрешённые несекретные метаданные.

Предпосылка считается выполненной по доступному проверяемому свидетельству, позволяющему отделить права/подготовку ACL от параметров или версии запуска. Одного обещания диагностики, нового PR или успеха команды в основном дереве недостаточно. Если ограничение хозяина также мешает чтению, оно сохраняется как причина невыполненной предпосылки; обход не предлагается.

После результата новый анализ 0366 продолжает это изменение: уточняет причинный ремонт, дельту, design и независимые tasks не длиннее двух часов. Узкие проверки supervisor планировать через `pnpm test:pipeline` после штатной установки, без полного verify и игры. Живой audit 0011 не превращать в пункт implement 0366; обеспечить передачу через действующий инцидент и доказанный допуск восстановления.

## Ограничения текущего прохода

`openspec validate restore-stage-command-startup --strict` завершился кодом 1 с единственной ошибкой `Change must have at least one delta. No deltas found.`; `openspec status --change restore-stage-command-startup` — 1/4 (только proposal). Исключение для завершённых бездельтовых изменений не применяется: это незавершённая проработка, сохранённая перед blocked. Design/specs/tasks не объявляются готовыми, этап audit не начинается.

Код, ACL, профили, настройки, чужие деревья, доска и `.pipeline` не менялись. Тесты, установка, sandbox setup и пробные сессии не запускались. Поиск имён диагностических файлов зашёл на защищённый `.sandbox-secrets` и получил `Access is denied`; содержимое не читалось, повторов не было. Поиск продолжен по доступному журналу `.sandbox`, не как обход защищённого каталога. Git status предупредил о `Permission denied` для пользовательского ignore, но завершился успешно. Ошибочные предположения об имени codex-stage-command.mjs и status.mjs исправлены чтением существующих provider.mjs и журналов.
