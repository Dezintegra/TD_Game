## Проверка адресного режима

07.09.2026. Выполнен содержательный проход по всем 14 строкам Verification matrix
из design.md: сопоставлены условия примеров с конечными инструкциями и общими
нормами. Это проверка текста правил; боевой конвейер, реальные карточки,
вливание PR и возвраты этапов для опыта не запускались.

Адреса ниже указывают разделы файлов в `supervisor/skills/` и заголовки норм.
Нумерация существующих шагов сохранена. Все три общие нормы ещё находятся
в открытых изменениях; архивные снимки и основные спецификации не менялись.

| Случай | Наблюдаемый результат сверки и основание |
| --- | --- |
| Переименование величины в открытой дельте | `design.md`, «Адресная правка открытой дельты» перед созданием изменения, разрешает исправить исходник в своём дереве, сохранить сценарии и не создавать вторую дельту. «Отчёт об адресной правке» требует цель, пути, заголовки и SHA отправленного коммита. Общая норма «Проработка ведётся через OpenSpec» в agent-backlog-pipeline ограничила создание нового комплекта обычным режимом. |
| Десять чужих задач и широкие Non-goals | `audit.md`, адресный раздел при сверке заказа, прямо оставляет Non-goals и tasks контекстом и проверяет узкий diff с последствиями. `implement.md`, адресный раздел перед циклом, запрещает исполнение и отметки чужих пунктов и требует собственный PR. Общие требования аудита и имплементации в agent-backlog-pipeline ссылаются на адресную норму. |
| Противоречие сценарию или соседней дельте | `audit.md`, адресный раздел, прямо квалифицирует оба противоречия как адресное замечание; общая норма аудита сохраняет проверку пересечений. Узость карточки не оправдывает конкурирующее требование. |
| CLI отвергает существующую дельту | `design.md`, адресный раздел, и `audit.md`, проверка формы, требуют validate/status исходного изменения и запрещают исключение для отсутствующих specs. Ошибка вне области требует решения по контракту, а не успешного результата. Диагностика 0251 в обоих файлах и второе требование admit-changes-without-deltas не изменены. |
| Возврат design | `design.md`, адресный раздел, сохраняет ту же цель на продолжении и после замечания; новый комплект не создаётся. |
| Цель отсутствует, архивирована или неоднозначна | `design.md` запрещает замену цели, архив, основу и выбор наугад, требует вопрос или предпосылку с доказательством; `audit.md` требует вернуть фактическую проблему по контракту. Запрет moot при заполненном change сохранён. |
| Пример в инструкции вне каталога | `design.md` прямо относит его к обычному бездельтовому изменению. Первое требование и сценарий правки текста admit-changes-without-deltas ограничены обычным режимом, адресное исключение названо отдельно. |
| Код вместе с дельтой | `design.md` требует обычной проработки и реализации после аудита; адресное исключение запрещает код. Не появляется право реализовать задачу на design. |
| Design закончила правку, PR нет | `implement.md`, адресный раздел и прежний раздел «Отчёт», совместно требуют первый собственный черновой PR из отправленных коммитов без фиктивной правки. Общая норма имплементации в agent-backlog-pipeline согласована; маршрут design → audit → implement сохраняется. |
| PR уже известен, нового коммита нет | `implement.md` прямо сохраняет прежнюю мерку следа: старый PR не новый след, создавать второй ради приёмки запрещено. Исключения к приёмке не добавлено. |
| CI зелёный, конфликтов и дефектов нет, чужая реализация не готова | `review.md`, вводная мерка, адресный раздел и мерка спецификации, не требуют чужого кода, тестов и отметок. Иные открытые пункты относятся к проверяемой карточке. Общая норма review в agent-backlog-pipeline и мерка открытых пунктов keep-ci-waiting-out-of-tasks согласованы: чужая незавершённость не замечание. Прежние условия, снятие черновика и вливание собственного PR сохранены. |
| Review находит дефект самой правки | `review.md`, адресный раздел, отменяет вливание и возвращает rejected с конкретным дефектом в revise. `revise.md`, адресный раздел, ограничивает перечень и цикл исправлений тем же каталогом, требует validate/status, узкие проверки, свежий коммит и немедленную отправку в прежний собственный PR. Повторное review сохраняет область. Сценарий «Ревью нашло ошибку» agent-backlog-pipeline теперь явно охватывает дефект адресного diff. |
| Красный CI или конфликт | `review.md` сохраняет обычный возврат без вливания; `revise.md` не разрешает расширение области из-за CI или конфликта. Исправляется относящаяся к карточке причина либо оформляется необходимая предпосылка. Три условия общей нормы review не ослаблены. |
| Нужен чужой код или переписывание плана | `revise.md`, адресный раздел, требует вопрос или обязательную предпосылку с необходимым результатом, запрещает чужой код, tasks и подмену предложения. `review.md` передаёт фактическое препятствие для этого решения, не заказывая чужую реализацию. |

Новый путь согласован во всех пяти инструкциях. Подробная нормативная мерка
остаётся в требовании «Адресная правка открытой дельты проходит этапы в границах
своей карточки»; три существующие дельты только согласуют свои общие формулировки.

## Выполненные проверки

- `pnpm test:pipeline`: 65 файлов, 1773 теста прошли, включая 253 теста transitions.
- `npx vitest run --root supervisor config/transitions.test.mjs`: отдельный предусмотренный планом прогон, 253 теста прошли.
- `npx prettier --check` по пяти инструкциям, трём изменённым общим дельтам и этому файлу: успешно.
- `openspec validate amend-existing-deltas-in-design --strict`: успешно; status показывает полный комплект 4/4.
- `git -C . diff --check`: успешно. Проверка diff подтверждает сохранение нумерации, соседних правил, всех прежних сценариев и диагностики 0251.
