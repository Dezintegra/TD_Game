## 1. Согласовать шаг 9 с каталогом назначения

- [x] 1.1 За один атомарный коммит (до 1 часа) удалить из шага 9 `supervisor/skills/deploy.md` предписание `Push-Location`, добавить пояснение назначенного снимка и соответствия `<хеш>` полю `deploymentRevision` по design, сохранить довод `git rev-parse --show-toplevel` и последствия запуска из чужого каталога. Выполнить проверки ниже, отметить этот пункт, закоммитить и сразу отправить; после первого коммита открыть черновой PR по правилам имплементации.

Перед правкой отдельно выполнить `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main` по действующим правилам этапа. Сверить пересечения с 0169 и 0253, названные в proposal, по свежей базе и отправленным артефактам. Если команда уже удалена, не возвращать её: дополнить только недостающее пояснение. Если весь результат уже присутствует, записать рядом с отметкой пункта проверенный хеш базы, коммит-источник и результат всех проверок; такой коммит фиксирует собственную проверку. В PR и отчёте назвать порядок общей правки с 0169 и фактический случай интеграции. Карточку 0167 и чужие артефакты не менять.

Ручная приёмка (результаты перечислить в отчёте реализации):

- В шаге 9 больше нет исполняемой команды смены каталога или её замены; текст прямо связывает текущий каталог с `worktree` из назначения, `deploymentRevision` и detached HEAD.
- Назначение сверено с `supervisor/lib/stage-command.mjs`, `supervisor/config/transitions.mjs` и `supervisor/lib/provider.mjs`: оба пускателя уже задают cwd. Не требуется новая команда перехода или изменение пускателя.
- В шаге сохранены PowerShell, прямой запуск `node scripts/deploy.mjs --ref <хеш> --no-perf`, полный закреплённый хеш из назначения, однократность запуска и довод о чужом дереве с `git rev-parse --show-toplevel`. Довод соответствует `scripts/deploy.mjs`.
- Нумерация и четыре причины сохранены; шаги проверки снимка и замера не изменены; независимая правка кодировки 0253/0169 не потеряна. Новые вставки кода находятся на одной физической строке.

Узкие проверки реализации из назначенного дерева, каждая отдельным вызовом:

```powershell
npx prettier --check supervisor/skills/deploy.md
```

```powershell
npx vitest run --root supervisor config/transitions.test.mjs
```

```powershell
openspec validate explain-assigned-deploy-directory --strict
```

```powershell
openspec status --change explain-assigned-deploy-directory
```

Формы команд покрыты `PowerShell(npx prettier:*)`, `PowerShell(npx vitest:*)`, `PowerShell(openspec:*)` и `PowerShell(git -C:*)` в `supervisor/config/stage-settings.json`. OpenSpec зовётся глобально. Для validate допустима только единственная ошибка с началом сообщения `Change must have at least one delta. No deltas found.` при отсутствии каталога specs и содержательном обосновании proposal; проверить весь вывод. Status ожидается 3/4, незакрыт только specs. Полные проверки, сборка, замер, арена и фактическая выкладка не нужны для этой правки текста.

Допустимые пути коммита перечислены полностью:

- `supervisor/skills/deploy.md` — только описанная правка шага 9;
- `openspec/changes/explain-assigned-deploy-directory/tasks.md` — отметка пункта и при необходимости запись проверенного соседнего результата.

Добавить фактически изменённые пути явным списком через `git -C <дерево> add -- <явные пути>`. До коммита проверить `git -C <дерево> diff --cached --name-only`, `git -C <дерево> diff --cached` и `git -C <дерево> diff --cached --check`: разрешены только названные пути и связанное содержание. После коммита проверить пути и содержание через `git -C <дерево> show --format=fuller --stat HEAD` и `git -C <дерево> show --format= --patch HEAD`, сразу выполнить `git -C <дерево> push -u origin HEAD`. Временные файлы PR и проверок не добавлять. Ожидание CI выполняет супервизор и в этот пункт не входит.
