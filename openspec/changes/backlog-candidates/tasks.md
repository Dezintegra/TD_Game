# Задачи

## 1. Состояние `candidate` в автомате

- [x] Добавить `candidate` в `STATES` первым: колонка входящих стоит слева
      от «Новых», а порядок колонок доски берётся из порядка состояний
- [x] Отнести `candidate` к классу `queue`: кандидат не занимает ни слота,
      ни исполнителя
- [x] Не включать `candidate` ни в `NEEDS_SESSION`, ни в `NEEDS_WORKTREE`
- [x] Объявить переход `candidate` → `new` в маршрутах типов `feature`
      и `note`; для `run` не объявлять
- [x] Проверки: переход `candidate` → `new` разрешён; `candidate` → `design`
      отвергается; задача в `candidate` не попадает в отбор работы

## 2. Заявки заводятся кандидатами

- [x] `planRequests`: заводить задачу в `candidate`, кроме `type: 'run'` —
      та по-прежнему в `new`
- [x] Проверки: заявка на `feature` даёт кандидата; заявка на `run` даёт
      задачу в работе; связь с породившей задачей проставляется в обоих
      случаях

## 3. Колонка на доске

- [x] `board-setup.mjs` заводит недостающую колонку «Кандидаты» и ставит
      её первой
- [ ] Проверка: сценарий настройки доски называет недостающую колонку
      и заводит её только с ключом исполнения

## 4. Схема знает новое состояние

- [x] `manage/schema.json`: `candidate` и `interpret` в перечне состояний
- [x] `validate-card.mjs` и `validate-task.mjs` принимают их
- [x] Проверки: карточка в колонке «Кандидаты» схему проходит

## 5. Этап `interpret` в маршруте прогона

- [x] `ROUTES.run`: `new` → `benchmark` → `interpret` → `closed`
- [x] `interpret` — класс `resource`, входит в `NEEDS_SESSION`, не входит
      в `NEEDS_WORKTREE`
- [x] Проверки: замер с исходом `done` ведёт в `interpret`, а не в `closed`;
      под `interpret` дерево не заводится

## 6. Скилл толкования

- [ ] Завести `plugins/pipeline/skills/interpret.md`: где взять числа
      (отчёт замера и журнал задачи), с чем сверять (`## Ожидаемый
      результат`), как выразить вывод, как подать заявки
- [ ] Прямо запретить запускать прогоны на этом этапе

## 7. Замер перестаёт толковать

- [ ] `benchmark.md`: убрать вынесение вердикта и подачу заявок, оставить
      запуск, ожидание и запись чисел
- [ ] Указать, что вывод делает следующий этап, и что числа надо оставить
      в отчёте полностью — толкователь не сможет их доснять

## 8. Проверка на живом конвейере

- [ ] Холостой прогон цикла: задача в `candidate` не появляется ни
      в действиях, ни в назначениях
- [ ] Настройка доски видит недостающую колонку и заводит её
