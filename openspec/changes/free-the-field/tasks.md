Порядок задач выбран так, чтобы **ценность появилась раньше риска**.
Масштаб идёт первым, хотя раскладка обсуждалась раньше: дефолтный
масштаб считается от высоты, оставшейся полю, какой бы она ни была,
и работает ещё до того, как полосы похудеют. После задачи 2.4 игрок
уже видит десять клеток вместо четырёх, а раскладка добавит к ним
запаса и вернёт полю низ экрана.

Каждая задача — отдельный коммит, отправляемый сразу (правило 4
в `CLAUDE.md`). Проверки гоняет CI, локально — только узкое по своим
файлам (правило 6).

## 1. Масштаб как арифметика: чистые функции и проверки

- [x] 1.1 В `iso.ts` вывести `CELL_SCREEN_HEIGHT_PX` — высоту ромба
      клетки на экране (`BX + BY`), с объяснением, почему это и есть
      «одна клетка по вертикали» в глазах игрока. Проверка:
      `iso.test.ts` сверяет величину с прямым замером ромба и с расчётом
      из углов проекции — вышло 50,91.
- [x] 1.2 В `camera.ts` завести `defaultScale(fieldHeight)`:
      `clamp(fieldHeight / (MIN_VISIBLE_ROWS · CELL_SCREEN_HEIGHT_PX),
      MIN_SCALE, 1)` при `MIN_VISIBLE_ROWS = 10` и `MIN_SCALE = 0,4`.
      Проверка: новый `camera.test.ts` — на 794 даёт единицу, на 311
      даёт 0,611, на 200 упирается в предел 0,4. Все три сошлись.
- [x] 1.3 Там же `clampZoom(zoom)` в диапазон `1 … 4` и
      `scaleOf(fieldHeight, zoom) = defaultScale · zoom`. Проверка:
      кратность 5 обрезается до 4, кратность 0,5 — до 1. Плюс проверка
      на то, ради чего кратность и заведена: при одном и том же `zoom`
      масштаб в портрете и в ландшафте разный.
- [ ] 1.4 Там же `zoomAt(camera, scaleBefore, scaleAfter, anchor,
      viewport)` — сдвиг камеры, оставляющий точку под пальцем на месте:
      `camera + (точка − центр) · (1/s − 1/s′)`. Проверка: мировая точка
      под якорем до и после смены масштаба совпадает с точностью
      до дробной части клетки.
- [ ] 1.5 Научить `clampCamera` принимать масштаб: окно в мировых
      единицах равно `ширина / s`, `высота / s`. Проверка: при `s = 0,5`
      камера доезжает вдвое ближе к краю карты, чем при `s = 1`,
      и пустоты за краем не остаётся.

## 2. Масштаб в сцене

- [ ] 2.1 Хранить в сцене `zoom` (кратность) и производный `scale`;
      `applyCamera` ставит `worldContainer.scale` и считает положение
      контейнера с учётом масштаба. Проверка: игра запускается, картинка
      на мониторе не изменилась ни на точку (масштаб равен единице).
- [ ] 2.2 Поправить `cellAtScreen`: делить экранное смещение на масштаб.
      Проверка: сквозная — постройка встаёт в ту клетку, по которой
      нажали, при масштабе, отличном от единицы.
- [ ] 2.3 Поправить `viewBounds` и `viewCorners`: половина окна делится
      на масштаб. Проверка: при уменьшенном масштабе объекты у края
      экрана отрисованы, а рамка обзора на миникарте выросла.
- [ ] 2.4 `resize` пересчитывает дефолтный масштаб от новой высоты
      контейнера, сохраняя кратность. Проверка: смена размера окна
      с двукратным приближением оставляет приближение двукратным.
      **После этой задачи ландшафт даёт десять клеток вместо четырёх** —
      посмотреть глазами и записать замер в описание коммита.

## 3. Жесты масштабирования

- [ ] 3.1 Колесо мыши над полем меняет кратность; точкой опоры служит
      курсор; `preventDefault` обязателен, слушатель неленивый
      (`passive: false`). Проверка: сквозная — колесо не прокручивает
      страницу, а меняет масштаб поля.
- [ ] 3.2 Вести в управлении список активных указателей вместо одного.
      Проверка: `controls.test.ts` — один указатель ведёт джойстик
      по-прежнему, все существующие сценарии свайпа целы.
- [ ] 3.3 Второй указатель прекращает джойстик, сбрасывает направление
      в «стоп» и включает щипок; расстояние между пальцами ведёт
      кратность, середина между ними служит точкой опоры. Проверка:
      `controls.test.ts` — после второго касания направление равно
      «стоп».
- [ ] 3.4 Отрыв одного из двух пальцев прекращает щипок и НЕ возобновляет
      джойстик. Проверка: `controls.test.ts` — направление остаётся
      «стоп» до следующего касания.

## 4. Выделение снимается включением режима

- [ ] 4.1 Завести в управлении `clearSelection()` и звать её из
      `setBuildKind`, `toggleNukeAim`, `toggleTargetAim`. Проверка:
      `controls.test.ts` — три случая, в каждом выделение снято
      и обработчику ушло `select(-1)`.
- [ ] 4.2 Сквозная проверка: выделить постройку, нажать плитку стены —
      окно сведений закрылось до указания клетки.

## 5. Раскладка: связь и меню уезжают поверх поля

- [ ] 5.1 На маленьком экране `.td-hud-headline` становится
      фиксированной под верхней полосой, фон прозрачный,
      `pointer-events` — только на кнопке меню. Проверка глазами:
      строка связи читается на светлой скале, нажатие мимо кнопки
      доходит до поля.
- [ ] 5.2 Уменьшить `--td-hud-top` на высоту этой строки: 144 → 114
      в портрете, 94 → 64 в ландшафте. Проверка: `tokens.test.ts`
      разбирает файл регулярным выражением — сверить, что разбор
      не сломался и числа те, что записаны.

## 6. Раскладка: нижняя полоса перестаёт быть полосой

- [ ] 6.1 `--td-hud-bottom` на маленьком экране становится нулём;
      `#hud-bottom` при свёрнутых характеристиках — фиксированный слой
      поверх поля, прозрачный, `pointer-events: none`, с включением
      событий на группах. Проверка: сквозная — нижний край поля совпал
      с нижним краем экрана.
- [ ] 6.2 Плитки заказа встают столбцом у правого края: постройки выше,
      юниты ниже. Проверка: сквозная — все шесть плиток видны целиком,
      верхний край группы юнитов ниже нижнего края группы построек.
- [ ] 6.3 Служебные кнопки — прокачка, генерал, ядерка, цель — встают
      блоком в левом нижнем углу поверх поля. Проверка: сквозная —
      середина каждой кнопки принадлежит ей самой.
- [ ] 6.4 Плитка в столбце теряет подпись и подсказку клавиши, оставляя
      значок и цену; фон плитки становится полупрозрачным. Проверка
      глазами: сквозь плитку виден объект под ней.
- [ ] 6.5 Убедиться, что открытая панель прокачки не сломалась: она
      по-прежнему фиксирована, со своим фоном, тремя рядами и закрывается
      нажатием мимо. Проверка: существующие сквозные проверки панели
      зелёные без правок.

## 7. Проверки раскладки и масштаба

- [ ] 7.1 Обновить сквозные проверки телефона: доля поля не меньше 85 %
      в портрете и 82 % в ландшафте; `#hud-bottom` больше не
      прямоугольник над полем.
- [ ] 7.2 Новая сквозная проверка: число видимых клеток по вертикали
      не меньше десяти в обеих ориентациях телефона.
- [ ] 7.3 Проверить `iso.test.ts` и `tokens.test.ts` на новых числах
      полос; при окне 1920 × 1080 видимая доля осталась в диапазоне
      25–55 %.

## 8. Замеры и глаза

- [ ] 8.1 Спросить у пользователя разрешение и снять замер кадров
      (`pnpm e2e:perf`) при уменьшенном масштабе. Записать цифру рядом
      с прошлой: порог 55 остаётся, и цифра решает, остаётся ли десять
      клеток окончательным числом. **Мерить только на тихой машине.**
- [ ] 8.2 Посмотреть глазами, не потерялась ли сетка земли при масштабе
      0,611. Потерялась — компенсировать толщину только у земли (она
      рисуется одной заливкой), но НЕ у скал: их перерисовка стоит
      1,3 секунды.
- [ ] 8.3 Посмотреть глазами, не мешает ли столбец заказа вести генерала
      свайпом от правого края. Мешает — обсудить с игроком, а не двигать
      столбец молча: место выбрано им.

## 9. Чего в этих задачах нет и почему

- **Проверки баланса не требуется.** Изменение не трогает ни правил
  игры, ни цен, ни дохода, ни силы юнита, ни темпа: `packages/sim`
  и `packages/ai` не меняются ни строкой. Записи в `balance/queue.json`
  не заводится.
- **Эталоны детерминизма не двигаются** по той же причине.
- **Архивация изменения** — решение пользователя, а не шаг реализации.
