## 1. Различение подготовки и обязательного обновления

- [ ] 1.1 Уточнить `supervisor/skills/implement.md` и защитить правило в `supervisor/config/transitions.test.mjs` одним атомарным коммитом (до 2 часов). В существующем шаге обновления явно ограничить три исхода слиянием `origin/main`; добавить блок подготовки по открытому PR, выбор способа из design/tasks, отмену дополнительного конфликтного слияния, проверку восстановления дерева и доступности способа, условное продолжение либо `failed` с конкретной причиной. Сохранить явные предусловия запуска, запрет неизвестных конфликтов вслепую, безусловное обновление main до правки и установки, отправку коммита слияния и правило следа. Номера шагов не менять. Дополнить проверки реального блока и отрицательные пробы в памяти по design, сохранив действующий сторож обновления. Проверка: `npx vitest run --root supervisor config/transitions.test.mjs`; `npx prettier --check supervisor/skills/implement.md supervisor/config/transitions.test.mjs`; ручная матрица всех семи строк design с записью фактического чтения инструкции в отчёте имплементации. В сценарии 0169 доступный файл после успешной отмены позволяет продолжить; конфликт main по-прежнему останавливает. Выполненный пункт отметить в этом tasks.md. Разрешённый состав коммита: только названные два рабочих файла и отметка выполнения в `openspec/changes/distinguish-pr-preparation-from-branch-merge/tasks.md`. Добавить все три пути явно, проверить имена и содержание индексированного diff, закоммитить и немедленно отправить; сразу открыть черновой PR по действующему скиллу. Проверить имена и содержание созданного коммита: только связанная правка и её отметка, без чужих изменений внутри разрешённых файлов.

Команды выполняются отдельно в назначенном дереве; `<дерево>` означает его абсолютный путь. Формы сверены с `supervisor/config/stage-settings.json`: `PowerShell(git -C:*)`, `PowerShell(npx vitest:*)` и `PowerShell(npx prettier:*)`. Подготовка зависимостей — по действующему этапу имплементации. Проверки и реализация не требуют слияния PR 126 или ветки 0169.

Проверка состава до коммита:

```powershell
git -C <дерево> add -- supervisor/skills/implement.md supervisor/config/transitions.test.mjs openspec/changes/distinguish-pr-preparation-from-branch-merge/tasks.md
git -C <дерево> diff --cached --name-only
git -C <дерево> diff --cached
```

Проверка созданного коммита после немедленной отправки:

```powershell
git -C <дерево> show --format=fuller --stat HEAD
git -C <дерево> show --format= --name-only HEAD
git -C <дерево> show --format= HEAD
```

Архивация, повторный запуск 0169, арена и ожидание CI в этот пункт не входят. Состояние CI отслеживает супервизор.
