## Порядок исполнения

Работать только в дереве 0250 после аудита. Освежить его отдельными вызовами `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main`; конфликт обрабатывать по правилам этапа. Каждый пункт — отдельный проверяемый коммит с отметкой в этом tasks.md и немедленным push. Формы команд сверены с `supervisor/config/stage-settings.json`: git -C, gh pr, openspec, node .matchlog/* и npx vitest разрешены.

В каждом коммите допустимы только `openspec/changes/bind-command-rationale-retry-prerequisite/verification.md` и `openspec/changes/bind-command-rationale-retry-prerequisite/tasks.md` с относящимися к пункту свидетельствами и отметкой. Добавлять оба пути явным перечнем. До коммита проверить имена и содержание `git -C <дерево> diff --cached --name-only` и `git -C <дерево> diff --cached`; после — `git -C <дерево> show --name-only --format=oneline HEAD` и `git -C <дерево> show HEAD`. Временная проба `.matchlog/0250-prerequisite.mjs` не должна присутствовать в индексе и коммите. Отсутствие пробы не разрешает посторонние изменения. Полный набор тестов, сборка, арена и ожидание CI не нужны.

## 1. Проверить действующий допуск

- [x] 1.1 За срок до 120 минут выполнить изолированную матрицу Decisions 2 на действующих модулях с ID 0181/0175 и PR 132. Пробу запустить `node .matchlog/0250-prerequisite.mjs`; дополнительно узко выполнить `npx vitest run --root supervisor lib/dependency-state.test.mjs lib/dependency-evidence.test.mjs`. Записать исходник пробы, SHA, входы, команды и полный вывод в verification.md. Проверка: все отрицательные случаи удерживают именно из-за зависимости без расхода attempts, положительный контроль разрешает штатное планирование, повтор не мутирует снимок, живые этапы/отчёты и postmortem не обходятся. Отметить пункт, проверить состав, закоммитить и сразу отправить `git -C <дерево> push -u origin HEAD`; открыть единственный черновой PR командой `gh pr create --draft --base main --title "Закрепить предусловие повторного implement 0181" --body-file <свой-файл-в-дереве>` по правилам implement.

## 2. Проверить канал и подготовить адресную передачу

- [ ] 2.1 За срок до 120 минут после подтверждённой поставки 0242 обновить своё дерево, сверить фактический контракт dependencyUpdates и доказательство ревизий принимающего супервизора/активных станций. Узко выполнить тесты поставленного адресного канала и сценарий 0181 из Decisions 2 на поддельном хранилище; точные найденные пути тестов и команды записать в verification.md. Зафиксировать сверенный payload Decisions 1 и инструкцию передачи свежей базы Decisions 4. Проверка: readback сохраняет обе записи и прежние данные, повтор идемпотентен, busy/конфликт/ошибка подтверждения не дают успех, устаревший старт не стирает условие и не тратит попытку; payload адресует ровно 0181. Отметить пункт только при подтверждённой готовности канала и успешных проверках; коммит двух разрешённых файлов и немедленный push. Если поставка или её подтверждение отсутствуют, оставить пункт открытым и вернуть question с рекомендацией завершить поставку 0242, без неподдерживаемого поля и без прямой записи доски.

## Передача и последующая приёмка

Последний отчёт implement включает dependencyUpdates из design только после выполнения 2.1. До независимого подтверждения это поручение, а не выполненная запись. Супервизор сохраняет и перечитывает карточку до перехода 0250. Review проверяет свидетельство по Decisions 3, включая обе новые записи и неизменность прежних зависимостей и метаданных. Отсутствие свидетельства не позволяет принять 0250; запрос идёт отчётом, не прямым доступом к доске.

Это не шаг ожидания CI или применения собственного отчёта. Положительная модель не разрешает реальный запуск: его ведёт супервизор на свежем снимке, а реализацию 0181 — отдельная назначенная сессия после проверки документов. Состояние postmortem и живые этапы руками не меняются.
