## Общий порядок

Каждый отмечаемый пункт — самостоятельный проверяемый коммит, не более двух часов, с немедленной отправкой. В назначенном дереве перед реализацией выполнить отдельными вызовами `git -C <дерево> fetch origin`, `git -C <дерево> merge origin/main`, `pnpm install --frozen-lockfile --prefer-offline`. Не менять main и чужие деревья. Шаги 1–4 проверяются подменёнными эффектами, без native-сессий и без запуска хозяина из sandbox. Игровые тесты, сборка, арена и полный verify не нужны.

## 1. Согласованное сохранение и проверка свидетельства

- [x] 1.1 Добавить `supervisor/lib/refresh-evidence.mjs` и `supervisor/lib/refresh-evidence.test.mjs`: versioned manifest до spawn, exclusive reservation, append/flush/seal, recovery корректного префикса, отдельные integrity/completeness/causalSufficiency и references. Сбор и его checker внедрить вместе; неизвестные поля не засчитывать. Проверка: `pnpm test:pipeline lib/refresh-evidence.test.mjs`. Нужны отрицательные контроли каждого indispensable поля, неверного хеша/порядка/пути, позднего prelaunch, reuse PID, смешанных сессий, legacy evidence, секретной канарейки, отказа записи до spawn и обрыва после первого результата. До двух часов. После коммита и push открыть собственный draft PR по правилам implement; его тело передать файлом.

## 2. Последовательность существующей диагностики

- [x] 2.1 Расширить `supervisor/lib/tool-diagnostics.mjs`, `supervisor/lib/tool-controls.mjs` и `supervisor/lib/tool-diagnostics.test.mjs` явным refresh-investigation: read/Git/read/Git одной сессии, два ограниченных последовательных запуска для сравнения fresh/later, потоковое сохранение primary events и учёт через прежние onStart/onResult. Если требуется удержать stdin до сохранения PID/creationTime, добавить узкий pre-input hook в `supervisor/lib/run-stage.mjs` и его проверки в `supervisor/lib/run-stage.test.mjs`; обычный startStage сохраняет поведение по умолчанию. Проверка: `pnpm test:pipeline lib/tool-diagnostics.test.mjs lib/run-stage.test.mjs lib/refresh-evidence.test.mjs`. Проверить первую и позднюю ошибку, отсутствие refresh event, несовпадение invocation, narrative-only success, finite budget, прерывание и отсутствие автоматического повтора; обычные controls Codex/Claude проходят прежние сценарии. До двух часов; коммит и немедленный push.

## 3. Наблюдение Windows без изменения прав

Техническое уточнение implement от 23.09.2026 записано в design, раздел
«Источник корреляции Windows»: проверенные stdout/helper log версии 0.155.1
не предоставляют требуемой связи refresh/PID/TID/effective token. Пункт 3.1
остаётся открытым до выбора реального источника по внешней статической описи
Windows-хозяина; подставной источник не считается реализацией. Лимит описи —
один заход до 30 минут, результат только новым файлом в `.matchlog/0370-refresh/`,
без native/model probes, tracing/debugger, остановки владельца или изменения
прав. Критерий остановки и требуемые доказательства определены в design.
Новые живые опыты до 4.3 по-прежнему запрещены. Независимый 4.1 выполнен раньше
3.1; 4.2–4.3 и 5.1 не отмечаются и host collector не объявляется готовым.

- [ ] 3.1 Реализовать `supervisor/lib/windows-refresh-observer.mjs`, при необходимости его локальную системную часть `supervisor/lib/windows-refresh-snapshot.ps1`, и `supervisor/lib/windows-refresh-observer.test.mjs`. Ограниченные read-only снимки target/parents и выбранных процессов: realpath/reparse/file identity, descriptor/owner/inheritance, PID/creationTime/image/hash/version, доступные effective-token сведения и read-only оценка ACL access с источником/ограничением. Наблюдатель вооружается до первого tool call, пропущенный короткоживущий helper даёт incomplete; токен хозяина или shell не заменяет refresh. Использовать подменённые системные ответы; не вызывать настоящий Windows helper на этом пункте. Проверка: `pnpm test:pipeline lib/windows-refresh-observer.test.mjs lib/refresh-evidence.test.mjs`: доступ запрещён, PID переиспользован, helper исчез, reparse/target изменился, token неизвестен, descriptor изменился штатным refresh, чужой процесс исключён, секреты не записаны, команд ACL-write/runas/setup нет. До двух часов; коммит и push.

## 4. Подключение и передаваемый маршрут хозяина

- [x] 4.1 Выделить callbacks бюджетного допуска/учёта 0244 в `supervisor/lib/diagnostic-accounting.mjs` и `supervisor/lib/diagnostic-accounting.test.mjs`, подключить их в `supervisor/lib/supervisor.mjs` с проверками `supervisor/lib/supervisor.test.mjs`. Сохранить действующие tokenAdmission/beginTokenLaunch/readCodexAnswer/commitTokenLedger, общий ledger и текущую политику лимитов; новая фабрика получает актуальное состояние владельца, не создаёт второй учёт. Проверка: `pnpm test:pipeline lib/diagnostic-accounting.test.mjs lib/supervisor.test.mjs lib/tool-diagnostics.test.mjs`: обычная диагностика сохраняет прежнее поведение, begin до spawn, новый admission после расхода, отказ записи не проглатывается и повтор не удваивает начисление. До двух часов; самостоятельный коммит и push, CLI ещё не публикуется.

- [ ] 4.2 Подключить collector/checker через `supervisor/bin/collect-refresh-evidence.mjs`, `supervisor/lib/refresh-diagnostic-runtime.mjs`, `supervisor/lib/refresh-diagnostics-contract.test.mjs`, `supervisor/refresh-diagnostics.md`. Одноразовый host owner принимает проверенный plan по контракту design 6.1–6.2, получает существующие guard/lock до чтения runtime, читает актуальные task/config/ledger и использует общую фабрику 4.1. Не импортирует побочные эффекты supervise и не запускает обычный цикл. Без доводов помощь, неизвестные/смешанные аргументы отвергаются до эффектов. Ввести чистый `--verify` с точным разрешением `PowerShell(node supervisor/bin/collect-refresh-evidence.mjs --verify:*)` в `supervisor/config/stage-settings.json`, без открытия host collection исполнителю; проверить в `supervisor/config/transitions.test.mjs`. Проверка: `pnpm test:pipeline lib/refresh-diagnostics-contract.test.mjs lib/diagnostic-accounting.test.mjs lib/tool-diagnostics.test.mjs lib/refresh-evidence.test.mjs lib/windows-refresh-observer.test.mjs config/transitions.test.mjs`. Через производственный entry/runtime с реальными lock и budget функциями проверить занятый/неизвестный lock, guard contention, budget denial, begin/result persistence и допуск второй сессии, отсутствие runtime reads до ownership, отсутствие incident/retry effects, неверный SHA/config/task, verify без запуска. Подмена только системных границ, не успешных callbacks. До двух часов; коммит и push.

- [ ] 4.3 Завершить управление прерыванием и возвратом владения в `supervisor/lib/refresh-diagnostic-runtime.mjs`, `supervisor/bin/collect-refresh-evidence.mjs`, `supervisor/lib/refresh-diagnostics-contract.test.mjs`, `supervisor/refresh-diagnostics.md`. Сохранить десятиминутный предел проб, partial results и usage до release; storage-blocked не отпускает несохранённое и не запускает новые пробы. После crash — существующее восстановление учёта по pending launch, без replay. Проверка: `pnpm test:pipeline lib/refresh-diagnostics-contract.test.mjs lib/diagnostic-accounting.test.mjs lib/supervisor-startup.test.mjs lib/supervisor.test.mjs`. Проверить реальные порядок flush/exit/release, ошибку записи результата с повторным сохранением того же launchId, cancellation, crash, конкурента-сторожа, защиту чужого lock и чтение нового расхода обычным runtime после передачи при сохранности чужих записей/отчётов. Негативные контроли early release и пропущенного gate должны падать. До двух часов; коммит и push. Живой сбор разрешён только после этого пункта; неполный CLI предыдущего коммита не передаётся хозяину для запуска.

До завершения 4.3 CLI из 4.2 предоставляет только помощь и `--verify`; `--collect` явно отвечает «маршрут ещё не готов» до любых эффектов. Производственная функция композиции проверяется через импорт с подменой системных границ. Включение host collection в 4.3 атомарно с проверками завершения/прерывания: промежуточный коммит остаётся работоспособным и не публикует небезопасный маршрут.

## Передача внешнего сбора после 4.3

В `supervisor/refresh-diagnostics.md` перенести точные команды и порядок design 6.2: предварительное сохранение контекста обычного владельца, пауза и доказанная завершённость/сохранённость работы до штатного `launch.mjs --stop`, подтверждение выхода, `--collect --plan` из отправленной оснастки, общие замки/учёт, сохранение usage, выход diagnostic owner и возврат прежнего home/config/provider. Назвать два запуска/восемь вызовов, таймауты, формат plan и критерии 6.3. Разрешение владельца уже дано; повторного согласования не требовать и не выполнять остановку внутри implement. Передать полный отправленный SHA оснастки, хеши её файлов, результаты проверок композиции и этот контракт в отчёте. Собственный source SHA после коммита указывается в отчёте, не требует самоссылочного SHA внутри коммита.

При отсутствии пригодного современного bundle сохранить работу и вернуть blocked по design, решение 6. Внешний штатный хозяин получает оснастку на отправленном SHA без ожидания вливания 0370. Не запускать host-only сбор из исполняющей sandbox через другую оболочку/обёртку, не поручать audit 0011 и не считать 0369 выполнением новой предпосылки. Не превращать создание заявки или наличие кода в выполнение 5.1. Новая заявка только после сверки фактического недостающего результата с доской; тот же результат у существующей карточки использовать по taskId. Это граница между сессиями, не шаг ожидания CI.

## 5. Приёмка современного результата для 0366

- [ ] 5.1 После внешнего результата проверить готовый bundle и записать `openspec/changes/capture-refresh-causal-evidence/validation.md`: абсолютные пути, SHA-256 и UTC каждого primary extract/manifest/supplement, source revision и реальные identities, результаты checker, таблицу сравнения четырёх гипотез. Проверка: `node supervisor/bin/collect-refresh-evidence.mjs --verify <абсолютный-путь-manifest>` в назначенном дереве и содержательное сопоставление первичных записей с design. Требуется complete/intact bundle и repair-supported либо experiment-supported с современными ссылками, пригодным контролем, переменной, инвариантами, разными предсказаниями, бюджетом и decision rule. Одиночные успехи/перечень неизвестных не закрывают пункт. Повреждённые, неполные или причинно недостаточные данные оставляют пункт открытым и требуют конкретного недостающего результата через действующую предпосылку; проверяющая сессия не запускает host collection сама. До двух часов; коммит validation и отметки tasks, немедленный push. Результат передать 0366 в отчёте, без правки её артефактов и без incidentVerification.

## Состав коммитов и финальная форма

Каждый пункт допускает только названные в нём рабочие пути плюс собственную отметку в `openspec/changes/capture-refresh-causal-evidence/tasks.md`. В пункте 2.1 изменения run-stage допустимы только для pre-input hook и его тестов. В пунктах 4.1–4.3 интеграция ограничена общими callbacks учёта, записью/проверкой свидетельства и владением диагностического режима; не менять политику инцидентов, счётчики попыток или размер лимитов. Запись фактического usage разрешена только штатными функциями единственного владельца.

Все пути добавлять явным перечнем: `git -C <дерево> add -- <свои-пути>`, при необходимости несколькими короткими вызовами. Перед коммитом проверять имена и содержание `git -C <дерево> diff --cached`; после — `git -C <дерево> show --stat HEAD` и `git -C <дерево> show HEAD`. В индексе и коммитах исключены `.matchlog/0370-refresh/`, `.pipeline/`, первичные внешние логи, секреты и чужие изменения; отсутствие этих путей не разрешает остальные посторонние файлы. После каждого коммита `git -C <дерево> push`; не копить хвост.

Узкий линт/формат changed JS и документации вне openspec — `npx eslint <свои-js-файлы>` и `npx prettier --check <свои-поддерживаемые-файлы>`. Проверить форму только своего изменения: `openspec validate capture-refresh-causal-evidence --strict`, `openspec status --change capture-refresh-causal-evidence`. Не добавлять шаг ожидания CI, архивации или вливания. PR и зелёный CI сами по себе не заменяют живое свидетельство пункта 5.1.
