## 1. Подтверждение CI в штатном переходнике

- [x] 1.1 За один атомарный коммит (до 2 часов) добавить `supervisor/lib/ci-confirmation.mjs`, подключить его только для противоречивых CheckRun из `readExternal` в `supervisor/lib/io.mjs` и одновременно добавить положительную регрессию и все отрицательные контроли в `supervisor/lib/io.test.mjs`. Перенести значимые фактические данные `evidence/pr-219-design.json` в независимую `supervisor/lib/testing/ci-pr-219.json`, сохранив оба противоречивых статуса; дополнительные ответы повторной сверки в тестах явно назвать моделируемыми. Реализовать весь алгоритм design: идентичность PR/repo/run/job, head SHA, актуальная попытка и более новые runs, все страницы и задания, прежние SUCCESS/SKIPPED, повторная сверка PR/runs, ограниченный бюджет и безопасные причины pending. Сохранить приоритет конфликта и обычный маршрут без дополнительных запросов. Отметить пункт в этом tasks.md в том же коммите.

  **Результат 09.09.2026:** 118/118 тестов io.test.mjs прошли; ESLint, Prettier и diff --check успешны. Обязательный pnpm test:pipeline выполнен дважды: последний результат 2409/2411, ошибки в посторонних report-delivery и watch-lifetime. Отдельный повтор io/report-store/report-delivery/watch-lifetime: 143/144, сохраняется только тайм-аут замены процесса watch-lifetime (известная задача 0284); остальные три файла зелёные. Первый прогон также встретил EPERM в файловых операциях report-store/watch-lifetime; запреты не обходились. Окончательная проверка общего набора остаётся CI собственного PR.

  **Проверка:** `npx vitest run --root supervisor lib/io.test.mjs`. Положительный контроль проводит сохранённый пример через настоящий `createIo().readExternal` с подставным запускателем: результат success; прежнее сведение без подтверждения — pending. Отдельные отрицательные сценарии: незавершённый workflow/job, отсутствующий conclusion/время, другой SHA, job/repo/PR не совпадает, новая попытка до и во время сверки, новый run при прежнем rollup, failure/cancelled/timed_out, другая работающая/красная проверка или workflow, отсутствие/дубликат job, неполная пагинация и отказ второй страницы, неизвестный тип, все skipped, повреждённый JSON/ошибка запроса на каждом этапе, исчерпанный бюджет, смена head/checks, CONFLICTING/UNKNOWN в первом и повторном ответах. Добавить положительный случай нескольких страниц и нескольких runs, чтобы защита не была безусловным отказом. Тесты проверяют итог и факт запросов подтверждения/повторной сверки. Отключение fallback ломает положительную регрессию; ослабление проверки SHA/attempt/полноты ломает соответствующие отрицательные контроли. Узкий линт: `npx eslint supervisor/lib/io.mjs supervisor/lib/ci-confirmation.mjs supervisor/lib/io.test.mjs`; формат: `npx prettier --check supervisor/lib/io.mjs supervisor/lib/ci-confirmation.mjs supervisor/lib/io.test.mjs supervisor/lib/testing/ci-pr-219.json`.

  **Различение конечных результатов:** в тех же тестах пункта 1.1 явно требовать `failure`, а не только отсутствие `success`, для завершённых FAILURE/CANCELLED/TIMED_OUT при подтверждённых полноте и актуальности. Положительная регрессия сохраняет четыре завершённых SKIPPED из PR 219 рядом с подтверждаемым SUCCESS и требует `success`. Отдельно требовать `pending` для действительно незавершённого задания без подтверждения исключения и для недоступного подтверждения. Эти контроли запрещают подмену всех результатов без SUCCESS общим pending.

  **Состав и доставка:** `git -C <дерево> add -- supervisor/lib/io.mjs supervisor/lib/ci-confirmation.mjs supervisor/lib/io.test.mjs supervisor/lib/testing/ci-pr-219.json openspec/changes/confirm-contradictory-ci-results/tasks.md`. Проверить имена и содержание индекса командами `git -C <дерево> diff --cached --name-only` и `git -C <дерево> diff --cached`; допустимы только реализация и проверки этого пункта плюс отметка/результаты проверок в собственном tasks.md. После коммита проверить `git -C <дерево> show --format=fuller --stat HEAD` и `git -C <дерево> show --format= --patch HEAD`, затем немедленно отправить `git -C <дерево> push -u origin HEAD` и открыть черновой PR по правилам implement. Этот пункт уже даёт самостоятельное исправление со всеми предохранителями.

## 2. Приёмка на исходном PR

- [x] 2.1 Отдельным коммитом (до 1 часа) сохранить результат однократного живого `readExternal({ links: { pr: 219 } }, 'ci')` из исправленного дерева в `openspec/changes/confirm-contradictory-ci-results/evidence/pr-219-verification.json` и отметить этот пункт с результатом проверки. Временный `.matchlog/confirm-ci-219.mjs` по схеме design только импортирует createIo/resolveConfig и выполняет штатное read-only чтение, сохраняет запросы и ответы без токенов; запуск `node .matchlog/confirm-ci-219.mjs`. Не вызывать цикл супервизора, не менять карточку 0052, PR 219 или его CI. Снимок проработки остаётся исходным свидетельством и не заменяется результатом реализации.

  **Результат 09.09.2026:** однократный живой readExternal вернул success за 7 read-only запросов. Противоречие сохранилось в GraphQL и REST; подтверждены PR 219, head 3ebe398ce704e377e5b6a16f4e30d3eef3f2553c, run 34276070514, attempt 1, все 7 заданий и повторная сверка. Проверенная реализация 03c382293b691a242377d9f3d977c85b78d6dec5. Полные ответы и коды сохранены в evidence/pr-219-verification.json; validate --strict и diff --check успешны.

  **Проверка:** артефакт содержит время UTC, SHA проверенной реализации, PR/head, run/attempt, полный состав проверок, подтверждающие ответы/коды и итог success на актуальном успешном CI. Если противоречие исчезло само, отдельно указать это в артефакте: живой success доказывает штатный путь, а разрешение прежнего противоречия доказывает регрессия пункта 1.1. Если PR, head или CI изменились и success не подтверждён — сохранить фактические сведения и вернуть причину по контракту implement, не отмечать пункт выполненным и не выдавать старый workflow за актуальный. Это однократная функциональная приёмка, не ожидание CI собственного PR. `openspec validate confirm-contradictory-ci-results --strict` и `git -C <дерево> diff --check` завершаются успешно.

  **Состав и доставка:** `git -C <дерево> add -- openspec/changes/confirm-contradictory-ci-results/evidence/pr-219-verification.json openspec/changes/confirm-contradictory-ci-results/tasks.md`. Проверить имена и содержание индекса и коммита теми же четырьмя командами пункта 1.1: допустимы только свидетельство и его отметка/результат. `.matchlog/confirm-ci-219.mjs` и иные временные файлы в индекс и коммит не входят. Немедленно отправить `git -C <дерево> push`.

Команды выполнять по одной из назначенного дерева. Формы сверены с `supervisor/config/stage-settings.json`: PowerShell(npx vitest:*), PowerShell(npx eslint:*), PowerShell(npx prettier:*), PowerShell(node .matchlog/*), PowerShell(openspec:*), PowerShell(git -C:*), PowerShell(gh pr:*), PowerShell(gh api:*). Тесты и живая приёмка относятся к implement; на design выполняется только валидация артефактов. Полные проверки выполняет CI, ожидание его результата не является пунктом tasks. Арена и игровые замеры не нужны, поскольку правила игры и отрисовка не меняются.
