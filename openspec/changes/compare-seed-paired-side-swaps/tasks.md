## 1. Разовое сравнение и доставка отчёта

- [x] 1.1 За один атомарный шаг (до 2 часов) прочитать две базы, проверить сопоставимость, сохранить полную таблицу пар и расчёты по design в `docs/analysis/side-swap-33105088891-33425774671.md`. Включить происхождение данных, воспроизводимый скрипт, матрицу 3×3, долю и направления смены исхода, g/l и парную разность побед с ошибкой, ограничения вывода. Вспомогательный скрипт разместить в `.matchlog/compare-seed-paired-side-swaps.mjs` собственного дерева и запустить `node .matchlog/compare-seed-paired-side-swaps.mjs`; базы открывать readOnly. Проверка: повторяемый вывод, контрольные случаи и независимый пересчёт матрицы из таблицы пар по разделу «Проверяемость результата» design; сумма матрицы равна числу пригодных пар, маргинальные суммы объясняют обе сводки и все исключения, Δ совпадает с разностью долей. При непригодности данных публиковать доказанный предел анализа, не выдумывать результат. Проверить формат командой `npx prettier --check docs/analysis/side-swap-33105088891-33425774671.md`. Отметить этот пункт выполненным и включить только документ и эту отметку в собственном tasks.md в коммит; после немедленной отправки открыть черновой PR по правилам implement.

Все команды выполняются из назначенного дерева, без склейки. Путь `.` в командах Git означает именно это дерево. Формы `PowerShell(git -C:*)`, `PowerShell(node .matchlog/*)`, `PowerShell(npx prettier:*)`, `PowerShell(gh run:*)` и `PowerShell(gh pr:*)` присутствуют в `supervisor/config/stage-settings.json`.

Перед коммитом добавить явным списком `git -C . add -- docs/analysis/side-swap-33105088891-33425774671.md openspec/changes/compare-seed-paired-side-swaps/tasks.md`. Проверить имена и содержание через `git -C . diff --cached --name-only` и `git -C . diff --cached`; после коммита — `git -C . show --stat --oneline HEAD` и `git -C . show --format=fuller HEAD`. Допустимы лишь анализ этой пары прогонов и отметка 1.1, никаких несвязанных правок. В индексе и коммите отсутствуют `.matchlog/compare-seed-paired-side-swaps.mjs`, базы SQLite и скачанные артефакты. Проверить отсутствие этих путей обоими diff, а не общим числом файлов.

Новый прогон арены, полный набор тестов, ожидание CI и архивация не являются задачами реализации этого изменения.
