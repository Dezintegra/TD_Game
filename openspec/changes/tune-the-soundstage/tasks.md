# Задачи

Каждая — отдельный коммит, отправляемый сразу. Порядок выбран так,
чтобы правки шли от самостоятельных к зависимым: панорама и отклик
ни от чего не зависят, музыка тянет за собой новый ассет и загрузчик.

## 1. Панорама меряется картой, а не окном

- [x] `placement.ts`: размах панорамы — шесть клеток, переведённых
      в экранные точки через проекцию; предел 0,8; поле `halfWidth`
      убрано из `Listener`
- [x] `bootstrap.ts`: перестать передавать ширину окна слушателю
- [x] `placement.test.ts`: тесты про узкое и нулевое окно заменены
      на «ближний бой слышен по сторонам» и «размах задан картой,
      а не окном»
- [x] `npx eslint` и `npx prettier --check` по своим файлам зелены

## 2. Отклик укорачивается до открытого поля

- [x] `reverb.ts`: хвост 1,4 → 0,45 с, ранние отражения ослаблены вдвое
- [x] `placement.ts`: доля отражений у дальнего 0,05 → 0,03
- [x] `placement.test.ts`: длина отклика проверяется порогом «не длиннее
      половины секунды», а не равенством прежней константе
- [x] `npx vitest run apps/client/src/audio/placement.test.ts` зелен

## 3. Ротор убавлен

- [x] `sounds.ts`: пик ротора 0,34 → 0,2
- [x] `assets.ts`: слой лопастей 0,33 → 0,22, срез низа 150 → 200 Гц
- [x] `sounds.test.ts`: ротор тише самого тихого события более чем вдвое

## 4. Пуск ракеты берётся записью

- [x] `assets/missile-launch.mp3` добавлен (5 КБ, тег снят)
- [x] `prepare.ts`: обрезка конца `takeSeconds`, порядок — после
      пересчёта скорости и до затухания
- [x] `prepare.test.ts`: обрезка оставляет начало нетронутым, а вместе
      с затуханием даёт короткий звук вместо длинного и чуть тише
- [x] `assets.ts`: `Sound.Missile` берётся записью, три варианта
      по скорости, `take` 0,62, `fadeFrom` 0,34

## 5. Музыка играет петлёй из записи

- [ ] `assets/music-loop.mp3` добавлен (447 КБ: 12 тактов при 129 BPM,
      четверть секунды тишины перед петлёй, 20 мс музыки после неё)
- [ ] `music.ts`: длина петли в секундах и её обоснование; расписание
      остаётся на месте как запасной путь
- [ ] `engine.ts`: загрузка петли, обрезка по числу отсчётов, замыкание
      стыка, зацикленный источник в шину музыки; расписание уводится
      в тишину, когда петля готова
- [ ] `music.test.ts`: длина петли считается числом отсчётов; проверка
      того, что расписание остаётся рабочим запасным путём

## 6. Спецификации и проверки

- [ ] Черновой PR открыт после первого коммита, все проверки CI зелены
- [ ] `openspec archive tune-the-soundstage` после слияния
