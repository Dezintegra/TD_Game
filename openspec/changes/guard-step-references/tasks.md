# Задачи

Проверка у всех шагов одна и та же, и другой быть не может: правки лежат
в `supervisor/`, а тесты конвейера живут своим корнем.

```powershell
pnpm test:pipeline
```

Голый `npx vitest` тут не годится: без ключа `--root supervisor` тесты
конвейера не находятся, и прогон зеленеет, ничего не прогнав.

Каждый шаг — отдельный коммит, отправляемый сразу. Первым идёт шаг 1:
после него имплементация уже вправе открыть черновой pull request, а
до открытия проверки CI на отправки в ветку не запускаются вовсе.

Весь код шагов ложится в `supervisor/config/transitions.test.mjs`, рядом
с `traceFormula`, `skillPrefix` и `FALSE_GROUND`. Отдельного модуля
не заводить: довод — в `design.md`, раздел «Где живёт разбор».

Числа, на которые опираются проверки шагов, сняты 04.09.2026 на вершине
`d839b24`. Разошлись — это находка, а не помеха: назови расхождение
в отчёте и разбери его, прежде чем править мерку.

## 1. Разбор шагов скилла и сторож нумерации

- [ ] Завести разбор нумерованных шагов файла правил: раздел, чей заголовок
      уровня `##` начинается со слова «Порядок», от него до следующего
      заголовка того же уровня; шагом считается пункт нумерованного списка
      верхнего уровня внутри этого раздела. Разбор обязан возвращать
      по каждому шагу номер, заголовок и текст до следующего шага.
- [ ] Назвать в пояснении, почему границы раздела обязательны: нумерованные
      списки стоят в этих файлах и вне порядка работы — у всех десяти
      в разделе «Отчёт», у `postmortem.md` ещё и в «Шести частых причинах»,
      — и разбор без границ считал бы шагами их тоже.
- [ ] Завести проверку «номера шагов идут подряд от единицы» по всем файлам
      `supervisor/skills/*.md`, называющую провинившийся файл вместе
      с полученным рядом номеров.
- [ ] Назвать в пояснении, что эта проверка сторожит не тексты, а сам
      разбор: сбившись на границе раздела или на отступе, он молча объявил
      бы часть ссылок битыми, а часть целыми.
- [ ] Завести пробу на порчу: разбор вымышленного текста, где номера идут
      с пропуском, обязан этот пропуск назвать.

**Проверка:** `pnpm test:pipeline` зелёный; разбор находит десять файлов
правил и в каждом ряд подряд идущих номеров — шесть шагов у `interpret.md`,
одиннадцать у `audit.md`, `benchmark.md`, `implement.md`, `postmortem.md`
и `review.md`, двенадцать у `deploy.md`, тринадцать у `design.md`
и `revise.md`, девять у `triage.md`.

## 2. Сторож существования номера

- [ ] Завести разбор ссылок вида «шаг N» во всех файлах `supervisor/`
      с расширениями `.md` и `.mjs`, считая все падежи слова и перечисления
      («шаги 4 и 5», «Строки шагов 5 и 9») — каждое число перечисления
      отдельной ссылкой.
- [ ] Нормализовать текст по пробелам **в пределах абзаца**, разделяя его
      по пустой строке. Назвать в пояснении обе отвергнутые крайности
      с живыми примерами: построчный разбор промахивается мимо межфайловой
      ссылки `design.md` («(`implement.md`,» на одной строке, «шаг 9)»
      на следующей) и сверяет не тот файл; склейка файла целиком принимает
      заголовок «### По шагам» и следующий за ним пункт «1.»
      из `supervisor/README.md` за ссылку.
- [ ] Определять адресата ссылки по имени файла правил в узком окне —
      семьдесят знаков до ссылки и шестьдесят после; окно вперёд нужно
      для формы «(шаг 9 `implement.md`)», окно назад — для формы
      «(`implement.md`, шаг 9)». Ссылка без имени внутри файла правил
      считается ссылкой на его собственный шаг.
- [ ] Назвать в пояснении, почему окно узкое: прототип с окном в тысячу
      двести знаков отнёс ссылку `audit.md` на его собственный шаг 6
      к `design.md` — только потому, что `design.md` упомянут абзацем выше.
- [ ] Отсечь два вида ссылок на чужие документы: составной номер («шаг 3.4»
      в `supervisor/lib/apply-report.test.mjs`) и «шаг N изменения `<имя>`»
      («шаг 2 изменения `point-the-form-note-at-a-live-rule`»
      в `design.md`). Назвать в пояснении, чем второе опасно: шаг 2
      у `design.md` есть, поэтому без отсечения сторож зеленел бы случайно,
      а при другом номере падал бы на верном тексте.
- [ ] Завести проверку «каждая ссылка указывает на существующий шаг»,
      называющую файл, номер и файл-адресат.
- [ ] Завести пробы на порчу на вымышленных текстах, по одной на решение:
      несуществующий номер назван; межфайловая ссылка, разорванная переносом
      строки, отнесена к названному файлу, а не к своему; заголовок
      с идущим за ним нумерованным списком ссылкой не признан; «шаг N
      изменения» и составной номер ссылками не признаны; перечисление
      «шаги 4 и 5» дало две ссылки, а не одну.

**Проверка:** `pnpm test:pipeline` зелёный — битых номеров в `supervisor/`
сегодня ноль; каждая проба на порчу падает при снятой мерке и проходит
при поставленной.

## 3. Ссылка вне файлов правил называет файл

- [ ] Выправить девять безадресных ссылок в
      `supervisor/config/permissions.mjs` — все они о `deploy.md`: «шаги 4
      и 5», «(шаг 7)» дважды, «шаг 7», «Шаги 4», «шагом 5», «шагов 5 и 9»,
      «шагов 4 и 7», «на шаге 5». Имя файла назвать в узком окне возле
      номера, а не одним упоминанием в начале блока: блок читают кусками,
      и упоминание за девять строк до ссылки её не адресует.
- [ ] Выправить пять безадресных ссылок в
      `supervisor/config/transitions.test.mjs`: «шага 2» и «шага 4»
      о `review.md`, «шаги 4 и 5» и «(шаг 7)» о `deploy.md`, «шагом 10»
      о `design.md` (там сегодня сказано «в скилле проработки» — назвать
      файл именем).
- [ ] Починить тем же коммитом найденное расхождение: пояснение о короткой
      выборке команд ревью говорит «(`gh pr checks` шага 2 в неё
      не входит)», а `gh pr checks` стоит в `review.md` шагом 3 — шагом 2
      там `gh pr view`. Ссылка становится «`gh pr checks` шага 3
      `review.md`».
- [ ] Завести проверку «ссылка вне файлов правил называет файл», называющую
      файл и окрестность безадресной ссылки.
- [ ] Назвать в пояснении, почему внутри файлов правил безымянная ссылка
      законна, а вне их — нет: у текста вне скиллов своего файла нет,
      и адресата не знает ни машина, ни читатель. Назвать там же
      отвергнутый вариант «пропускать безадресные»: он оставляет
      несторожёнными ровно те ссылки, которые ломаются молчаливее прочих,
      и единственное живое расхождение нашлось именно среди них.
- [ ] Завести пробу на порчу: безадресная ссылка в вымышленном тексте вне
      файлов правил названа, а такая же ссылка внутри файла правил — нет.

**Проверка:** `pnpm test:pipeline` зелёный; поиск по `supervisor/` не даёт
ни одной ссылки на номер шага вне `supervisor/skills/`, рядом с которой
не стоит имя файла правил; ссылки на `gh pr checks` шага 2 в репозитории
не осталось.

## 4. Сверка ссылки с содержимым шага — предупреждением

- [ ] Завести мягкую мерку: имя команды в обратных кавычках, стоящее в том
      же предложении, что и ссылка, сверяется с текстом названного шага.
      Предложение брать от ближайшей точки перед ссылкой; из имён исключить
      короче четырёх знаков и оканчивающиеся на `.md` — последние есть
      адресат, а не команда.
- [ ] Печатать расхождения перечнем — файл, номер, файл-адресат, команда —
      и набор проверок при этом НЕ ронять.
- [ ] Записать поимённо два известных расхождения, признанных ложными,
      каждое со своим доводом: `audit.md` («`links.change` … с найденным
      на шаге 6» — шаг 6 и есть проверка пересечений, а `links.change`
      берётся не из него) и `review.md` («отказ самого `gh pr merge`
      в шаге 9» — шаг 9 разбирает неудачу вливания, а сама команда стоит
      шагом 8).
- [ ] Назвать в пояснении замер, ради которого мерка мягкая: проба
      04.09.2026 по всей фактуре `supervisor/` дала два срабатывания, оба
      ложные, пойманных настоящих ноль. Назвать там же причину — ссылка
      говорит о шаге, а не цитирует его, — и цену падения: единственное
      лекарство от ложной тревоги есть ослабление мерки, и вместе с ложными
      она унесла бы настоящие.
- [ ] Завести проверку, что перечень известных расхождений не пуст
      и совпадает с тем, что мерка находит сегодня: разошёлся — это либо
      новое расхождение, либо мерка перестала работать, и оба случая надо
      увидеть.

**Проверка:** `pnpm test:pipeline` зелёный; в выводе прогона стоят ровно
два расхождения, оба из перечня известных; удаление любого из перечня
красит набор.

## Чего в этом списке нет намеренно

- **Шага «дождаться зелёного CI».** Закрыть его исполнителю запрещено
  по устройству, а ожидание проверок механизировано супервизором.
- **Прогона арены и замера кадров.** Правила игры, цены, доход, сила юнита
  и темп не тронуты: изменение правит проверки конвейера и комментарии
  в двух его файлах.
- **Правки `supervisor/config/stage-settings.json`.** Новых команд
  изменение не вводит; `pnpm test:pipeline` покрыт правилом
  `PowerShell(pnpm test:pipeline:*)`, а узкие проверки — `npx prettier:*`
  и `npx vitest:*`.
- **Правки самих шагов и их нумерации.** Правятся только ссылки на них.
