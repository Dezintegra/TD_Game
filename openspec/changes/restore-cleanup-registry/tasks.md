## 1. Восстановление реестра и регрессия штатной уборки

- [x] 1.1 Разделить условия `adopt-worktree` и `finish-claim` в `supervisor/lib/reconcile.mjs` и в том же коммите добавить регрессионные проверки в `reconcile.test.mjs` и `execute.test.mjs` (до 2 часов). Сохранить `NEEDS_WORKTREE` без `cleanup`; проверить своё существующее дерево, отсутствие владельца, чужого владельца, отсутствие дерева, основное и ручное деревья, отсутствие задачи и `closed`. На пустом изменяемом реестре провести настоящую цепочку `reconcile` → `repairWorld` → `execute(cleanup)` и проверить обе мерки безопасности (PR merged/open/unknown; без PR ownCommits 0/положительное/null), фактическое удаление перед `closed`, отсутствие создания дерева, сохранение записи и `cleanup` при отказе удаления дерева или ветки и успешный повтор после временного отказа дерева. Использовать таблицу design; не подменять `mayCleanup` и `cleanup`. Проверка: `npx vitest run --root supervisor lib/reconcile.test.mjs lib/execute.test.mjs lib/repair.test.mjs lib/cleanup.test.mjs lib/remove-worktree.test.mjs` — все названные файлы обнаружены, сценарии проходят; `npx eslint supervisor/lib/reconcile.mjs supervisor/lib/reconcile.test.mjs supervisor/lib/execute.test.mjs` и `npx prettier --check supervisor/lib/reconcile.mjs supervisor/lib/reconcile.test.mjs supervisor/lib/execute.test.mjs` — без ошибок. Включить отметку этого пункта в тот же коммит, отправить его и сразу открыть один черновой PR; после этого шага изменение уже имеет проверяемую реализацию.

Команды исполняются из назначенного дерева, по одной на вызов. Формы `npx vitest:*`, `npx eslint:*`, `npx prettier:*`, `git -C:*` и `gh pr:*` сверены с `supervisor/config/stage-settings.json`. Для Git использовать `git -C <дерево> …`; файлы добавлять явным списком вместе с этим `tasks.md`, отправлять сразу через `git -C <дерево> push -u origin HEAD`. Тело PR передавать файлом согласно правилам этапа реализации. Артефакты `openspec/` не требуют prettier.

Полный набор проверок остаётся CI этого PR. Опрос CI и последующие переходы выполняет супервизор. Игровые правила не меняются, заявка на арену не нужна.
