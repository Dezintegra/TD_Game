# Задачи

Каждый пункт — отдельный коммит, отправляемый сразу.

Порядок против записанного: скилл (4.1) заведён первым. Проверка связности
требует файла правил для каждого состояния из `NEEDS_SESSION`, и обратный
порядок дал бы красный коммит на ровном месте. Колонка доски (5.1) вошла
в 1.1 по той же причине.

## 1. Автомат состояний

- [x] 1.1 Добавить `postmortem` в `STATES` и `CROSSCUT`, в `NEEDS_SESSION`
      и в `STATE_CLASS` (класс `resource`). В `ROUTES` не объявлять:
      единственный выход — сквозной переход в `failed`. В `NEEDS_WORKTREE`
      не добавлять. Проверяемо: `transitions.test.mjs` — вход разрешён
      из каждого рабочего состояния, выход разрешён только в `failed`,
      из `postmortem` в `implement` отказ
- [x] 1.2 Наследовать состояние возврата при переходе из сквозного
      в сквозное (`applyTransition`) и обнулять счётчики попыток при входе
      в сквозное. Проверяемо: `task-file.test.mjs` — путь
      `implement → postmortem → failed` даёт `returnTo: 'implement'`,
      а исчерпанные продолжения обнуляются на входе в разбор

## 2. Пути остановки

- [x] 2.1 Ввести в `apply-report.mjs` расчёт состояния остановки
      (`postmortem`, а для задачи в разборе — `failed`) и провести через
      него все пять точек `applyReport` и точку `applyExternal`.
      Проверяемо: `apply-report.test.mjs` — шесть случаев ведут в разбор,
      те же шесть на задаче в разборе ведут в `failed`
- [x] 2.2 Провести через тот же расчёт `failStage` и отменённую уборку
      в `execute.mjs`, забывая при переводе прежнюю сессию разбора.
      Проверяемо: `execute.test.mjs` — задача уходит в разбор,
      `forgetSession` позван с этапом `postmortem`
- [x] 2.3 Дать разбору срок в `stageTimeoutMinutes` и пустой перечень
      в `STAGE_NEEDS`. Проверяемо: `defaults.test.mjs` — срок назван,
      `missingForStage` для разбора пуст

## 3. Лог упавшего этапа

- [x] 3.1 Добавить в супервизор чтение лога этапа и выдержку из него
      (голова, хвост, число пропущенных знаков). Проверяемо:
      отдельный тест выдержки — короткий лог не режется, длинный содержит
      обе части и число пропущенного
- [x] 3.2 Класть выдержку и путь к логу в промпт назначения разбора,
      беря этап из `returnTo`; при отсутствии лога писать об этом.
      Проверяемо: `stage-prompt.test.mjs` — раздел есть у разбора
      и отсутствует у прочих этапов

## 4. Скилл разбора

- [x] 4.1 Написать `plugins/pipeline/skills/postmortem.md` по образцу
      `triage.md`: что дано, порядок работы, чего делать нельзя, поля
      отчёта, два образца — с заявкой и без. Проверяемо: файл читается,
      имя совпадает с состоянием, запуск этапа находит его по пути

## 5. Доска и документы

- [x] 5.1 Добавить колонку «Разбор ошибки» в настройку колонок и в проверку
      устройства доски, поставив её перед «Ошибкой». Проверяемо:
      `board-setup.test.mjs` — нехватка колонки названа
- [x] 5.2 Добавить `postmortem` в `manage/schema.json` и поправить в
      `validate-card.mjs` подсказку, называющую число состояний неверно.
      Проверяемо: `validate-card.test.mjs` — карточка в колонке разбора
      проходит проверку
- [x] 5.3 Описать разбор ошибки в `manage/README.md`: колонка, что она
      значит для владельца продукта, откуда берутся кандидаты по итогам
      разбора. Проверяемо: раздел про колонки не противоречит настройке

## 6. Повторная причина и блокирующая причина

- [ ] 6.1 Разобрать дополнения отчёта (`planAmendments`): задача есть,
      не закрыта и не остановлена, не она сама, фактура непуста.
      Проверяемо: `requests.test.mjs` — годное принимается, каждый из
      четырёх отказов назван причиной и не отменяет остальных
- [ ] 6.2 Завести в обоих хранилищах дописывание фактуры в журнал задачи
      (`amendTask`): файловое коммитит журнал, доска пишет комментарий.
      Проверяемо: `io.test.mjs` — запись уезжает своим коммитом, состояние
      задачи не тронуто
- [ ] 6.3 Применять дополнения при переносе отчёта, связывая источник
      с дополненной задачей. Проверяемо: `execute.test.mjs` — журнал
      дополнен, колонка не менялась, ссылка проставлена
- [ ] 6.4 Слушать признак блокирующей заявки только у разбора ошибки:
      такая заявка заводится в `new`, прочие — в `candidate`.
      Проверяемо: `requests.test.mjs` — заявка разбора с признаком встаёт
      в очередь, та же заявка от разбора замечаний — в кандидаты
- [ ] 6.5 Описать в скилле разбора оба хода: дополнение вместо дубликата
      и мерку блокирующей причины. Проверяемо: образцы отчёта содержат
      `amendments` и `blocking`
