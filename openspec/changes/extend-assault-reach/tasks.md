# Задачи

Прогон арены шагом не делается: он живёт отдельной задачей бэклога.
Заведённая под него `0062` сейчас в состоянии `failed` — упала оттого,
что была заказана до появления кода, — поэтому прогон заказывается
заново ПОСЛЕ вливания pull request, вместе с ожиданием из `design.md`,
раздел «Что и как мерить ареной». Об этом заведена заметка `0090`,
и потерять ожидание вместе с `0062` она и не даёт.

Полный набор проверок локально не гоняется — это работа CI
(правило 6 в `CLAUDE.md`); здесь названы только точечные прогоны
по своим файлам.

Перечень задетых мест собран на этапе проработки чтением, а не по
красноте CI, и записан в `design.md`, раздел «Что стерегло прежние две
клетки». Пункты ниже ссылаются на него, а не пересказывают.

## 1. Число и всё, что от него выведено

- [ ] 1.1 В `packages/shared/src/balance.ts` заменить
      `BASE_UNIT_RANGE_CELLS = 2` на `4`. В комментарии к постоянной
      записать три вещи: что величина заказана владельцем продукта;
      что живой потребитель у неё ровно один — штурмовик, и второго
      заводить нельзя (как это уже разъезжалось со снайпером, сказано
      в комментарии к нему); что преимущество базовой башни падает
      с трёх клеток до одной и держится теперь на одной.

      Тем же коммитом — всё, что от числа выведено. Порознь эти правки
      не проверяемы: пока они не сделаны, дерево красное, откат первого
      коммита не возвращает работоспособного состояния, и CI на пуше
      перестаёт что-либо значить (правило 4).

      **Четыре стража требования** — в `packages/shared/src/balance.test.ts`
      три первых, в `packages/sim/src/step.test.ts` четвёртый:

      - дальность штурмовика равна `cellsToUnits(4)`;
      - дальность базовой башни строго больше дальности штурмовика —
        именно соотношением, а не сравнением с записанным числом;
      - дальности снайпера и Теслы строго больше дальности штурмовика.
        Сценарий спеки «Дальнобойные типы перестреливают штурмовика»
        сегодня не закрыт ничем: соседние проверки
        (`balance.test.ts:266`) сравнивают обоих с ГЕНЕРАЛОМ, а не
        со штурмовиком, и отрыв, ужавшийся с четырёх клеток до двух,
        не стережёт никто;
      - **прокачка дальность штурмовика не двигает.** Существующая
        проверка `balance.test.ts:469` говорит о другом — что в таблице
        веток у штурмовика нет ветки дальности (`rangeBranch === -1`);
        это состав таблицы, а не следствие, и сценарий спеки «Прокачка
        не двигает дальность штурмовика» ею не закрыт. Нужен страж,
        который покупает по уровню КАЖДОЙ ветки `UpgradeTarget.UnitAssault`
        (перебором `UPGRADE_BRANCHES` по полю `target`, а не списком
        индексов) и убеждается, что
        `playerStats(player).units[UnitType.Assault].range` осталась
        равной `cellsToUnits(4)`. Место — рядом с уже существующим
        `step.test.ts:437` («дальность снайпера растёт от прокачки,
        а штурмовика — нет»): там же лежат `playerStats`, `buy` и
        `richWorld`. Страж обязан покраснеть, если штурмовику завести
        ветку дальности, — проверить это временной вставкой ветки
        и результат записать в тело коммита.

      **Прикрытие генерала** — `packages/ai/src/profile.ts:486`
      и `packages/ai/src/profile.match.test.ts:67`. Формула `escortRadius`
      верна и правки не требует, а вот оба разбора рядом с ней врут уже
      сейчас: в `profile.ts` написано «при нынешних двух клетках у обоих
      получается четыре» (генерала давно перевели на пять, выходит семь),
      в тесте — «пять клеток у генерала плюс две у штурмовика дают семь».
      Ожидание `cellsToUnits(7)` в `profile.match.test.ts:74` меняется
      на `cellsToUnits(9)`, оба комментария переписываются под пять
      плюс четыре.

      **Оба эталона** — `packages/sim/src/determinism.golden.match.test.ts`
      и `packages/ai/src/profile.golden.match.test.ts`. У первого, кроме
      суммы (`GOLDEN_CHECKSUM`), в том же файле лежат наблюдаемые величины:
      два пуска и два подрыва, снесённая постройка, ветеранский ранг
      не ниже первого, непустые списки построек и юнитов. Разойдись любая
      из них — это не «пересъёмка эталона», а находка, и её надо
      объяснить, а не переписать. У второго к новому числу дописывается
      разбор шести наблюдаемых величин (`structures`, `units`,
      `upgradeLevels`, `energy`, `commands`, `furthestFraction`): какие
      сдвинулись и чем это объясняется. Так заведено в этом файле
      с самого начала.

      **Стенды, где две клетки были не комментарием, а расстановкой** —
      `packages/sim/src/step.test.ts`, три разбора подряд: «остановка
      юнита на противнике» (`:1256`), «огонь на ходу» (`:1335`)
      и «остановка юнита на стреляющей постройке» (`:1466`). Во всех трёх
      `THEIRS = cellIndex(20, 22)` выбрана КАК ГРАНИЦА дальности —
      «ровно две клетки, то есть впритык», — и на четырёх клетках
      окажется глубоко внутри радиуса: разборы останутся зелёными,
      перестав стеречь то, ради чего заведены. Расстояние вывести
      из `UNIT_STATS[UnitType.Assault].range`, а не переписать
      с двойки на четвёрку: следующая правка дальности не должна
      повторять эту работу. Вместе с ним поедут два соседа, у которых
      расстояние тоже осмысленное: стена между стрелком и башней
      (`:1524`, обязана остаться МЕЖДУ ними) и снайперская башня
      «вне радиуса юнита» (`:1543`, обязана остаться ВНЕ его). Оба
      выразить относительно того же выведенного расстояния.

      **Канарейка темпа** — `packages/ai/src/siege.match.test.ts`.
      Он держится на ДЛИНЕ матча на зерне 31337: осадный профиль
      успевает купить Теслу только потому, что этот мир единственный
      из пяти, где матч тянется до 8:24 (замер зондом записан таблицей
      в шапке файла). Подъём дальности штурмовика — правка темпа, и по
      этому файлу она бьёт первой: покраснение здесь ожидается, а не
      случается. Значит и лечится оно ТЕМ ЖЕ коммитом, наравне
      с эталонами: то и другое — одного вида collateral, «правка правил
      сдвинула наблюдаемое», и откладывать его на отдельный шаг значит
      оставить дерево красным между коммитами (правило 4).

      Красное разбирается по различию, а не подгонкой, и различие это
      двух родов:

      - короткий матч и не купленная Тесла — «окно кончилось». Лечится
        выбором другого зерна: прогнать те же пять миров зондом, взять
        тот, где осадный профиль доживает до покупки, и переписать
        таблицу в шапке новым замером — старая после правки говорит
        о несуществующем мире. Замер и есть довод, что это выбор стенда,
        а не подгонка;
      - не случившийся рывок роя (`:206`) при матче ПРЕЖНЕЙ длины —
        это уже поведение, а не окно. Сменой зерна не лечится: искать
        надо, что именно в решении профиля сдвинулось.

      Оба ожидания проверяются прямо — «осадный профиль заказывает
      Теслу» (`:168`) и «роевой профиль совершает рывок» (`:206`).

      _Проверка:_ точечные прогоны зелёные —
      `npx vitest run packages/shared/src/balance.test.ts`,
      `npx vitest run packages/sim/src/step.test.ts`,
      `npx vitest run packages/sim/src/combat.test.ts`,
      `npx vitest run packages/sim/src/targeting.test.ts`,
      `npx vitest run packages/ai/src/profile.match.test.ts`,
      `npx vitest run packages/sim/src/determinism.golden.match.test.ts`,
      `npx vitest run packages/ai/src/profile.golden.match.test.ts`,
      `npx vitest run packages/ai/src/siege.match.test.ts`.
      Три последних — матчевые и идут минутами; это та узкая работа,
      без которой эталон не переснять и канарейку не проверить,
      а не прогон набора.

## 2. Черновой pull request

- [ ] 2.1 Открыть черновой pull request на `main` сразу после первого
      коммита: `gh pr create --draft --base main --title ... --body-file ...`.
      Раньше этого проверки CI не запускаются вовсе — пуши в ветку
      без pull request не запускают ничего.
      _Проверка:_ `gh pr checks <номер>` показывает запущенные проверки.

## 3. Поведение у базы

- [ ] 3.1 Тест в `packages/sim`: штурмовик, идущий к базе противника,
      назначенной общей целью, останавливается, едва до края её
      основания остаётся четыре клетки, и наносит базе урон с этого
      расстояния. Класть рядом с разбором «остановка у назначенной цели»
      (`step.test.ts:1566`): там уже есть и `targetStructure`, и мерка
      `squaredDistanceToFootprint`, которой это правило и меряется.
      _Проверка:_ тест зелёный; и он же обязан покраснеть, если временно
      вернуть постоянной двойку — прогнать оба раза, четвёрку вернуть,
      результат обеих проверок записать в тело коммита. Тест, который
      проходит и без правки, стережёт не то, ради чего заведён.

## 4. Замысел и подписи

Отдельным коммитом: правки здесь не меняют поведения, дерево зелено
и до них, и после.

- [ ] 4.1 `docs/game-design.md`: в 5.1 утверждение «снайпер втрое
      сильнее и втрое дальше бьёт живых» перестало быть правдой —
      шесть клеток против четырёх это в полтора раза. Исправить
      и назвать новую дальность штурмовика там же; в разделе 8 оставить
      причину, по которой ветки дальности у него по-прежнему нет.
      _Проверка:_ поиск по документу не находит утверждений о
      трёхкратной дальности снайпера; `npx prettier --check docs/game-design.md`
      зелёный.
- [ ] 4.2 Три утверждения о двух клетках, живущие комментариями.
      Все три зелены и после правки — потому и вынесены сюда, — но
      каждое сообщает читателю неправду о действующем правиле:
      - `apps/client/src/game/machines.ts:476` — «две клетки — то, что
        делает его дешёвым мясом ближнего боя»;
      - `apps/client/src/game/machines.test.ts:227` — «две клетки — то,
        что отличает его от дальнобойных типов»;
      - `apps/client/src/game/stat-rows.test.ts:173` — то же слово
        в слово.

      Переписать под четыре клетки, сохранив довод: отличает штурмовика
      от дальнобойных не число, а то, что число это МЕНЬШЕ и не растёт
      от прокачки.
      _Проверка:_ `npx vitest run apps/client/src/game/machines.test.ts`
      и `npx vitest run apps/client/src/game/stat-rows.test.ts` зелёные;
      `npx eslint apps/client/src/game/machines.ts` зелёный.

## 5. Матчевые проверки, чувствительные к темпу

- [ ] 5.1 Записать разбор различия по осадному матчу. Сам файл к этому
      шагу уже зелен: и прогон, и лечение зерном сделаны пунктом 1.1 —
      здесь остаётся объяснить, ЧТО сдвинулось, потому что цифра без
      объяснения через месяц читается как подгонка. В описание
      pull request отдельным разделом: длина матча и время покупки Теслы
      до и после (числа из шапки файла), менялось ли зерно и почему,
      состоялся ли рывок роя. Если ничего не сдвинулось — записать
      и это: «канарейка темпа не шелохнулась» тоже вывод, и вывод
      неожиданный, раз правка темпа была.
      _Проверка:_ раздел в описании pull request есть, и всякое
      названное в нём число совпадает с тем, что лежит в шапке
      `siege.match.test.ts`.
- [ ] 5.2 Тем же порядком — три оставшихся матчевых разбора, которые
      играют настоящий матч и потому чувствительны к темпу:
      `packages/ai/src/opponent.match.test.ts`,
      `packages/ai/src/islands.match.test.ts`,
      `packages/netplay/src/convergence.match.test.ts`. Ожидание такое:
      первые два опираются на дальность башни, а не юнита, третий сверяет
      два мира между собой и от баланса не зависит вовсе, — то есть все
      три должны остаться зелёными. Пункт нужен именно затем, чтобы это
      было проверено, а не предположено.
      _Проверка:_ три точечных прогона зелёные. Любое красное — находка:
      объяснить в теле коммита, что именно сдвинулось, и чинить
      по существу.

## 6. Сверка с требованием

- [ ] 6.1 Пройти по пяти сценариям дельта-спецификации и убедиться,
      что у каждого есть проверка, а не мнение: четыре клетки; башня
      бьёт дальше штурмовика (соотношением); прокачка дальность
      не двигает; снайпер и Тесла перестреливают штурмовика; по базе
      штурмовик стреляет с подхода.
      _Проверка:_ у каждого сценария назван файл и имя теста; список
      попадает в описание pull request.
