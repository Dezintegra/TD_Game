## 1. Воспроизводимый разбор исторических повторов

- [x] 1.1 За один независимо проверяемый коммит (до 2 часов) подготовить `results.md` и `queries.md` по design: проверить паспорта двух SQLite, восстановить точные счётчики и поматчевые последовательности, разложить разницу повторов по общему горизонту и хвостам, сопоставить обе обороны и доступные ограничения покупок, сформулировать диагноз с границами наблюдаемости. Проверка: два запуска `node .matchlog/0108-tesla-repeat-orders.mjs` в назначенном дереве дают одинаковые числовые таблицы и порядок; SHA256 входов до/после совпадают; простые независимые GROUP BY по command воспроизводят суммы поматчевой таблицы и тождество C=I+R, а сумма префикса и хвоста воспроизводит полную разницу. Исходные 60/60, 110/92, 27/28 либо подтверждены, либо расхождение объяснено фильтрами/данными. Ручная сверка выбранных трёх миров с исходными строками подтверждает тики и ограничения; полный текст вычислений, команды, версия Node и результаты проверок сохранены в queries.md/results.md. Если скрипт не нужен, сохранить эквивалентный полностью воспроизводимый путь расчёта и повторить его дважды.

Результат первого и единственного шага уже пригоден для чернового PR: исследование и его доказательства едут вместе. Самостоятельно проверяемого промежуточного исправления игрового кода здесь нет. После коммита немедленно отправить ветку и открыть черновой PR по правилам implement; ожидания CI в задачах нет.

Разрешённые пути рабочего коммита перечислены полностью:

- `openspec/changes/explain-siege-tesla-repeat-orders/results.md`;
- `openspec/changes/explain-siege-tesla-repeat-orders/queries.md`;
- `openspec/changes/explain-siege-tesla-repeat-orders/tasks.md` — только отметка фактически выполненного шага.

Добавить эти пути явным списком через `git -C . add -- <пути>`. До коммита проверить `git -C . diff --cached --name-only`, `git -C . diff --cached` и `git -C . diff --cached --check`; после — `git -C . show --stat HEAD` и `git -C . show --format=fuller HEAD`. Проверяется и состав путей, и содержание: без несвязанных правок даже внутри разрешённых файлов. `.matchlog/0108-tesla-repeat-orders.mjs`, его временные выходы и обе исходные базы не входят ни в индекс, ни в коммит. Существующий siege.mjs в основном дереве не изменять. Команды выполнять отдельными вызовами из назначенного дерева; форма `git -C .` и `node .matchlog/*` сверена с supervisor/config/stage-settings.json.

Проверить `openspec validate explain-siege-tesla-repeat-orders --strict` и `openspec status --change explain-siege-tesla-repeat-orders`: допустима только единственная ошибка отсутствия дельт при отсутствии каталога specs и сохранённом содержательном обосновании в proposal. Ожидаемый status — 3/4, незавершён только specs. Новых матчей, тестов игры, полного verify, сборки или замеров этот шаг не запускает.
