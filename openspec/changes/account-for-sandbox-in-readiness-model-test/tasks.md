## 1. Исправить проверку модели после подготовки

- [ ] 1.1 В `supervisor/lib/stage-model.test.mjs` параметризовать существующий тест готовности значениями `win32` и `linux`; возвращать успешный маркер для подготовки `sandbox`, отдельно находить единственную команду `exec`, проверять единственный `--model` и значение `gpt-5.6-terra`. Проверить последовательности `sandbox → exec` и `exec`, отсутствие модели у подготовки; результат модельной заглушки оставить неуспешным, как сейчас. Выполнить узкие проверки ниже, отметить этот пункт и включить отметку вместе с тестом в один коммит, сразу отправить его и открыть черновой PR по правилам имплементации. Оценка: до 1 часа. Ожидаемый результат: оба случая платформ проходят на текущей машине, а отсутствие `exec`, удаление/дублирование `--model` или другая модель нарушают явные утверждения теста; остальные проверки файла сохраняются.

Команды проверок выполняются из назначенного дерева отдельными вызовами. Они покрыты `PowerShell(npx vitest:*)`, `PowerShell(npx eslint:*)` и `PowerShell(npx prettier:*)` в `supervisor/config/stage-settings.json`:

```powershell
npx vitest run --root supervisor lib/stage-model.test.mjs
```

```powershell
npx eslint supervisor/lib/stage-model.test.mjs
```

```powershell
npx prettier --check supervisor/lib/stage-model.test.mjs
```

В diff реализации допустимы только тестовый файл и отметка этого пункта. Рабочий код readiness и конфигурация не меняются. Подготовку зависимостей выполняет этап имплементации по своим правилам. Полные проверки выполняет CI; ожидание их результата ведёт супервизор. Арена не нужна: игровые правила не затронуты.
