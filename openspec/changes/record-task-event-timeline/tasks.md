## 1. Журнал задачи

- [x] 1.1 (до 2 часов) Добавить безопасный дописываемый JSONL-журнал по полному ID задачи и преобразование событий Codex/Claude в ограниченные записи действий и ошибок. Проверить отдельными тестами порядок запусков, границы полей, отсутствие сырого вывода, отказ записи и сохранение записей после повторного открытия файла. Запустить узкий Vitest, ESLint, Prettier и `git diff --check`; отметить пункт и отправить атомарный коммит.

## 2. Подключение

- [x] 2.1 (до 2 часов) Подключить запись к рождению/отказу процесса, потоковым событиям и завершению этапа; сбой записи назвать в журнале супервизора, не меняя исход задачи. Добавить проверку обрыва до финального stage log и описание пути в `supervisor/README.md`. Запустить `pnpm test:pipeline`, узкий ESLint/Prettier, `openspec validate record-task-event-timeline --strict` и `git diff --check`; отметить пункт, отправить коммит и дождаться зелёного CI PR.
