# Задачи

Каждый пункт — отдельный коммит, отправляемый сразу.

Тесты конвейера обычным `vitest` не видны, у них свой корень:
`npx vitest run --root supervisor <файл>`.

## 1. Правило

- [ ] 1.1 Завести изменение OpenSpec: предложение, замысел, дельта
      спецификации `pipeline-task-routing`, задачи. Проверяемо:
      `openspec validate --changes --strict` зелёный

## 2. Часть дробления заводится в очередь

- [ ] 2.1 Ставить заявке этапа дробления рабочее состояние вместо
      `candidate` в `taskFromRequest` (`supervisor/lib/requests.mjs`):
      область `pipeline` даёт `maintenance`, прочие части — `new`.
      Проверяемо: `requests.test.mjs` — часть заводится в `new`, часть
      с областью `pipeline` в `maintenance`, часть проходит схему
      бэклога, а находка попутного этапа остаётся кандидатом

## 3. Документы

- [ ] 3.1 Сказать в `manage/README.md` и `supervisor/skills/decompose.md`,
      что части раздроблённой задачи одобрения не ждут. Проверяемо:
      `npx prettier --check` по обоим файлам зелёный
