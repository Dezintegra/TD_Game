## 1. Доставка полного вердикта

- [x] 1.1 (до 2 часов) В `supervisor/lib/stage-prompt.mjs` выделить последнюю полную запись `audit → design` либо последнюю из `review → revise`/`pr → revise` из уже склеенного журнала и показать её отдельным разделом, только когда она вытеснена из ограниченного хвоста. При отсутствующем конечном report-маркере сообщить о неполноте без выдачи тела за полный вердикт. В `supervisor/lib/stage-prompt.test.mjs` проверить 0372-подобный длинный журнал, несколько циклов, audit, PR, уже видимый вердикт, отсутствующий маркер и склеенный по частям текст. Проверить узким Vitest, ESLint, Prettier и `git diff --check`; отметить пункт, коммитить и отправить только свои файлы, затем открыть один draft PR.

## 2. Правила исполнителя и подтверждение

- [x] 2.1 (до 1 часа) Согласовать `supervisor/skills/design.md` и `supervisor/skills/revise.md` с отдельным разделом последней доступной записи и обязательной проверкой её актуальности; журнал и review.log не подменять. Повторить узкий тест промпта, проверки форматирования/линта затронутых файлов и `openspec validate keep-current-return-verdict --strict`; проверить отсутствие незапушенных коммитов, результат CI точного PR-коммита и задокументировать ограничения.
