## 1. Исправление инструкции запуска

- [x] 1.1 Обеспечить отсутствие строки `[Console]::OutputEncoding = [Text.Encoding]::UTF8` в командном блоке шага 9 `supervisor/skills/deploy.md` по двум случаям ниже, сохранив остальные строки и пояснения текущей базы. Не менять `scripts/deploy.mjs`, разрешения, порты и рабочий каталог; не возвращать `Push-Location`, если его уже убрали 0252 или 0169. Отметить этот пункт выполненным в том же коммите и добавить непосредственно под ним краткую запись фактически выполненной проверки. Время: до 30 минут. Проверка: в исполняемом блоке шага 9 нет `OutputEncoding` и `Text.Encoding`, прямой вызов сценария с прежними ключами сохранён; `npx prettier --check supervisor/skills/deploy.md` и `git -C <дерево> diff --check` завершаются успешно. Объясняющие упоминания .NET из 0169 не удалять. Новые тесты и запуск выкладки не нужны.

  Проверено 07.09.2026: после fetch и merge база HEAD — `68874fdc29365556c815ab9c7834a11b57fb2e10`, строка присутствовала. Удалена только она; просмотр diff подтвердил отсутствие `OutputEncoding` и `Text.Encoding` в блоке и сохранность `node scripts/deploy.mjs --ref <хеш> --no-perf` и соседних строк. Prettier и `git -C . diff --check` прошли; `pnpm test:pipeline` — 60 файлов, 1602 теста успешно. Валидатор OpenSpec выдал только допустимую ошибку отсутствия дельт. Выкладка не запускалась.

  Перед выбором случая выполнить отдельными вызовами `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main` по правилам имплементации. Основной порядок общей правки: 0253 удаляет строку и вливается первой, 0169 (`document-permission-independent-guards`, пункт 1.1) сохраняет результат согласно её существующему условию не восстанавливать исправленные соседями строки. Описать этот порядок в PR и отчёте; чужие артефакты не править.

  Если строка присутствует — удалить только её; записать проверенный до удаления HEAD, полученный через `git -C <дерево> rev-parse HEAD`, и результаты проверки. Diff шага содержит это удаление, отметку пункта и запись проверки.

  Если 0169 или другая задача уже удалила строку и её работа включена в обновлённую базу — не менять deploy.md. Найти источник через `git -C <дерево> log -p -S OutputEncoding -- supervisor/skills/deploy.md`, проверить удаляющий коммит через `git -C <дерево> show <коммит> -- supervisor/skills/deploy.md` и его включение через `git -C <дерево> merge-base --is-ancestor <коммит> HEAD` (код 0). В записи проверки назвать хеш источника, проверенный HEAD и сохранность вызова сценария. Тогда diff шага содержит только отметку и доказательство в собственном tasks.md; повторное удаление не требуется. Без подтверждённого источника или при исчезнувшем запуске сценария пункт не отмечать, назвать проблему в отчёте по правилам имплементации. Исход `moot` для уже заведённого изменения не использовать.

  При удалении в индекс добавлять только явные пути командой `git -C <дерево> add -- supervisor/skills/deploy.md openspec/changes/remove-deploy-dotnet-encoding/tasks.md`; при уже удалённой строке — `git -C <дерево> add -- openspec/changes/remove-deploy-dotnet-encoding/tasks.md`. До коммита проверить имена через `git -C <дерево> diff --cached --name-only` и содержимое через `git -C <дерево> diff --cached`: допускаются только правки выбранного случая, никакие другие изменения внутри этих файлов. После коммита проверить имена и содержание через `git -C <дерево> show --format=fuller --stat HEAD` и `git -C <дерево> show --format= --patch HEAD`. Сразу отправить коммит через `git -C <дерево> push` и открыть черновой PR по правилам имплементации. При обновлении базы перед вливанием повторно сверить общий участок с 0169, не восстанавливая удаление ради diff. Этого единственного шага в любом из двух случаев достаточно для проверяемого PR; ожидание CI ведёт супервизор.

Изменение без дельт: `specs/` не создавать. При проверке `openspec validate remove-deploy-dotnet-encoding --strict` допускается исключительно одна ошибка с началом сообщения `Change must have at least one delta. No deltas found.` по правилам этапа; прочитать вывод целиком. `openspec status --change remove-deploy-dotnet-encoding` ожидаемо показывает 3 из 4 артефактов. Проверки перечисленных команд сверены с `supervisor/config/stage-settings.json`: формы `PowerShell(git -C:*)`, `PowerShell(npx prettier:*)`, `PowerShell(openspec:*)` и `PowerShell(gh pr:*)` присутствуют.
