## 1. Исправление инструкции запуска

- [ ] 1.1 Удалить из командного блока шага 9 `supervisor/skills/deploy.md` строку `[Console]::OutputEncoding = [Text.Encoding]::UTF8`, сохранив остальные строки и пояснения текущей базы. Не менять `scripts/deploy.mjs`, разрешения, порты и рабочий каталог; не возвращать `Push-Location`, если его уже убрала задача 0252. Отметить этот пункт выполненным в том же коммите. Время: до 30 минут. Проверка: поиск `OutputEncoding` и `Text.Encoding` в шаге 9 не даёт совпадений; просмотр diff показывает только удаление строки и отметку этого пункта; `npx prettier --check supervisor/skills/deploy.md` и `git -C <дерево> diff --check` завершаются успешно. Новые тесты и запуск выкладки не нужны.

  В индекс добавлять только явные пути командой `git -C <дерево> add -- supervisor/skills/deploy.md openspec/changes/remove-deploy-dotnet-encoding/tasks.md`. До коммита проверить имена через `git -C <дерево> diff --cached --name-only` и содержимое через `git -C <дерево> diff --cached`: допускаются только описанная правка и её отметка, никакие другие изменения внутри этих файлов. После коммита проверить имена и содержание через `git -C <дерево> show --format=fuller --stat HEAD` и `git -C <дерево> show --format= --patch HEAD`. Сразу отправить коммит через `git -C <дерево> push` и открыть черновой PR по правилам имплементации. Этого единственного шага достаточно для проверяемого PR; ожидание CI ведёт супервизор.

Изменение без дельт: `specs/` не создавать. При проверке `openspec validate remove-deploy-dotnet-encoding --strict` допускается исключительно одна ошибка с началом сообщения `Change must have at least one delta. No deltas found.` по правилам этапа; прочитать вывод целиком. `openspec status --change remove-deploy-dotnet-encoding` ожидаемо показывает 3 из 4 артефактов. Проверки перечисленных команд сверены с `supervisor/config/stage-settings.json`: формы `PowerShell(git -C:*)`, `PowerShell(npx prettier:*)`, `PowerShell(openspec:*)` и `PowerShell(gh pr:*)` присутствуют.
