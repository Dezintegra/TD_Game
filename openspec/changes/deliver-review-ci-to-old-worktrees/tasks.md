## 1. Подготовка полного комплекта

- [ ] 1.1 Добавить `supervisor/lib/review-ci-runtime.mjs` и `supervisor/lib/review-ci-runtime.test.mjs`: явный полный манифест, сохранение структуры импортов, фиксированный launcher, digest, проверки путей/ignore/ссылок/отслеживаемых файлов, публикация целого комплекта и ограниченный preflight настоящего CLI без обращения к GitHub. Проверить повторную подготовку и пропажу транзитивного модуля, не только bin. Пока не подключать новый модуль к запуску: существующий путь остаётся рабочим. Проверка: `npx vitest run --root supervisor lib/review-ci-runtime.test.mjs`, затем узкие eslint/prettier этих файлов. Оценка: до 2 часов. После коммита и немедленной отправки открыть один draft PR по правилам implement, тело передать через файл.

## 2. Атомарное подключение к review

- [ ] 2.1 В `supervisor/lib/supervisor.mjs` подключить подготовку до stageCommand/spawn для нового и продолжаемого review, сохранив охрану children/orphans и штатную диагностику not-born. В `supervisor/skills/review.md`, `supervisor/config/permissions.mjs`, `supervisor/config/stage-settings.json` и `supervisor/lib/review-ci-contract.test.mjs` одновременно заменить оба вызова на `node .matchlog/review-ci-runtime/review-ci.mjs --pr <pr> --head <sha>` и согласовать узкие разрешения. Добавить `supervisor/lib/review-ci-launch.test.mjs`: обе ветви provider, continuation, отсутствие CLI в дереве, отказ подготовки до spawn, неизменный cwd; использовать настоящую подготовку хотя бы в одном сценарии, не только mock. Допустимо уточнить `supervisor/lib/stage-command.mjs` и `supervisor/lib/provider.mjs` исключительно для передачи актуального пути в продолжение, с проверками в новом launch-тесте. Проверка: `npx vitest run --root supervisor lib/review-ci-launch.test.mjs lib/review-ci-contract.test.mjs lib/stage-command.test.mjs lib/provider.test.mjs`; узкие eslint/prettier изменённых файлов. Сторож учитывает все покрывающие allow, сохраняет deny и оба подтверждения до diff/ready. Маршрут pending из актуальной 0313 не откатывать. Оценка: до 2 часов.

## 3. Сквозная регрессия старого дерева

- [ ] 3.1 Добавить `supervisor/lib/review-ci-old-worktree.test.mjs`: самостоятельная fixture Git внутри .matchlog назначенного дерева до появления CLI, без node_modules; настоящий доставленный Node-вход с подменой только gh. Покрыть ordinary/confirmed success, pending/failure/conflict/UNKNOWN, неверные PR/SHA/аргументы, тайм-аут и повреждённый ответ, два вызова со сменой CI/HEAD, обновление комплекта при continuation и повреждение цепочки после подготовки. Проверять JSON, stdout/stderr/code/signal и неизменность HEAD/ветки/индекса/отслеживаемых файлов; на Windows выполнить ту же команду через PowerShell. Отрицательный контроль отключённой доставки должен воспроизводить отсутствие входа, удалённая зависимость — провал загрузки. Проверка: `npx vitest run --root supervisor lib/review-ci-old-worktree.test.mjs lib/review-ci-runtime.test.mjs lib/review-ci.test.mjs lib/review-ci-contract.test.mjs`; узкие eslint/prettier нового теста. Любые исправления реализации в этом пункте ограничены `supervisor/lib/review-ci-runtime.mjs` и сопровождаются воспроизводящим тестом. Оценка: до 2 часов.

## 4. Диагностическое назначение и проверяемое свидетельство

- [ ] 4.1 Добавить `supervisor/lib/review-ci-recovery-command.mjs`, `supervisor/lib/review-ci-recovery-command.test.mjs` и `supervisor/skills/review-ci-recovery.md`. Переиспользовать формирование запуска в `supervisor/lib/stage-command.mjs` и `supervisor/lib/provider.mjs` без изменения обычных запусков: новый диагностический контекст с cwd 0242, текущим provider/моделью review и теми же разрешениями; фиксированное задание вместо обычного review.md. Запретить resume, diff, ready/merge и правки; назвать только Git-чтения и один native-вызов CI. Пока не подключать к боевому циклу. Проверка: `npx vitest run --root supervisor lib/review-ci-recovery-command.test.mjs lib/stage-command.test.mjs lib/provider.test.mjs`; проверить оба provider, отсутствие ослабляющих overrides и команды остановки хозяина. Узкие eslint/prettier названных файлов. Оценка: до 2 часов.

- [ ] 4.2 Добавить `supervisor/lib/review-ci-recovery-evidence.mjs` и `supervisor/lib/review-ci-recovery-evidence.test.mjs`: проверка связанных native-событий, PR/SHA, Git-снимков, JSON и кода; отдельные availability/ciState; атомарные дескриптор и квитанция по Migration Plan. Текст модели без событий, потерянный результат, чужой tool-use id, лишняя команда, policy denial, неизвестные SHA и ошибка записи не дают confirmed. Проверить валидные pending/failure/conflict и сохранение прежней квитанции при новой попытке; секреты и сырой поток не сохранять. Проверка: `npx vitest run --root supervisor lib/review-ci-recovery-evidence.test.mjs`, узкие eslint/prettier этих файлов. Оценка: до 2 часов.

## 5. Исполняемый вход владельца установки

- [ ] 5.1 Добавить `supervisor/bin/review-ci-recovery.mjs`, `supervisor/lib/review-ci-recovery.mjs` и `supervisor/lib/review-ci-recovery.test.mjs`: строгий разбор repair-task/target-task/pr, чтение карточки и реестра host-кодом, исключительное владение через существующий supervisor-startup, проверки процессов/отчётов, настоящая доставка, построитель 4.1, процесс под run-stage с лимитом пять минут, учёт расхода за repairTask и сохранение свидетельства 4.2. Не добавлять allow для этого входа. CLI не вызывает scan/return-task и не пишет в доску. Проверить отказ при живом владельце, неизвестной живости, подмене PR/ветки/пути, недоступном snapshot, ошибке spawn и повторе прерванной попытки; lock освобождается только своим владельцем. Проверка: `npx vitest run --root supervisor lib/review-ci-recovery.test.mjs lib/review-ci-recovery-command.test.mjs lib/review-ci-recovery-evidence.test.mjs`, узкие eslint/prettier новых файлов. Оценка: до 2 часов.

- [ ] 5.2 Добавить `supervisor/lib/review-ci-recovery-flow.test.mjs`: сквозной маршрут конечного входа в изолированном Git-стенде старого дерева, источник отдельно, настоящий CLI и транзитивные модули. Native provider в регрессии заменён записанным протоколом событий, gh — процессной fixture; это тест механизма, не живая приёмка. Обязательная матрица: 0242 failed с fixedBy=[незавершённая 0312], 0242 без recovery/returnTo; в обоих случаях диагностика достигает CLI, сохраняет связанную квитанцию, а настоящий scan всё ещё не возвращает 0242 при незавершённом fixedBy. Проверить неизменность карточки, attempts/sessionId, PR 177, branch, change, HEAD, индекса и tracked-файлов. Ошибки, timeout, обрыв после сохранения дескриптора и повторный запуск не дают ложного успеха. Отрицательные контроли: замена диагностики на return-task, отключённая доставка, удалённый транзитивный импорт и результат только текстом модели разрушают соответствующее доказательство. Допустимые исправления ограничены `supervisor/lib/review-ci-recovery.mjs`, `supervisor/lib/review-ci-recovery-command.mjs`, `supervisor/lib/review-ci-recovery-evidence.mjs`. Проверка: `npx vitest run --root supervisor lib/review-ci-recovery-flow.test.mjs lib/review-ci-old-worktree.test.mjs`, узкие eslint/prettier изменённых файлов. Оценка: до 2 часов.

## 6. Удержание завершения до живой приёмки

- [ ] 6.1 Добавить `supervisor/config/review-ci-recovery.json`, `supervisor/lib/review-ci-recovery-gate.mjs` и `supervisor/lib/review-ci-recovery-gate.test.mjs`; подключить проверку квитанции перед удалением/завершением в `supervisor/lib/execute.mjs`, чтение свидетельства и перенос безопасной сводки в журнал — через `supervisor/lib/io.mjs`. Сохранить существующий формат карточки и recovery. Конфигурация точно связывает repair change/0312 с 0242/PR177/change; прочие cleanup не меняются. Проверить настоящий путь cleanupTask: нет квитанции, испорчены события, другой digest, не влит repair PR, ошибка сохранения журнала — skipped без удаления; валидное свидетельство с pending допускает завершение только 0312 и не меняет 0242. Повтор не дублирует сводку; свидетельство остаётся вне удаляемого дерева. Проверка: `npx vitest run --root supervisor lib/review-ci-recovery-gate.test.mjs lib/execute.test.mjs lib/cleanup.test.mjs`; узкие eslint/prettier названных файлов. Оценка: до 2 часов.

## 7. Доставка протокола установки

- [ ] 7.1 Добавить `supervisor/review-ci-recovery.md` и ссылку в `supervisor/README.md`: владелец установки станции 0242, точная команда нового входа, окно обслуживания до первого cleanup 0312, сохранение отчёта review, установка новой версии под исключительным владением, живой native-вызов, квитанция и возобновление нового цикла с gate. Явно объяснить, почему старое самообновление после оборота не защищает первый cleanup, и запретить исполнителю этапа самому запускать/останавливать владельца. Согласовать с Migration Plan и пунктами 4–6. Передать в отчёте implement команду, путь ожидаемой квитанции и честный статус живой приёмки, не выдавая fixture за неё. Проверка: `npx prettier --check supervisor/review-ci-recovery.md supervisor/README.md` и `openspec validate deliver-review-ci-to-old-worktrees --strict`; чтением проверить исполнимость пути с незавершённой 0312 в fixedBy и без заполненного recovery. Оценка: до 1 часа. Установка, запуск из настоящего окружения 0242 и обычный return-task принадлежат владельцу установки/циклу и не выдаются за выполненный пункт implement.

## Правила коммитов и проверки состава

Каждый пункт — отдельный коммит с немедленной отправкой `git -C <дерево> push`. В каждом пункте разрешена также только его отметка выполнения в `openspec/changes/deliver-review-ci-to-old-worktrees/tasks.md`. Все рабочие пути перечислены в пункте; добавлять их через `git -C <дерево> add -- <явный перечень путей>` вместе с tasks.md. До коммита проверить имена и содержание `git -C <дерево> diff --cached --name-only` и `git -C <дерево> diff --cached`; после — `git -C <дерево> show --stat HEAD` и `git -C <дерево> show --format=fuller HEAD`. Допуск paths не разрешает несвязанные правки внутри файлов.

В индексе и коммитах должны отсутствовать изменения по `.matchlog/review-ci-runtime/` и `.matchlog/review-ci-old-worktree-tests/` (временные fixture всех тестов размещать под вторым путём), а также чужие артефакты 0242 и 0313. Не ограничивать общее количество файлов: рабочая правка и обязательная отметка допустимы одновременно. Полный pnpm verify, арена, ожидание CI и захват дерева 0242 в implement запрещены; остальное проверяет CI собственного PR. Живую приёмку нельзя объявлять выполненной по fixture.
