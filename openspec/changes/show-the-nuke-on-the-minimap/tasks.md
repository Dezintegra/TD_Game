# Задачи

Каждый пункт — отдельный коммит, отправляемый сразу. Каждый собирается
и проверяется сам по себе: откат любого возвращает работоспособное
состояние.

## 1. Окружность на земле переезжает в общий модуль

- [x] 1.1 Завести `apps/client/src/game/ground-circle.ts` с функцией
      `traceGroundCircle(graphics, centreX, centreY, radiusCells, project, steps)`:
      считает точки окружности в мировых клетках и проецирует каждую
      переданной проекцией. Тело — нынешний `traceWorldCircle`
      из `overlays.ts`, у которого проекция и число отрезков стали
      параметрами.
- [x] 1.2 Переключить `overlays.ts` на общий помощник: `worldToScreen`
      проекцией, `CIRCLE_STEPS = 56` числом отрезков. Своей копии
      в `overlays.ts` не остаётся.
- [x] 1.3 Завести `apps/client/src/game/ground-circle.test.ts`: при
      подстановке `worldToScreen` ломаная совпадает с прежней (первая
      точка на угле ноль, число точек равно `steps + 1`, замыкание
      в исходную), а отношение ширины габарита к высоте равно тому,
      которое даёт проекция для круга на земле.

**Проверка:** `npx vitest run apps/client/src/game/ground-circle.test.ts`
зелёный; `npx eslint apps/client/src/game/ground-circle.ts apps/client/src/game/overlays.ts`
без замечаний.

**После этого пункта открывается черновой pull request** — до его
открытия проверки CI на пуши в ветку не запускаются вовсе.

## 2. Цвет тревоги доезжает до миникарты

- [x] 2.1 Добавить в `MinimapColors` поле `strike` — цвет угрозы,
      тот же `--td-strike`, которым удар размечен на поле.
- [x] 2.2 Прочитать токен в `readMinimapColors` в `scene.ts`:
      `strike: token('--td-strike', 0xff5c5c)`.
- [x] 2.3 Дополнить литералы `MinimapColors` в `minimap.test.ts`
      новым полем, чтобы типы сходились.

**Проверка:** `npx vitest run apps/client/src/game/minimap.test.ts`
зелёный (поведение не изменилось, но типы сошлись); `npx eslint`
по трогнутым файлам без замечаний.

## 3. Отметка подлетающего удара на миникарте

- [x] 3.1 В `drawMinimapEntities` пройти по `world.nukes` и на каждую
      ракету нарисовать круг накрываемой площади: центр — середина
      клетки `nuke.cell`, радиус — `unitsToCells(nuke.radius)`,
      помощником из пункта 1 с проекцией `projectToMinimap` и двадцатью
      отрезками. Обоснование числа — в `design.md`.
- [x] 3.2 Нарисовать отметку эпицентра постоянного экранного размера.
- [x] 3.3 Поставить отрисовку ударов после построек, юнитов и генералов,
      но перед рамкой обзора.
- [x] 3.4 Дописать в шапку `minimap.ts` абзац о том, почему круг удара
      выведен из-под правила постоянного экранного размера, а отметка
      эпицентра — нет.
- [x] 3.5 Тесты в `minimap.test.ts`:
      пустой `world.nukes` не добавляет ни одной фигуры;
      ракета в полёте добавляет круг и отметку;
      вдвое больший `radius` даёт вдвое больший габарит круга;
      отношение ширины круга к высоте совпадает с тем, что даёт
      `traceGroundCircle` с `worldToScreen` на поле;
      отметка эпицентра одного размера в разных углах карты.

**Проверка:** `npx vitest run apps/client/src/game/minimap.test.ts`
зелёный; `npx eslint apps/client/src/game/minimap.ts` без замечаний.

## 4. Свой удар отличается от чужого

- [x] 4.1 Расширить заглушку `Graphics` в `minimap.test.ts`: запоминать
      цвет и толщину обводки, а не проглатывать их. Без этого различие
      меток проверить нечем.
- [x] 4.2 Свой удар — цветом `self`, с точкой в эпицентре. Чужой —
      цветом `strike`, с перекрестием в эпицентре. Владелец берётся
      сравнением `nuke.owner` с `localPlayer`.
- [x] 4.3 Тест: при одном своём и одном чужом ударе цвета обводки
      различаются И число фигур в эпицентре различается. Проверять
      только цвет нельзя — различие обязано переживать дальтонизм.

**Проверка:** `npx vitest run apps/client/src/game/minimap.test.ts`
зелёный; `npx eslint apps/client/src/game/minimap.test.ts` без замечаний.

## 5. Зелёный CI

- [ ] 5.1 Дождаться зелёных проверок на pull request:
      `gh pr checks <номер> --watch`. Красное — читать лог
      (`gh run view --job <id> --log-failed`), чинить отдельным
      атомарным коммитом и ждать снова.

**Проверка:** все проверки pull request зелёные. Задача не сделана,
пока GitHub не сказал «зелено»: локальный прогон из дополнительного
дерева этого не заменяет.
