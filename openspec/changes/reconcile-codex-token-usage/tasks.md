## 1. Общий расчёт снимков

- [x] 1.1 (до 2 часов) Добавить чистую нормализацию и reducer накопительных снимков в token-budget.mjs без переключения существующих вызовов. Зафиксировать fixture с provenance Codex 0.146.0 из design: 1000/100 → 1600/140, равные снимки, уменьшение и невалидные целые; проверить итог 1740, отсутствие повторного cached/reasoning и явный unknown. Проверка: `npx vitest run --root supervisor lib/token-budget.test.mjs`; Vitest должен обнаружить и успешно выполнить тесты указанного файла. Коммит совместим с прежними вызовами; после отправки открыть один draft PR на изменение.

## 2. Версия реестра и восстановление

- [ ] 2.1 (до 2 часов) Добавить чтение/запись v2 и миграцию числового формата, сохранив совместимость действующих вызовов до переключения в 3.1. Проверить сохранение каждой суммы, legacy-unknown, пустой файл данных, повреждённую схему, неизвестную версию, повторный round-trip и ошибку записи до rename без преждевременного обновления памяти. Проверка: `npx vitest run --root supervisor lib/token-budget.test.mjs`; Vitest должен обнаружить и успешно выполнить тесты указанного файла. Совместимый старый путь временно сохраняет прежнее поведение, новый используется после полного подключения.

## 3. Адаптер, события и бюджетная граница

- [ ] 3.1 (до 2 часов) Одним атомарным коммитом переключить provider.mjs, supervisor.mjs и scan.mjs на новый контракт: долговечные launchId/baseline, все completed через общий reducer, одинаковые ключи для watch/finish, unknown и запрет обычного запуска при неполном включённом бюджете. При необходимости обновить сериализацию в io.mjs; убрать временный старый путь и использование stage usageBaseline. Одновременно заменить сценарий Persistent cumulative usage в support-both-agents/specs/agent-provider/spec.md на сопоставимые накопительные снимки плюс неизвестность, сохранив Truncated stream, ChatGPT subscription, Unknown consumption и прочие требования; согласовать абзац о максимумах в его design.md. Проверки: `npx vitest run --root supervisor lib/provider.test.mjs lib/token-budget.test.mjs lib/supervisor.test.mjs lib/scan.test.mjs` (Vitest должен обнаружить и успешно выполнить тесты каждого указанного файла), `openspec validate reconcile-codex-token-usage --strict`. Наблюдаемый результат: watch+finish дают 1740, resume до 2180 даёт расход запуска 440, уменьшение не даёт отрицательного числа или разрешения тратить неизвестный остаток. Не добавлять доставку отчётов из задачи 0235.

## 4. Регрессии жизненного цикла

- [ ] 4.1 (до 2 часов) Добавить интеграционные проверки с подставным процессом и сохранённым реестром: повтор finish; replay более раннего снимка того же запуска; одинаковый payload разных completed; несколько сессий; restart после watch до finish; забывание пары task/stage; новый resume с меньшим usage; валидный completed перед failed exit/невалидным отчётом/оборванным ходом; сбой сохранения и повтор; старый live без launchId и недоступный stdout как unknown. Проверить известный предел, legacy-unknown ниже предела, отключённый предел, отсутствие расходования попыток при удержании и прежние исключения decompose/сквозных состояний. Проверка: `npx vitest run --root supervisor lib/supervisor.test.mjs lib/token-budget.test.mjs lib/scan.test.mjs lib/provider.test.mjs`; Vitest должен обнаружить и успешно выполнить тесты каждого указанного файла. В PR описать несовместимость отката v2 и удержание legacy-unknown; тесты не вызывают настоящую модель.

Каждый пункт — отдельный коммит вместе с отметкой этого пункта, отправляемый сразу. Команды выполняются в назначенном дереве; Git — только формой `git -C <дерево> …`. Указанные приставки проверены в supervisor/config/stage-settings.json: git -C, npx vitest, openspec. Полные проверки и ожидание CI не являются шагами исполнителя. Арена не нужна: баланс игры не меняется. На design никакие из перечисленных тестов не запускаются.
