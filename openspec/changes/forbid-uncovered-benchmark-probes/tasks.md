## 1. Общий запрет разведки окружения

- [ ] 1.1 За один атомарный коммит (до 1 часа) изменить `supervisor/skills/benchmark.md`: добавить самостоятельный общий запрет непокрытых вызовов для всего этапа с источником `supervisor/config/stage-settings.json`, примерами `node --version`, `node --help`, `where`, `command -v` и покрытым контрпримером `node supervisor/bin/launch.mjs --help`; объяснить подготовленное заранее окружение и проверку покрытия чтением, без проб. Удалить разведку из локальных перечней ожидания и согласовать слова «все четыре формы». Сохранить штатный watch, запреты циклов/фонового ожидания и независимых предохранителей, `--check-only` и анализ результатов. Согласовать пояснения о последствиях отказа в шаге 5, шаге 7 и пункте ожидания с разделом «Отчёт», не выдавая историческую приёмку за нынешнюю. Проверка: прочитать итоговый текст по четырём сценариям дельты и сверить формы примеров с allow/deny без их исполнения; убедиться, что в файле не осталось утверждений о безусловной потере отчёта из-за попутного отказа, а раздел «Отчёт» сохранён. Выполнить `npx prettier --check supervisor/skills/benchmark.md`, `openspec validate forbid-uncovered-benchmark-probes --strict` и `git -C <дерево> diff --check`. Проставить отметку выполнения этого пункта, добавить явными путями только рабочую правку и свою отметку: `git -C <дерево> add -- supervisor/skills/benchmark.md openspec/changes/forbid-uncovered-benchmark-probes/tasks.md`. Перед коммитом проверить имена и содержание через `git -C <дерево> diff --cached --name-only` и `git -C <дерево> diff --cached`: допустимы только названные изменения, в том числе в tasks.md. После коммита сверить те же ограничения через `git -C <дерево> show --stat --oneline HEAD` и `git -C <дерево> show --format=fuller HEAD`, немедленно отправить `git -C <дерево> push -u origin HEAD` и открыть черновой PR по правилам implement. Полный набор проверок, арена и ожидание CI в этот пункт не входят.
