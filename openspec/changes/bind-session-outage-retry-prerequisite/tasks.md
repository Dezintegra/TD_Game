## Порядок исполнения

Это задачи реализации 0247 после аудита, а не задачи функциональности 0243. Все правки выполняются в назначенном дереве 0247. Перед работой отдельными вызовами `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main` освежить базу по правилам этапа. Не перехватывать дерево 0243, не править доску, не возвращать failed и не запускать настоящую сессию.

Каждый пункт — самостоятельный коммит с отметкой в этом tasks.md и немедленной отправкой. Команды отдельными вызовами: `git -C <дерево> add -- <явные-пути>`, `git -C <дерево> commit -m "<заголовок>" -m "<обоснование>"`, `git -C <дерево> push`. Формы git -C, gh pr, openspec, node .matchlog/* и npx vitest сверены с allow в `supervisor/config/stage-settings.json`. Тесты выполняются только в implement и только узкие; полный pnpm verify, сборка, арена и ожидание CI сюда не входят.

## 1. Согласованное предусловие повторного implement

- [x] 1.1 Исправить tasks.md и design.md существующего `stop-launches-on-session-outages` по Decisions 1–2: общее удержание всего повторного implement до closed 0242 и MERGED PR 177; модель 1.1 остаётся независимым коммитом после допуска; прежнее условие 3.1 становится повторной проверкой; до реализации обязательно освежение дерева и проверка результата PR 177. Если каталога ещё нет в свежей main, перенести полный опубликованный каталог артефактов 0243 по зафиксированному SHA исключительно в своё дерево, не её код; не перетирать уже существующую новую версию. Начать `verification.md` в этом изменении: источник/SHA и diff относительно него, отсутствие изменений прочих артефактов и дельты 0243. Проверка: `git -C <дерево> diff <SHA-источника> -- openspec/changes/stop-launches-on-session-outages` показывает только согласованные изменения двух плановых файлов; `openspec validate stop-launches-on-session-outages --strict` проходит. Проверить вручную отсутствие разрешения начинать 1.1 до общего допуска, наличие проверки предка mergeCommit PR 177 и сохранность всех чекбоксов/сценариев. Оценка: 90 минут. Отметить пункт, закоммитить, сразу отправить и открыть единственный черновой PR 0247 командой `gh pr create --draft --base main --title "Закрепить предусловие повторного implement 0243" --body-file <файл-в-дереве>` по правилам implement.

## 2. Изолированная проверка и подготовка доставки

- [ ] 2.1 Выполнить матрицу Decisions 4 на настоящем существующем планировщике с внедрёнными снимками/runner: полные ID 0243/0242 и PR 177, OPEN, отсутствие доказательства, незакрытый предшественник, неверные поля, удержание start/continuation/лимитного отказа, сохранность всего снимка при повторном скане; положительный допуск и действие прочих ограничений. Пробу запускать `node .matchlog/0247-prerequisite.mjs`, её исходный текст и полный вывод зафиксировать в verification.md. После подтверждённой поставки 0242 освежить дерево, сверить точный контракт dependencyUpdates и сведения о ревизии принимающего супервизора; узко проверить существующие тесты `npx vitest run --root supervisor lib/dependency-state.test.mjs` и файлы тестов адресного канала, найденные в поставленном PR 177 (их точные пути/команды записать в verification.md). Подготовить адресное поручение ровно из Decisions 3 для последнего отчёта implement. Проверка: все модельные ожидания выполнены без Trello и spawn, готовый payload содержит только одно дополнение нужному адресату, сохранение не объявлено свершившимся до независимого GET; состояние реальной failed-карточки не менялось исполнителем. Оценка: 120 минут. Отметить пункт только после проверок и подтверждения готовности канала; отдельный коммит verification.md и tasks.md, немедленная отправка. Если канал не поставлен, вернуть предусмотренный этапом вопрос о поставке с результатами уже выполненного, не выдавать неподдерживаемое поле и не отмечать пункт завершённым.

## Передача и приёмка последующими этапами

Промежуточный результат implement 07.09.2026 по 2.1: изолированная матрица (380 проверок решений) и 17 тестов dependency-state прошли, исходник и полный вывод сохранены в verification.md. PR 177 остаётся OPEN; поставка канала и ревизия принимающего супервизора не подтверждены. Пункт не завершён, dependencyUpdates не передано. Продолжить после подтверждённой поставки: освежить дерево, сверить контракт и ревизию, проверить тесты адресного канала и передать поручение последним отчётом. Черновой PR этой работы — 179.

В последний отчёт implement включить `dependencyUpdates` из design. Передачу описать как поручение, пока не подтверждённое записью. Супервизор 0242 применяет его и делает независимый GET до перехода 0247; review проверяет переданное свидетельство адресата, обеих новых записей, сохранности всех прежних зависимостей/метаданных и успешного подтверждения. При отсутствии свидетельства запросить его через отчёт, а не выполнять прямое чтение или запись доски. Это проверка следующего этапа, не чекбокс ожидания собственного отчёта.

Review также сверяет актуальные артефакты ветки 0243 с перенесённой версией и сохранение правок при интеграции. 0247 не принимается без доставки исправленных tasks.md/design.md и подтверждения метаданных. Положительный модельный допуск не доказывает право реального запуска: failed сохраняется, а любое последующее разрешённое продолжение 0243 заново проходит закрытие 0242, доказательство PR 177, прочие ограничения и обновление своего дерева. Архивация и реализация 0243 не являются шагами этого изменения.
