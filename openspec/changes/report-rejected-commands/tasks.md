# Задачи

## 1. Причины отказа в общих типах

- [x] 1.1 Добавить перечисление `RejectReason` в `packages/shared`
      рядом с `CommandKind`, с комментарием к каждой причине о том,
      какая именно проверка её порождает.
      *Проверка:* `pnpm --filter @td/shared build` зелёный, тип
      экспортируется из `@td/shared`.
- [x] 1.2 Добавить тип `Rejection` (игрок, вид команды, причина)
      в `packages/sim/src/world.ts` рядом с `ShotState` и поле
      `rejections` в `WorldState`. В комментарии сослаться на `ShotState`
      как на прецедент и объяснить, почему запись живёт один тик,
      а не `SHOT_LIFETIME_TICKS`.
      *Проверка:* `pnpm --filter @td/sim typecheck` зелёный.
- [x] 1.3 Записать в `checksum.ts` явным комментарием, что `rejections`
      в сумму не входят и почему — рядом с существующим объяснением
      про следы выстрелов.
      *Проверка:* `determinism.golden.test.ts` зелёный **без** правки
      эталонного числа. Если число поменялось — значит изменилось
      правило, а не только отчётность, и это ошибка реализации.

## 2. Отказы в применении команд

- [x] 2.1 Провести `rejections` через `toWorking`/`fromWorking`
      в `packages/sim/src/working.ts`: список обнуляется в начале тика
      и переезжает в новое состояние в конце.
      *Проверка:* `pnpm --filter @td/sim test` зелёный.
- [x] 2.2 Заменить молчаливые `return` в `applyCommand`, `moveGeneral`
      и `build` (`packages/sim/src/apply.ts`) на возврат с причиной.
      Развести слитые проверки: непроходимая клетка и клетка с живым
      объектом должны давать разные причины.
      *Проверка:* новый тест `apply.reject.test.ts` — по одному случаю
      на каждую ветку `build`, все причины различимы.
- [x] 2.3 То же для `trainUnit`, `setTarget`, `buyUpgrade`, `launchNuke`.
      *Проверка:* тот же тест покрывает все виды команд; проверка
      «на каждый вид команды есть хотя бы одна достижимая причина
      отказа» проходит.
- [x] 2.4 Отдельный случай — команда после победы (`working.winner !== null`)
      и команда от несуществующего игрока. Первая даёт `MatchOver`,
      вторая не даёт ничего: игрока нет, некому и сообщать.
      *Проверка:* тест на оба случая.
- [x] 2.5 Тест на пересчёт: один и тот же тик, просчитанный дважды
      от одного состояния, даёт одинаковые списки отказов.
      *Проверка:* тест зелёный; он же страхует от соблазна сделать
      отказы накопительными.

## 3. Отклик в интерфейсе

- [x] 3.1 Читать отказы в `onTick` в `apps/client/src/game/bootstrap.ts`,
      фильтруя по `LOCAL_PLAYER`, и класть в store. В комментарии
      объяснить, почему не в снимке HUD (снимок раз в `HUD_EVERY_TICKS`
      пропустил бы пять отказов из шести).
      *Проверка:* в браузере отказ доезжает до store — видно в React
      DevTools или временным логом.
- [x] 3.2 Гашение повторов: отказ с той же парой «вид команды, причина»
      в пределах секунды второго сообщения не порождает.
      *Проверка:* юнит-тест на функцию гашения — тридцать одинаковых
      отказов подряд дают одно сообщение.
- [x] 3.3 Показ сообщения в HUD: короткая строка с причиной, гаснет
      за пару секунд. Цвета и длительности — только через переменные
      из `packages/ui/src/tokens.css`.
      *Проверка:* `pnpm lint` зелёный (литералов цвета нет); визуально
      в браузере сообщение появляется и гаснет.
- [x] 3.4 Вздрагивание элемента панели там, где он есть: шкала энергии
      при `NotEnoughEnergy`, панель очереди при `QueueFull`.
      *Проверка:* визуально — заказ юнита без денег заставляет шкалу
      энергии вздрогнуть в том же кадре.

## 4. Завершение

- [x] 4.1 Прогнать `pnpm verify` целиком.
      *Проверка:* линт, типы, тесты и сборка зелёные.
- [x] 4.2 Визуальная проверка в браузере по списку: постройка без денег,
      постройка вне радиуса, постройка в скалу, постройка под юнитом,
      заказ при полной очереди, улучшение без денег, удар у базы.
      Каждый случай даёт своё сообщение.
      *Проверка:* выполнено прогоном в странице через динамические импорты
      Vite (панель браузера в сессии не отображается, rAF не идёт).
      Семь случаев дали семь разных причин: Build → NotEnoughEnergy,
      OutsideBuildRadius, CellBlocked, CellOccupiedByLiving;
      BuyUpgrade → NotEnoughEnergy; TrainUnit → QueueFull;
      LaunchNuke → NukeNearBase. Сообщения отрисованы под полосой матча,
      цвет из --td-error, шкала энергии вздрагивает. Ни одного
      молчания.
