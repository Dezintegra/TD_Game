## 1. Восстановить описание действующего контракта

- [x] 1.1 Переписать описания `requests` в `supervisor/skills/triage.md`, `supervisor/skills/implement.md`, `supervisor/skills/review.md`, `supervisor/skills/audit.md`, `supervisor/skills/revise.md` по решениям design.md; сверить `supervisor/skills/deploy.md` и менять его только при расхождении в описании поля. Сохранить ограничения triage, особенности пустого поля, примеры и соседние пояснения. Проверка: прочитать все шесть окончательных абзацев по таблице design.md, сопоставить типы и правила `run` с `taskFromRequest`; все фразы читаются по-русски, вставки закрыты и не разорваны, таблица implement цела, дубли и разрешение `note` в triage устранены. Выполнить `git -C . diff --check` и проверить diff: только описания поля в перечисленных скиллах и отметка этого пункта в tasks.md. Результат сверки deploy явно назвать в отчёте реализации. Оценка: до 1 часа. Это один самостоятельный коммит; после отметки пункта закоммитить и сразу отправить его, затем открыть черновой PR по правилам этапа implement.

Новые тесты на точное написание абзацев не нужны. Полные проверки, сборка и арена локально не запускаются; ожиданием CI управляет супервизор. Команда проверки использует `git -C`, покрытый `PowerShell(git -C:*)` в `supervisor/config/stage-settings.json`.
