## Исходная сверка

13.09.2026: PR 172 имеет state MERGED, mergedAt 2026-09-08T19:31:24Z,
headRefOid 29d14c293c252eaee600d776e70899558f795f12. Перед правками выполнены
fetch и merge origin/main (136611bec4c9f0446f895c209ca46f30aeb9d411).
Последняя правка исследуемых файлов — e3d95c2c; установленной починки none нет.

Первый `npx vitest run --root supervisor lib/report-recovery.test.mjs`:
12 тестов прошли. Исторический failed не воспроизведён; успешный прогон
не доказывает исправления.

## Наблюдаемость

Сообщение первого assertion содержит point, фазы перезапуска/execute,
исходный result.why, полный конверт с идентичностью, планом и progress,
маршруты локального получателя, снимок эффектов и число запусков.
Пассивная обёртка диска очереди сохраняет операции и исходные
message/code/syscall/path файловых исключений и повторно бросает их.
Управляемый before-put отдельно проверяет пригодность контекста:
причина получателя, фаза execute, непустой progress, попытка PUT,
нулевые PUT/POST эффекты. None не получает инъекции.

## Пока не доказано

Причина исторического отказа неизвестна. Проверяются граница записи очереди,
сопоставление ожидаемой карточки с повторным чтением и жизненный цикл фикстуры.
Ни Windows, ни sleep(0), ни случайный UUID сами по себе причиной не объявлены.
