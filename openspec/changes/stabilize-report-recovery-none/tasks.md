## 1. Диагностика на влитой основе (до 90 минут)

- [x] 1.1 Перед изменением кода сверить `gh pr view 172 --json state,headRefOid,mergedAt`, выполнить `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main`, проверить наличие окончательной реализации 0233 и последующих починок. При невлитой зависимости остановить реализацию с отчётом о предусловии; чужую ветку не сливать. В своём дереве добавить пассивный контекст отказа к report-recovery и его фикстурам по design, сохранив строгий исход none и изоляцию подставного получателя. Завести `investigation.md` внутри этого изменения с точной ревизией, наблюдениями и ещё не доказанными гипотезами. Проверка: `npx vitest run --root supervisor lib/report-recovery.test.mjs`; отдельно управляемый отказ подставного получателя должен показать исходную причину, фазу, прогресс и эффекты, не превращая none в тест с инъекцией. Проверять только названный файл; число найденных тестов не нулевое. Этот коммит самостоятелен: добавляет наблюдаемость, не меняет семантику доставки и не добавляет заведомо красной регрессии. Отметить пункт, закоммитить явные пути, сразу отправить `git -C <дерево> push -u origin HEAD` и открыть черновой PR по правилам implement; он охватывает всё изменение. Если диагностика уже обнаружила дефект, сохранить падение, не маскировать повтором и не объявлять починку завершённой.

## 2. Причинная починка и регрессия (до 120 минут)

- [ ] 2.1 Установить воспроизведением с диагностикой либо конкретной цепочкой по коду причину none; в `investigation.md` назвать ответственный слой и доказательства. Исправить только установленную причину и в том же коммите добавить управляемую регрессию на её условие. Сохранить none без инъекции, все варианты частичного отказа и проверки очереди/расхода/сессий; усилить проверку полного комментария и отсутствия планируемого повтора implement после следующего перезапуска согласно design. Проверка: `npx vitest run --root supervisor lib/report-recovery.test.mjs lib/report-delivery.test.mjs`; при правке store дополнительно `npx vitest run --root supervisor lib/report-store.test.mjs`, при иной границе — её конкретный существующий файл теста, названный в investigation. Контроль на прежнем поведении должен падать по установленной причине, исправленное поведение — проходить; сохранить обе команды и результаты, контрольную порчу не коммитить. Для изменённых файлов выполнить узкие `npx eslint <файлы>` и `npx prettier --check <файлы>`; openspec проверять `openspec validate stabilize-report-recovery-none --strict`. Если причина не доказана, пункт остаётся открытым, а успешные повторы не считаются его выполнением. Отметить пункт только после причинной проверки, закоммитить починку, регрессию и доказательства явными путями и сразу отправить `git -C <дерево> push`. Полный набор остаётся CI; ожидание CI ведёт супервизор.

Формы команд сверены с `supervisor/config/stage-settings.json`: `PowerShell(git -C:*)`, `PowerShell(npx vitest:*)`, узкие eslint/prettier и gh. `<дерево>` означает только назначенное дерево 0238. Каждый пункт — один самостоятельный коммит с обновлением этого списка. Имена соседних тестов сверить после вливания PR 172; если они изменились, записать точную эквивалентную команду в investigation до проверки. Проверки первой задачи не означают доказанной починки; без завершённой второй задачи изменение не готово.
