## 1. Диагностический контракт

Предусловие runtime-интеграции: `gh pr view 172 --json state,mergedAt,mergeCommit` подтверждает вливание 0233; затем `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main` дают фактический report-store. Если PR не влит, вернуть вопрос по правилам этапа; соседнюю реализацию не копировать. Каждая отмечаемая задача ниже — отдельный коммит до двух часов с немедленной отправкой `git -C <дерево> push`. В каждом коммите отмечается соответствующий пункт этого файла. Команды узких проверок покрыты `PowerShell(npx vitest:*)` и `PowerShell(git -C:*)` из stage-settings; запуск полного набора и живой диагностики production для этих задач не нужен.

- [ ] 1.1 Добавить чистый нормализатор структурированных фактов и классификатор контрольных проверок в `stage-tool-health.mjs` с `stage-tool-health.test.mjs`: confirmed/healthy/inconclusive, независимость от текста, локального EPERM, нулевых denials и предыдущего успеха. Runtime пока не подключать. Проверка: `npx vitest run --root supervisor lib/stage-tool-health.test.mjs`. После отправки этого самостоятельного коммита открыть черновой PR через `gh pr create --draft --base main --title "Confirm in-stage tool outages" --body-file .matchlog/0244-pr-body.md`; наблюдаемый результат — номер draft PR с этим коммитом. Файл тела подготовить и убрать способом скилла имплементации.
- [ ] 1.2 Выделить переиспользуемые фиксированные контроли readiness, добавить provider-адаптеры диагностики с теми же cwd/env/permissions и строгими supervisor-owned check ids, таймаутом 120 секунд, child-process probe без зависимостей проекта и SSH только в deploy. Текст агента не выбирает команды, отсутствие исполнения даёт inconclusive. Сохранить старый интерфейс стартовой readiness, runtime завершения пока прежний. Проверка: `npx vitest run --root supervisor lib/stage-tool-health.test.mjs lib/codex-readiness.test.mjs lib/provider.test.mjs`; проверить оба provider, другой cwd, неподтверждённый контекст, transport versus ref rejection, запрет расширения прав и отсутствие реального push/deploy.

## 2. Устойчивое удержание

- [ ] 2.1 После исполнения предусловия 0233 расширить её конверт и миграцию disposition/evidence/nullable report/retry claim; обеспечить снимок фактически начисленного launch continuation, Git known/unknown evidence и сохранение исходного результата по launchId. Добавить тесты store на старые ordinary планы, null report, повреждение записи и восстановление diagnosing/held. Новая ветвь пока не создаётся runtime. Проверка: `npx vitest run --root supervisor lib/report-store.test.mjs lib/stage-tool-health.test.mjs`; старые конверты сохраняют обычный перенос, отсутствующая информация не превращается в чистую ветку.
- [ ] 2.2 Подключить finish к сохранению diagnosing до release, затем к bounded-пробе и обычному переносу либо infrastructure-held; подключить существующую паузу, восстановление hold до recovery/scan и защиту задачи и batch от transfer/fail-stage/push-tail/cleanup. Живых детей не снимать. Ошибка store закрывает планирование. На этом шаге held не разрешает повтор: это самостоятельное безопасное поведение. Тем же коммитом оговорить в требовании «Сессия не просится, пока места нет» изменения count-only-real-launches и сценарии «Пределы исчерпаны при занятой машине» исключение для подтверждённого hold по design; проверить, что исчерпанная обычная задача получает fail-stage при занятых местах, а held — нет. Внести оговорки обычного failed/timeout в supervise-the-pipeline и диагностического конверта в persist-unapplied-stage-reports по design, сохранив сценарии. Проверка: `npx vitest run --root supervisor lib/supervisor.test.mjs lib/scan.test.mjs lib/stage-tool-health.test.mjs lib/report-store.test.mjs`; воспроизвести success fetch/merge → command failure → exit 0/failed/0 denials, перезапуск при паузе, неизвестный Git, обычный тест и жалобу без подтверждения.

## 3. Безопасный повтор

- [ ] 3.1 Добавить infrastructure settlement plan через квитанции 0233: неизменный stage, журнал полного исходного результата и проверки, фактический расход, однократный адресный refund только списанного continuation, retry-ready без запуска. Неподтверждённые записи повторяются по тому же reportId; чужой launch/state удерживается. Не выполнять заявки/исключения исходного failed как обычный отчёт. Проверка: `npx vitest run --root supervisor lib/execute.test.mjs lib/report-store.test.mjs lib/report-recovery.test.mjs`; потеря ответа после записи не удваивает refund/расход/журнал, initial launch не уменьшает прежние attempts.
- [ ] 3.2 Подключить снятие паузы к свежей полной диагностике и выдаче одного замещающего запуска только для этапов кроме deploy: durable claim/new launchId до spawn, recovery/adopt при неопределённом результате, без начисления continuation, с сохранением лимитов токенов/доверия/владения. Сданный отчёт получает свежую сессию; незавершённый без отчёта — допустимый resume. Передать prompt с работой, исходным отчётом и требованием сверить эффекты; сохранять диагностический материал до снятия active-конверта. Deploy на этом шаге остаётся удержанным: даже healthy и retry-ready не разрешают claim/spawn, пока в 3.3 не появится проверка удалённой ревизии. Согласовать оба требования count-only-real-launches — «Продолжение засчитывается за родившийся процесс» и «Сессия не просится, пока места нет», включая сценарий «Пределы исчерпаны при занятой машине» и новый сценарий replacement по design; согласовать норму resume supervise-the-pipeline и acknowledgement/pending 0233 по design. Проверка: `npx vitest run --root supervisor lib/supervisor.test.mjs lib/scan.test.mjs lib/execute.test.mjs lib/stage-prompt.test.mjs lib/report-recovery.test.mjs`; healthy после pause даёт ровно один запуск, unknown повторно взводит pause, crash до/после spawn не порождает дубль, token hold не обходится; исчерпанный обычный этап останавливается даже при занятых местах, подтверждённый удерживается и после восстановления/освобождения места получает ровно один replacement, после потребления entitlement обычный предел снова действует; deploy с healthy и retry-ready при непроверенной удалённой ревизии не получает claim/spawn и сохраняет весь пакет.
- [ ] 3.3 Завершить deploy recovery и впервые разрешить его replacement в этом же коммите: сохранить ведущую/batch/revision, перед retry контрольным чтением сверить удалённый эффект, передать уже опубликованную ревизию как завершённую работу, удерживать при неопределённости; новые члены не добавляются. Снять запрет deploy claim/spawn из 3.2 только для случая успешно проверенной удалённой ревизии и эффектов; при unknown он сохраняется. Согласовать batch-deploy по design, сохранить обычный partial report delivery. Проверка: `npx vitest run --root supervisor lib/execute.test.mjs lib/scan.test.mjs lib/report-recovery.test.mjs`; SSH outage после возможной публикации не переводит членов в failed/cleanup и не публикует повторно, успешный pending deploy только досылает эффекты.

## 4. Проверки границ и документация

- [ ] 4.1 Добавить сквозную регрессию `stage-tool-recovery.test.mjs` на перезапускаемом получателе Trello из 0233 и реальном report-store: confirmed после успешных команд, recovery, неподтверждённая жалоба, тестовый assertion failure, отсутствующие tool events, child-process failure, deploy SSH failure и restart в точках persist/refund/claim/handoff. Диагностические процессы подставные, живая доска/SSH не вызываются. Описать оператору различие отчёта и вердикта, снятие паузы, проверку unknown Git/remote и ограничения отката в документации supervisor; проверить согласованные открытые требования или их архивированную основу по текущим путям. Проверка: `npx vitest run --root supervisor lib/stage-tool-recovery.test.mjs lib/report-recovery.test.mjs`, `openspec validate confirm-stage-tool-outages --strict`. Наблюдаемый результат — один refund и один replacement по launchId, сохранный исходный результат, отсутствие повторной публикации и неизменный обычный failed-путь.
