# Задачи

## 1. Гребень базы

- [x] 1.1 В `apps/client/src/game/base-structure.ts` вынести смещения мачты
      и надбавку тарелки в именованные константы: сейчас `-1.35`, `0.13`,
      `14` и `3` повторяются в двух местах и в вычислении гребня понадобятся
      в третий раз.
      *Проверка:* числовых литералов положения мачты в модуле не осталось,
      картинка не изменилась.
- [x] 1.2 Экспортировать `baseCrestPoint(centreX, centreY)` — экранную точку
      над самой высокой деталью базы, посчитанную из тех же констант.
      *Проверка:* тест — точка выше вершины мачты не менее чем на высоту
      тарелки со штырём, а по горизонтали совпадает с центром базы.

## 2. Слой поверх тел

- [x] 2.1 Добавить в `EntityLayers` слой `overhead` — для того, что не должно
      прятаться за телами.
      *Проверка:* типы собираются, `entities.test.ts` дополнен слоем.
- [x] 2.2 Завести соответствующий `Graphics` в `scene.ts` над слоем трассеров
      и очищать его вместе с прочими слоями сущностей.
      *Проверка:* слой очищается каждый кадр, следов прошлого кадра
      не остаётся.

## 3. Полоса прочности базы

- [x] 3.1 В `entities.ts` рисовать полосу базы вместо того, чтобы пропускать
      базу целиком: тело по-прежнему за территорией, полоса — в `overhead`.
      *Проверка:* тест — при отрисовке мира с базами слой `overhead` получает
      геометрию, а полосы глубины не запрашиваются.
- [x] 3.2 Полоса шире и выше юнитской, видна при полной прочности, имеет
      отметки по четвертям и окантовку цветом стороны.
      *Проверка:* тест — у нетронутой базы полоса есть, у целой стены нет.
- [x] 3.3 Отсекать невидимое по точке полосы, а не по основанию базы.
      *Проверка:* тест — база ниже нижней кромки экрана с видимой полосой
      не отсекается.

## 4. Сдача

- [x] 4.1 `pnpm verify` — линт, типы, тесты, сборка.
      *Проверка:* всё зелёное, 361 тест — прогнано перед архивацией.
- [x] 4.2 Визуальная проверка в браузере: полоса над обеими базами,
      не задевает антенну, читается издали, при уроне укорачивается
      и краснеет.
      *Проверка:* выполнена автором проекта в запущенной игре, замечаний
      нет. Снимков экрана нет: панель браузера в сессиях не отображалась,
      и сделать их было нечем.
- [x] 4.3 Дописать раздел 7.2 `docs/game-design.md`: у базы есть полоса
      прочности, видна всегда и не прячется за телами.
      *Проверка:* документ не противоречит экрану.
