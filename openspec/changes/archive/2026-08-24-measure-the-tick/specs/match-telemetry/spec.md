## ADDED Requirements

### Requirement: Запись матча несёт отметки реального времени

Запись матча SHALL содержать отметку реального времени рядом
с контрольной суммой: сколько миллисекунд прошло от начала матча
к моменту, когда эта сумма снята.

Сегодня запись безвременна по построению — в ней есть номера тиков
и нет ни одной отметки часов, — и восстановить по ней **темп** матча
невозможно. Видно, что случилось, и не видно, вовремя ли.

Отметка SHALL быть необязательной: записи, снятые раньше, обязаны
читаться по-прежнему, а воспроизведение матча обязано идти
и без неё.

Отметка на воспроизведение влиять MUST NOT. Мир восстанавливается
из seed и команд; время — сведение о том, как матч шёл, а не о том,
что в нём произошло. Воспроизведение, сверяющее контрольные суммы,
SHALL давать тот же результат независимо от наличия отметок.

Частота отметок SHALL совпадать с частотой контрольных сумм — раз
в игровую секунду. Чаще незачем: неровность темпа накапливается,
и секундного разрешения хватает, чтобы её увидеть.

#### Scenario: Темп матча восстановим из записи

- **WHEN** записан матч, в котором сервер спотыкался
- **THEN** по отметкам времени видно, что между двумя суммами,
  отстоящими на секунду игры, прошло заметно больше секунды реального
  времени

#### Scenario: Старая запись читается

- **WHEN** читается запись матча, снятая до появления отметок
- **THEN** она разбирается и воспроизводится без ошибок

#### Scenario: Отметки не меняют воспроизведение

- **WHEN** один и тот же матч воспроизведён из записи с отметками
  и из записи без них
- **THEN** контрольные суммы совпадают на всех сверяемых тиках
