## 1. Константы и типы

- [x] 1.1 Добавить в `@td/shared` константы карты: `MAP_WIDTH_CELLS` и `MAP_HEIGHT_CELLS` равные 96, целевую плотность скал, параметры разрастания массивов. Заменить прежние `FIELD_WIDTH_CELLS` и `FIELD_HEIGHT_CELLS`. Проверка: `pnpm typecheck` проходит, старые константы нигде не остались.
- [x] 1.2 Добавить константы изометрии: `TILE_WIDTH_PX` = 96 и `TILE_HEIGHT_PX` = 48, с комментарием о том, как эти числа выведены из требования видеть десятую часть карты. Проверка: юнит-тест подтверждает соотношение 2:1.
- [x] 1.3 Описать тип клетки карты (`Terrain`: земля / скала / стена) и хелпер `isPassable`. Проверка: `typecheck` проходит.

## 2. Генерация мира

- [x] 2.1 Реализовать симметричное заполнение карты: помечая клетку, помечать и парную ей относительно поворота на 180°. Проверка: юнит-тест на симметрию.
- [x] 2.2 Реализовать разрастание скальных массивов случайным блужданием из seeded PRNG. Проверка: карта содержит связные кляксы, а не одиночные точки.
- [x] 2.3 Останавливать разрастание по достижении целевой плотности, а не после фиксированного числа массивов. Проверка: тест плотности на ста seed.
- [x] 2.4 Реализовать расстановку баз в противоположных углах на симметричных позициях с расчисткой окрестности радиусом две клетки. Проверка: тест на симметричность позиций и проходимость окрестности.
- [x] 2.5 Реализовать проверку связности обходом в ширину от базы первого игрока. Проверка: юнит-тест на искусственной карте с заведомо отрезанной областью.
- [x] 2.6 Реализовать прорубание симметричного коридора шириной три клетки по прямой между базами при обнаружении разрыва. Проверка: тест — после прорубания связность есть, симметрия не нарушена.
- [x] 2.7 Собрать всё в функцию `generateMap(seed)` и включить карту в `WorldState`. Проверка: `createWorld(seed)` возвращает мир с картой, `typecheck` проходит.
- [x] 2.8 Включить карту в расчёт `checksum`. Проверка: golden-тест обнаружил изменение состояния мира и потребовал обновления эталона — ровно то поведение, ради которого он существует.

## 3. Тесты генерации

- [x] 3.1 Тест воспроизводимости: одинаковый seed даёт побитово одинаковую карту. Проверка: сравнение всех клеток и позиций баз.
- [x] 3.2 Тест симметрии на ста seed: для каждой клетки проходимость совпадает с парной относительно центра. Проверка: тест зелёный.
- [x] 3.3 Тест связности на тысяче seed: путь между базами существует всегда. Проверка: тест зелёный.
- [x] 3.4 Тест плотности на ста seed: доля скал в диапазоне от 8 до 25 процентов. Проверка: тест зелёный, фактическая плотность около 15 процентов.
- [x] 3.5 Обновить golden-тест детерминизма с учётом карты в состоянии мира и зафиксировать новый эталон `3784177154`. Проверка: тест зелёный.

## 4. Изометрическая проекция

- [x] 4.1 Реализовать в `apps/client` функции `worldToScreen` и `screenToWorld`. Проверка: юнит-тест на круговое преобразование.
- [x] 4.2 Покрыть тестом соотношение 2:1: смещение по соседней клетке даёт вдвое больший сдвиг по горизонтали, чем по вертикали. Проверка: тест зелёный.
- [x] 4.3 Реализовать вычисление числа видимых клеток для заданного размера окна. Проверка: тест — при окне 1920 × 1080 видимая доля попадает в диапазон 7–14 процентов карты (фактически 9,8).

## 5. Каркасный рендер территории

- [x] 5.1 Отрисовать сетку клеток длинными линиями через всю карту, а не отрезками на каждую клетку. Проверка: 194 отрезка вместо 36 тысяч.
- [x] 5.2 Отрисовать рёбра скал только на границе между проходимой и непроходимой клеткой. Проверка: юнит-тест — у монолита 3 × 3 отрисовано 12 рёбер, а не 36; число рёбер растёт как периметр, а не как площадь.
- [x] 5.3 Отрисовать контур карты и метки баз. Проверка: базы различимы цветами своей и чужой стороны из дизайн-токенов.
- [x] 5.4 Строить геометрию территории один раз и кешировать её в контейнере. Проверка: `setMap` перестраивает геометрию только при смене карты, движение камеры сдвигает контейнер.
- [x] 5.5 Взять все цвета из дизайн-токенов, без литералов в коде отрисовки. Проверка: цвет чужой стороны переведён из oklch в шестнадцатеричный вид, чтобы его мог прочитать и рендерер; литералы остались только как запасные значения.
- [x] 5.6 Сгруппировать отрезки одного стиля в одну обводку. Проверка: обращений к видеокарте шесть вместо нескольких тысяч.

## 6. Камера

- [x] 6.1 Реализовать камеру как смещение контейнера сцены, с ограничением по границам карты. Проверка: при попытке уйти за край камера останавливается.
- [x] 6.2 Подключить управление камерой: перетаскивание мышью и стрелки. Проверка: карта прокручивается из угла в угол, e2e-тест доезжает до базы.
- [x] 6.3 Пересчитывать ограничения камеры при изменении размера окна. Проверка: обработчик resize вызывает пересчёт и повторное ограничение.

## 7. Интеграция

- [x] 7.1 Заменить заглушку сетки в `scene.ts` новым изометрическим рендером. Проверка: страница открывается, видна изометрическая карта со скалами.
- [x] 7.2 Вывести в HUD seed текущей карты, долю видимой площади и плотность скал. Проверка: значения отображаются и меняются при смене seed.
- [x] 7.3 Добавить перегенерацию карты по клавише `R`. Проверка: e2e-тест подтверждает смену seed.

## 8. Проверка и документация

- [x] 8.1 Обновить e2e-тест: на странице изометрическая карта, HUD виден, seed меняется, ошибок в консоли нет. Проверка: `pnpm e2e` зелёный, 4 теста.
- [x] 8.2 Измерить частоту кадров при непрерывном движении камеры. Проверка: e2e-тест требует не ниже 55; фактически 60. Потребовалось включить аппаратный рендеринг флагами запуска — без них headless-Chromium рисует через SwiftShader и выдаёт около 16 кадров на любой сцене.
- [x] 8.3 Прогнать полный цикл: `pnpm verify` и `pnpm e2e`. Проверка: 72 юнит-теста и 4 e2e зелёные, линт, типы и сборка проходят.
- [x] 8.4 Сослаться на `docs/game-design.md` из `README.md` и `CLAUDE.md`. Проверка: ссылки на месте, документы не противоречат друг другу.
- [x] 8.5 Починить конфигурацию двух окружений для `sim`. Проверка: обнаружено, что ключ `test.projects` появился только в Vitest 3, а у нас 2.1 — тесты шли в одном окружении. Прогон перенесён в корневой `vitest.workspace.ts`, теперь ядро действительно проверяется и в Node, и в браузерном окружении.
