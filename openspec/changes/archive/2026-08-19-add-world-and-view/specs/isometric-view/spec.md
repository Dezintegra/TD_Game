## ADDED Requirements

### Requirement: Изометрическая проекция с фиксированным углом

Клиент SHALL отображать мир в изометрической проекции с соотношением сторон
клетки 2:1 — ширина клетки на экране вдвое больше её высоты. Угол обзора
SHALL быть неизменным: ни игрок, ни игровая логика не могут его поворачивать
или наклонять.

Преобразование мировых координат в экранные SHALL быть чистой функцией,
а обратное преобразование SHALL восстанавливать исходную клетку.

#### Scenario: Прямое и обратное преобразование согласованы

- **WHEN** координаты произвольной клетки переводятся в экранные и обратно
- **THEN** восстановленная клетка совпадает с исходной

#### Scenario: Соотношение сторон соблюдено

- **WHEN** на экран проецируются две соседние клетки по каждой из осей мира
- **THEN** горизонтальное смещение вдвое превышает вертикальное

#### Scenario: Угол обзора неизменен

- **WHEN** выполняются любые действия игрока, включая движение камеры
- **THEN** матрица проекции остаётся прежней

### Requirement: Каркасная отрисовка территории

Территория SHALL отрисовываться линиями: сетка клеток, рёбра непроходимых
областей и границы карты. Заливки и текстуры для территории MUST NOT
использоваться.

Цвета линий SHALL браться из дизайн-токенов и не задаваться литералами
в коде отрисовки.

#### Scenario: Сетка клеток видна

- **WHEN** карта отрисована
- **THEN** на экране присутствует сетка линий, повторяющая клетки в изометрии

#### Scenario: Препятствия выделены на фоне пустых клеток

- **WHEN** на карте есть непроходимые клетки
- **THEN** их рёбра отрисованы линиями, визуально отличными от линий обычной сетки

### Requirement: Территория не перестраивается каждый кадр

Геометрия территории SHALL строиться заново только при изменении карты.
Между изменениями отрисовка SHALL переиспользовать готовую геометрию,
а движение камеры SHALL выполняться смещением контейнера сцены.

#### Scenario: Движение камеры не вызывает перестроение

- **WHEN** камера перемещается по карте в течение ста кадров подряд
- **THEN** геометрия территории не перестраивается ни разу

#### Scenario: Изменение карты вызывает перестроение

- **WHEN** клетка карты меняет проходимость
- **THEN** геометрия территории перестраивается

### Requirement: Ограничение камеры границами карты

Камера SHALL быть ограничена так, чтобы видимая область не выходила за
пределы карты. При попытке сместиться дальше камера SHALL остановиться
у границы.

#### Scenario: Камера упирается в край карты

- **WHEN** камера смещается за пределы карты в любом из четырёх направлений
- **THEN** её положение ограничивается краем, и за границей карты пустота не показывается

### Requirement: Видимая доля карты

При стандартном размере окна видимая область SHALL составлять примерно
десятую часть карты, в диапазоне от 7 до 14 процентов её площади.

#### Scenario: Видимая площадь укладывается в заданный диапазон

- **WHEN** страница открыта в окне 1920 × 1080
- **THEN** число одновременно видимых клеток составляет от 7 до 14 процентов от общего числа клеток карты

### Requirement: Стабильная частота кадров при отрисовке территории

Отрисовка территории SHALL укладываться в бюджет кадра: при непрерывном
движении камеры по полной карте частота кадров SHALL оставаться не ниже
55 кадров в секунду на эталонной машине разработки.

#### Scenario: Частота кадров держится при движении камеры

- **WHEN** камера непрерывно движется по карте в течение пяти секунд
- **THEN** средняя частота кадров не опускается ниже 55
