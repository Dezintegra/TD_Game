# Задачи

## 1. Убрать тень генерала

- [x] 1.1 Удалить `SHADOW_PARTS` и `generalShadow` из
      `apps/client/src/game/models.ts`, вместе с `shadowCache` и его
      сбросом в `ensurePalette` и `resetModelCache`.
      *Проверка:* поиск `generalShadow|SHADOW_PARTS` по `apps/` пуст.
- [x] 1.2 Удалить отрисовку тени и константу `SHADOW_ALPHA`
      из `apps/client/src/game/entities.ts`; убрать необязательный
      аргумент `alpha` у `paintSilhouette` — он был только для тени.
      *Проверка:* `pnpm typecheck` зелёный.
- [x] 1.3 Снять тесты тени в `models.test.ts`. Отдельный тест «тени нет»
      не заводится: тени нет в типах, и проверять её отсутствие
      в рантайме значило бы проверять компилятор. Взамен закреплено,
      что отражение по-прежнему не несёт окантовки.
      *Проверка:* `pnpm test` зелёный.

## 2. Переделать модель генерала в вертолёт

- [x] 2.1 Завести помощник `disc(forward, side, radius, corners)` рядом
      с `box` и `taper`: правильный многоугольник в местных координатах,
      с полушаговым сдвигом первого угла.
      *Проверка:* через готовый силуэт, а не в обход инкапсуляции, —
      тест «несёт ровно два светящихся хувера» считает многоугольники
      чистого цвета стороны и требует ровно двух.
- [x] 2.2 Заменить `JET_PARTS` на `GUNSHIP_PARTS` по составу
      из `design.md`: киль, стабилизатор, балка, кабина, нос, фонарь,
      два пилона, два хувера, два неоновых диска.
      *Проверка:* тесты «идёт над землёй», «шире любого юнита»,
      «поворачивается на всех восьми румбах» зелёные.
- [x] 2.3 Переписать шапку раздела в `models.ts`: она называлась
      «Истребитель генерала» и объясняла самолёт.
      *Проверка:* поиск `истребител` по `apps/client/src` пуст.
- [x] 2.4 Проверить, что признак «оторван дальше всех» не пострадал:
      `GENERAL_ALTITUDE` не тронута, нижняя точка модели выше нуля.
      *Проверка:* тест «идёт над землёй».

## 3. Посмотреть глазами

- [x] 3.1 Собрать предпросмотр SVG по рецепту из
      `preview-svg-instead-of-screenshot`: генерал на восьми румбах
      и штурмовик для масштаба, отдельной полосой — в натуральную
      величину.
      *Проверка:* под генералом одно пятно, а не два; силуэт читается
      вертолётом на приближении и различим на сорока пикселях.
- [x] 3.2 Проверить отражение: хуверы и их свечение попадают
      в отражение и ложатся глубже кабины.
      *Проверка:* та же картинка, область под машиной.

## 4. Закрыть

- [x] 4.1 Обновить `docs/game-design.md` там, где генерал описан
      истребителем и где упомянута его тень.
      *Проверка:* поиск `истребител` по `docs/game-design.md` пуст.
- [x] 4.2 `npx openspec validate --changes --strict`.
      *Проверка:* без ошибок.
- [x] 4.3 `pnpm verify`.
      *Проверка:* линт, типы, тесты и сборка зелёные.

## 5. Остаётся владельцу изменения

- [x] 5.1 Посмотреть машину в живом матче, а не только на предпросмотре:
      как она читается в строю, при осаде базы и рядом с чужим генералом.
      *Проверка:* владелец изменения посмотрел игру 21.08.2026 и принял
      облик: вся техника, включая генерала, читается пропорционально.
      Предпросмотр этого не доказывал — он показывает машину в пустоте,
      а вопрос был про соседство с другими.
- [x] 5.2 Заархивировать **после** `turn-field-into-mirror` — порядок
      и его причина записаны в `README.md`. Условие выполнено:
      `turn-field-into-mirror` заархивирован раньше, 21.08.2026.
      *Проверка:* требование `Генерал выглядит ударным вертолётом
      и идёт над землёй` есть в `openspec/specs/unit-appearance/spec.md`,
      требования про истребителя там нет, Purpose спеки цел.
