# Задачи

## 1. Правило в ядре

- [x] 1.1 Добавить `BASE_BUILD_EXCLUSION` в `packages/shared/src/balance.ts`
      с комментарием, из чего величина выведена: основание базы плюс
      кольцо, в котором обычно появляется юнит. Отправная точка — три
      клетки от центра базы.
      *Проверка:* сборка зелёная; величина заметно меньше радиуса поиска
      свободной клетки в `spawnUnit`.
- [x] 1.2 Добавить причину отказа `TooCloseToBase` в `RejectReason`.
      Отдельную, а не общую с «клетка занята»: занятость означает
      «целься в другое место», близость к базе — «здесь нельзя
      в принципе».
      *Проверка:* тип экспортируется из `@td/shared`.
- [x] 1.3 Проверку в `build` (`packages/sim/src/apply.ts`) поставить
      **до** проверок занятости и живых объектов. В комментарии объяснить
      порядок: клетка у базы, где вдобавок стоит юнит, — это прежде всего
      клетка у базы.
      *Проверка:* тест — в клетке у базы с чужим юнитом причина отказа
      «слишком близко к базе», а не «в клетке живой объект».
- [x] 1.4 Тесты правила: у своей базы, у чужой базы, за пределами
      радиуса — строится.
      *Проверка:* три случая проходят; при откате правки 1.3 падают.

## 2. Работоспособность под осадой

- [x] 2.1 Тест: обстроить базу настолько, насколько правила позволяют,
      и заказать юнита.
      *Проверка:* юнит появляется. Это главный тест изменения — ради
      него всё и делается.
- [x] 2.2 Тест: то же с возрождением генерала.
      *Проверка:* генерал возвращается в игру.
- [x] 2.3 Тест: укрепление собственной базы за пределами кольца
      по-прежнему работает.
      *Проверка:* башня появляется; правило не превратилось в запрет
      обороны.

## 3. Интерфейс

- [x] 3.1 Добавить правило в `isHoverAllowed`
      (`apps/client/src/game/bootstrap.ts`), взяв ту же константу,
      а не переписав числом.
      *Проверка:* клетка у базы подсвечена недоступной до нажатия.
- [x] 3.2 Подпись причины в `REJECT_LABEL`.
      *Проверка:* сообщение появляется и отличается от «клетка занята».

## 4. Эталон и завершение

- [x] 4.1 Обновить эталонное число в `determinism.golden.test.ts` тем же
      коммитом. Правило игры изменилось — число обязано измениться,
      и это правильный сигнал.
      *Проверка:* тест зелёный с новым числом.
- [x] 4.2 Проверить, что сценарий golden-теста **всё ещё доводит дело
      до боя**: у него есть отдельная проверка на это, и если запрет
      отсёк слишком много построек, сценарий выродится в пустой прогон,
      продолжая «проходить».
      *Проверка:* проверка на бой зелёная; если нет — правится сценарий,
      а не ослабляется правило.
- [x] 4.3 Прогнать пачку матчей на арене до и после.
      *Проверка:* число построек у базы не обвалилось, матчей с нулевым
      производством не появилось. Если обвалилось — радиус выбран
      неверно, править его, а не правило.
- [x] 4.4 Прогнать `pnpm verify`.
      *Проверка:* линт, типы, тесты и сборка зелёные.
