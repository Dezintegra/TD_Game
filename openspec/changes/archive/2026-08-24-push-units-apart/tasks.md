## 1. Числа баланса

- [x] 1.1 Завести в `packages/shared/src/balance.ts` радиусы расталкивания
      по типам юнитов (штурмовик 0,20, снайпер 0,19, Тесла 0,24 клетки)
      с комментарием, откуда взяты габариты корпуса. Проверка: значения
      видны из `@td/shared`, `pnpm typecheck` зелёный
- [x] 1.2 Завести там же клиренс от непроходимых клеток (0,26 клетки),
      множитель затухания (0,5) и правило потолка толчка (половина
      скорости машины). Проверка: тест в `balance.test.ts`, что радиус
      каждого типа меньше половины длины его корпуса, а клиренс —
      больше любого радиуса

## 2. Проход расталкивания

- [x] 2.1 Создать `packages/sim/src/crowd.ts`: сетка соседей с ячейкой
      в одну клетку, заполнение из живых юнитов. Проверка: тест кладёт
      машины в известные клетки и убеждается, что в выборку соседей
      попадают только девять ячеек вокруг
- [x] 2.2 Добавить расчёт попарных смещений с разведением совпавших
      по номерам сущностей. Проверка: тест «две машины в одной точке
      расходятся» из спецификации
- [x] 2.3 Добавить отталкивание от занятых клеток на клиренс: ближайшая
      точка квадрата клетки, мягкий толчок. Проверка: тест на машину,
      поставленную вплотную к скале
- [x] 2.4 Сделать применение смещений вторым проходом (двухфазность),
      с потолком от скорости машины и затуханием. Проверка: тест
      «порядок машин в массиве не влияет на исход»
- [x] 2.5 Добавить жёсткую проверку точки назначения со скольжением
      вдоль свободной оси. Проверка: тест «прижатая к скале машина
      не проваливается в неё»

## 3. Встраивание в тик

- [x] 3.1 Вызвать проход в `packages/sim/src/step.ts` между `moveUnits`
      и `resolveCombat`, с комментарием о том, почему именно там.
      Проверка: `pnpm test --filter @td/sim` проходит, кроме эталонов
- [x] 3.2 Убедиться, что в состояние мира не добавилось ни одного поля
      и `PROTOCOL_VERSION` не тронут. Проверка: `git diff` по
      `packages/protocol` и `world.ts` пуст по полям состояния

## 4. Тесты правил

- [x] 4.1 `crowd.test.ts`: разведённые машины остаются на месте; радиус
      зависит от типа (две Теслы стоят дальше друг от друга, чем два
      штурмовика)
- [x] 4.2 `crowd.test.ts`: зеркальные войска получают зеркальный строй
      до единицы
- [x] 4.3 `crowd.test.ts`: Теслу не смещает быстрее половины её скорости
      даже в самой плотной толпе
- [x] 4.4 `crowd.test.ts`: генерал и погибшие машины в расталкивании
      не участвуют
- [x] 4.5 `step.test.ts`: сто машин проходят сквозь проход шириной
      в клетку и все оказываются по другую сторону — проверка, что мягкое
      расталкивание нигде не запирает войско
- [x] 4.6 `step.test.ts`: остановившаяся у цели машина стреляет в том же
      тике, в котором её сместило расталкивание

## 5. Эталоны

- [x] 5.1 Пересчитать эталон `packages/sim/src/determinism.golden.test.ts`
      и записать новое значение тем же коммитом, что и правило.
      Проверка: тест зелёный, в diff видно старое и новое число
- [x] 5.2 Пересчитать эталон `packages/ai/src/profile.golden.test.ts`.
      Проверка: тест зелёный

## 6. Замеры на живом коде

- [x] 6.1 Снять цену прохода внутри `step` на 400 машинах (p50/p95/max)
      и записать таблицу в `openspec/changes/push-units-apart/README.md`.
      Проверка: таблица в файле, а не в переписке
- [x] 6.2 Повторить замер плотности (макс. машин в квадрате 3×3, число
      стрелков у базы, машины с центром в стене) **на трёх разных картах**
      и записать таблицу туда же. Проверка: три строки в таблице,
      расхождение между картами прокомментировано

## 7. Проверка баланса ареной

- [x] 7.1 Прогнать арену профилем, который покупает Теслу, до и после
      изменения; записать доли побед и длительность матчей в таблицу
      в `README.md` изменения. Проверка: таблица с числом матчей,
      а не «прогнал, всё нормально»
- [x] 7.2 По результату решить, нужен ли рычаг накрытия (ближний радиус
      1 → 1,5 клетки). Если нужен — **не делать здесь**, завести отдельное
      изменение. Проверка: решение записано в `README.md` с числами

## 8. Документы

- [x] 8.1 Закрыть в `docs/game-design.md` открытый вопрос «Столкновения
      юнитов» (строка 891): заменить на описание принятого правила
      с числами. Проверка: раздела «не решено» по этому пункту больше нет
- [x] 8.2 Поправить там же обоснование накрытия (строка 337): куча больше
      не состояние по умолчанию, накрытие наказывает кучу, собранную
      игроком. Проверка: текст не противоречит новому правилу
- [x] 8.3 Прогнать `pnpm verify` целиком. Проверка: линт, типы, тесты
      и сборка зелёные
