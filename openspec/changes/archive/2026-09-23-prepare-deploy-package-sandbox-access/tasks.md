## 1. Deploy sandbox package access

- [x] 1.1 Add snapshot-scoped Windows deploy package writable roots and focused regression coverage; verify generated arguments and the pinned-CLI install in the previously failing snapshot.
