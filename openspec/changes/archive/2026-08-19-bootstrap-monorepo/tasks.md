## 1. Корень репозитория

- [x] 1.1 Создать корневой `package.json`: приватный, ESM, поля `engines` (Node >= 22) и `packageManager` (pnpm), скрипты `dev`, `build`, `typecheck`, `lint`, `format`, `test`, `e2e` — все делегируют Turborepo. Проверка: `pnpm run` выводит весь список скриптов.
- [x] 1.2 Создать `pnpm-workspace.yaml` с шаблонами `apps/*` и `packages/*`. Проверка: `pnpm ls -r --depth -1` показывает корень и пустой список пакетов без ошибок.
- [x] 1.3 Создать `tsconfig.base.json`: `strict: true`, `target: ES2022`, `module: ESNext`, `moduleResolution: Bundler`, `isolatedModules`, `noUncheckedIndexedAccess`. Проверка: `npx tsc --showConfig -p tsconfig.base.json` печатает конфиг без ошибок.
- [x] 1.4 Создать `turbo.json` с задачами `build` (зависит от `^build`, выходы `dist/**`), `typecheck`, `lint`, `test` и `dev` (без кеша, персистентная). Проверка: `pnpm exec turbo run build --dry-run` строит корректный граф.
- [x] 1.5 Добавить `.gitignore` (`node_modules`, `dist`, `.turbo`, `coverage`, `.env*`), `.editorconfig` и `.npmrc`. Проверка: `git status` не показывает служебные каталоги.
- [x] 1.6 Настроить Prettier: корневой `.prettierrc` и `.prettierignore`. Проверка: `pnpm format:check` проходит на текущем содержимом репозитория.

## 2. Линт и защита архитектурных границ

- [x] 2.1 Создать корневой `eslint.config.js` (flat config) с базовыми правилами TypeScript и интеграцией Prettier. Проверка: `pnpm lint` проходит на пустом репозитории.
- [x] 2.2 Добавить правило запрета недетерминированных API в `packages/sim`: `no-restricted-globals` и `no-restricted-properties` на `Math.random`, `Date`, `performance`, `window`, `document`, `process`. Проверка: временный файл с `Math.random()` в `packages/sim` даёт ошибку линта, после удаления файла линт снова чистый.
- [x] 2.3 Добавить правило направления зависимостей через `no-restricted-imports`: запрет импортов `apps/*` из библиотек и запрет взаимных импортов между `client` и `server`. Проверка: временный импорт из `apps/client` внутри `apps/server` даёт ошибку уровня `error`.

## 3. Пакет @td/shared

- [x] 3.1 Создать `packages/shared` с `package.json` (имя `@td/shared`, `type: module`, поле `exports` с раздельными путями для типов и кода) и `tsconfig.json`, наследующим базовый. Проверка: `pnpm --filter @td/shared build` создаёт `dist`.
- [x] 3.2 Описать базовые доменные типы: `PlayerId`, `TickNumber`, `MatchId`, `Vec2` в целочисленных единицах, перечисление типов команд игрока. Проверка: `pnpm --filter @td/shared typecheck` проходит.
- [x] 3.3 Добавить игровые константы: частота тика, размер поля, множитель фиксированной точки и хелперы перевода между клетками и внутренними единицами. Проверка: юнит-тест на круговое преобразование клетка → единицы → клетка.

## 4. Пакет @td/sim

- [x] 4.1 Создать `packages/sim` с зависимостью только от `@td/shared`. Проверка: в `package.json` нет других внутренних зависимостей, `pnpm --filter @td/sim build` проходит.
- [x] 4.2 Реализовать seeded PRNG (xorshift32), чьё состояние является частью состояния мира. Проверка: юнит-тест — одинаковый seed даёт одинаковую последовательность из 1000 значений.
- [x] 4.3 Описать структуру `WorldState` целиком на целых числах и функцию `createWorld(seed)`. Проверка: типы запрещают дробные значения в координатах, `typecheck` проходит.
- [x] 4.4 Реализовать чистую функцию `step(state, inputs) → state` с фиксированным тиком без мутации входного состояния. Проверка: юнит-тест подтверждает, что исходный объект состояния не изменился после вызова.
- [x] 4.5 Реализовать `checksum(state)` — детерминированную свёртку состояния в 32-битное число. Проверка: юнит-тест — одинаковые состояния дают одинаковую сумму, отличие в одном поле меняет её.
- [x] 4.6 Добавить golden-тест: 1000 тиков на фиксированном seed и записи входов, сверка итоговой контрольной суммы с эталоном в репозитории. Проверка: тест проходит, а искусственная правка константы в ядре его роняет. Эталон: `2497802443`.
- [x] 4.7 Настроить прогон тестов `sim` в двух окружениях Vitest — `node` и `jsdom`. Проверка: `pnpm --filter @td/sim test` выполняет оба проекта, контрольные суммы совпадают.

## 5. Пакет @td/protocol

- [x] 5.1 Создать `packages/protocol` с зависимостью только от `@td/shared`. Проверка: `pnpm --filter @td/protocol build` проходит.
- [x] 5.2 Описать перечисление типов сообщений и версию протокола в заголовке кадра. Проверка: `typecheck` проходит, версия экспортируется как константа.
- [x] 5.3 Реализовать кодек `encode`/`decode` поверх `DataView` для сообщений `Ping` и `Pong`. Проверка: round-trip-тест — закодированное и раскодированное сообщение равно исходному.
- [x] 5.4 Добавить обработку неизвестного типа сообщения и несовпадения версии — декодер возвращает явную ошибку, а не бросает исключение. Проверка: юнит-тест на буфер с чужой версией.

## 6. Пакет @td/ui

- [x] 6.1 Создать `packages/ui` с React как peer-зависимостью и дизайн-токенами (цвета, отступы, типографика) в виде CSS-переменных. Проверка: `pnpm --filter @td/ui build` проходит.
- [x] 6.2 Добавить два базовых компонента — `Button` и `Panel` — с типизированными props. Проверка: юнит-тест рендера через Vitest и Testing Library.

## 7. Приложение apps/server

- [x] 7.1 Создать `apps/server` с Fastify и эндпоинтом `GET /health`, возвращающим `{ status: 'ok' }`. Проверка: запрос на запущенный сервер отдаёт 200.
- [x] 7.2 Добавить WebSocket-слой на `ws`, спрятанный за интерфейсом транспорта `GameTransport`. Проверка: интерфейс не упоминает `ws` в своей сигнатуре.
- [x] 7.3 Реализовать обработчик: на входящий бинарный `Ping` отвечать `Pong` с тем же номером тика через `@td/protocol`. Проверка: интеграционный тест на Vitest с реальным WebSocket-клиентом.
- [x] 7.4 Настроить режим разработки с автоперезапуском при правках. Проверка: изменение файла перезапускает сервер без ручного вмешательства.

## 8. Приложение apps/client

- [x] 8.1 Создать `apps/client` на Vite с React 19 и TypeScript. Проверка: `pnpm --filter @td/client dev` открывает страницу без ошибок в консоли.
- [x] 8.2 Инициализировать PixiJS v8: приложение монтируется в отдельный контейнер, `canvas` растягивается на окно, запускается цикл `requestAnimationFrame`. Проверка: в DOM есть `canvas` с ненулевыми размерами.
- [x] 8.3 Реализовать `GameLoop` вне React: фиксированный шаг симуляции с накоплением остатка времени и интерполяцией при отрисовке. Проверка: счётчик тиков растёт равномерно при искусственном изменении частоты кадров.
- [x] 8.4 Завести store на Zustand как единственный канал обмена между PixiJS и React. Проверка: правка HUD не затрагивает код сцены и наоборот.
- [x] 8.5 Сделать HUD-оверлей на React поверх `canvas` с индикатором состояния соединения и текущим номером тика. Проверка: индикатор меняется при остановке сервера.
- [x] 8.6 Подключить WebSocket-клиент: соединение, отправка `Ping` раз в секунду, отображение полученного `Pong`. Проверка: в HUD видно растущее число успешных обменов.
- [x] 8.7 Убедиться, что горячая перезагрузка HUD не переинициализирует сцену PixiJS. Проверка: правка текста в HUD применяется, счётчик тиков продолжает расти с прежнего значения.

## 9. Тестовый и сборочный контур

- [x] 9.1 Настроить корневую конфигурацию Vitest с workspace-режимом, покрывающим все пакеты. Проверка: `pnpm test` выполняет тесты всех пакетов одной командой.
- [x] 9.2 Настроить Playwright: конфиг поднимает сервер и клиент перед прогоном. Проверка: `pnpm e2e` стартует оба процесса и корректно их гасит.
- [x] 9.3 Написать e2e-тест сквозной вертикали: страница открывается, `canvas` отрисован, HUD виден поверх поля, индикатор соединения активен, `pong` получен за 5 секунд. Проверка: тест зелёный.
- [x] 9.4 Добавить workflow GitHub Actions: `pnpm install --frozen-lockfile`, затем формат, линт, типы, тесты и сборка. Проверка: workflow описан и повторяет локальную команду `pnpm verify`.
- [x] 9.5 Проверить кеширование Turborepo: повторный `pnpm build` без правок отдаёт задачи из кеша. Проверка: вывод содержит `FULL TURBO` и время порядка десятков миллисекунд.

## 10. Документация

- [x] 10.1 Написать корневой `README.md`: структура репозитория, назначение каждого пакета, команды запуска, требования к окружению. Проверка: junior-разработчик поднимает проект по README без дополнительных вопросов.
- [x] 10.2 Создать `CLAUDE.md` с архитектурными правилами: границы пакетов, запрет недетерминированных API в `sim`, разделение React и PixiJS, порядок работы через OpenSpec. Проверка: файл существует и не противоречит `openspec/config.yaml`.
- [x] 10.3 Прогнать полный цикл проверок и зафиксировать результат: `pnpm install && pnpm verify && pnpm e2e`. Проверка: все команды завершаются успешно.
