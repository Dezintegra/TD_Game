## 1. Подавление браузерных жестов

- [x] 1.1 Задать `touch-action: none` контейнеру сцены в
      `apps/client/src/styles.css`.
      Проверка: протяжка пальцем по полю не прокручивает страницу,
      события движения указателя доходят до управления.
- [x] 1.2 Разрешить горизонтальную прокрутку тулбару: `touch-action: pan-x`
      и `overflow-x: auto` под `@media (pointer: coarse)`.
      Проверка: тулбар шире экрана едет вбок и остаётся одной строкой,
      высота полосы не меняется.

## 2. Замирающий свайп

- [x] 2.1 Добавить в `ControlState` состояние джойстика: точка отсчёта
      и текущее положение пальца в экранных координатах, либо null.
      Проверка: проверка типов проходит; по сети признак не уходит.
- [x] 2.2 Развести разбор указателя по `pointerType`: касание ставит
      точку отсчёта, мышь работает как прежде.
      Проверка: юнит-тест — `pointerdown` с `pointerType: 'mouse'`
      выделяет сразу, с `'touch'` не выделяет ничего.
- [x] 2.3 Включать джойстик по превышению порога сдвига и считать
      направление той же `worldDirection`, что и для клавиш.
      Проверка: юнит-тест — свайп вверх даёт то же направление, что
      клавиша движения вверх; сдвиг меньше порога даёт «стоп».
- [x] 2.4 Держать направление при неподвижном пальце и останавливать
      генерала по `pointerup` и по `pointercancel`.
      Проверка: юнит-тест — после отрыва направление «стоп»;
      `pointercancel` останавливает так же, как отрыв.

## 3. Действие по отрыву

- [x] 3.1 Перенести действие касания (выделение, постройка, удар, цель,
      миникарта) в `pointerup`, выполняя его только если палец
      не уходил за порог.
      Проверка: юнит-тест — тап выделяет, свайп не выделяет и не строит.
- [x] 3.2 Убедиться, что ветка мыши по-прежнему действует в момент
      нажатия.
      Проверка: юнит-тест — нажатие мышью выделяет, не дожидаясь
      отпускания.

## 4. Цель атаки кнопкой

- [x] 4.1 Завести режим наведения цели рядом с наведением удара:
      взаимное исключение с прочими режимами, снятие через `cancelModes`.
      Проверка: юнит-тест — `Esc` снимает режим цели; включение режима
      цели гасит режим стройки.
- [x] 4.2 Провести признак режима в снимок матча и в команду HUD.
      Проверка: проверка типов проходит.
- [x] 4.3 Нарисовать пиктограмму цели и добавить плитку рядом с ядерным
      ударом; подсветка активного режима как у остальных.
      Проверка: линт не находит литеральных цветов; e2e — нажатие плитки
      и клетки назначает цель.
- [x] 4.4 Сохранить назначение цели правой кнопкой мыши без режима.
      Проверка: e2e — правая кнопка по чужой постройке меняет цель.

## 5. Отрисовка джойстика

- [x] 5.1 Провести состояние джойстика в намерение игрока, которое
      получает сцена.
      Проверка: проверка типов проходит.
- [x] 5.2 Нарисовать джойстик отдельным слоем сцены в экранных
      координатах: точка отсчёта, порог, текущее положение пальца.
      Цвета из токенов.
      Проверка: заглушка Graphics — при джойстике за порогом рисуются
      обе точки; без джойстика не рисуется ничего.
- [x] 5.3 Убедиться, что джойстик не ездит с камерой.
      Обеспечено СТРОЕНИЕМ, а не проверкой: слой джойстика висит прямо
      на сцене, рядом с миникартой, и в мировой контейнер не входит —
      камера двигает именно его, а не сцену. Отдельный тест на это
      проверял бы, что мы не переложили слой, то есть повторял бы чтение
      кода; поэтому его нет, а причина записана здесь и комментарием
      в `scene.ts`.

## 6. Крупные цели нажатия

- [x] 6.1 Завести в `packages/ui/src/tokens.css` токен размера цели
      нажатия и переопределить его под `@media (pointer: coarse)`.
      Проверка: юнит-тест читает токены и находит оба значения,
      крупное — не меньше 44.
- [x] 6.2 Перевести кнопки покупки уровня и плитки тулбара на токен.
      Проверка: измерение в браузере при эмуляции сенсора — кнопка
      не меньше 44 точек по обеим сторонам.

## 7. Проверки

- [x] 7.1 Прогнать `pnpm verify` целиком.
      Результат: 1290 из 1292. Упали ровно две — `siege.test.ts` из main,
      разобранные раньше и к этой ветке отношения не имеющие.
      По дороге один раз упал тест темпа тиков сервера (33 при пределе 32)
      — это машина под нагрузкой, серверного кода изменение не касается;
      в чистом прогоне он проходит.
- [x] 7.2 Прогнать сквозные проверки.
      Результат: 22 из 22 — 14 в `smoke.spec.ts` и 8 в `lobby.spec.ts`,
      прогнанных по отдельности. В общем прогоне под нагрузкой плавают
      проверки лобби (`Failed to find context` при закрытии контекста,
      таймаут в 30 секунд); по отдельности проходят все восемь.

      Одна настоящая регрессия была найдена и починена: при окне 1280
      десятая плитка не помещалась, флекс ужимал плитки, подписи внутри
      строк характеристик переносились, и полоса вырастала со 156 до 187
      при отведённых 177 — то есть наезжала на поле, молча. Лечение:
      плитка не сжимается, полоса едет вбок на любом указателе,
      по вертикали расти ей запрещено. Проверка переведена на 1920 × 1080
      — окно, для которого требование и записано.
- [x] 7.3 Проверить жесты в браузере с эмуляцией касания.
      Проверено синтезированными pointer-событиями с `pointerType: 'touch'`
      в живой странице:
      нажатие пальцем не делает ничего (0 команд);
      сдвиг за порог включает джойстик, направление не «стоп»;
      **замирание на 900 мс даёт ровно ОДНУ команду направления** — свайп
      держит направление и не заваливает сеть;
      отрыв даёт «стоп» и не выделяет;
      тап без сдвига выделяет клетку по отрыву.

      Размеры: на эмулированном телефоне `pointer: coarse` срабатывает,
      цель нажатия 44 точки, наименьшая стрелка ровно 44 × 44, тулбар
      едет вбок. На ноутбуке 1366 цель 20 точек, тулбар 1279 из 1366.
      При 1920 × 1080 запас 626 точек.

      Живого сенсорного устройства здесь нет: проверена грамматика жестов
      и размеры, но не эргономика — удобно ли это пальцем на самом деле,
      подтверждает владелец.
