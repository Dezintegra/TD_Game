# Задачи

## 1. Таблица рангов в балансе

- [x] 1.1 Завести в `packages/shared/src/balance.ts` общие пороги
      `VETERAN_RANK_KILLS = [1, 3, 6, 10, 15]`, `VETERAN_MAX_RANK = 5`
      и ДВЕ таблицы множителей: `VETERAN_STRUCTURE_PPM`
      (110 / 125 / 145 / 170 / 200 %) и `VETERAN_UNIT_PPM`
      (105 / 112 / 120 / 130 / 150 %).
      _Проверка:_ длины всех трёх массивов равны `VETERAN_MAX_RANK`,
      и множитель машины на каждом ранге меньше башенного.
- [x] 1.2 Записать в комментарии вывод порогов: треугольные числа,
      каждый следующий ранг дороже предыдущего ровно на одно убийство.
      И вывод множителей: шаг растёт (+10, +15, +20, +25, +30 п.п.)
      вслед за ценой, вершина ровная — ровно вдвое.
      _Проверка:_ в комментарии названо, откуда числа, а не только какие они.
- [x] 1.3 Функции `veteranRank(kills)` и `veteranPpm(rank)` там же.
      Не в `packages/sim`: таблица нужна и клиенту, чтобы рисовать погоны,
      а тянуть ядро симуляции ради двух чисел он не должен.
      _Проверка:_ `apps/client` импортирует их из `@td/shared`, линт границ
      пакетов молчит.
- [x] 1.4 Удалить `TOWER_KILL_GROWTH_PERCENT` и `TOWER_GROWTH_CAP_PPM`.
      В шапке `percent.ts` поправить перечисление «трёх мест, где величина
      растёт геометрически»: рост башни оттуда уходит.
      _Проверка:_ по репозиторию нет упоминаний обеих констант.
- [x] 1.5 Тесты таблицы: 0 убийств → ранг 0; 1 → 1; 2 → всё ещё 1;
      3 → 2; 14 → 4; 15 → 5; 40 → 5. Множитель пятого ранга —
      ровно `2 × PPM_ONE`.
      _Проверка:_ семь проверок зелёные.

## 2. Состояние мира: убийства вместо множителя

- [x] 2.1 В `packages/sim/src/world.ts` заменить у `StructureState`
      поле `growthPpm` на `kills` и добавить такое же `kills`
      к `UnitState`. Комментарий у обоих: это не прокачка, это заслуга
      конкретного объекта, и она гибнет вместе с ним.
      _Проверка:_ типы собираются, `createWorld` ставит нули.
- [x] 2.2 То же в `packages/sim/src/working.ts` — в `WorkingStructure`,
      `WorkingUnit`, `toWorking` и `fromWorking`.
      _Проверка:_ поле переживает круг «состояние → рабочая копия →
      состояние» (тест).
- [x] 2.3 В `packages/sim/src/checksum.ts` подмешать `kills` у построек
      **и у юнитов**. Порядок полей не менять — только заменить строку
      с `growthPpm` и дописать строку в блок юнитов.
      _Проверка:_ сумма меняется при изменении числа убийств у любого
      объекта (тест).
- [x] 2.4 Тест: множитель нигде не хранится, а выводится. Два объекта
      с одинаковым числом убийств дают одинаковые характеристики,
      даже если пришли к нему разными путями.
      _Проверка:_ тест зелёный.

## 3. Характеристики с учётом ранга

- [x] 3.1 В `packages/sim/src/stats.ts` заменить `structureMaxHealth`
      и `structureAttack` на приём числа убийств вместо `growthPpm`
      (или оставить приём `ppm`, а `ppm` считать вызывающему —
      выбрать одно и держаться его во всех вызовах).
      _Проверка:_ ни один вызов не передаёт `growthPpm`.
- [x] 3.2 Добавить `unitMaxHealth(baseline, kills)` и
      `unitAttack(baseline, kills)` рядом, с тем же комментарием
      про перемножение прокачки и ранга.
      _Проверка:_ функции покрыты тестом на перемножение: ×2 прокачки
      и ×2 ранга дают ×4.
- [x] 3.3 Проверить, что ранг **не** попал в дальность, скорость
      и перезарядку ни у юнита, ни у башни.
      _Проверка:_ тест — у объекта пятого ранга дальность и перезарядка
      те же, что у нулевого.

## 4. Награда за убийство

- [x] 4.1 В `packages/sim/src/combat.ts` перенести выдачу награды
      из `dealDamage` в `fire`: `dealDamage` возвращает признак гибели,
      `splash` — число убитых, `fire` складывает прямую цель с накрытием
      и награждает одним вызовом, передавая СЧЁТ.
      Счёт честный: убитые накрытием считаются все — в этом и смысл
      урона по площади. Один вызов вместо нескольких — условие
      правильности, а не оптимизация: здоровье добавляется разницей
      максимумов, и повтор сделал бы объект крепче своего максимума.
      _Проверка:_ два теста — разряд, положивший троих, даёт три
      убийства; здоровье после залпа ровно на максимуме нового ранга.
- [x] 4.2 Награждать и юнита, и башню. Базу и стену — не награждать
      (они и не стреляют). Генерала — не награждать рангом, его награда
      энергией остаётся как есть.
      _Проверка:_ четыре теста — башня, юнит, стена, генерал.
- [x] 4.3 При повышении ранга добавлять текущему здоровью разницу
      максимумов — и у башни, и у юнита.
      _Проверка:_ объект с полным здоровьем после повышения ранга
      по-прежнему на полном здоровье.
- [x] 4.4 Заменить предупреждение про накрытие в `dealDamage`: опасность
      снята правилом, а не рассуждением, и комментарий должен объяснять
      правило, а не пугать будущего автора.
      _Проверка:_ в комментарии названо, где именно правило исполняется.
- [x] 4.5 Убедиться, что убийства ядерным ударом ранга не дают: удар
      идёт мимо `dealDamage`, в `step.ts`.
      _Проверка:_ тест — после удара, накрывшего десять машин, рангов
      ни у кого не прибавилось.

## 5. Здоровье, привязанное к максимуму

- [x] 5.1 В `packages/sim/src/apply.ts` при покупке ветки прочности
      добавлять разницу максимумов **и юнитам тоже**: их максимум
      теперь зависит ещё и от ранга.
      _Проверка:_ тест — покупка прочности повышает здоровье живого
      ветерана на верную величину, а не на базовую.
- [x] 5.2 ~~В `packages/sim/src/step.ts` считать максимум юнита с рангом
      там, где здоровье ограничивается сверху.~~ **Отменено: чинить
      нечего.** Проверено обходом `Math.min` по ядру — здоровье юнита
      сверху не ограничивается нигде. Единственный `Math.min` с максимумом
      стоит в `advanceConstruction` и касается постройки, а она считается
      с рангом с самого начала. Задача была заведена по предположению,
      а не по факту.

## 6. Погоны на поле

- [x] 6.1 Новый `apps/client/src/game/rank-insignia.ts`: геометрия
      подложки, ёлочки и звезды. Ёлочка — обводкой, не заливкой:
      залитый треугольник в четыре пикселя сливается в пятно.
      Звезда — внешний радиус 4, внутренний 0,382 внешнего.
      _Проверка:_ юнит-тест на число вершин и на то, что звезда
      пятиконечная.
- [x] 6.2 Три токена в `packages/ui/src/tokens.css`: `--td-rank-field`,
      `--td-rank-stripe`, `--td-rank-gold`. **Шестнадцатеричные**:
      `toPixiColor` разбирает только `#rrggbb`.
      _Проверка:_ `token()` в `scene.ts` возвращает не запасное значение.
- [x] 6.3 Отрисовка в `apps/client/src/game/entities.ts` — в тот же
      `Graphics` полосы глубины, что и полоса здоровья: у построек
      и у юнитов, у своих и у чужих, не у генерала.
      _Проверка:_ в предпросмотре погон виден над башней и над машиной.
- [x] 6.4 Положение: выше полосы здоровья на 14 пикселей и левее центра
      на 9. Погон юнита поднимается вместе с машиной (`altitude`),
      иначе повиснет отдельно от неё.
      _Проверка:_ у повреждённого объекта видны и погон, и полоса,
      и они не перекрываются; погон качается вместе с машиной.
- [x] 6.5 В отражении машины погона нет.
      _Проверка:_ тест «в отражении машины погона нет» в `entities.test.ts`:
      машина размещается дважды (тело и отражение), погон рисуется один.
      Скриншотом это не проверить — машины запечены в текстуры, и в SVG
      их не видно.
- [x] 6.6 Ранг **не** попадает в ключ кеша запечённых машин
      (`machine-sprites.ts`). Попав туда, он умножил бы кеш вшестеро.
      _Проверка:_ размер кеша в коде не изменился.
- [x] 6.7 Замер стоимости погона на кадре.
      _Замерено_ (вызовов к `Graphics` / вершин на один знак):

      | ранг | вызовов | вершин |
      | ---- | ------- | ------ |
      | 1    | 7       | 7      |
      | 2    | 11      | 10     |
      | 3    | 15      | 13     |
      | 4    | 5       | 14     |
      | 5    | 5       | 14     |
      | _полоса здоровья_ | _4_ | _8_ |

      **Замер опроверг то, что было написано в коде.** В шапке
      `rank-insignia.ts` стояло «вдвое меньше, чем у полосы здоровья»;
      на деле погон дороже — вчетверо по вызовам и вдвое по вершинам.
      Число было выдумано, а не измерено; комментарий исправлен по
      замеру, а не подогнан.

      Запекать всё равно не нужно, но довод другой: полоса висит над
      ПОВРЕЖДЁННЫМИ (в осаде их сотни), погон — над ОТЛИЧИВШИМИСЯ
      (единицы). Полтора десятка вызовов на десяток объектов теряются
      рядом с сотнями полос.

      Порог закреплён тестом «не дороже пятнадцати вызовов и шестнадцати
      вершин»: погон рисуется в кадре, и обзаведись он однажды тенью
      и каймой — числа уехали бы молча.
      _Проверка:_ два теста в `rank-insignia.test.ts` зелёные.

## 7. Окно сведений

- [x] 7.1 В `apps/client/src/game/bootstrap.ts` отдавать в снимок ранг,
      число убийств и остаток до следующего ранга вместо `growthPercent`.
      _Проверка:_ поля есть в `SelectionView` (`store.ts`) и типы сходятся.
- [x] 7.2 В `apps/client/src/hud/StructureInfo.tsx` строка ранга вместо
      строки процентов. У пятого ранга следующего не обещать.
      У базы и стены строки ранга нет вовсе.
      _Проверка:_ три случая проверены в предпросмотре.

## 8. Эталоны, замысел и проверка

- [x] 8.1 Обновить `packages/sim/src/determinism.golden.test.ts`.
      Число менялось трижды: 2125526797 (ввод рангов), 1820139674 (ранг
      дошёл до атаки юнита), 1152514732 (смягчение таблицы машин плюс
      соседняя прокачка ядерного удара). Последнее записано владельцем
      соседней правки; здесь дописано, что означает ранговая часть.
      Честный счёт убийств за накрытие сумму НЕ сдвинул, и это ожидаемо:
      Теслы в эталонном сценарии нет — она стоит 250 при доходе,
      который целиком съедают покупки, и заказ отклоняется.
      _Проверка:_ тест зелёный, 219 тестов пакета `sim` проходят.
- [x] 8.2 Обновить `packages/ai/src/profile.golden.test.ts` — **обе**
      проверки: контрольную сумму и набор наблюдаемых величин.
      _Проверка:_ оба теста зелёные.
- [x] 8.3 Проверить, доходит ли хоть один объект в эталонных сценариях
      до третьего ранга. Если нет — записать это прямо в задаче
      и опереться на точечные тесты: зелёный эталон здесь ничего
      не доказывает.
      _Проверка:_ ответ записан числом, а не предположением.
- [x] 8.4 Поправить `docs/game-design.md`: раздел 4.2 (абзац
      про +5 процентов), 4.4 (убийства теперь приносят ранг, но
      по-прежнему не приносят энергии), 7.2 (погоны в облике),
      строка 15 в таблице соответствия тезисам.
      _Проверка:_ по документу нет упоминаний «+5 процентов».
- [x] 8.5 Прогнать матч против компьютера и посмотреть, как ранги
      ложатся на настоящий бой.
      _Замерено_ на трёх seed, осадный профиль против профиля
      по умолчанию, с рангами и без них:

      | seed | было (старое правило) | стало | лучшая машина |
      | ---- | --------------------- | ----- | ------------- |
      | 4242 | не кончился за 15 мин | 5,6 мин | 7 убийств |
      | 7    | 7,5 мин               | 5,7 мин | 5 убийств |
      | 99   | 5,9 мин               | 4,9 мин | 11 убийств |

      **Пятый ранг не берёт никто, а матчи стали короче проектной вилки
      10–15 минут.** Причина — ранги ЮНИТОВ: с отключёнными рангами
      юнитов и той же таблицей у башен матч на seed 4242 снова не
      кончается за 15 минут, а лучшая башня набирает 102 убийства.

      **Решение владельца баланса:** машине таблицу помягче
      (105 / 112 / 120 / 130 / 150 %). Перезамер на тех же трёх seed
      дал 4242 — «больше 15 минут», 7 — «больше 15 минут»,
      99 — 9,4 минуты, то есть матчи стали ДЛИННЕЕ прежних.

      Перезамер повторён после того, как соседняя правка ядерного удара
      села и `pnpm build` стал зелёным, — числа совпали до десятых,
      то есть ядерка на исход этих матчей не влияла:

      | seed | старое правило | общая таблица | мягкая таблица |
      | ---- | -------------- | ------------- | -------------- |
      | 4242 | >15 мин        | 5,6 мин       | >15 мин        |
      | 7    | 7,5 мин        | 5,7 мин       | >15 мин        |
      | 99   | 5,9 мин        | 4,9 мин       | 9,4 мин        |

      ⚠️ **Мягкая таблица перелетела в другую сторону.** Два матча
      из трёх перестали заканчиваться за пятнадцать минут, а раньше
      заканчивались. Причина видна в тех же прогонах: лучшая машина
      набирает 115 и 52 убийства — войска обеих сторон стали живучее,
      и бой превратился в размен. Три seed — не приговор, но и не шум:
      судить о балансе положено ареной (десятки матчей, все профили),
      и её прогон здесь не делался.
- [ ] 8.6 `pnpm verify` зелёный целиком.
      _Состояние на момент передачи:_ сборка зелёная, `sim` — 219 тестов,
      `shared` — 68, клиентские погоны и сущности — 26. Красными
      остаются два места, и НИ ОДНО из них не вызвано этим изменением:

      1. `packages/ai/src/siege.test.ts` — «осадный профиль заказывает
         Теслу». Проверено изоляцией: при полностью обнулённых таблицах
         рангов (и башенной, и машинной) осадный профиль всё равно
         не заказывает ни одной Теслы за двадцать минут. При этом
         денег ему хватает — пик казны 15 450 при цене Теслы 7 500.
         До того как в дерево приземлилась соседняя правка ядерного
         удара, тот же зонд давал четыре заказа, первый на 337-й секунде.
         Правка удешевила удар с 37 500 до 6 000 и добавила две ветки
         прокачки — то есть переставила всю развесовку трат противника.
         Чинить это надо там же, где и заводили, и у проекта уже есть
         для этого изменение `fix-ai-spending`.
      2. `apps/client/src/game/shots.ts` — неиспользованный импорт
         `CELL_SCALE_PX` роняет линт. Тоже чужая незакоммиченная правка.
