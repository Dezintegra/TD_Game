# Задачи

## 1. Приоритет генерала в выборе цели

- [x] 1.1 Разложить `chooseTarget` в `packages/sim/src/combat.ts`
      на пять явных ступеней приоритета; генерал становится второй
      ступенью, отдельным поиском ближайшего видимого вражеского
      генерала, ничья по меньшему индексу.
      *Проверка:* `pnpm --filter @td/sim test` — прежние тесты боя зелёные.
- [x] 1.2 Тесты приоритета целей. Вынесены в отдельный файл
      `packages/sim/src/targeting.test.ts`, а не дописаны в `combat.test.ts`:
      предмет у них свой (кого выбрали), фикстуры свои (генералы обоих
      игроков на поле), и проверяются они прямым вызовом `chooseTarget`,
      потому что по упавшему здоровью не отличить «выбрал не того»
      от «выбрал того, но не достал».
      *Проверка:* девять тестов проходят, при откате правки 1.1 падают.
- [x] 1.3 Обновить эталон в `packages/sim/src/determinism.golden.test.ts`
      и комментарий к нему.
      *Проверка:* эталон 519025619 отвечает правилам сразу двух изменений —
      этого и `add-detailed-models`, которое велось параллельно и добавило
      в состояние мира румб разворота. Тест зелёный.

## 2. Длительность матча как балансная величина

- [x] 2.1 Добавить `MATCH_TARGET_SECONDS` в
      `packages/shared/src/balance.ts` с обоснованием: величина уже
      жила в комментарии «расчёт сделан на матч длиной 10–15 минут».
      *Проверка:* сборка зелёная; эталон детерминизма от этого числа
      не меняется — ядро его не читает.

## 3. Развесовка моделей поведения

- [x] 3.1 Новый модуль `apps/client/src/game/ai/posture.ts`: переводные
      коэффициенты «урон → энергия», выведенные из `balance.ts`,
      и горизонты, выведенные из скорости генерала и дальности башни.
      Заодно выделен `approach.ts`: вероятный путь понадобился обоим
      модулям, а держать его в `opponent.ts` значило бы завести
      круговой импорт.
      *Проверка:* `pnpm typecheck` зелёный.
- [x] 3.2 Функция входящего урона по клетке с разбивкой на «от построек»
      и «всего».
      *Проверка:* три теста — пустое место даёт ноль, башня в дальности
      даёт свой урон в тик, башня вне дальности не считается.
- [x] 3.3 Карта уже накрытых клеток пути и покрытие по непокрытому.
      *Проверка:* тест — своя башня в клетке обнуляет прирост покрытия
      для новой башни там же.
- [x] 3.4 Оценка одного рубежа: выгода, риск, вычет дороги из горизонта,
      ограничение удержания огнём вражеских построек.
      *Проверка:* четыре теста на обстановки из таблицы в `design.md`.
- [x] 3.5 Выбор лучшего рубежа и запасной вариант «выгоды нет нигде —
      самый дальний рубеж без предрешённой гибели».
      *Проверка:* тесты на пустую оборону, укреплённую базу врага,
      массированную атаку на свою базу и повторяемость выбора.

## 4. Движение генерала по маршруту

- [x] 4.1 Поле расстояний от генерала: достижимость и время дороги
      до всех рубежей одним обходом.
      *Проверка:* недостижимые клетки отсеиваются в `frontiersOf`;
      покрыто тестами выбора рубежа.
- [x] 4.2 Поле расстояний до выбранного рубежа с кешем по паре
      «клетка рубежа, `navRevision`»; прицел на несколько клеток вперёд
      по градиенту; явная команда «стоять» по достижении рубежа.
      *Проверка:* тест «не топчется взад-вперёд» на матче в три минуты.
- [x] 4.3 Удалить `escapeDirection`, `directionIsFree`, `Detour`,
      `trackStuck` и их константы.
      *Проверка:* `pnpm lint` зелёный, неиспользуемого кода не осталось.

## 5. Строительство по убывающей отдаче

- [x] 5.1 Переписать `towerBuildCell` на покрытие по непокрытым клеткам;
      удалить `TOWERS_AROUND_GENERAL` и проверку числа построек
      в окрестности.
      *Проверка:* существующий тест «каждая башня накрывает вероятный
      путь» плюс тест из задачи 3.3.
- [x] 5.2 Связать отсутствие места под башню с выбором рубежа.
      *Проверка:* тест «за матч уходит на вражескую половину пути».

## 6. Сопровождение на дальнем рубеже

- [x] 6.1 Юниты в начало порядка трат, **пока сопровождения нет**.
      Безусловная перестановка, задуманная сначала, оказалась ловушкой:
      штурмовик дешевле башни вчетверо, перебор трат останавливается
      на первой удавшейся покупке, и за две минуты матча противник
      не построил ни одной постройки и не купил ни одного улучшения.
      Порог в восемь живых юнитов чинит это полностью.
      *Проверка:* тесты «строит постройки» и «вкладывается в экономику
      в начале матча» снова зелёные.

## 7. Проверка целиком

- [x] 7.1 Обновить `apps/client/src/game/ai/opponent.test.ts`: убран тест
      про застревание, добавлены «ходит по карте», «не топчется
      взад-вперёд» и «за матч уходит на вражескую половину пути».
      *Проверка:* пятнадцать тестов зелёные.
- [x] 7.2 Прогон матча в десять минут: восемь башен и рубеж 0,56
      к первой минуте, двадцать пять башен и 0,73 к третьей, дальше
      рубеж 1,00 и осада у чужой базы до её разрушения на восьмой.
      *Проверка:* стоимость тика в прежних пределах, тест скорости зелёный.
- [x] 7.3 `pnpm verify` целиком.
      *Проверка:* линт, типы, 362 теста и сборка зелёные.
- [x] 7.4 Проверка в браузере на живом dev-сервере: модули
      `opponent`, `posture` и `approach` загружаются, и трёхминутный матч,
      прогнанный прямо в странице, даёт 25 башен, 4 стены, дальний рубеж
      0,96 и 19 возвратов на 420 шагов по клеткам. Пять тысяч тиков
      посчитаны за 1,7 с — 0,31 мс на тик при бюджете кадра в 33 мс.
      Ошибок в консоли нет.
      *Проверка:* динамический импорт через Vite и прогон в консоли
      страницы.
- [x] 7.5 Визуальная проверка глазами: генерал компьютера идёт к фронту
      не дёргаясь, строит башни вдоль пути, при атаке на базу
      возвращается; генерал игрока попадает под огонь, стоя рядом
      со своими юнитами.
      *Проверка:* выполнена автором проекта в запущенной игре, замечаний
      нет. Ни одна из сессий сделать это не могла: панель браузера в них
      не отображается, `requestAnimationFrame` не срабатывает, и игровой
      цикл стоит на тике ноль. Числа из задачи 7.4 покрывали поведение,
      но не картинку — этот пробел закрыт человеком.
