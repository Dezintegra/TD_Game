## MODIFIED Requirements

### Requirement: Звук размещён на карте

Громкость звука SHALL убывать с удалением источника от центра обзора.
Панорама SHALL следовать за экранным смещением источника относительно
центра обзора. Удалённый источник SHALL звучать глуше близкого
и с бо́льшей долей отражений: воздух гасит верх, а доля отражений —
главный признак удалённости, какой у слуха есть.

Размах панорамы SHALL задаваться постоянным расстоянием на экране
и MUST NOT зависеть от размера окна. Размер окна не имеет отношения
к тому, где на карте случилось событие; привязка к нему означала бы,
что на широком мониторе бой звучит ровнее, чем на узком, — то есть
хуже. Расстояние, на котором панорама достигает предела, SHALL быть
соизмеримо с ближним боем, а не с шириной видимой области.

Отражений SHALL быть ровно столько, чтобы работал признак расстояния,
и не больше. Ближний источник SHALL звучать без отражений вовсе.
Поле открытое, и на нём одновременно грохочет десяток источников;
любое заметное эхо складывается само с собой и превращается в кашу
быстрее, чем успеет дать простор.

Непрерывный источник SHALL получать долю отражений заметно меньшую,
чем событийный. Событие отзвучало и перестало питать отражения,
непрерывное питает их без перерыва, и хвост накапливается сам на себя.

Размещение SHALL пересчитываться каждый кадр для каждого звучащего
источника, а не однократно в момент запуска. Источник неподвижен
на карте; при прокрутке карты он SHALL перемещаться в панораме вместе
с картинкой. Долгие звуки — ядерный удар звучит восемь секунд — иначе
остались бы висеть там, где экран был в момент их начала.

Игра SHALL иметь отклик помещения, применяемый ко всем звукам событий.
Отклик SHALL соответствовать открытому полю: короткая задержка,
различимые ранние отражения и хвост **не длиннее половины секунды**.
Длинный гул MUST NOT применяться — он превращает поле в помещение,
а бой в кашу, в которой не различить, кто по кому бьёт. Простор SHALL
держаться на ранних отражениях, а не на длине хвоста: хвост сообщает
о стенах, которых на поле нет.

Событие дальше отсечки MUST NOT звучать и MUST NOT занимать места среди звучащих.
Отсечка SHALL быть заметно меньше размера карты: карта в 48 клеток,
и слышимый на всю её ширину бой не сообщал бы ничего о направлении.

Панорама MUST NOT достигать крайних значений. Полностью уведённый
в один канал звук в наушниках читается как «звук в ухе», а не как
«звук слева на карте».

Правило одинаково для своих и чужих: тумана войны нет, и слышно должно
быть всё, что видно.

#### Scenario: Дальнее тише ближнего

- **WHEN** два одинаковых события произошли на разном удалении от центра обзора
- **THEN** громкость дальнего меньше

#### Scenario: За отсечкой тишина

- **WHEN** событие произошло дальше отсечки
- **THEN** звук не воспроизводится вовсе

#### Scenario: В центре обзора панорамы нет

- **WHEN** событие произошло ровно в центре обзора
- **THEN** панорама равна нулю

#### Scenario: Ближний бой слышен по сторонам

- **WHEN** событие произошло в нескольких клетках сбоку от центра обзора
- **THEN** панорама превышает половину своего предела

#### Scenario: Размер окна на панораму не влияет

- **WHEN** одно и то же событие размещается для узкого и для широкого окна
- **THEN** панорама совпадает

#### Scenario: Ближнее звучит насухо

- **WHEN** событие произошло в центре обзора
- **THEN** в отражения от него не уходит ничего

#### Scenario: Прокрутка карты двигает источник

- **WHEN** звук ещё звучит, а игрок прокрутил карту так, что источник перешёл с одной стороны экрана на другую
- **THEN** панорама звука сменила знак

#### Scenario: Дальнее глуше ближнего

- **WHEN** два одинаковых события произошли на разном удалении
- **THEN** у дальнего частота среза ниже, а доля отражений выше

#### Scenario: Хвост отклика короток

- **WHEN** проверяется отклик помещения
- **THEN** его длина не превышает половины секунды, а затухание монотонно

### Requirement: Генерал звучит ротором, пока жив

Каждый живой генерал — и свой, и чужой — SHALL иметь непрерывный звук
ротора, размещённый на карте по общим правилам.

Громкость и высота ротора SHALL зависеть от признака движения генерала:
в движении громче и выше, на месте тише и ниже. Признак берётся
из состояния мира и MUST NOT выводиться из разницы положений между
кадрами.

Ротор SHALL быть заметно тише самого тихого звука события. Он звучит
весь матч и над самим игроком; уровень, умеренный для одиночного
события, для непрерывного источника становится подложкой, поверх
которой не расслышать чужой стрельбы. Слой, добавляющий ротору
узнаваемую рубку лопастей, SHALL читаться поверх основы и MUST NOT
спорить с ней по громкости.

Стоящий генерал MUST NOT замолкать полностью: он висит в воздухе,
и тишина под ним читалась бы как «машина выключилась и не падает».

Низ у непрерывного звука SHALL срезаться. Гул, безобидный в звуке
одиночном, у непрерывного не гаснет никогда и заполняет собой тот
диапазон, в котором живут взрывы, ничего при этом не сообщая.

Переходы между состояниями SHALL быть плавными. Мгновенное переключение
громкости щёлкает.

Погибший генерал SHALL замолкать.

#### Scenario: Движение слышно

- **WHEN** игрок нажимает клавишу движения генерала
- **THEN** громкость ротора нарастает в том же кадре

#### Scenario: Остановка слышна

- **WHEN** игрок отпускает клавиши движения
- **THEN** ротор стихает до уровня висения, но не до тишины

#### Scenario: Ротор тише боя

- **WHEN** уровень ротора сравнивается с уровнем самого тихого события
- **THEN** ротор тише более чем вдвое

#### Scenario: Гибель выключает ротор

- **WHEN** генерал погиб
- **THEN** его ротор смолкает

### Requirement: Фон держит пульс матча

Игра SHALL иметь фоновую музыку: ритмическую петлю, звучащую в матче.

Музыка SHALL быть негромкой относительно боя и SHALL оставлять середину
диапазона звукам событий. Задача у неё одна — держать пульс; сообщать
она ничего не должна.

Музыка MAY браться записанной петлёй и MAY собираться из запечённых
инструментов по расписанию. Запрещено единственное: назначать ноты
или круги петли **по таймеру браузера**. Таймеры дрожат на десятки
миллисекунд, и ритм разъехался бы слышимо. Расписание SHALL опираться
на часы звукового контекста, а запись SHALL зацикливаться средствами
самого звукового узла.

Записанная петля SHALL иметь длину, кратную такту записанного
материала, и SHALL задаваться числом отсчётов, а не длиной
декодированного буфера: кодировщик добавляет к записи свою задержку
и паддинг, и длина файла к длине петли отношения не имеет.

Стык петли MUST NOT щёлкать.

Отсутствие или порча записи MUST NOT приводить к тишине: музыка SHALL
звучать посчитанным расписанием. Подмена SHALL быть односторонней —
запись перекрывает расписание, расписание запись никогда.

Петля SHALL быть длиннее нескольких тактов: короткая петля через три
круга начинает считаться как метроном.

#### Scenario: Ритм не плывёт

- **WHEN** музыка играет минуту
- **THEN** доли приходятся на моменты, отстоящие друг от друга ровно на длительность доли

#### Scenario: Петля замкнута

- **WHEN** расписание доходит до последнего шага
- **THEN** следующий шаг совпадает с первым

#### Scenario: Длина петли задана числом

- **WHEN** записанная петля подготовлена к воспроизведению
- **THEN** число отсчётов в ней равно заданной длительности, умноженной на частоту дискретизации

#### Scenario: Пропавшая запись не оставляет тишины

- **WHEN** файл петли не загрузился или не декодировался
- **THEN** музыка звучит посчитанным расписанием, ошибки не возникает
