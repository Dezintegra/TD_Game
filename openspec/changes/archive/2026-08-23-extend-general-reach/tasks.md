# Задачи

## 1. Число

- [x] 1.1 В `packages/shared/src/balance.ts` заменить
      `range: cellsToUnits(BASE_UNIT_RANGE_CELLS)` у `GENERAL_STATS`
      на `cellsToUnits(BASE_TOWER_RANGE_CELLS)`.
      _Проверка:_ сборка зелёная.
- [x] 1.2 В комментарии записать оба совпадения, из которых выбрана
      пятёрка: дальность базовой башни и радиус строительства генерала.
      Второе важнее первого, и без него число снова станет
      подобранным.
      _Проверка:_ в комментарии названы обе величины.
- [x] 1.3 Тест в `balance.test.ts`: дальность генерала равна дальности
      базовой башни и равна его радиусу строительства.
      _Проверка:_ два сравнения зелёные; при откате правки 1.1 падают.

## 2. Проверки, что генерал не стал слишком силён

- [x] 2.1 Тест дуэли: генерал и обычная башня без прокачки стреляют
      друг в друга — погибает генерал. Это главная проверка изменения:
      если генерал начнёт выигрывать, отменяется центральная механика
      замысла.
      _Проверка:_ тест зелёный.
- [x] 2.2 Тест: снайпер, гранатомётчик и снайперская башня
      по-прежнему перестреливают генерала.
      _Проверка:_ три сравнения дальностей зелёные.
- [x] 2.3 Тест выгодности фермы: награда за убийство штурмовика,
      делённая на время, которое генерал тратит на это убийство,
      меньше пассивного дохода за то же время.
      _Проверка:_ тест зелёный. Он защищает от будущих правок атаки
      генерала, а не только от этой.
- [x] 2.4 Тест: стена перекрывает генералу выстрел в пределах новой
      дальности. Правило существует, но на двух клетках почти
      не проявлялось.
      _Проверка:_ урон не наносится.

      Сделано 23.08.2026: `combat.test.ts`, «стена перекрывает выстрел
      генералу в пределах его дальности». Цель в трёх клетках — за старой
      дальностью и внутри новой, поэтому тест сторожит заодно и саму
      пятёрку. Измерено: чистое поле 100 → 90, за стеной 100 → 100.

## 3. Противник под управлением компьютера

- [x] 3.1 Тест: оценка опасности точки (`assessThreat` в `posture.ts`)
      считает угрозой от чужого генерала круг новой величины.
      «Читается из таблицы, значит подхватится само» — проверить,
      а не поверить.
      _Проверка:_ точка на расстоянии четырёх клеток от чужого генерала
      считается опасной, на шести — нет.

      Сделано 23.08.2026: `posture.test.ts`, «круг угрозы от чужого
      генерала» — три проверки. Обе величины из условия сошлись:
      четыре клетки под угрозой, шесть — нет.

      **Имя в задаче устарело.** `assessThreat` в `packages/ai` больше
      не существует; ту же работу делает `incomingAt`, и он действительно
      берёт дальность из таблицы (`enemyStats.general.range`), ничего
      не зная о числе. Генерал в тесте ставится в дальний угол
      по побочной диагонали — иначе в счёт вошла бы ещё и база, и тест
      говорил бы о ней.
- [x] 3.2 Убедиться, что нигде в `packages/ai` дальность генерала
      не записана числом.
      _Проверка:_ поиск по литералам дальности в пакете ничего
      не находит.

## 4. Порядок, эталон, замер

- [ ] 4.1 Убедиться, что `calibrate-general-risk` применён **раньше**.
      Обратный порядок обесценивает его замеры: он калибрует формулу
      по наблюдениям, снятым на прежней дальности.
      _Проверка:_ изменение `calibrate-general-risk` архивировано.

      **Отметка о выполнении снята 23.08.2026: условие не выполнено.**
      `calibrate-general-risk` не заархивирован и на сегодня закрыт
      на 9 задач из 14 — его раздел «Мерило» пуст целиком, базовая линия
      на seed 2000–2019 не снята.

      Порядок нарушен, и последствие уже наступило: дальность в коде
      давно пятёрка, а сверху лёг ещё и `give-the-general-a-salvo`
      (перезарядка вчетверо короче). Снять «наблюдения на прежней
      дальности» больше нельзя — прежней дальности в дереве нет.

      **Что с этим делать.** Не переписывать историю галочкой, а признать:
      базовую линию `calibrate-general-risk` придётся снимать заново,
      уже на нынешних правилах, и его собственные числа в предложении
      считать устаревшими. Задача остаётся открытой как запись о том,
      что порядок был нарушен; закрывать её нечем, потому что выполнить
      её задним числом невозможно.
- [x] 4.2 Обновить эталон в `determinism.golden.test.ts` тем же
      коммитом.
      _Проверка:_ тест зелёный с новым числом.
- [x] 4.3 Прогнать арену до и после и свести: доля времени с мёртвым
      генералом по минутам, убийств генералом за матч, энергия
      от наград, потери башен, длительность матча.
      _Проверка:_ таблица приложена к изменению.

      **Отметка стояла, а таблицы не было.** Обнаружено 23.08.2026:
      в `design.md` лежал только список ожиданий. Замер снят заново
      в тот же день — seed 2000–2019, двадцать матчей, дальность
      откачена `git checkout` и замерена, потом возвращена.

      Две величины из пяти снять не удалось: «убийств генералом
      за матч» и «энергия от наград» в базе арены не записываются
      вовсе, для них нужна правка схемы и записи матча.
- [x] 4.4 Разобрать замер. Потери башен заметно выросли — пятёрка
      выбрана щедро, вернуться к четырём. Доля мёртвого генерала
      не упала — дело не в дальности, и запись об этом идёт
      в `design.md`, к разделу про непрокачиваемую дальность.
      _Проверка:_ вывод записан под замером.

      Сделано 23.08.2026, разбор — раздел «Что замер показал»
      в `design.md`. **Оба предположения этой задачи замером
      не подтвердились**, и это главное, что стоит здесь прочитать.

      «Доля мёртвого генерала не упала» — упала: 14,3% → 10,6%,
      а с седьмой минуты 13,0% → 9,9%. Отсылка к разделу
      про непрокачиваемую дальность отменяется: дело оказалось
      именно в дальности.

      «Потери башен выросли, вернуться к четырём» — выросли (26,77 →
      45,60), но не по той причине, которую критерий сторожил. Матч стал
      длиннее на 37%, башен на поле — на 37% больше. На одну живую башню
      в минуту потери **упали**: 2,25 → 2,05. К четырём по этому доводу
      возвращаться не за чем.

      **Настоящая цена нашлась в другом месте.** Матч удлинился
      с 594 до 815 секунд, ничьих стало 7 из 20 против 3 из 20.
      Проектная длительность — 720 секунд, и правка уводит от неё.
      Это отдельный вопрос к балансу, к дальности напрямую
      не привязанный; решать его правкой этого числа значило бы лечить
      следствие. Вынесено на решение владельца проекта.
- [x] 4.5 Прогнать `pnpm verify`.
      _Проверка:_ линт, типы, тесты и сборка зелёные.
