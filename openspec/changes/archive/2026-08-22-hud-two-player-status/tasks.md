## 1. Раскладка: поле перестаёт быть перекрытым

- [x] 1.1 Объявить в `packages/ui/src/tokens.css` переменные высоты полос.
      Вышло `--td-hud-top: 108px` и `--td-hud-bottom: 116px`, а не 84 и 132,
      как прикидывалось заранее: верхняя полоса несёт три строки на сторону
      и в 84 не влезала, а тулбар после сжатия плитки до двух строк уложился
      в 116. Сумма 224 при пределе 340.
      Проверка: переменные читаются в `apps/client/src/styles.css`.
- [x] 1.2 В `apps/client/src/styles.css` снять с `#scene` `inset: 0`
      и задать отступы сверху и снизу по этим переменным; `#hud` сделать
      сеткой из трёх строк тех же высот.
      Проверка: в браузере прямоугольник canvas не пересекается ни с верхней
      полосой, ни с нижним тулбаром.
- [x] 1.3 Заменить правило `#hud > * { pointer-events: auto }` поимённым
      включением событий на панелях; средняя строка сетки остаётся
      прозрачной для нажатий.
      Проверка: нажатие в пустом месте поля выделяет объект, а не гасится
      слоем интерфейса.
- [x] 1.4 В `apps/client/src/game/scene.ts` начать `resize()` с `app.resize()`,
      чтобы `app.screen` обновлялся до пересчёта камеры и миникарты.
      Проверка: юнит-тест сцены либо ручная смена высоты полосы через
      инструменты разработчика — миникарта переезжает в новый угол.
- [x] 1.5 В `apps/client/src/game/bootstrap.ts` добавить `ResizeObserver`
      по контейнеру сцены; нулевые размеры игнорировать; отписку — в `stop()`.
      Обработчик `window resize` при этом ОСТАВЛЕН, хотя задумывалось его
      заменить. Причина выяснилась при проверке в браузере: наблюдатель
      не срабатывает ни разу, пока вкладка не рисуется, — уведомления он
      доставляет в шаге отрисовки, а в скрытой вкладке этого шага нет.
      Повторной работы не выходит: пересчёт запоминает размеры, по которым
      уже уложился.
      Проверка: e2e «панели не перекрывают игровое поле» меняет
      `--td-hud-bottom` через стиль документа, не трогая окно, и ждёт,
      что холст сожмётся, — то есть проверяет ровно путь наблюдателя.
- [x] 1.6 Добавить в `apps/client/src/game/iso.test.ts` проверку, что при
      окне 1920 × 1080 и полосах суммарной высоты 340 точек видимая доля
      карты не меньше 25 процентов.
      Проверка: `pnpm --filter @td/client test` зелёный.

## 2. Снимок матча: обе стороны

- [x] 2.1 Завести в `apps/client/src/game/store.ts` тип `SideView`
      (прочность базы и её предел, жив ли генерал, секунды до возрождения,
      числа юнитов по типам, числа построек по видам) и поле `sides`
      в `MatchSnapshot`; заполнить его в `EMPTY_MATCH`.
      Проверка: `pnpm --filter @td/client build` проходит проверку типов.
- [x] 2.2 В `snapshot()` из `apps/client/src/game/bootstrap.ts` заменить
      проход по своим юнитам на накопление по обеим сторонам; добавить
      проход по постройкам с выделением баз.
      Проверка: юнит-тест на `snapshot()` — мир с известным составом даёт
      ожидаемые числа по обеим сторонам.
- [x] 2.3 Убедиться, что дополнительных обходов списков мира не добавилось:
      по юнитам один проход, по постройкам один.
      Проверка: чтение кода плюс отсутствие просадки частоты кадров
      в матче с полным потолком юнитов.

## 3. Верхняя полоса

- [x] 3.1 Собрать компонент половины стороны: имя, пометка «компьютер»,
      состояние генерала, прочность базы, шесть групп «пиктограмма и число».
      Проверка: компонент рисуется с подставленными данными в тесте
      компонентов.
- [x] 3.2 Нарисовать пиктограммы трёх типов юнитов и трёх видов построек,
      цветами из токенов, без литералов.
      Сделано в `apps/client/src/hud/icons.tsx`, а НЕ в `packages/ui`, как
      предполагалось изначально: чтобы нарисовать штурмовика, надо знать,
      что он есть, а `packages/ui` собран из вещей, ничего не знающих
      об игре. Цвет не задаётся вовсе — линии рисуются `currentColor`,
      и сторону назначает родитель.
      Проверка: линт не находит литеральных цветов; пиктограммы различимы
      на глаз в обоих цветах сторон.
- [x] 3.3 Собрать верхнюю полосу: своя сторона слева, соперник справа,
      общее матчу в середине (энергия и доход, время, цель атаки).
      Имена берутся из `session-store`, числа — из `useHudStore`.
      Проверка: в матче со стороной 1 слева стоят величины стороны 1.
- [x] 3.4 Вынести строку связи отдельной строкой в самый верх экрана:
      состояние соединения, время оборота, задержка ввода — по отдельности.
      Сохранить `data-testid="connection-status"` с атрибутом `data-status`.
      Проверка: `e2e/smoke.spec.ts` по-прежнему видит состояние соединения.
- [x] 3.5 Убедиться, что полоса умещается по ширине в окно 1366 точек.
      Измерено в браузере при 1366 × 768 с подставленным снимком, где числа
      заведомо крупные: прочность базы 50 000, войска 128/44/17, постройки
      23/11/6. Своя сводка 263 точки, чужая 324, середина 358; переполнения
      нет ни по ширине, ни по высоте, страница не прокручивается.
      Сокращать разделение построек по видам не понадобилось.

## 4. Диагностика и удаление панелей

- [x] 4.1 Завести листовой компонент диагностики: пустой элемент
      с `data-seed`, `data-visible-percent`, `data-rock-percent`,
      `data-tick`, `data-fps`, `data-pong-count`, `data-sync-tick`,
      `data-sync-checksum`. Подписки на store — внутри него.
      Проверка: корневой `Hud` больше не подписан на часто меняющиеся
      величины.
- [x] 4.2 Удалить панели «Соединение» и «Карта» из `apps/client/src/hud/Hud.tsx`
      вместе с левой колонкой.
      Проверка: на экране нет ни seed, ни доли скал, ни номера тика текстом.
- [x] 4.3 Убрать показ очереди производства из
      `apps/client/src/hud/ActionBar.tsx` вместе с компонентом `Queue`.
      Правила не трогать.
      Проверка: в интерфейсе нет строки про очередь; тесты `packages/sim`
      остаются зелёными без правок.
- [x] 4.4 Уложить нижний тулбар в отведённую высоту: панели производства,
      строительства и режима атаки в одну строку.
      Отведено 116 точек, а не 132: плитка стала двухстрочной — цена
      и горячая клавиша встали в одну строку вместо двух, — и полоса
      похудела на треть. Измерено в браузере: содержимое 929 × 88 внутри
      полосы в 116, переполнения нет.
- [x] 4.5 Перепривязать оверлеи поля (отказы, полоса состояния матча,
      сведения о постройке, панель прокачки, окно итога) к средней строке
      сетки.
      Панель прокачки и окно сведений стоят `position: absolute` с отсчётом
      от правого нижнего угла, а средняя строка сетки — `position: relative`.
      Значит нижняя граница обеих панелей совпадает с верхней границей
      тулбара по построению, а не по подобранному отступу: заехать под него
      они не могут в принципе.

## 5. Меню матча по Esc

- [x] 5.1 Научить `cancelModes()` в `apps/client/src/game/controls.ts`
      возвращать признак «что-то отменено»; при отсутствии отменяемого
      вызывать новый обработчик `requestMenu()`.
      Проверка: юнит-тест управления — Esc с выбранной постройкой
      не запрашивает меню, Esc без режимов запрашивает.
- [x] 5.2 Добавить в `controls` состояние «меню открыто»: игровые клавиши
      не разбираются, набор нажатых очищается, направление пересчитывается.
      Проверка: юнит-тест — при открытии меню с зажатой `W` направление
      становится «стоп».
- [x] 5.3 Завести в `useHudStore` признак открытого меню и команду его
      переключения в `MatchCommands`.
      Проверка: проверка типов проходит; признак по сети не уходит.
- [x] 5.4 Собрать окно меню матча: «Новый матч» (только против компьютера),
      «Выйти в меню» с прежним подтверждением, перечень горячих клавиш
      из `labels.ts`.
      Проверка: в матче с человеком перезапуска в меню нет.
- [x] 5.5 Оставить выход и перезапуск в окне итога матча без изменений.
      Проверка: после матча выход доступен без открытия меню.

## 6. Проверки

- [x] 6.1 Поправить `e2e/smoke.spec.ts`: `seed` и `pong-count` читаются
      атрибутами; «Новый матч» открывается через Esc.
      Проверка: `pnpm --filter @td/e2e test` либо корневая команда e2e.
- [x] 6.2 Поправить `e2e/lobby.spec.ts`: сравнение seed у двух участников
      и чтение `data-sync-tick` / `data-sync-checksum` — с нового элемента
      диагностики.
      Проверка: сценарий сетевого матча проходит.
- [x] 6.3 Прогнать `pnpm verify` целиком.
      Проверка: линт, типы, тесты и сборка зелёные.
- [x] 6.4 Посмотреть матч глазами: поле не перекрыто, числа обеих сторон
      сходятся с полем, Esc отменяет режим и открывает меню, генерал
      не уходит при открытом меню.
      Проверка: наблюдение в браузере.
