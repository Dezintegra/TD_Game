## 1. Правило, исправления и сторож одним проверяемым коммитом

- [ ] 1.1 За не более чем 2 часа добавить правило с причиной и допустимой заменой во все одиннадцать скиллов, устранить переносы вставок и восстановить отступы по инвентаризации design.md, добавить в `supervisor/config/transitions.test.mjs` поиск нарушений, проверку всех файлов каталога, проверку наличия правила и контрольные образцы (включая порчу копии живого скилла в памяти). Содержание `requests` сохранить, разрешается только соединение строк; учесть уже влитые исправления 0186. Проверить по процедуре ниже, отметить этот пункт в том же коммите, отправить коммит и сразу открыть черновой PR.

Пункт атомарный: сторож без исправлений не проходит, исправления без сторожа не защищены. Дополнительных шагов ожидания CI, архивации и прогона арены нет.

### Проверка пункта 1.1

Все команды выполняются отдельными вызовами из назначенного дерева после подготовки среды по правилам implement. Формы сверены с `supervisor/config/stage-settings.json`: `PowerShell(npx vitest:*)`, `PowerShell(npx prettier:*)`, `PowerShell(npx eslint:*)`, `PowerShell(git -C:*)`, `PowerShell(gh pr:*)`.

1. Выполнить `npx vitest run --root supervisor config/transitions.test.mjs`: живые скиллы принимаются; негативные образцы проверяют конкретные находки, позитивные не дают ложных находок. Удаление правила из копии скилла обнаруживается; перенесённая вставка в копии живого файла отвергается тем же помощником. Проверки не создают повреждённых файлов на диске.
2. Выполнить `npx prettier --write "supervisor/skills/*.md" supervisor/config/transitions.test.mjs` и `npx eslint supervisor/config/transitions.test.mjs`. Просмотреть `git -C <дерево> diff -- supervisor/skills supervisor/config/transitions.test.mjs`: вложенность исправленных списков восстановлена, слова и значения прежних правил сохранены, новых файлов тестов и правок конфигурации форматтера нет. Первый прогон может привести существующее оформление скиллов к формату; содержательные исправления чужих задач сюда не включать.
3. После отметки пункта добавить изменённые файлы явным списком: `git -C <дерево> add -- supervisor/skills/audit.md supervisor/skills/benchmark.md supervisor/skills/decompose.md supervisor/skills/deploy.md supervisor/skills/design.md supervisor/skills/implement.md supervisor/skills/interpret.md supervisor/skills/postmortem.md supervisor/skills/review.md supervisor/skills/revise.md supervisor/skills/triage.md supervisor/config/transitions.test.mjs openspec/changes/keep-skill-code-spans-on-one-line/tasks.md`.
4. Повторить `npx prettier --write "supervisor/skills/*.md" supervisor/config/transitions.test.mjs`. Команда `git -C <дерево> diff --exit-code -- supervisor/skills supervisor/config/transitions.test.mjs` должна вернуть 0 и пустой diff относительно подготовленного индекса. Выполнить `npx prettier --check "supervisor/skills/*.md" supervisor/config/transitions.test.mjs`, затем повторно узкий тест из пункта 1 после форматирования. Повтор нужен для проверки, что форматтер не вернул обнаруживаемый дефект. Несходимость не закрывать исключением из Prettier: вернуть пример на проработку.
5. Проверить `git -C <дерево> diff --cached --check`, `openspec validate keep-skill-code-spans-on-one-line --strict` и состав индекса через `git -C <дерево> diff --cached --name-only`. В индексе только перечисленные файлы реализации и отметка tasks.md. После коммита сразу `git -C <дерево> push -u origin HEAD`, затем `gh pr create --draft --base main --title "fix(pipeline): keep skill code spans on one line" --body-file .matchlog/0187-pr-body.md`; файл тела подготовить и убрать по правилам implement. Наблюдаемый результат — отправленный атомарный коммит и номер чернового PR в отчёте, без ожидания проверок сессией.
