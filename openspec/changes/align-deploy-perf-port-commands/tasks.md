## 1. Согласовать команды выкладки и защитить их проверкой

- [ ] 1.1 Одним атомарным коммитом (до 2 часов) исправить шаг 8 `supervisor/skills/deploy.md` и расширить `scripts/perf-instructions.test.mjs` по design.md. Удалить четыре присваивания окружения; проверке и запуску передать `--client-port 5199 --port 3055`, только проверке оставить `--check-only`. Сохранить history и политику замера, пояснить согласованную замену пары и вычисление адресов/порта метрик. Тест извлекает исполняемые блоки, проверяет оба ключа, режимы, отсутствие присваиваний и склейки; контроли в памяти обнаруживают потерю каждого ключа и одностороннюю замену порта, но игнорируют упоминания команды в прозе. Существующее покрытие benchmark.md сохранить. Проверки: `npx vitest run --root scripts perf-instructions.test.mjs`, `npx eslint scripts/perf-instructions.test.mjs`, `npx prettier --check supervisor/skills/deploy.md scripts/perf-instructions.test.mjs`, `openspec validate align-deploy-perf-port-commands --strict`. Наблюдаемый результат: тест принимает реальный скилл и отвергает перечисленные испорченные варианты, настоящий perf не запускается. После выполнения отметить этот пункт в собственном tasks.md, закоммитить, немедленно отправить и открыть черновой PR по правилам implement; этот первый шаг уже даёт полный проверяемый результат.

Выполнение команд — из назначенного дерева, каждая отдельным вызовом. Перед правкой освежить базу командами `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main` по контракту implement. Формы `npx vitest:*`, `npx eslint:*`, `npx prettier:*`, `openspec:*` и `git -C:*` покрыты PowerShell-правилами в `supervisor/config/stage-settings.json`; новые разрешения не нужны.

Состав коммита реализации: только `supervisor/skills/deploy.md`, `scripts/perf-instructions.test.mjs` и отметка выполнения в `openspec/changes/align-deploy-perf-port-commands/tasks.md`. Добавлять эти пути явным перечнем. До коммита проверить имена через `git -C <дерево> diff --cached --name-only` и содержание через `git -C <дерево> diff --cached`; после коммита — `git -C <дерево> show --stat --oneline HEAD` и `git -C <дерево> show --format=fuller HEAD`. Допустимы только связанные правки шага и его отметка, включая внутри разрешённых файлов; временные файлы и чужие изменения недопустимы.

Локальные полный verify, сборка, замер, арена и ожидание CI в этот план не входят. Порядок архивации: после `accept-perf-port-flags`; архивация не является задачей implement.
