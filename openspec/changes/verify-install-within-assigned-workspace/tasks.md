## 1. Проверяемая библиотека установки

- [x] 1.1 Реализовать `supervisor/lib/install-snapshot-check.mjs` и `supervisor/lib/install-snapshot-check.test.mjs` по design (до 120 минут): новая копия отправленного HEAD внутри собственного `.matchlog`, проверка realpath и исходной чистоты, локальное отключение пользовательского ignore/fsmonitor/hooks, настоящий дочерний pnpm без смены cwd родителя, таймаут и диагностика, непустой store и проектный источник ignore, независимые отрицательные контроли исходника и постороннего файла. Быстрые тесты используют реальные Git-фикстуры и подменённый установщик, охватывают ошибки процессов/статуса, пустой кеш, неверный ignore, повторный запуск и выход пути наружу; удаление правила store в фикстуре должно делать контроль красным. Проверки отдельными командами: `npx vitest run --root supervisor lib/install-snapshot-check.test.mjs`, `npx eslint supervisor/lib/install-snapshot-check.mjs supervisor/lib/install-snapshot-check.test.mjs`, `npx prettier --check supervisor/lib/install-snapshot-check.mjs supervisor/lib/install-snapshot-check.test.mjs`. Отметить пункт и закоммитить только эти файлы и свой tasks.md через `git -C <дерево> add -- <явные пути>` и `git -C <дерево> commit`; сразу `git -C <дерево> push -u origin HEAD`. Открыть черновой PR командой `gh pr create --draft --base main --title "feat: verify nested pnpm installation" --body-file .matchlog/0232-pr-body.md`. Библиотека уже полностью проверяема независимо от будущего CLI; ждать CI исполнителю не требуется.

## 2. Поддерживаемая команда и инструкция

- [x] 2.1 Добавить тонкий `supervisor/bin/check-install-snapshot.mjs`, узкий `supervisor/bin/check-install-snapshot.test.mjs`, точные PowerShell-разрешения помощи и режима `--run` в `supervisor/config/stage-settings.json`, их проверку в новом `supervisor/config/install-snapshot-permissions.test.mjs` и раздел в `docs/agent-setup.md` (до 90 минут). CLI без доводов только объясняет запуск; неизвестные доводы отклоняет до записи. Рабочий режим вызывает библиотеку в текущем назначенном cwd и возвращает её результат. Тест покрытия проверяет обе точные формы и отсутствие разрешения посторонних bin-команд/произвольных доводов. Инструкция объясняет границу sandbox, отличие от cwd дочернего процесса, версионность диагностики и обязательный повтор живой пробы после смены способа запуска или sandbox; называет разрешённый импорт через `.matchlog` до доставки нового правила в основной каталог. Проверки: `npx vitest run --root supervisor bin/check-install-snapshot.test.mjs config/install-snapshot-permissions.test.mjs lib/install-snapshot-check.test.mjs`; `npx eslint supervisor/bin/check-install-snapshot.mjs supervisor/bin/check-install-snapshot.test.mjs supervisor/config/install-snapshot-permissions.test.mjs`; `npx prettier --check supervisor/bin/check-install-snapshot.mjs supervisor/bin/check-install-snapshot.test.mjs supervisor/config/install-snapshot-permissions.test.mjs supervisor/config/stage-settings.json docs/agent-setup.md`. Отметить пункт, закоммитить перечисленные файлы и свой tasks.md, сразу `git -C <дерево> push`.

## 3. Настоящая установка в новой Windows-копии

- [ ] 3.1 Из назначенного дерева под действующим Windows sandbox запустить реализованную проверку на уже отправленном HEAD и сохранить результат в `openspec/changes/verify-install-within-assigned-workspace/validation.md` (до 60 минут). До пробы убедиться через `git -C <дерево> rev-list --count @{u}..HEAD` в отсутствии хвоста; в PowerShell аргумент `@{u}..HEAD` передать в одиночных кавычках. Создать локальный `.matchlog/0232-install-check.mjs`, импортирующий ту же библиотеку, и выполнить разрешённую команду `node .matchlog/0232-install-check.mjs`, сохраняя cwd ИНСТРУМЕНТА в назначенном дереве; после доставки точного разрешения штатный эквивалент — `node supervisor/bin/check-install-snapshot.mjs --run`. Не запускать новую sandbox с cwd копии. Зафиксировать SHA, версии Node/pnpm/Git/Codex и профиль из контекста сессии, реальные пути родителя и ребёнка, отсутствие старого кеша, исходную чистоту, exit 0 настоящего pnpm, файлы в локальном store, пустой полный статус, источник ignore и оба независимых отрицательных контроля с итоговым восстановлением чистоты. Старые копии сохранить. Сетевая/средовая ошибка или неподтверждённое условие оставляет пункт открытым; записать дословную ошибку без отключения защиты. Успешный импортный запуск является той же проверкой библиотеки, а CLI отдельно покрыт пунктом 2.1. Отметить пункт только после полного успеха, закоммитить validation.md и свой tasks.md, сразу `git -C <дерево> push`. Проверка формы: `openspec validate verify-install-within-assigned-workspace --strict`. Полный test:pipeline, сборка, замеры, арена и выкладка не запускаются.
