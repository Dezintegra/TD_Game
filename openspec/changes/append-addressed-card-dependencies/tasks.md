## 1. Проверяемое планирование дополнений

- [x] 1.1 Добавить чистый `supervisor/lib/dependency-updates.mjs` и его тесты: строгая оболочка, полный ID, дубликаты адресатов и карточек (включая архивные/негодные), допустимый failed, запрещённые closed/source, объединение с сохранением порядка, неизменность входов, идентичный повтор, конфликт PR и цикл кандидатов. Использовать существующие валидаторы dependencies.mjs. Оценка: 90 минут. Проверка: `pnpm test:pipeline lib/dependency-updates.test.mjs`. После этого самостоятельного коммита отправить ветку и открыть черновой PR; публичный контракт в скиллах пока не объявлять.

## 2. Согласованная запись и подтверждение

Операция шага 2.1 включает обязательный callback контекста перед PUT и при подтверждённом no-op; тесты проверяют порядок callback до сетевой записи и его вызов при последующем исключении. До подключения канала отчёта callback используется тестовым контекстом, а шаг 3.1 связывает его с набором инвалидации execute.

- [x] 2.1 Реализовать специализированную операцию Trello/IO с тестами в `backlog-trello.test.mjs`: актуальная однозначная адресация, свежий разбор после временного захвата, сохранение исходного служебного объекта и человеческого текста, PUT только desc с обоими полями, независимый GET и структурное подтверждение, no-op при повторе, корректное освобождение собственного захвата. Старый неподдерживаемый адаптер должен отказывать явно. Проверить отказ/исключение чтения, записи, подтверждения и освобождения, занятый адресат, новые зависимости после снимка и потерю каждого поля при readback. Оценка: 120 минут. Проверка: `pnpm test:pipeline lib/backlog-trello.test.mjs lib/dependency-updates.test.mjs`; метод ещё не вызывается переносом отчёта, существующее поведение сохраняется.

- [x] 2.2 Защитить обычный Trello-start после acquire и до saveTask/дерева/spawn: независимое свежее чтение карточки и графа, сравнение с основанием action, повтор неизменного pendingDependencies с доказательствами цикла. Изменившееся основание, недоступное чтение и неудовлетворённый допуск откладывают действие без перехода и расхода попыток; освобождается только новое назначение этого вызова. Различать новое назначение и восстановление по свежим данным; допустимый старт строить из свежей задачи с сохранением исходной подложки описания и неизвестных ключей. В том же коммите интеграционный тест двух независимых Trello IO/Map: B выбирает старт, A записывает через операцию 2.1 и подтверждает зависимость, освобождает захват, B получает захват и не выполняет старый saveTask/дерево/spawn; дополнение остаётся. Контроли: неизменный разрешённый старт проходит; новый PR без доказательства удерживается; ошибка свежего чтения не запускает задачу и не снимает прежний захват. Оценка: 120 минут. Проверка: `pnpm test:pipeline lib/execute.test.mjs lib/backlog-trello.test.mjs lib/scan.test.mjs`. Канал отчёта ещё не подключён, предохранитель самостоятельно проверяем.

## 3. Канал отчёта целиком

- [x] 3.1 Подключить `dependencyUpdates` в transferReport после проверки личности/этапа/исхода/доверия и до побочных записей requests/amendments и перехода источника. Ввести набор недействительных адресатов исполнения: синхронный callback до PUT и подтверждённый no-op добавляют ID, каждое оставшееся действие проверяет набор, failed/исключение его не сбрасывают. Публиковать в Map только подтверждённые данные. Одним коммитом добавить обработчик, интеграционные тесты и описание поля в `manage/README.md` и разделах отчёта всех `supervisor/skills/*.md`: форма, основание согласования, failed без возврата, busy без захвата чужой задачи, подтверждение, повтор, ошибки. Обязательная интеграционная трасса: PUT сохранился, GET упал, последующие старт и запись адресата отложены, источник/отчёт сохранены, неподтверждённый кандидат не опубликован; варианты с потерянным ответом PUT и исключением также сохраняют инвалидацию. Оценка: 120 минут. Проверка: `pnpm test:pipeline lib/execute.test.mjs lib/backlog-trello.test.mjs lib/dependency-updates.test.mjs`; отсутствующее поле не вызывает новых IO обработчика отчёта (свежая проверка обычного старта независима), частичный успех безопасно повторяется после нового чтения. Обработчик и объявленный контракт доставляются в одном PR.

## 4. Контроль границ и документации

- [ ] 4.1 Добавить недостающие регрессионные сценарии реальной композиции IO: повтор отчёта после неудачи сохранения источника, независимое подтверждение вместо readTask из Map, сохранение неизвестных ключей и последующей новой зависимости; сторож согласованности примера dependencyUpdates в документации с чистым валидатором. Оценка: 60 минут. Проверка: `pnpm test:pipeline lib/dependency-updates.test.mjs lib/execute.test.mjs lib/backlog-trello.test.mjs lib/scan.test.mjs`. Проверить `git -C <дерево> diff origin/main -- supervisor/lib/dependencies.mjs` — семантика допуска не изменена; при необходимости общий вспомогательный экспорт формы ID допустим без изменения поведения. `openspec validate append-addressed-card-dependencies --strict` проверяет свои артефакты. Живая запись 0238 и прогон её планировщика не выполняются, заявка арены не нужна.

Каждый отмечаемый пункт — самостоятельный коммит с немедленной отправкой. Команды исполняются отдельно из назначенного дерева; git только формой `git -C <дерево> …`. Приведённые приставки `pnpm test:pipeline:*`, `git -C:*` и `openspec:*` покрыты `supervisor/config/stage-settings.json`. Локально только названные узкие проверки; ожидание CI ведёт супервизор и в чеклист не входит.
