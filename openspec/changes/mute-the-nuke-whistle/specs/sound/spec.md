## MODIFIED Requirements

### Requirement: У каждого события боя есть звук

События мира, у которых уже есть облик, SHALL иметь и звук. Набор
закрыт и совпадает с набором записей, которые отрисовка уже читает:
выстрел (`WorldState.shots`), взрыв (`WorldState.blasts`), движение
генерала (`GeneralState.direction`).

Из набора **названным исключением** выведена ядерная ракета в снижении
(`WorldState.nukes`): облик у неё есть, а звука нет. Исключение
временное и держится на одном обстоятельстве — записи под этот звук
не существует, а посчитанный свист звучит неправдоподобно: чистый тон
с развёрткой высоты опознаётся синтезатором, и тянется он три секунды
поверх самого напряжённого события матча. Пока запись не подобрана,
ракета SHALL подлетать молча. Посчитанный свист MUST NOT выдаваться —
он не запасной путь, а отвергнутый звук.

Исключение названо здесь, а не подразумевается умолчанием, ровно затем,
чтобы правило «слышно то, что видно» осталось правилом. Незаписанное
исключение через месяц читается как забытый звук, и его «чинят»
возвратом того, от чего отказались осознанно.

Звук SHALL звучать в тот же кадр, в который событие появляется
на экране. Отставание звука от собственной вспышки MUST NOT превышать
длительности одного кадра.

Событие, у которого облика нет, звука не получает. В частности, снос
постройки по приказу MUST NOT давать взрыва — ни на экране, ни в звуке:
вспышка и грохот в этом языке означают «по тебе попали», а на разобранной
стене это была бы ложная тревога.

#### Scenario: Выстрел звучит

- **WHEN** в состоянии мира появляется запись выстрела
- **THEN** на том же кадре начинает звучать звук соответствующего оружия

#### Scenario: Гибель звучит

- **WHEN** в состоянии мира появляется запись взрыва
- **THEN** на том же кадре начинает звучать звук соответствующего вида взрыва

#### Scenario: Снос молчит

- **WHEN** постройка снесена по приказу игрока
- **THEN** записи взрыва не появляется и звука не звучит

#### Scenario: Снижающаяся ракета молчит

- **WHEN** в состоянии мира есть запись о ядерной ракете в снижении
- **THEN** сигнала на неё не возникает ни на этом кадре, ни на последующих, и до самой детонации нового звука не начинается

### Requirement: Ядерный удар предупреждает и глушит

Предупреждение об ударе SHALL быть видимым: появление ядерной ракеты
в состоянии мира отмечается на поле. Три секунды задержки даны
на реакцию, и они работают, только если о них сообщили.

Звукового предупреждения о снижающейся ракете нет. Пока не подобрана
запись, появление ядерной ракеты в состоянии мира MUST NOT начинать
никакого звука.

Известная цена этого решения записана здесь, чтобы её не пришлось
открывать заново: видимая метка рисуется на самом поле, в кадр
помещается около десятой части карты, и об ударе за краем экрана игрок
не узнаёт. Возмещается это отметкой удара на миникарте, заводимой
отдельно.

Детонация SHALL приглушать остальной звук и возвращать его плавно.
Бой возвращается быстрее музыки.

Приглушение MUST NOT затрагивать сам удар. Он его и вызывает, и попав
под собственное приглушение, оказывается тише всего ровно в тот миг,
когда должен быть громче всего. При этом громкость его SHALL
регулироваться тем же ползунком, что и прочий бой: разделение
существует ради приглушения, а не ради ещё одного регулятора.

Звук детонации SHALL быть самым громким и самым долгим в игре.

#### Scenario: Ракета подлетает молча

- **WHEN** запущен ядерный удар
- **THEN** звука снижения не возникает, и до детонации игра звучит так же, как звучала бы без запуска

#### Scenario: Мир глохнет

- **WHEN** происходит детонация
- **THEN** громкость прочих звуков снижается и плавно возвращается

#### Scenario: Сам удар не приглушается

- **WHEN** происходит детонация
- **THEN** громкость самого удара не снижается

### Requirement: Частое считается, редкое можно записью

Каждый звук SHALL иметь посчитанное представление — чистую функцию вида
«частота дискретизации → массив отсчётов». Оно обязательно для всех
и служит запасным путём.

Звуки частых событий — выстрелы всех видов и гибель машины — SHALL
звучать посчитанным представлением либо записью, дающей не менее трёх
различимых вариантов. Одна запись без вариантов для частого события
MUST NOT применяться: повторённая полсотни раз в секунду, она слышится
дребезжанием пулемёта, и разброс скорости этого не лечит — ухо ловит
повторяющийся рисунок шума, а не высоту.

Звуки редких и тяжёлых событий — ядерный удар, разрушение постройки —
MAY браться записью.

Отсутствие или порча записи MUST NOT приводить к тишине и к ошибке:
звук SHALL воспроизводиться посчитанным представлением.

Правило запасного пути относится к звукам, которые игра выдаёт.
Звук, выведенный из набора выдаваемых названным исключением, не звучит
вовсе, и посчитанное представление служит ему не запасным путём,
а заготовкой на возврат: держать его обязательно, выдавать —
запрещено. Иначе отвергнутый звук вернулся бы через чёрный ход ровно
в тот день, когда подобранная запись не загрузится.

Подмена SHALL быть односторонней: запись перекрывает посчитанное,
посчитанное запись — никогда. Оба готовятся одновременно и приходят
в непредсказуемом порядке; выигрывающий по времени прихода означал бы,
что звучит то одно, то другое от запуска к запуску.

Звук MAY складываться из нескольких записей, звучащих одновременно.
Это не варианты и не замена, а слои одного события: там, где одна
запись даёт нужный характер, но не даёт нужного тела, вторая
добавляет недостающее.

Громкость звука MUST NOT зависеть от того, записью он взят или
выкладкой: уровень назначается одной таблицей после загрузки. Записи
приходят с самым разным уровнем, и выравнивать их вручную — занятие
без конца.

Звук SHALL готовиться один раз за сеанс и переиспользоваться. Пересчёт
отсчётов в кадре MUST NOT происходить.

Один и тот же звук, воспроизведённый подряд, SHALL звучать с разбросом
высоты. Разброс SHALL выводиться из ключа события, чтобы одно и то же
событие звучало одинаково при повторной отрисовке.

#### Scenario: Частое событие звучит по-разному

- **WHEN** одно и то же частое событие происходит пятьдесят раз подряд
- **THEN** прозвучало не менее трёх различимых вариантов

#### Scenario: Пропавшая запись не рушит звук

- **WHEN** файл записи не загрузился или не декодировался
- **THEN** событие звучит посчитанным представлением, ошибки не возникает

#### Scenario: Посчитанное не затирает запись

- **WHEN** посчитанное представление готово позже, чем загрузилась запись
- **THEN** звучит запись

#### Scenario: Отсчёты считаются один раз

- **WHEN** за матч происходит тысяча выстрелов одного вида
- **THEN** массив отсчётов для этого вида вычислен ровно один раз

#### Scenario: Звук не щёлкает

- **WHEN** проверяется любой запечённый звук
- **THEN** его первый и последний отсчёты равны нулю

#### Scenario: Звук не перегружен

- **WHEN** проверяется любой запечённый звук
- **THEN** ни один отсчёт не выходит за пределы от минус единицы до единицы и ни один не равен `NaN`

#### Scenario: Отвергнутый звук не возвращается запасным путём

- **WHEN** звук выведен из набора выдаваемых названным исключением, а записи под него нет
- **THEN** событие молчит, посчитанное представление не воспроизводится, ошибки не возникает
