# Задачи

## 1. Выбор цели

- [x] 1.1 Заменить в `pushTargeting` (`packages/ai/src/opponent.ts`)
      деление карты пополам на выбор ближайшей по вероятному пути
      стреляющей постройки, накрывающей путь.
      *Проверка:* в функции не осталось множителя `0.36` и сравнения
      с расстоянием до своей базы.
- [x] 1.2 Дальность вражеской постройки брать из характеристик её
      владельца, а не из таблицы баланса.
      *Проверка:* прокачанная башня противника накрывает больше клеток
      пути, чем непрокачанная.
- [x] 1.3 Расстояние мерить по ближайшей накрытой клетке пути, а не
      по клетке самой постройки: постройка делает свою клетку
      непроходимой, и расстояние у неё не определено.
      *Проверка:* тест «башня на пути становится целью» падает при
      возврате к расстоянию по клетке постройки.

## 2. Тесты

- [x] 2.1 Тест: башня, накрывающая путь у чужой базы, становится целью.
      *Проверка:* тест падает на прежнем правиле.
- [x] 2.2 Тест: из двух башен на пути выбирается ближняя по проходимым
      клеткам.
      *Проверка:* тест зелёный.
- [x] 2.3 Тест: стена целью не становится.
      *Проверка:* целью остаётся база.
- [x] 2.4 Тест: при чистом пути целью остаётся база.
      *Проверка:* тест зелёный.

## 3. Эталон и замер

- [x] 3.1 Обновить эталон `profile.golden.test.ts` тем же коммитом.
      *Проверка:* тест зелёный с новым числом.
- [ ] 3.2 Прогнать арену до и после и свести: личный рост вражеских
      башен к концу матча, потери юнитов, снятое с базы здоровье.
      *Проверка:* таблица приложена к изменению.
- [ ] 3.3 Проверить страховочную величину: снятое с базы здоровье
      не упало.
      *Проверка:* если упало — разбираться с составом волны, а не
      возвращать прежнее целеуказание.
- [x] 3.4 Прогнать `pnpm verify`.
      *Проверка:* линт, типы, тесты и сборка зелёные.
