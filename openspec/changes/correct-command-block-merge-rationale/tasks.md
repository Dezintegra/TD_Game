## Предусловие реализации

PR 132 должен быть влит. Проверить `gh pr view 132 --json state,mergedAt`, затем освежить своё дерево командами `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main`, отдельными вызовами. Слияние и возможный конфликт обрабатываются по действующим правилам этапа. До появления целевых документов через main правку не начинать; не переносить файлы из чужой ветки. Если изменение архивировано, использовать его единственную архивную копию.

## 1. Исправить факт и вывод одним коммитом (до 90 минут)

- [ ] 1.1 Согласовать proposal.md, design.md и tasks.md изменения `mark-every-command-block` по решениям собственного design.md: покрытая форма с `-C`, снятие вывода о невозможности обновления и неисправимом CI, уточнение условия невозможности fast-forward, сохранение отсрочки 0177 ради расширения общего парсера из PR 126, корректное разграничение покрытия команды и правил работы с конфликтом. Старые отметки исполнения сохранить как историю конкретной базы. Выполнить проверки ниже, отметить этот пункт в собственном tasks.md, закоммитить только три целевых документа и собственную отметку, сразу отправить `git -C <дерево> push -u origin HEAD`; после этого первого коммита реализации открыть черновой PR собственной задачи.

Проверка результата: прочитать три документа целиком; ни один актуальный довод не утверждает, что merge с `-C` запрещён разрешениями, что обновление требует пересоздания дерева или что сторож неисправимо валит PR. Все три документа одинаково объясняют отсрочку через 0177 и парсер PR 126. Отмеченное исполнение старой задачи не расширено задним числом. Инструкция о конфликте отсылает к действующему этапу и не поручает ревью обновлять базу.

Проверка состава: `git -C <дерево> diff --check` перед коммитом и `git -C <дерево> show --stat --oneline HEAD` после него. В коммите реализации три документа цели и собственный tasks.md с отметкой; код, основные спецификации и дельты не меняются. Собственные артефакты проработки уже лежат в отдельном коммите этой ветки.

Проверка формы собственного изменения: `openspec validate correct-command-block-merge-rationale --strict` допускает только единственную ошибку `Change must have at least one delta. No deltas found.` при отсутствии specs/ и наличии раздела «Почему дельты нет»; `openspec status --change correct-command-block-merge-rationale` показывает 3 из 4 артефактов. Новые тесты для единичной правки объяснений не нужны. Ожидание CI выполняет супервизор, пунктом задач оно не является.
