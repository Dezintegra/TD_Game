# Задачи

## 0. Предусловие: свежая база (своей правки не несёт)

- [x] Проверить по `origin/main`, а **не по дереву**, что
      `admit-changes-without-deltas` (pull request 131) влит:
      `git fetch origin`, затем
      `git log --oneline origin/main -- supervisor/skills/design.md` —
      среди коммитов должен быть «назвать ключ архивации бездельтового
      изменения» (на 03.09.2026 это `71fb775`).

  **Исход `failed` даёт только отсутствие этого коммита в `origin/main`.**
  Отсутствие текста в дереве беды не значит и работы не отменяет: ветка
  `worktree-0173` ответвлена от `8d2e5f8`, то есть раньше вливания, и
  отставшее дерево выглядит в точности как невлитый pull request.

- [x] Подтянуть свежую базу в свою ветку:
      `git -C <путь к своему дереву> merge origin/main`. Форма с `-C`
      покрыта правилом `PowerShell(git -C:*)`
      (`supervisor/config/stage-settings.json:65`). Слияние даёт коммит
      слияния, но своей правки не несёт — следом этапа он не служит.
      Отправить сразу (`git push`): неотправленный коммит — хвост, а этап
      с хвостом в ветке следа не имеет вовсе.

  **Проверка:** после слияния в дереве находятся поиском по содержимому
  и абзац с `--skip-specs` в `supervisor/skills/design.md`, и оговорка
  «Проба этой команды на изменении без дельт ещё не ставилась», и абзац
  «Изменение, не меняющее ни одного требования, ведётся без `specs/`»
  в `CLAUDE.md`. Чего-то из этого нет при влитом `71fb775` — значит
  слияние не прошло; разобрать, а не идти дальше.

## 1. Назвать в правилах этапов единственный случай архивации

- [x] В `supervisor/skills/design.md`, в запрете «**Архивировать изменение
      (`openspec archive`)**», к абзацу про `--skip-specs` добавить
      оговорку: единственный случай, когда этап вправе позвать архивацию, —
      нарочно заведённая бездельтовая пустышка ради пробы, ключом
      `--skip-specs` и без включения в коммит. Настоящее изменение —
      своё или чужое — этап не архивирует по-прежнему, и запрет этот
      стоит рядом с оговоркой, а не заменяется ею.
- [x] В `supervisor/skills/implement.md` назвать тот же случай теми же
      словами: сегодня скилл молчит об архивации вовсе (проверено
      `git grep -n "archive" origin/main -- supervisor/skills`), а молчание
      исполнитель вправе прочесть как разрешение.
- [x] `supervisor/skills/audit.md` **не трогать** — обоснование
      в `proposal.md`, разделом Non-goals.

  **Почему первым, а не после пробы:** пробу ставит эта же сессия, и звать
  `openspec archive` она обязана под правилами, которые случай уже
  называют. Иначе дельта и правила разойдутся в тот самый день, когда
  изменение влито.

  **Проверка:** `npx prettier --check supervisor/skills/design.md` и
  `npx prettier --check supervisor/skills/implement.md` — зелено (каталог
  `supervisor/` из проверки формата не исключён, в отличие от `openspec/`);
  `pnpm test:pipeline` — зелено, сторожа текстов скиллов не покраснели;
  слово «пустышк» находится поиском в обоих файлах.

  **После этого коммита открыть черновой pull request** — до него отправки
  в ветку не запускают ни одной проверки CI.

## 2. Поставить пробу и записать ответ в скилл проработки

- [ ] Завести пустышку в своём рабочем дереве:
      `openspec new change probe-archive-sandbox --description "Пустышка
      для пробы архивации бездельтового изменения"`. Убедиться, что
      каталога `specs/` у неё нет (состав каталога — поиском файлов).
- [ ] Основная проба:
      `openspec archive probe-archive-sandbox --skip-specs --yes`.
      Записать команду, код возврата и вывод.
- [ ] Если основная проба отвергнута встроенной проверкой — вторая проба
      той же пустышкой (или заново заведённой, если первая уже уехала):
      `openspec archive probe-archive-sandbox --skip-specs --no-validate --yes`.
      Ключ `--yes` обязателен: без него `--no-validate` спросит
      подтверждения, а неотвеченное подтверждение в автономном запуске
      оборачивается отказом, и проба покажет поведение оболочки вместо
      поведения инструмента.
- [ ] В `supervisor/skills/design.md`, в запрете «Архивировать изменение»,
      заменить абзац «**Проба этой команды на изменении без дельт ещё
      не ставилась**…» на ответ пробы. В ответе назвать: исход, версию
      OpenSpec (`openspec --version`), дату пробы и — если понадобился
      `--no-validate` — прямое следствие, что путь открыт человеку
      с живой консолью и закрыт автономной сессии конвейера.
- [ ] Пустышку и её архивированную копию **в коммит не включать**:
      `git add -- supervisor/skills/design.md` и ничего сверх. Убрать
      их сессия не может — удаление файлов оболочкой правилами
      не открывается, — но неотслеживаемый файл уйдёт вместе с деревом.
- [ ] Протокол пробы (команда, код возврата, ключевые строки вывода)
      положить в тело коммита повторными `-m`, по абзацу на `-m`.

  **Проверка:** `git status --porcelain` показывает архивированную
  пустышку строками `??` и ни одной строкой в индексе, кроме
  `supervisor/skills/design.md`; в коммите ровно один файл
  (`git show --stat HEAD`); слов «проба… не ставилась» в скилле
  не осталось, а оговорка про пустышку из шага 1 осталась нетронутой.

## 3. Записать тот же ответ в памятку проекта

- [ ] В `CLAUDE.md`, в абзаце «Изменение, не меняющее ни одного
      требования, ведётся без `specs/`», рядом с `openspec archive <имя>
      --skip-specs` назвать ответ пробы — теми же словами, что и в скилле,
      с версией и датой. Расхождение двух текстов хуже отсутствия одного
      из них: читатель не знает, какому верить.
- [ ] Если проба показала, что нужен `--no-validate`, — назвать это здесь
      прямо: памятку читает человек, и ему этот путь как раз открыт.

  **Проверка:** `npx prettier --check CLAUDE.md` — зелено (в отличие
  от `openspec/`, памятка проверкой формата покрыта); ответ в памятке
  и в скилле совпадает дословно в части исхода, версии и даты.

## 4. Сторож на форму ответа

- [ ] В `supervisor/config/transitions.test.mjs` — там уже сторожат
      тексты скиллов — добавить проверку: в `supervisor/skills/design.md`
      и в `CLAUDE.md` рядом с `--skip-specs` стоит версия вида `1.6.0`
      и дата вида `03.09.2026`, а фразы о непоставленной пробе рядом
      с ней нет.
- [ ] В комментарии теста объяснить «почему»: утверждение о поведении
      стороннего инструмента живёт до его обновления, и без версии
      читатель не знает, к чему ответ относится. Проверка сторожит
      не текст, а обязанность называть версию.

  **Проверка:** `pnpm test:pipeline` — зелено; временная порча текста
  (убрать версию из скилла) роняет новый тест, а не какой-нибудь
  соседний.

## 5. Дождаться зелёного CI

- [ ] `gh pr checks <номер> --watch` — дождаться всех проверок.
- [ ] Красное разобрать по `gh run view --job <id> --log-failed`
      и починить отдельным атомарным коммитом.

  **Проверка:** все проверки зелёные. Задача не сделана, пока GitHub
  не сказал «зелено».

  Черновой статус **здесь не снимают**: `gh pr ready` — первый подшаг
  вливания, и делает его ревью. На изменении, уезжающем в доработку,
  черновик обязан остаться.
