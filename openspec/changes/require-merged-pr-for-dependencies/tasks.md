## 1. Контракт и безопасное удержание

- [x] 1.1 Добавить `dependencyResults` в `supervisor/config/task-schema.json`, перенос в `card.mjs` и `requests.mjs`, полную общую проверку формы и семантики в `dependencies.mjs`; расширить pendingDependencies/scan входами доказательств с безопасным unknown по умолчанию. Если файловый режим ещё существует, расширить также `manage/schema.json` как временную совместимость; если пункт 9.1 `backlog-in-trello` уже доставлен, удалённую схему, адаптер и их тесты не восстанавливать. Вместе добавить тесты round trip, заявок, неправильного вида/PR/повторов/ребра вне dependsOn и удержания закрытого предшественника без доказательства. Эти тесты проверяют путь Trello и общий валидатор без импорта файловой схемы/адаптера, включая отказ загрузчика при обращении к ним, и остаются после удаления файлового режима; проверки временной схемы держать отдельно. До подключения чтения GitHub новые условия остаются удержанными, старые карточки работают как раньше; коммит самодостаточен. Проверка: `npx vitest run --root supervisor lib/card.test.mjs lib/requests.test.mjs lib/scan.test.mjs`; пока файловая схема существует, дополнительно `npx vitest run --root supervisor lib/validate-task.test.mjs`. Оценка: до 2 часов. После коммита немедленно отправить свою ветку и открыть единственный черновой PR по правилу этапа implement; ждать CI исполнителю не нужно.

## 2. Доказательства из архива и GitHub

- [x] 2.1 Добавить общий `dependency-evidence.mjs` с внедряемым runner и расширить `backlog-trello.mjs` сведениями об архивных предшественниках и неоднозначных ID. Покрыть совпадение ссылки и ожидаемого PR, обе разновидности closed, активные/архивные/негодные дубликаты, MERGED с датой и нужной базой, OPEN/CLOSED/другая база/номер/null, ошибки JSON, команд и тайм-аут, дедупликацию чтений и ноль чтений для старых карточек. Проверка: `npx vitest run --root supervisor lib/dependency-evidence.test.mjs lib/backlog-trello.test.mjs`. Оценка: до 2 часов. Модуль пока не включён в рабочий снимок; предыдущий коммит продолжает безопасно удерживать неизвестное. Коммит и немедленная отправка.

## 3. Одинаковый допуск в обеих точках входа

- [x] 3.1 Подключить общий сбор доказательств к `bin/supervise.mjs` и `bin/cycle.mjs`, передать snapshot в scan; добавить ограничение чтения 10 секунд к используемому runner, не меняя тайм-ауты остальных команд. Интеграционные тесты с подставным runner проверяют фактический путь сборки state для обеих точек входа без выполнения их main и без живой доски; при необходимости выделить общий вызываемый сборщик. Проверить сохранение implement и счётчиков при maxConcurrent=1, готовую feature рядом с удержанным run, исключение удержанного deploy из пакета/exclusive, отсутствие действий по лимитам, сохранение running и transfer-report, закрытие+вливание на следующем снимке, смену номера и перезапуск. Проверка: `npx vitest run --root supervisor lib/dependency-evidence.test.mjs lib/scan.test.mjs lib/cycle.test.mjs lib/supervisor.test.mjs`. Оценка: до 2 часов. Коммит и немедленная отправка.

## 4. Описание эксплуатации и проверка артефактов

- [x] 4.1 Обновить `manage/README.md`: форма дополнительного результата, закрытие вместе с вливанием, ожидаемый PR и links.pr, архив, ошибки чтения, порядок включения на всех станциях и безопасного отката; привести пример без правки доски. Назвать постоянную проверку в общем валидаторе и временный характер файловой совместимости: при последующей уборке по пункту 9.1 `backlog-in-trello` удаляются только файловая схема/адаптер и их отдельные тесты, гарантии Trello сохраняются. Если уборка уже доставлена, описывать только действующий путь. Сверить описание с тестами и всеми сценариями дельты. Проверка: `npx prettier --check manage/README.md`, `openspec validate require-merged-pr-for-dependencies --strict` и `git -C <дерево> diff --check`. Оценка: до 30 минут. Коммит и немедленная отправка.

Каждый пункт включает свою отметку в этом файле тем же коммитом. Команды выполняются из назначенного дерева отдельными вызовами; `<дерево>` заменяется его путём. Формы `npx vitest:*`, `npx prettier:*` и `git -C:*` сверены с `supervisor/config/stage-settings.json`. Полный verify, арена и запуск боевого супервизора для этой работы не требуются. При необходимости добавить узкие тестовые файлы к указанному вызову Vitest с тем же `--root supervisor`.
