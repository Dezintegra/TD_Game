# Задачи

Продолжение 0124 после ответа владельца: исполнить только открытые пункты
раздела 7. Разделы 1–6 сохраняют историю PR #45. Обнаруженная недоделка
2.2 вынесена в 7.1; старые замеры и отметки не подтверждают коэффициент.
Это доработка существующего изменения с кодом после аудита.

Исторический порядок: **сначала базовая линия, потом правки.**
Сравнивать будет не с чем, если снять её после.

Правки едут в новый профиль, базовый остаётся нетронутым. Так их можно
стравить напрямую и откатить сменой имени, не откатывая код.

## 1. Базовая линия

- [x] 1.1 Прогнать `baseline-2026-08` против себя на seed 2000–2019,
      заингестить, сохранить сводку в файл.
      *Проверка:* сводка получена; значения совпадают с приведёнными
      в предложении по порядку величины (47% дохода в ударах, казна
      ≈29 000, юнитов 5–9, ничьих около половины).
- [x] 1.2 Записать запросы, которыми сняты величины из таблицы
      предложения, в `apps/arena/README.md` — чтобы после правок мерить
      тем же, а не похожим.
      *Проверка:* запросы выполняются на существующей базе и дают числа
      из предложения.

## 2. Окупаемость удара

- [x] 2.1 Считать ценность цели по оставшейся стоимости, а не по головам:
      `стоимость × здоровье / полное здоровье`, для чужих со знаком плюс,
      для своих со знаком минус.
      *Проверка:* юнит-тест — скопление из десяти добитых юнитов
      оценивается заметно дешевле десяти целых.
- [x] 2.2 Вывести порог из цены удара и стоимости юнита. Убрать
      `worthUnits` из профиля. **Уточнено 13.09.2026:** обещанный здесь
      коэффициент не реализован; его выполнение и проверка теперь в 7.1.
      *Проверка:* тест — при нынешнем балансе порог равен цене удара,
      делённой на стоимость юнита; при удвоении цены удара в балансе
      порог удваивается сам.
- [x] 2.3 Тест на отказ от невыгодного удара: шесть целых юнитов
      в радиусе — удара нет; полсотни — есть.
      *Проверка:* оба случая проходят.

## 3. Запас по обстановке

- [x] 3.1 Разделить в `tryNuke` поиск цели и решение бить: поиск
      возвращает лучшее скопление и его ценность, решение сравнивает
      с порогом. Обход карты при этом остаётся один — второй ради
      запаса недопустим.
      *Проверка:* число обходов карты за решение не выросло; замер
      стоимости матча в арене не ухудшился.
- [x] 3.2 Заменить `reserve` фазы с признака «держать цену удара»
      на «удар в этой фазе интересен». Держать запас только когда поиск
      нашёл оправданную цель.
      *Проверка:* тест — при пустой карте энергия доступна тратам целиком;
      при скоплении, оправдывающем удар, — удерживается.
- [x] 3.3 Обновить `profile.ts`: поле `reserve` фазы меняет смысл,
      комментарий объясняет, почему запас стал свойством обстановки.
      В комментарии назвать тупик, из которого выбираемся: энергия
      заперта → нечем производить → армии малы → скопления не набираются
      → удар не оправдан → запас не тратится.
      *Проверка:* `pnpm --filter @td/ai typecheck` зелёный.

## 4. Накопление не глушит дешёвое — СНЯТО

**Раздел закрыт не выполнением, а отменой.** Сравнение по цене
опробовано в `value-towers-honestly` и отвергнуто замером: юнит вчетверо
дешевле улучшения, поэтому «покупай что дешевле желаемого» вырождается
в «покупай юнитов всегда» — за две минуты 59 машин, ноль построек
и ни одного улучшения сверх первого.

Задачу решает то же изменение, но сравнением **по прибавке на единицу
энергии** (`saving` и `outvalues` в `opponent.ts`, `SavingForBetter`
в `observer.ts`). Требование «Накопление не блокирует покупки дешевле
желаемого» из дельты этого изменения убрано — его место занимает
«Накопление уступает очередь тому, что выгоднее» из `value-towers-honestly`.

- [x] 4.1 Провести через исход попытки прибавку желаемого: `wait` сообщает,
      ради чего копится.
      *Проверка:* `AttemptNote.SavingForBetter` и поле `gain` в `observer.ts`
      доезжают до наблюдателя и до таблицы `attempt`.
- [x] 4.2 Прерывать перебор трат только для покупок, не превосходящих
      желаемое по прибавке на единицу энергии.
      *Проверка:* сделано в `value-towers-honestly`, раздел 3.
- [x] 4.3 Сверить, что предел терпения срабатывает реже.
      *Проверка:* обрывов очереди из-за накопления 10 554 → 884
      (замер `value-towers-honestly`, раздел 5).

## 5. Покупка с большей прибавкой обходит очередь

- [x] 5.1 Считать прибавку от юнита: его урон за ожидаемое время жизни,
      переведённый в энергию курсом, который уже есть в `posture.ts`.
      *Проверка:* юнит-тест — величина растёт с прокачкой атаки
      и не зависит от размера войска.
- [x] 5.2 Считать прибавку от боевого улучшения: процент прибавки,
      применённый к тому, чем игрок владеет. В комментарии объяснить
      главное: прокачка умножает то, что уже есть, и при пяти юнитах
      умножать нечего.
      *Проверка:* тест — при войске в пять юнитов прибавка мала,
      при войске в полсотни велика; отношение примерно десятикратное.
- [x] 5.3 Разрешить покупке с большей прибавкой обойти очередь фазы.
      Историческое исключение экономики отменено последующей основной
      спецификацией: добыча участвует общей меркой будущего дохода
      за существующий горизонт. Дельта согласована на design 0124;
      реализацию экономики этот пункт заново не заказывает.
      *Проверка:* тест на оба случая из спецификации — при малом войске
      покупается машина, при большом прокачка. Прежнее ожидание отсутствия
      сравнения экономики больше не является критерием приёмки.
- [x] 5.4 Сверить, что сопровождение генерала стало частным случаем
      этого правила, а не осталось вторым механизмом рядом.
      *Проверка:* поведение при уходе генерала вперёд без прикрытия
      не изменилось; отдельная ветка `escorting` либо исчезла, либо
      сведена к настройке порога.

## 6. Перемер и решение

- [x] 5.1 Завести профиль с правками (имя с датой, как принято)
      и стравить с базовым на seed 2000–2019.
      *Проверка:* прогон и сводка получены.
- [x] 5.2 Сравнить все семь величин из таблицы предложения.
      *Проверка:* каждая двинулась в заявленную сторону. Если хоть одна
      пошла против — не подкручивать до нужного числа, а разбираться:
      несогласованные величины означают, что правка сделала не то,
      что задумано.
- [x] 5.3 Обновить эталон `profile.golden.test.ts`, если менялся профиль
      по умолчанию.
      *Проверка:* тест зелёный, новое число в том же коммите, что и правка.
- [x] 5.4 Решить судьбу ничьих. Если после правок доля ничьих упала
      сама — отдельная работа над добиванием не нужна. Если осталась —
      завести изменение с измеренными числами, а не с догадками.
      *Проверка:* решение записано в этом файле с числом.

      **Решение: отдельная работа над добиванием НЕ заводится, но вопрос
      поставлен в очередь баланса.** Числа (все — `baseline.md`):

      - у базового профиля ничьих **7 из 20**, а не 10, как в предложении;
      - у «Стратега» после правок раздела 3 — **7 из 20** против 3 до них,
        то есть доля выросла, а не упала;
      - но здоровья базы в тех ничьих снимается **47,9%** против 38,0%
        до правок и 9% в предложении.

      Последнее число и есть довод против отдельной работы. Беда,
      описанная в предложении, — это ничья, в которой стороны едва
      царапают друг друга: девять процентов базы за двадцать минут.
      Такой ничьей больше нет. Нынешняя — это матч, дошедший до половины
      чужой базы и не успевший, а это вопрос темпа, а не добивания.

      Чинить его вслепую нельзя ещё и потому, что рост с 3 до 7 у одного
      «Стратега» объясняется, похоже, не темпом вовсе, а тем, что он
      сошёлся с базовым профилем (у которого те же 7 из 20). Проверяется
      это разными профилями, а не зеркальным матчем, и стоит записью
      `fix-ai-spending-strategist-strength` в `balance/queue.json`.

- [x] 5.5 Прогнать `pnpm verify`.
      *Проверка:* линт, типы, тесты и сборка зелёные.

      Сделано **не локально, а в CI**, и это не поблажка: работа шла
      в дополнительном рабочем дереве, а оттуда `pnpm verify` не гоняют
      (`CLAUDE.md`, правило 6). Задача считается сделанной по зелёным
      проверкам GitHub. Все шесть зелёные на PR #45, включая матчевый
      набор — тот, где живёт эталон `profile.golden.match.test.ts`.

## 7. Коэффициент допуска убытка — продолжение 0124

Каждый пункт — один самостоятельный коммит с немедленной отправкой.
Все команды выполняются в назначенном дереве; `git -C .` указывает на него.
Добавлять только перечисленные для пункта пути, включая отметку его
выполнения в этом `tasks.md`. Перед коммитом проверить пути и содержание
`git -C . diff --cached`; после — `git -C . show --stat --oneline HEAD`
и `git -C . show HEAD`. Разрешённые пути не разрешают несвязанные правки
внутри них. Временные файлы, код соседних задач и основная спецификация
в коммиты не входят. Полные тесты, типы, сборка и матчевый golden — CI;
ожидание CI исполняющей сессии не назначается.

- [x] 7.1 Реализовать необязательный `nuke.minValueRatio`, нормализацию
      и общий порог в существующей `nukeWorthIt`; передать коэффициент
      в пуск и `nukeAwaited`. Сохранить оплату полной текущей цены пуска
      и существующий базовый резерв `NUKE_COST` без скидки коэффициентом,
      единичный поиск цели, условия `invest`, фазы, достижимости и отката.
      Дополнить быстрые проверки реальных решений, параметризовав стенд
      по профилю. Пути: `packages/ai/src/profile.ts`,
      `packages/ai/src/opponent.ts`, `packages/ai/src/nuke.test.ts`,
      `openspec/changes/fix-ai-spending/tasks.md`. Оценка: до двух часов.
      Проверка: `npx vitest run packages/ai/src/nuke.test.ts` и
      `npx eslint packages/ai/src/profile.ts packages/ai/src/opponent.ts packages/ai/src/nuke.test.ts`.
      Проверить отсутствие/1/0.5/2, некорректные 0, отрицательное, NaN,
      обе бесконечности; значения ниже/на/выше порога; undefined-цель,
      нулевой и отрицательный net; рост фактической цены после прокачки.
      В решениях при одной подходящей только для 0.5 цели: богатый ИИ
      пускает при 0.5 и не пускает при 1; энергии между сниженным порогом
      и полной ценой на пуск не хватает; бедный поздний Стратег при 0.5
      удерживает резерв, при 1 делает конкретную обычную покупку;
      исчезновение цели освобождает резерв на нынешнем механизме до 0026.
      При `invest: false`, откате либо недостижимом базовом `NUKE_COST`
      коэффициент не включает резерв. Для проверки границы достижимости
      сравнить сумму `NUKE_COST` с `savingLimit`: равенство допускает запас,
      меньший лимит исключает его. Прокачка меняет проверку выгоды и оплату
      пуска, но не сумму нынешнего резерва: явно проверить это различие,
      не включать сюда исправление `reserveOf` из 0026.
      Сохранить положительный контроль для запретной зоны и собственных
      потерь. Сравнить последовательности команд отсутствующего коэффициента
      и явной 1 на одном стенде. Проверки должны падать при удалении
      передачи коэффициента отдельно в пуск и отдельно в запас.
      После отправки этого коммита implement открывает черновой PR.

- [ ] 7.2 Добавить замороженный профиль `strategist-loss-half-2026-09`
      в существующий `PROFILES`: копия `STRATEGIST_PROFILE`, отличающаяся
      только id и коэффициентом 0.5. Добавить быстрый тест регистрации,
      неизменности исходного профиля, заморозки и единственности отличия.
      Пути: `packages/ai/src/profile.ts`, `packages/ai/src/nuke.test.ts`,
      `openspec/changes/fix-ai-spending/tasks.md`. Оценка: до одного часа.
      Проверка: `npx vitest run packages/ai/src/nuke.test.ts` и
      `npx eslint packages/ai/src/profile.ts packages/ai/src/nuke.test.ts`;
      `profileByName` возвращает новый профиль, штатный Стратег сохраняет
      эффективную 1, выбор боевых комнат не меняется. В CI прежний
      `profile.golden.match.test.ts` должен пройти без смены эталона.

Обязательная арена уже заказана задачами 0335 и 0336: по 60 матчей
для обеих расстановок контрольного и пробного Стратега, seed 3000–3059,
предел 1200 секунд, один SHA после реализации. Ожидания — в новом разделе
design.md; повтор старых заказов 0006 и 0120 не требуется.
Арена и последующая архивация не являются пунктами implement.

После 0124 задача 0026 согласует свою дельту и реализацию по разделу
«Согласование с 0026 после аудита» в design.md: сохраняет коэффициент,
заменяет проверку немедленного освобождения проверками памяти на горизонт
и исправляет сумму резерва с достижимостью. Это работа существующей 0026,
не дополнительный открытый пункт 0124 и не условие завершения 7.1/7.2.
