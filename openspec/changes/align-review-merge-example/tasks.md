## 1. Согласовать образец вливания

- [x] 1.1 В командном блоке шага 8 `supervisor/skills/review.md` заменить только `gh pr merge <pr> --rebase` на `gh pr merge <pr> --merge`, сохранив соседний текст и все остальные команды. Оценка — 15 минут. Проверка: прочитать блок и пояснение, сопоставить ключ с `gh pr merge 1 --merge` в `supervisor/config/permissions.mjs`; в исправленном блоке нет `--rebase`, запреты `--squash`, `--delete-branch` и `--admin` и подшаг `gh pr ready` сохранены. Выполнить `git -C <дерево> diff --check`. Отметить этот пункт выполненным и добавить явным списком только `supervisor/skills/review.md` и `openspec/changes/align-review-merge-example/tasks.md` командой `git -C <дерево> add -- supervisor/skills/review.md openspec/changes/align-review-merge-example/tasks.md`. До коммита проверить пути через `git -C <дерево> diff --cached --name-only` и содержимое через `git -C <дерево> diff --cached`: допустимы только замена ключа и отметка этого пункта, без несвязанных правок даже в этих двух файлах. После коммита сверить тот же состав и содержание через `git -C <дерево> show --format=fuller --stat --patch HEAD`, сразу отправить коммит. Этот единственный шаг даёт готовую правку для открытия чернового PR по правилам имплементации; ожидание CI выполняет супервизор.
