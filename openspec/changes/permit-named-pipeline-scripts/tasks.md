## 1. Открыть поимённые вызовы и согласовать их границы

Предусловие реализации: PR 126 задачи 0156 (`one-shell-for-stages`) влит
в main. Проверить отдельным вызовом `gh pr view 126 --json state,mergedAt`:
ожидаются `MERGED` и непустое `mergedAt`. Затем отдельными вызовами
`git -C . fetch origin` и `git -C . merge origin/main` получить результат
в своём дереве по правилам этапа. До выполнения предусловия реализацию
не начинать; чужую ветку сюда не сливать. Это порядок интеграции,
а не пункт ожидания CI. Итоговая основа: нет Bash в `allow`, общая мерка
проверяет только PowerShell; существующие `deny` обеих оболочек сохранены.

- [ ] 1.1 Одним атомарным коммитом (до 2 часов) добавить шесть поимённых
  разрешений `pnpm run` для шести имён из design в
  `supervisor/config/stage-settings.json` только для PowerShell,
  согласовать `$pnpmNote`, заменить
  blanket-сторож в `supervisor/config/transitions.test.mjs` матрицей
  покрытия и мутационными случаями, сохранив прямые запреты управления
  супервизором в обеих оболочках и сторож отсутствия Bash-разрешений из 0156.
  Тем же коммитом согласовать proposal/design и два требования
  дельты `permit-the-pipeline-scripts` по решению 3 design, сохранив остальные
  сценарии, а обещание разрешений обеих оболочек заменить на PowerShell.
  Проверки и наблюдаемые результаты перечислены ниже. Отметить
  этот пункт выполненным в том же коммите, отправить его и сразу открыть
  черновой PR по правилам этапа реализации.

Коммит самодостаточен: отдельная правка разрешений уронила бы старый сторож,
а раздельная правка контракта оставила бы требования противоречивыми.
На этапе проработки перечисленные ниже проверки реализации не запускаются.

Проверки реализации из назначенного дерева, каждая отдельным вызовом:

```powershell
npx vitest run --root supervisor config/transitions.test.mjs
```

Ожидается: штатная настройка проходит; копии с удалённым правилом, снятым
хвостом, перекрывающим запретом и расширенным перечнем дают ожидаемую
диагностику. Запрещённые операции и выкладка тестом не запускаются.
В `allow` ровно семь правил `pnpm run`: точное перечисление и шесть
PowerShell-приставок. Все шесть имён проверяются без аргументов и с ними;
остальные имена пакета и выдуманные имена не покрыты. Добавление
`Bash(pnpm run build:*)` обнаруживается как возврат запрещённой оболочки;
замена им PowerShell-правила дополнительно даёт непокрытую команду.
Штатные Bash-запреты не вызывают ошибку. Покрытие в Bash не требуется.

```powershell
npx eslint supervisor/config/transitions.test.mjs
```

```powershell
npx prettier --check supervisor/config/stage-settings.json supervisor/config/transitions.test.mjs
```

```powershell
openspec validate permit-named-pipeline-scripts --strict
```

```powershell
openspec validate permit-the-pipeline-scripts --strict
```

Вторая адресная валидация нужна только при реализации, поскольку там
согласуется дельта этого изменения. Общий прогон всех дельт не нужен.

```powershell
git -C . diff --check
```

```powershell
git -C . diff --stat
```

Ожидается: формат и адресные проверки проходят; diff содержит только
настройку, её тест, три согласуемых плановых файла старого изменения
и отметку в собственном tasks.md. В старой дельте нет абсолютного запрета
именованных вызовов и утверждения, что deploy/ship закрыты; требование
осмотра инструмента и его сценарии сохранены.
В согласованных текстах разрешения обещаны только для PowerShell,
а запреты управления сохранены для обеих оболочек.

Формы команд покрыты текущими `npx vitest:*`, `npx eslint:*`,
`npx prettier:*`, `openspec:*`, `git -C:*`. Штатный набор тестов конвейера
остаётся проверкой CI этого PR; ожидание CI выполняет супервизор.
Арена и фактические замеры не требуются, поскольку игровые правила не меняются.
