# Задачи

**Проб ставить заново не надо.** Таблица в `design.md` снята делом
04.09.2026 в проработке этой задачи. Повторять её — это девять заведомых
отказов в журнале задачи и ни одной новой цифры.

**Единственный неснятый вопрос шагом здесь не закрывается.** Сверяется ли
названная разбором часть с правилами, видно только после вливания: файл
разрешений этап получает ключом `--settings` от каталога инструмента,
а не из ветки (`supervisor/lib/stage-command.mjs:92`). Шаг 6 кладёт
пробные правила, ответ снимает отдельная задача бэклога.

**Порядок с соседями объявлен: изменение идёт после вливания pull
request 126** (`one-shell-for-stages`, задача 0156). Оно правит все десять
скиллов, `stage-settings.json` и `transitions.test.mjs` — весь набор файлов
шагов 2–6, — и вливающийся вторым перепишет чужое молча. Разбор пересечения
целиком, вместе с выводом «требования не спорят», стоит в `proposal.md`,
раздел «Пересечения с открытыми изменениями»; ниже из него следуют только
указания по месту правки.

## 1. Подтянуть свежую базу и подготовить дерево

- [ ] `git -C <дерево> fetch origin`, затем `git -C <дерево> merge origin/main`.
      Форма `git -C` обязательна: голая приставка покрыта правилами
      не у всякой подкоманды, и `git merge` не покрыт ни одним
      (`supervisor/config/stage-settings.json`, задача 0180). Без этого
      шага правка ложится на базу, которой в `main` уже нет: проработка
      этой самой задачи 04.09.2026 отстала на пять коммитов и не увидела
      влитого `write-covered-commands-in-tasks`, правящего те же два файла.

- [ ] **Повторить подтягивание перед последним пушем**, если за время
      работы влился pull request 126. Влился ли он, отвечает одна команда:
      `gh pr view 126 --json state,mergedAt`. После такого слияния перечитать
      шаги 5 и 6: раскладка `transitions.test.mjs` и `stage-settings.json`
      сменится, а в скиллах появится раздел «## Оболочка команд».

- [ ] `pnpm install --frozen-lockfile` в своём дереве. Без `node_modules`
      не запустятся ни `npx prettier`, ни `pnpm test:pipeline`, а они нужны
      каждому шагу ниже.

  **Проверка:** `git -C <дерево> rev-list --left-right --count origin/main...HEAD`
  печатает `0` слева — база свежая. Коммит этот шаг даёт только тогда,
  когда `main` ушёл вперёд, и pull request на нём открывать нечем: у ветки
  без своей правки нет диффа с `main`, и `gh pr create` отвечает отказом.

## 2. Переписать абзац в скилле проработки и открыть черновой pull request

- [ ] В `supervisor/skills/design.md` заменить пункт «Склеивать команды
      в одном вызове» текстом из раздела «Абзац целиком» в конце этого
      файла. Правка образцовая: остальные девять файлов повторят её слово
      в слово. Пункт живёт в разделе «Чего делать нельзя»; пункта шага 10
      про форму `git -C <дерево>` не касаться вовсе — он влит задачей 0182,
      и его сторож ищет в файле подстроки `git -C <дерево>`
      и «Перечень покрытого живёт».

- [ ] В соседнем пункте «Перечислять файлы и каталоги через оболочку»
      заменить ложный довод «составную команду не покрывает никакое правило
      разрешений» верным — тем же, что в новом тексте: разбор требует, чтобы
      открыто было каждое действие строки, и хватает одного непокрытого.
      Сам запрет на перечисление через оболочку сохраняется дословно.

- [ ] Открыть черновой pull request сразу после коммита — до этого проверки
      CI на пуши в ветку не запускаются вовсе:
      `gh pr create --draft --base main --title "<заголовок>" --body-file <файл>`.

  **Проверка:** `npx prettier --check supervisor/skills/design.md` зелёный;
  поиском по файлу не находится ни «составную не разрешить заранее никаким
  правилом», ни «не покрывает никакое правило», зато находятся `node --eval`
  и `.matchlog`.

## 3. Разнести абзац по остальным восьми скиллам этапов

- [ ] Тот же текст дословно в `implement.md`, `revise.md`, `audit.md`,
      `review.md`, `benchmark.md`, `deploy.md`, `triage.md`,
      `postmortem.md`. Дословно — потому что сторож шага 5 сверяет каждый
      файл, а разъехавшиеся копии одного абзаца это известная беда
      конвейера.

- [ ] В тех же восьми файлах поправить ложный довод в пункте «Перечислять
      файлы и каталоги через оболочку» — он стоит там дословно тот же.

- [ ] В `benchmark.md` абзац встречается со своим давним запретом
      на `node -e` — это абзац, начинающийся словами «**`node -e "…"`
      не применяй ни для этого, ни для чтения журнала замеров**» (в `main`
      это строки 148–155; после вливания pull request 126 он уезжает
      строк на двадцать ниже, поэтому искать его надо по тексту, а не
      по номеру). Запрет оставить: довод в нём верен и изменение с ним
      согласно. Убрать только то, что теперь сказано дважды, и сослаться
      на общий абзац.

  **Проверка:** `npx prettier --check supervisor/skills` зелёный; поиск
  по содержимому находит `node --eval` в девяти файлах из десяти —
  `interpret.md` пока без абзаца, им занят шаг 4.

## 4. Дописать абзац в скилл толкования

- [ ] В `supervisor/skills/interpret.md` абзаца нет вовсе. Положить его
      в раздел «Чего делать нельзя», рядом с запретом на перечисление
      файлов через оболочку, и тем же движением поправить ложный довод
      в этом запрете.

- [ ] Дописать оговорку, которой прочим скиллам не нужно: запрет
      «Запускать что-либо» касается прогонов, замеров и тестов — то есть
      добычи новых чисел, — а счёт по уже снятому `balance/journal.jsonl`
      скриптом из `.matchlog` им не запрещён. Без этой фразы сессия
      прочтёт запрет шире и не сделает ни того, ни другого.

- [ ] Раздела «## Оболочка команд», который заводит в этом файле pull
      request 126, не касаться: он стоит перед разделом «Порядок»,
      наш пункт — в «Чего делать нельзя», и сторож `shell.test.mjs`
      ищет в нём свои три приметы.

  **Проверка:** `npx prettier --check supervisor/skills/interpret.md`
  зелёный; в файле есть и запрет строки кода, и названная замена.

## 5. Завести сторожа абзаца

- [ ] В `supervisor/config/transitions.test.mjs` — проверка по каждому
      скиллу этапа с сессией, все десять (`NEEDS_SESSION` и `skillText`
      уже есть в файле): текст называет строку кода в доводе, называет два
      разделителя пути подряд и называет покрытую форму запуска скрипта
      из `.matchlog`. Отдельной строкой — что ни в одном из десяти файлов
      не осталось подстроки «не покрывает никакое правило».

- [ ] Место — конец `describe('этапы и скиллы')`, следом за последним
      стоящим там `it`. Третьего файла сторожей не заводить: `shell.test.mjs`
      появился отдельным именно из-за переписывания `transitions.test.mjs`
      изменением `hold-uncovered-stages`, оно влито, а расхождение двух
      файлов уже названо задачей 0176 бэклога. Проб покрытия команд
      вливания, которые pull request 126 переписывает под `STAGE_SHELLS`,
      наш сторож не касается вовсе.

- [ ] Мерка по существу, а не по фразе целиком. Дословная сверка запретила
      бы редактировать абзац, не переписав тест, — и однажды тест перепишут
      не глядя. Рядом уже стоят сторожа того же рода: «ни один скилл
      не показывает коммит многострочной строкой» и «ни один скилл
      не посылает исполнителя за слотом».

  **Проверка:** `pnpm test:pipeline` зелёный. И красный, если убрать абзац
  из любого одного скилла: проверить это делом и вернуть файл на место.
  Сторож, которого нельзя уронить, ничего не сторожит.

## 6. Положить два пробных правила в настройку разрешений

- [ ] В `allow` файла `supervisor/config/stage-settings.json` — две строки
      **точной формы**, без хвоста `:*`: `PowerShell(node -e "1")`
      и `PowerShell('hello' -match 'l')`.

- [ ] Класть их **после блока правил `PowerShell(...)`**, отдельной группой
      рядом со своей оговоркой, а не в тот участок списка, откуда pull
      request 126 вынимает правила `Bash(...)`. Правил `Bash(...)` не заводить
      ни одного: после 126 их отсутствие сторожится (`shell.test.mjs`,
      проба «в списке разрешений нет ни одного правила Bash»), и возврат
      хотя бы одного покрасит сторожа.

- [ ] Рядом ключ `$codeStringNote` с тремя вещами: что это проба, а не
      разрешение на `node -e`; что точная форма покрывает ровно одну строку
      (`supervisor/config/permissions.mjs:34–39`) и открывает буквально
      печать единицы; что означает каждый из двух исходов живого захода —
      прошли обе строки или отказались обе.

- [ ] Оговорка обязательна и сокращению не подлежит. Строка
      `PowerShell(node -e "1")` в `allow`, прочтённая мельком, выглядит
      разрешением на `node -e` вообще, то есть ровно тем, чего изменение
      не делает.

  **Проверка:** `pnpm test:pipeline` зелёный — правила разбираются
  `patternOf` как точная форма, ни одного сторожа покрытия не ломают,
  и после вливания 126 не ломают ни одной пробы `shell.test.mjs`:
  в перечень снятых команд наши строки не входят, а запретов мы
  не добавляем вовсе.

## Абзац целиком

Ниже тот текст, который едет в десять файлов. Отступ вложенных абзацев
двумя пробелами, как у соседних пунктов раздела «Чего делать нельзя».

---

- **Склеивать команды в одном вызове.** Одна команда — один вызов: ни точки
  с запятой, ни двойного амперсанда, ни конвейера в чужую команду, ни захода
  в каталог отдельным звеном цепочки. Разбор разрешений раскладывает строку
  на действия и требует, чтобы открыто было **каждое**; у составной команды
  их несколько, и хватает одного непокрытого — а какое из них среда сочтёт
  непокрытым, заранее не угадать. Автономной сессии такой отказ не работу
  замедляет: команда не выполнится, а ты пойдёшь дальше, считая её
  выполненной. Проверено 27.08.2026 на первом же живом исполнителе. Нужен
  другой каталог — бери ключ самой команды, у `git` это `-C <путь>`. Нужны
  не все строки вывода — отбирай их у себя, а не конвейером в оболочке.

  **Строка кода в доводе режется сама по себе, без единого знака
  из перечисленных.** Ни `node -e`, ни `node --eval`, ни `node -p`,
  ни бесприставочное выражение оболочки вида `'…' -match '…'` не выполнятся:
  разбор видит в доводе строку, которую надо будет исполнить, и объявляет
  её действием внутри действия. Проверено пробой 04.09.2026: отказалась
  даже `node -e "1"` — без скобок, без вложенных кавычек, без точки.

  **Замена:** положи проверку скриптом в `.matchlog/` и запусти покрытой
  формой — `node .matchlog/<имя>.mjs`, относительным путём и через прямые
  косые. Оба конца открыты правилами: `Write(.matchlog/**)`
  и `PowerShell(node .matchlog/*)`. Каталог числится игнорируемым,
  и в коммит такой файл не попадёт, а проверка вдобавок останется
  читаемой — её видно в отчёте и можно перезапустить.

  **Второй предохранитель — сторож сетевых путей, и он не про действия.**
  Довод, в котором стоят два разделителя пути подряд, режется сообщением
  про путь UNC, и правилами разрешений это не отменяется: сторож с ними
  не сверяется вовсе. Считаются любые два подряд — `\\`, `/\` и `//`, —
  поэтому режется и запись шаблона `/\d/`, и обычный веб-адрес. Одиночный
  разделитель безвреден. Лечится переписыванием довода: шаблон — записью
  без `\d`, скажем через `[0-9]`; длинный текст с адресом внутри — уводом
  в файл и передачей команде ключом файла.
