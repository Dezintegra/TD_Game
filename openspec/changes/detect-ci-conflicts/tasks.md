## 1. Обработка конфликтов при ожидании CI

- [x] 1.1 Добавить сведения о возможности слияния в существующий опрос GitHub,
  возврат подтверждённого конфликта в `revise` и сохранение причин ожидания.
  Проверить переход через реальный исполнитель с подставными внешними данными,
  неизвестное состояние, сбой чтения, пустой и зелёный CI, повторный опрос
  после возврата; описать поведение в документации конвейера.

Проверки: узкие `io.test.mjs`, `apply-report.test.mjs`, `execute.test.mjs`,
формат и линт изменённых файлов; полный обязательный набор — CI того же PR
и последнего коммита. Архивация после зелёного CI.

Локально: 260 проверок переходника и переходов, 253 проверки правил этапов;
линт и формат изменённых файлов, строгая проверка изменения OpenSpec прошли.
