## 1. Фиксированные контроли

Перед реализацией освежить назначенное дерево командами `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main`; проверить пересечения с результатами 0381/0379, не ждать их завершения. Подготовка зависимостей: `pnpm install --frozen-lockfile --prefer-offline`. Каждая задача ниже — отдельный проверяемый коммит не более двух часов, отправляемый сразу. Все пути добавлять явным перечнем. В каждом коммите допустима и обязательна отметка выполненного пункта в `openspec/changes/diagnose-historical-assignments/tasks.md`; проверить имена и содержание `git -C <дерево> diff --cached` перед коммитом и `git -C <дерево> show --stat --oneline HEAD` вместе с `git -C <дерево> show HEAD` после него. Посторонние изменения, включая несвязанные правки tasks.md, не включать.

- [x] 1.1 Добавить historical-revise, historical-revise-design и historical-audit в tool-controls.mjs и совместимый выбор профиля diagnoseStageTools; поддержать отдельные чтения инструкций и Git status без optional locks, общий deadline и явные not-run. Сохранить прежний набор/ранний выход обычного 0244. Проверить точные команды и запрет произвольного профиля, все заказанные контроли при раннем confirmed, deadline и structured/inconclusive результаты: `pnpm test:pipeline lib/tool-controls.test.mjs lib/tool-diagnostics.test.mjs lib/stage-tool-health.test.mjs`. Пути: названные три теста, tool-controls.mjs, tool-diagnostics.mjs и собственный tasks.md. После коммита и push открыть черновой PR через `gh pr create --draft --base main --title "feat(pipeline): diagnose historical assignments" --body-file <файл-тела-в-своём-дереве>`; последующие пункты не нужны для проверяемости этого совместимого шага.

## 2. Долговечность и единый учёт

Пункты 2.1–2.2 исполняются с контрактом решения 4.1 design.md: основа — поставленные 0233/0244. Открытый план keep-the-report-on-disk из ветки 0080 не разрешает заменять store, запускать report-queue или менять supervisor.reports на объект `.all`. Перед правкой перечитать актуальные артефакты 0080 через `git -C <дерево> show origin/worktree-0080-otchet-zavershivshegosya-etapa-obyazan-p:openspec/changes/keep-the-report-on-disk/tasks.md` и сопоставить с действующим подключением; совместимый остаток 0080 и границы переданы ей через отчёт проработки. Чужие пункты не выполнять и не закрывать, завершения всей 0080 не ждать.

- [x] 2.1 Расширить report-store.mjs отдельной diagnosticRequests-коллекцией и методами accepted/launch intent/raw result/completion/get, не меняя reports и их доставку. Проверить чтение старой версии, запрет downgrade-затирания, fingerprint conflict, readback, ошибку сохранения до/после spawn, повтор get и сохранение uncertain после рестарта. Команда: `pnpm test:pipeline lib/report-store.test.mjs`. Пути: supervisor/lib/report-store.mjs, supervisor/lib/report-store.test.mjs и собственный tasks.md.
- [ ] 2.2 Выделить общий адаптер допуска и учёта диагностики из supervisor.mjs в supervisor/lib/tool-diagnostic-accounting.mjs; сохранить текущую семантику 0244, добавить возможность сохранения raw run до расчёта usage и идемпотентную сверку launchId. Проверить budget refusal без spawn, Codex tokens, Claude cost, повтор результата без двойной записи и ошибку учёта с сохранённым run. Команда: `pnpm test:pipeline lib/tool-diagnostic-accounting.test.mjs lib/stage-tool-recovery.test.mjs lib/supervisor.test.mjs`. Пути: supervisor/lib/tool-diagnostic-accounting.mjs, одноимённый тест, supervisor/lib/supervisor.mjs, supervisor/lib/supervisor.test.mjs, supervisor/lib/stage-tool-recovery.test.mjs и собственный tasks.md. Этот шаг подключает общий адаптер к прежнему пути, а не оставляет его непроверенным до появления нового входа.

Дополнительные критерии самостоятельной проверки 2.1: fixtures версий 1 и 2 сохраняют reportId/launchId, plan/progress/rejection и все состояния hold; поочерёдная запись reports/diagnosticRequests и повторное открытие файла сохраняют обе коллекции; acknowledge одного отчёта сохраняет соседние записи обеих коллекций. verifySaved проверяет весь store, сохраняя смысл отчётного count. Критерий 2.2: существующий toolHold по-прежнему получает тот же store, общий адаптер и прежнюю семантику учёта/settlement; никаких новых очередей или писателей. Проверки и допустимые пути остаются перечисленными в 2.1–2.2.

## 3. Защищённый адресный вход

- [ ] 3.1 Реализовать addressed-tool-diagnostics.mjs как библиотечный обработчик с переданными доверенными runtime-проверками и настоящим report-store. Проверить назначения без envelope в blocked/failed, с существующим envelope, неизвестную принадлежность, чужого владельца, смену ветки/контекста между авторизацией и spawn, ручную паузу, scoped-разрешение хозяина, существующие launch restrictions и неизменность source task/report/attempts/merge. Проверить взаимное исключение с обычной выдачей этапа и другим диагностическим запросом, дедупликацию submit/get и uncertain launch. Команда: `pnpm test:pipeline lib/addressed-tool-diagnostics.test.mjs lib/tool-diagnostics.test.mjs lib/report-store.test.mjs`. Пути: supervisor/lib/addressed-tool-diagnostics.mjs, одноимённый тест и собственный tasks.md. До production-подключения обработчик безопасно проверяется через внедряемые адаптеры, без живых исходных назначений.
- [ ] 3.2 Добавить host CLI diagnose-assignment.mjs и локальный транспорт supervisor/lib/tool-diagnostic-endpoint.mjs с проверкой владельца/поколения/полномочий, ограниченным размером JSON и таймаутом клиента; подключить обработчик к реально владеющему runtime в supervise.mjs/supervisor.mjs. Проверки обязаны использовать production wiring и настоящий локальный endpoint на Windows в собственной фикстуре: неизвестная авторизация, подмена поколения, чужой клиент, недоступный owner, busy при гонке scheduler, потеря ответа и повтор get без нового spawn. Команда: `pnpm test:pipeline lib/tool-diagnostic-endpoint.test.mjs lib/addressed-tool-diagnostics.test.mjs lib/stage-tool-recovery.test.mjs lib/supervisor.test.mjs`. Пути: supervisor/bin/diagnose-assignment.mjs, supervisor/bin/supervise.mjs, supervisor/lib/tool-diagnostic-endpoint.mjs, одноимённый тест, supervisor/lib/addressed-tool-diagnostics.mjs, одноимённый тест, supervisor/lib/supervisor.mjs, supervisor/lib/supervisor.test.mjs и собственный tasks.md. stage-settings.json, исходные карточки и чужие деревья не изменять. Проверка OS-доступа должна завершаться отказом, если из выбранного транспорта невозможно подтвердить ограничения; не считать possession одного публичного PID полномочием.

В проверку 3.2 включить фикстуру совместного рестарта из решения 4.1: ordinary report с частично подтверждённым plan, infrastructure-held и addressed request в одном файле; использовать production-фабрику создания runtime после locks и передачу одного store в supervisor/io/endpoint. После пересоздания ordinary delivery повторяет только недостающие эффекты, get не вызывает settlement/spawn/charge, acknowledge отчёта сохраняет hold и запрос. Отдельно проверить отказ записи/verifySaved и сохранение блокировки рестарта. Проверки разместить в уже разрешённых lib/supervisor.test.mjs и lib/tool-diagnostic-endpoint.test.mjs и запускать командой 3.2; тест только отдельного обработчика эту приёмку не заменяет.

## 4. Подготовка хозяина и проверка адресатов

Команды нового CLI ниже исполняет только подтверждённый Windows-хозяин, не обычная сессия implement. Исполнитель готовит документацию и проверяет полученные свидетельства; назначение 0383 не даёт ему права писать в .pipeline, обновлять main, останавливать supervisor либо расширять собственные permissions. Без результата хозяина следующие пункты не отмечаются. Нельзя подменить их файлом-поручением, синтетическими результатами или чужим cwd; необходимая подготовка остаётся частью результата этой карточки. Возникшее препятствие оформляется по действующему контракту этапа с сохранением открытых чекбоксов.

- [ ] 4.1 Подготовить docs/historical-tool-diagnostics.md с точной схемой submit/get, безопасным примером запроса и порядком установки согласованного SHA хозяином, авторизации четырёх назначений, проверки отсутствия конкуренции, паузы и доступности результатов потребителю. Получить и проверить свидетельство фактической загрузки хозяином входа (SHA, версия provider, runtime identity, endpoint generation, полномочия и проверенные назначения). В acceptance.md сохранить конкретные команды вместо placeholders и результат пробного обращения к маршруту; отсутствие готового runtime не считать успешной подготовкой. Проверки: `pnpm test:pipeline lib/tool-diagnostic-endpoint.test.mjs` и наблюдаемое подтверждение хозяина, связанное с SHA поставки. Пути коммита: docs/historical-tool-diagnostics.md, openspec/changes/diagnose-historical-assignments/acceptance.md и собственный tasks.md; служебные настройки/метаданные хозяина не коммитить.
- [ ] 4.2 Получить через хозяина отдельные submit/get для 0156 и 0208 с профилем historical-revise. Проверить task/stage, source provenance, cwd/branch, текущий и исторический контекст, наличие/отсутствие envelope, каждый контроль, launchId/sessionId либо доказанное отсутствие, UTC, структурированный результат, учтённый расход, очищенные первичные файлы и SHA-256. Повторить get после перезапуска клиента и доказать те же идентичности без нового запуска/расхода. Подтверждённый helper refusal допустим; недоступность входа — нет. Проверка — два адресных результата и совпадающие хеши повторного чтения. Коммит: acceptance.md, явно перечисленные собственные очищенные файлы openspec/changes/diagnose-historical-assignments/evidence/0156.json и evidence/0208.json, собственный tasks.md. При внешнем хранении первичных записей в этих файлах сохранить проверенные пути, хеши и ограничения доступа.
- [ ] 4.3 Получить тем же маршрутом отдельные submit/get для 0175 (historical-revise-design) и 0074 (historical-audit), с полным набором полей и повторным получением как в 4.2; для 0074 отразить оба исторических audit либо явно недоступные сведения. Сформировать manifest.json со всеми четырьмя ответами и SHA-256, проверить доступность файлов для 0381, записать путь/хеш манифеста и проверенный способ get в acceptance.md и отчёт реализации. Проверка — четыре полных адресных записи, два новых доказанных повторных чтения, отсутствие изменений исходных состояний и непротиворечивый учёт каждого launch. Коммит: acceptance.md, evidence/0175.json, evidence/0074.json, manifest.json внутри собственного изменения и собственный tasks.md. Сохранность исторического слияния, healthy после ремонта и incidentVerification этим не объявлять.

## 5. Проверка комплекта

При подготовке хозяина в 4.1 записать в acceptance.md фактическую связку runtime → pending-reports.json → supervisor/io/endpoint и подтверждение, что нет подключения report-queue из старого плана 0080. При итоговой проверке 5.1 повторно сопоставить свежую базу и пересечения 0080 с решением 4.1; сохранить результат совместной проверки в acceptance.md. Эта проверка совместимости не требует закрытия 0080 и не объявляет её собственную приёмку выполненной.

- [ ] 5.1 Сопоставить acceptance.md и manifest.json со всеми сценариями дельты; закрепить интеграционные регрессии для no-envelope, blocked/failed, busy, same request/different payload, restart/uncertain, storage/accounting failure и сохранения старого hold. Проверка: `pnpm test:pipeline lib/addressed-tool-diagnostics.test.mjs lib/tool-diagnostic-endpoint.test.mjs lib/tool-diagnostic-accounting.test.mjs lib/report-store.test.mjs lib/tool-diagnostics.test.mjs lib/stage-tool-recovery.test.mjs`, затем `openspec validate diagnose-historical-assignments --strict`. Допустимые пути: перечисленные тесты, acceptance.md и собственный tasks.md. Коммит отправить сразу; ожидания зелёного CI, архивации, вливания 0381 и действий над исходными конфликтами в список работ не добавлять.
