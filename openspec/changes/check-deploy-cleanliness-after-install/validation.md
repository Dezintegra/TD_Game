# Проверка установки

Пункт 3.1 выполнен 06.09.2026 после доставки починки 0232. Свежая установка,
непустой кеш, проектный источник исключения и обе отрицательные проверки
подтверждены. История неудачных попыток сохранена ниже.

## Успешная свежая проба 06.09.2026 после #167 и #169

База обновлена через `git -C . fetch origin` и `git -C . merge origin/main`;
коммит слияния `a62543df5709255a36890b4e9b53d7a81a761f27` сразу отправлен
в существующий черновой PR #163. Из этой отправленной ревизии создана новая
копия `.matchlog/install-check-8nccSo/snapshot/`; прежние копии сохранены.
Версии: Node `v24.3.0`, pnpm `10.12.4`, Git `2.45.1.windows.1`, Windows.

Воспроизводимая команда из чистого назначенного дерева, совпадающего с upstream:

```powershell
node supervisor/bin/check-install-snapshot.mjs --run
```

Код возврата 0, `ok: true`, `stage: complete`. Штатный сценарий задачи 0232
выгружает `git archive` текущего коммита, создаёт изолированный Git-репозиторий,
коммитит явный список отслеживаемых путей и задаёт локальный `core.excludesFile`
на отсутствующий файл своей фикстуры. Git вызывается с `-C`, отключённым
fsmonitor и точечным `safe.directory`; глобальные настройки и ACL не меняются.
Cwd инструмента всё время остаётся в назначенном дереве.

Установка выполняется через `spawnSync(process.execPath, [pnpmCli, 'install',
'--frozen-lockfile', '--store-dir', '.pnpm-store'], { cwd: snapshot, ... })`
без оболочки, с `windowsHide: true`. Именно дочерний Node-процесс получает
cwd новой копии. Путь pnpm определяется штатным сценарием из установленного CLI.

| Наблюдение | Фактический результат |
| --- | --- |
| Исходный статус | Код 0, пустой stdout |
| Исходные кеш и node_modules | Отсутствуют, `cacheAbsent: true` |
| Настоящая установка | Код 0, 346 пакетов скачано и добавлено, 32.2 с, stderr пуст |
| Непустой кеш | Подтверждены индекс полного пакета, размеры и SHA-512 связанных файлов |
| Статус после установки | Код 0, пустой stdout |
| `check-ignore -v` каталога и файла кеша | Код 0, оба исключены правилом `.gitignore:2:.pnpm-store/` |
| Изменение исходника | Код 0, ` M apps/arena/src/arena.match.test.ts` |
| После восстановления исходника | Код 0, пустой stdout |
| Посторонний файл | Код 0, `?? install-check-untracked.txt` |
| После удаления собственного контрольного файла | Код 0, пустой stdout |

Статусы получены командой `git -C <снимок> status --porcelain --untracked-files=all`;
вывод не фильтруется. Сценарий отклоняет ненулевой код и `error:`/`fatal:` в stderr.
Диагностика с полным выводом установки и путём файла кеша сохранена локально
в `.matchlog/install-check-8nccSo/result.json`.

`pnpm install --frozen-lockfile --prefer-offline` в назначенном дереве также
завершился кодом 0. Отказов согласования команд не было. Git в назначенном
дереве предупреждал о недоступном пользовательском ignore, сохраняя код 0;
в тестовой копии личные исключения отключены локальной настройкой.
Сборка, замеры, арена и выкладка не запускались.

Узкая повторная проверка на обновлённой базе:
`npx vitest run --root supervisor config/transitions.test.mjs lib/deploy-snapshot.test.mjs`
— код 0, все 92 теста прошли (84 и 8), включая отрицательные примеры порядка,
обнаружение настоящих изменений и контроль без исключения кеша.

## История проверки 05.09.2026

Пункты 1.1 и 2.1 выполнены. Проба пункта 3.1 **не состоялась** из-за ошибки среды; пункт остаётся открытым.

## Автоматические проверки

- `pnpm install --frozen-lockfile --prefer-offline` в назначенном дереве: код 0.
- ESLint и Prettier изменённых файлов: код 0.
- `pnpm test:pipeline` после пункта 2.1: код 1, 1238 тестов прошли, один упал. Все 84 теста `config/transitions.test.mjs` и восемь тестов `lib/deploy-snapshot.test.mjs` прошли, включая отрицательные примеры порядка и контроль без исключения кеша.
- Единственный сбой: `lib/stage-model.test.mjs:75`, «проверка готовности выкладки Codex использует Terra». На Windows заглушка возвращает код 1 на предварительной подготовке sandbox; тест читает аргумент команды подготовки как модель. Этот тест и `lib/codex-readiness.mjs` совпадают с `origin/main`; они не менялись в задаче. Требуется отдельная правка конвейера.
- Однократный просмотр CI первого коммита `1d5d437` в PR #163: быстрые тесты, формат и линт прошли; игровые проверки пропущены по составу diff. Проверки последующих коммитов здесь не ожидаются.

## Проба настоящей установки

Версии: pnpm `10.12.4`, Git `2.45.1.windows.1` (команды версий завершились кодом 0).
Исходная ревизия: `dc526c19b687abc9ed4891c9bf454ca6cf6feebf`.

`node .matchlog/0226-install-check.mjs` завершился кодом 0. Скрипт выгрузил отслеживаемое содержимое ревизии через `git archive` в свежий изолированный репозиторий `.matchlog/0226-install-check/`, сохранил базу коммитом и настроил локальный `core.excludesFile` на отсутствующий файл `absent-personal-ignore` внутри копии. Исходный `git -C <снимок> status --porcelain` завершился кодом 0 с пустым stdout.

Из рабочего каталога снимка, заданного параметром инструмента, выполнено:

```powershell
pnpm install --frozen-lockfile --store-dir .pnpm-store
```

Обе попытки завершились кодом 1: `EPERM: operation not permitted, mkdir` для `.matchlog/0226-install-check/.pnpm-store`. Успешная установка и непустой кеш не подтверждены. Права, защитные настройки и расположение кеша не менялись; ошибка не обходилась.

Последующий `git -C .matchlog/0226-install-check status --porcelain` вернул код 0 и пустой stdout, но stderr содержал `error: daemon terminated`. Этот вывод не считается доказательством успешной подготовки: установка уже завершилась ошибкой. Проверка происхождения исключения после успешной установки не выполнялась.

Нужен повтор пункта 3.1 после устранения средовой ошибки. Сборка, замер, арена и выкладка не запускались. Коммит `9fa4652` повторно не доставлялся, чужие деревья не изменялись. Отказов автоматического согласования команд не было; зафиксированы ошибка доступа pnpm и предупреждения Git о недоступном личном ignore.

## Повтор 06.09.2026

После `git -C . fetch origin` и успешного слияния `origin/main` коммит
слияния `c3a1d64aa1dfe384924a50eda03cd1e0ae6218e6` немедленно отправлен.
`pnpm install --frozen-lockfile --prefer-offline` в назначенном дереве
завершился кодом 0 (pnpm `10.12.4`). PR #163 открыт и остаётся черновиком.

`node .matchlog/0226-install-check.mjs` создал новую изолированную копию
в `.matchlog/0226-install-check/retry-20260906/` из ревизии
`c3a1d64aa1dfe384924a50eda03cd1e0ae6218e6`. Предыдущая копия сохранена.
Скрипт завершился кодом 0: исходный статус пуст, Git `2.45.1.windows.1`,
локальный `core.excludesFile` указывает на отсутствующий файл внутри новой копии.

`pnpm install --frozen-lockfile --store-dir .pnpm-store`, запущенный с cwd
новой копии через параметр инструмента, вновь завершился кодом 1:
`EPERM: operation not permitted, mkdir` для её `.pnpm-store`.
Последующий `git -C .matchlog/0226-install-check/retry-20260906 status --porcelain`
вернул код 0 и пустой stdout, но stderr содержал `error: daemon terminated`.
Это не подтверждает успешную установку:
непустой кеш и чистота после успешной установки по-прежнему не доказаны.

Пункт 3.1 остаётся открытым. Ошибка создания кеша уже является предметом
связанной задачи `0232-obespechit-sozdanie-lokalnogo-kesha-pnpm`; новая заявка
не создаётся. Права и настройки защиты не менялись, обход ошибки не выполнялся.
Отказов согласования команд не было. Сборка, замеры, арена и выкладка не запускались.
