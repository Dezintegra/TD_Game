## 1. Повторная проверка и охрана порядка

- [ ] 1.1 Обновить шаг 6 `supervisor/skills/deploy.md` и его сторож в `supervisor/config/transitions.test.mjs` одним атомарным коммитом (до 90 минут): сохранить исходную проверку и запрет починки снимка, затем явно предписать установку, повторный статус и остановку до замера при ошибке установки/статуса или непустом выводе. Добавить отрицательные строковые примеры удаления второй проверки, перестановки до установки/после начала замера и удаления условия остановки. Проверка: `npx vitest run --root supervisor config/transitions.test.mjs`; действующий текст проходит, все перечисленные порчи распознаются с диагностикой файла и нарушения. Формат: `npx prettier --check supervisor/skills/deploy.md supervisor/config/transitions.test.mjs`; линт: `npx eslint supervisor/config/transitions.test.mjs`. Отметить этот пункт, закоммитить только названные файлы и свой tasks.md, сразу отправить через `git -C <дерево> push -u origin HEAD` и открыть черновой PR командой `gh pr create --draft --base main --title "fix: check deploy cleanliness after install" --body-file .matchlog/0226-pr-body.md`. После этого коммита поведение уже целостно и доступно CI.

## 2. Граница между кешем и изменениями

- [ ] 2.1 Дополнить `supervisor/lib/deploy-snapshot.test.mjs` реальными Git-фикстурами по design (до 90 минут): брать текущий проектный `.gitignore`, отключить личные исключения локальным `core.excludesFile` на отсутствующий файл фикстуры, создать вложенный кеш, проверить пустой статус, источник исключения и повторное принятие прежнего снимка; независимо проверить изменённый отслеживаемый `source` и посторонний `unexpected-source.txt` рядом с кешем, отказ повторной подготовки и сохранность содержимого. Не менять рабочую `.gitignore`, код подготовки и чужое дерево `codex/ignore-pnpm-store`. Проверка: `npx vitest run --root supervisor lib/deploy-snapshot.test.mjs`; контрольный вариант фикстуры с удалённым исключением кеша обязан показывать кеш в статусе и получать отказ подготовки. Формат: `npx prettier --check supervisor/lib/deploy-snapshot.test.mjs`; линт: `npx eslint supervisor/lib/deploy-snapshot.test.mjs`. Отметить пункт, сохранить тест и свой tasks.md отдельным коммитом, сразу `git -C <дерево> push`.

## 3. Настоящая установка в свежем проверочном снимке

- [ ] 3.1 Провести пробу установки и записать результат в `openspec/changes/check-deploy-cleanliness-after-install/validation.md` (до 60 минут). Вспомогательный скрипт `.matchlog/0226-install-check.mjs`, запускаемый как `node .matchlog/0226-install-check.mjs`, создаёт свежий изолированный тестовый Git-репозиторий из отслеживаемого содержимого текущего коммита внутри `.matchlog/0226-install-check/`; берёт проектный `.gitignore`, коммитит базу и настраивает только локальный `core.excludesFile` на отсутствующий файл внутри своего каталога. Зафиксировать исходную ревизию проекта и пустой `git -C <снимок> status --porcelain`. Выполнить `pnpm install --frozen-lockfile --store-dir .pnpm-store` с cwd этого снимка, заданным параметром инструмента; проверить код 0, созданный непустой кеш, пустой повторный `git -C <снимок> status --porcelain` и проектный источник исключения через `git -C <снимок> check-ignore -v .pnpm-store/`. Проверка считается выполненной только при этих наблюдаемых результатах; сетевую или средовую ошибку записать как несостоявшуюся пробу. Сборку, замер, арену и выкладку не запускать. В validation.md сохранить версии pnpm/Git, исходную ревизию, команды, коды возврата и результаты без локальных секретов; отметить пункт и отправить validation.md со своим tasks.md отдельным коммитом через `git -C <дерево> push`.

Каждая команда выполняется отдельным вызовом. `<дерево>` — назначенное рабочее дерево задачи; `<снимок>` — только собственная проверочная копия внутри него. Скрипты и тело PR остаются в игнорируемом `.matchlog/`, в коммит не включаются. Командные формы сверены с `supervisor/config/stage-settings.json`: `PowerShell(git -C:*)`, `PowerShell(npx vitest:*)`, `PowerShell(npx prettier:*)`, `PowerShell(npx eslint:*)`, `PowerShell(pnpm install:*)`, `PowerShell(node .matchlog/*)`, `PowerShell(gh pr:*)`. Проработка эти проверки реализации не запускает.

Коммит `9fa4652124c358c7b670ce2d27aef84d0ee4e93c` повторно не доставлять: эквивалентный `08257f439f55cc198f6ec0886ae5058a2c61897d` (#158) уже предок базы. Архивация изменения и ожидание CI не являются пунктами исполнителя этого списка.
