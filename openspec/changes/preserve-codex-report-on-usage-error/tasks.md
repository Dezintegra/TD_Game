## 1. Сохранение ответа и безопасная диагностика

Предусловие реализации: 0236 `reconcile-codex-token-usage` влита в origin/main первой. Перед пунктом 1.1 выполнить отдельными вызовами `git -C <дерево> fetch origin` и `git -C <дерево> merge origin/main` по правилам этапа; проверить наличие в базе её влитой реализации общего reducer, v2 и долговечного status. Наблюдаемый результат — подтверждённое вливание 0236 и отсутствие старого stage usageBaseline в рабочих вызовах. При отсутствии зависимости не начинать код, не создавать временный старый путь и не ждать в сессии; вернуть невыполненное предусловие по правилам этапа. Порядок вливания и архивации: 0236 → 0235.

- [x] 1.1 Одним атомарным коммитом (до 2 часов) изменить `supervisor/lib/provider.mjs` и `supervisor/lib/supervisor.mjs`: отделить ошибку учёта от ошибки протокола, сохранить result после успешного завершения до отказа по учёту, вывести неизвестность расхода и неприменение в журнал/консоль, сохранить читаемый итоговый текст в журнале этапа и предупреждающую метку. Не менять арифметику, реестр, лимиты и защиту reports.push. Согласовать только затронутые абзацы требований «Ответ сессии берётся из кода возврата и вывода» в `supervise-the-pipeline` и «Шапка лога этапа различает ответ сессии и исход отчёта» в `name-both-outcomes-in-the-log`, сохранив все сценарии. В `provider.test.mjs` и `supervisor.test.mjs` добавить минимальную матрицу: уменьшение/отсутствие обязательного usage × корректный done/некорректный JSON; проверять неизменность текста, failed, причину учёта, причину разбора для некорректного JSON, предупреждающую метку, запись до раннего возврата и отсутствие отчёта в очереди. Проверка: `npx vitest run --root supervisor lib/provider.test.mjs lib/supervisor.test.mjs`; узкие формат/линт: `npx prettier --check supervisor/lib/provider.mjs supervisor/lib/supervisor.mjs supervisor/lib/provider.test.mjs supervisor/lib/supervisor.test.mjs` и `npx eslint supervisor/lib/provider.mjs supervisor/lib/supervisor.mjs supervisor/lib/provider.test.mjs supervisor/lib/supervisor.test.mjs`. После коммита отправить `git -C <дерево> push` и открыть черновой PR стандартным действием этапа имплементации; наблюдаемый результат — номер draft PR. Этот шаг уже работоспособен сам по себе.

  использовать результат общего reducer и долговечный status 0236 как единственный источник unknown и причин. Не добавлять собственное сравнение с baseline этапа. Минимальная матрица уменьшения относится к input/output при включённом бюджете; добавить контроль снижения только cached при неизменных input/output и полной истории: расход известен, нет failed по учёту, корректный done проходит обычный допуск. Перед журналированием учитывать status после идемпотентной записи наблюдений в finish, включая историческую неполноту и отказ записи бюджета. Уже в первом коммите история unknown → валидный больший снимок → корректный done должна сохранять result при failed, историческую причину и предупреждающую метку без reports.push. Это часть того же атомарного коммита, не отложенная до пункта 2.1 защита.

## 2. Регрессионные границы

- [x] 2.1 Отдельным коммитом (до 2 часов) расширить тесты адаптера и finish: непригодные поля обязательного usage; числовой usage при неизвестном расходе; чужой этап вместе с ошибкой учёта; отсутствие текста; выключенный лимит без usage; несколько agent_message с выбором последнего; новый turn.started после завершённого хода; отсутствие turn.completed, turn.failed, error, ненулевой код, killedBy и ошибка запуска. Подтвердить сохранение прежнего успешного пути, stdout/stderr/отказов, отсутствие передачи ошибочного отчёта через следующий цикл супервизора, неизменные порог токенов, resume-учёт и долларовый лимит Claude существующими узкими тестами. Проверка: `npx vitest run --root supervisor lib/provider.test.mjs lib/supervisor.test.mjs lib/token-budget.test.mjs`; формат и линт только изменённых тестовых файлов предыдущими покрытыми формами. Наблюдаемый результат — ошибочный JSON done нигде не превращает failed в принятый этап, а корректный ответ без ошибки проходит прежний путь. Сверить дельту командой `openspec validate preserve-codex-report-on-usage-error --strict` и отправить коммит `git -C <дерево> push`.

  под «неизменным resume-учётом» понимается влитый контракт 0236, а не нынешняя арифметика baseline этапа. Расширить совместные проверки в `provider.test.mjs`, `supervisor.test.mjs`, `token-budget.test.mjs` и `scan.test.mjs`: уменьшение отдельно input и output × корректный done/некорректный JSON; исторический unknown → валидный последний снимок для обоих видов JSON; снижение только cached без unknown; повтор watch/finish без списания; известная сумма 1740 и resume до 2180 с расходом запуска 440; сохранение неполноты после забывания этапа. Для каждого бюджетного отказа проверять читаемый текст в журнале до возврата, обе причины для некорректного JSON, предупреждение и отсутствие применения в текущем и следующем циклах. При выключенном лимите unknown показывается, но сам по себе не блокирует корректный отчёт. Проверка совместной матрицы: `npx vitest run --root supervisor lib/provider.test.mjs lib/supervisor.test.mjs lib/token-budget.test.mjs lib/scan.test.mjs`; должны быть обнаружены и выполнены тесты всех четырёх файлов. Общие проверки арифметики переиспользовать из 0236, не реализовывать её заново. Пункт остаётся отдельным коммитом до 2 часов.

Команды выполняются отдельными вызовами PowerShell из назначенного дерева. Формы `npx vitest:*`, `npx eslint:*`, `npx prettier:*`, `openspec:*`, `git -C:*` и `gh pr:*` покрыты `supervisor/config/stage-settings.json`. В каждый коммит входит отметка своего пункта tasks.md. Полные проверки выполняет CI; ожидание его результата не является пунктом реализации. Игровой баланс не затронут, заявка на арену не требуется.
