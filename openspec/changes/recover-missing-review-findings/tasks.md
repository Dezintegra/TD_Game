## 1. Порядок восстановления и согласованная подсказка

- [x] 1.1 Одним атомарным коммитом реализации (до 2 часов) дополнить supervisor/skills/revise.md порядком восстановления до исправлений, согласовать маркер и отдельное пояснение в supervisor/lib/stage-prompt.mjs, расширить supervisor/lib/stage-prompt.test.mjs и добавить supervisor/lib/revise-log-recovery.test.mjs. Не перенумеровывать шаги revise, не менять алгоритм обрезки, формат логов и поля назначения. В том же коммите добавить verification.md в каталог этого изменения с входами и результатами матрицы из design.md: восстановленный перечень из подходящего лога; отсутствие/недоступность файла; другая задача/этап; прежний заход той же задачи; недоказанная актуальность и повторяющийся вердикт; неполный итог; отсутствующий ответ владельца; отдельный путь красного CI. Проверка: `npx vitest run --root supervisor lib/stage-prompt.test.mjs lib/revise-log-recovery.test.mjs`; `npx prettier --check supervisor/skills/revise.md supervisor/lib/stage-prompt.mjs supervisor/lib/stage-prompt.test.mjs supervisor/lib/revise-log-recovery.test.mjs`; `npx eslint supervisor/lib/stage-prompt.mjs supervisor/lib/stage-prompt.test.mjs supervisor/lib/revise-log-recovery.test.mjs`; ручная сверка verification.md с дельтой показывает источник и основание допуска для положительных случаев и явный failed до исправлений для отрицательных. Проверить форму артефактов: `openspec validate recover-missing-review-findings --strict`. Отметить этот пункт, отправить коммит через `git -C <дерево> push -u origin HEAD` и сразу открыть черновой PR по правилам этапа implement; наблюдаемый результат — PR на назначенной ветке и отсутствие неотправленных коммитов по `git -C <дерево> log --oneline '@{u}..HEAD'`. Ожидание CI в задачу не входит, его ведёт супервизор.
