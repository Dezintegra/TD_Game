## Метод и входы

Ручной разбор синтетических входов по порядку revise.md выполнен 06.09.2026.
Новая сессия и боевой супервизор не запускались, служебные логи не менялись.
Контрактные тесты читают реальную инструкцию; приведённые решения — результат
применения её условий к входам, а не доказательство следования модели.

Общие входы: назначение taskId `0042-demo`, stage `revise`; задача id
`0042-demo`, status `revise`, links `{ "pr": 137, "change": "demo" }`.
Первая запись worktree list — `C:/src/TD_Game`, своё дерево —
`C:/src/TD_Game/.claude/worktrees/0042-demo`. Проверяемый источник во всех
строках ниже: `C:/src/TD_Game/.pipeline/logs/0042-demo-review.log` (далее L).
Других источников, в том числе Trello, нет. Неуказанные поля наследуются
из этих входов и базового лога; каждая строка меняет только названное.

Базовый полный лог L:

```text
задача: 0042-demo
этап: review
сессия: S2
начат: 2026-09-06T10:00:00.000Z
ответ сессии: done
--- итоговый текст ---
{"taskId":"0042-demo","stage":"review","outcome":"rejected","summary":"P1: обработчик demo теряет последний элемент при лимите 17.","decisions":["Сохранить последний элемент при лимите 17.","Добавить регрессионный тест для пустого списка."],"links":{"pr":137,"change":"demo"},"requests":[]}
--- stdout ---
Черновик: переписать все обработчики.
--- stderr ---
```

Базовая последняя запись журнала: `review → revise: P1: обработчик demo
теряет последний элемент при лимите 17.` В синтетической истории прежний
вердикт S1 касался только заголовка окна, этого фрагмента не содержал.
Отметки текущего контекста: начало ревью 10:00 UTC, возврат 10:05 UTC
06.09.2026; последующих переходов нет. Необходимых продуктовых решений нет.

## Матрица решений

| Случай и отличия входа | Проверка причины и шапки | Проверка текущего захода и полноты | Полученное решение |
| --- | --- | --- | --- |
| Неполный базовый журнал, полный L | review → revise; задача, review, S2, начало и ссылки совпали; итог rejected независимо от ответа сессии done | Отличимый фрагмент последнего вердикта совпал, прежняя S1 его не содержала; время не противоречит возврату; итог полный | Допустить L. Восстановлены два пункта: сохранить последний элемент при лимите 17 (закрыть проверкой граничного списка); добавить регрессионный тест пустого списка (закрыть успешным тестом). Черновое «переписать все обработчики» не включено. В отчёте указать L, S2, начало 10:00 UTC и совпавший отличимый фрагмент. |
| Журнал пуст; reason назначения: «Текущий возврат из review сессии S2, начало 2026-09-06T10:00:00.000Z» | Причина прямо задана назначением; шапка и итог базового L согласованы | Прямая ссылка на S2 и начало совпала; полный итог не требует отсутствующего ответа владельца | Допустить L; тот же полный перечень из двух пунктов и критерии закрытия. Основание отчёта: L, S2, 10:00 UTC, прямая ссылка назначения. |
| L отсутствует | Причина известна; файл не найден | Шапку и итог проверить нельзя | failed до исправлений: «Не удалось восстановить замечания: L отсутствует». |
| Чтение L возвращает EACCES | Причина известна; чтение недоступно | Доказательств нет | failed до исправлений: «Не удалось восстановить замечания: L, ошибка чтения EACCES». |
| В шапке и JSON L taskId `0043-other` | Задача не совпала с назначением | Дальнейший допуск прекращён | failed до исправлений: «L относится к 0043-other, назначена 0042-demo». |
| Шапка и JSON L называют stage `audit` | Нужен review, найден audit | Дальнейший допуск прекращён | failed до исправлений: «L содержит audit вместо review». |
| Назначение прямо называет S2/10:00, L содержит S1/09:00 при тех же taskId и PR | Задача и PR совпали, формат шапки корректен | Сессия и начало противоречат текущему возврату | failed до исправлений: «L относится к S1/09:00, текущий возврат указывает S2/10:00». |
| Журнал пуст, reason «нужна сессия», L базовый | Статус revise не устанавливает причину возврата | Ни последней записи, ни явной привязки нет; свежая дата не помогает | failed до исправлений: «Актуальность L не доказана: текущий возврат не связан с конкретным review». |
| Последний фрагмент «исправить тесты», та же фраза в S1 и S2; прямой ссылки на сессию/начало нет | review → revise известен; поля L совпадают | Повторяющийся вердикт не различает заходы | failed до исправлений: «Актуальность L не доказана: вердикт повторялся в S1 и S2». |
| Прямая ссылка S2/10:00, но текущий возврат датирован 09:55 | Шапка и ссылка совпали | Начало позже возврата, временное противоречие | failed до исправлений: «L начат после текущего возврата». |
| Шапка L: пустая сессия либо начат `неизвестно` | Нет непустой сессии либо корректного времени начала | Проверка шапки не пройдена | failed до исправлений: «L не содержит проверяемой сессии/времени начала». |
| Итог L обрывается после summary; полный список есть только в stdout | Шапка совпала | Нет полного итогового JSON; stdout не запасной отчёт | failed до исправлений: «Итоговый отчёт L оборван; замечания из stdout недопустимы». |
| Итог L имеет outcome `done` либо links.pr 138 | Шапка совпала | Требуется rejected и PR 137, найдено противоречие | failed до исправлений: «Исход/ссылка итогового отчёта L не соответствует возврату». |
| Итог L: summary с базовым P1, decisions «см. замечания выше» | Текущий источник подтверждён базовым фрагментом | Весь перечень не восстановлен | failed до исправлений: «L ссылается на отсутствующие замечания». Частичное исправление P1 не начато. |
| Итог L дополнительно требует «применить выбранный владельцем вариант»; ответа в журнале нет | Источник текущего ревью подтверждён | Не хватает необходимого ответа владельца; пересказ в L не равен ответу | failed до исправлений: «L не восполняет отсутствующий необходимый ответ владельца». |
| Последняя запись `pr → revise: проверки красные: lint`, текущий PR 137; L — старое rejected | Установлена причина CI | Старое review не является текущим возвратом; допуск L не нужен | Штатный разбор проверок PR 137 по шагам 6–7. Замечания из L не добавлены; исход CI заранее не объявляется. |

Во всех отрицательных случаях решение включает полный путь L, указанный выше,
и конкретную причину; исправления по недостоверным замечаниям не разрешены.
Положительные случаи дополняют журнал и сохраняют спецификацию меркой правильности.
История логов, поля назначения и алгоритм обрезки не изменены.
