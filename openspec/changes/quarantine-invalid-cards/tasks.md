# Задачи

Каждый пункт — отдельный коммит, отправляемый сразу.

## 1. Сведения о негодной карточке

- [x] 1.1 Дополнить запись негодной карточки состоянием и метками
      (`{ id, problems, status, flags }`) при чтении бэклога — в супервизоре
      и в смотрящем прогоне. Проверяемо: обе сборки дают одинаковую форму
      записи, и она содержит все четыре поля

## 2. Решение сканера

- [x] 2.1 Выдавать действие карантина для негодной карточки и действие
      очистки метки для исправленной. Пропускать карточку под живым этапом
      и уже стоящую в карантине, называя причину в журнале. Проверяемо:
      `scan.test.mjs` — четыре случая, включая два пропуска

## 3. Исполнение на доске

- [x] 3.1 Завести у доски перевод карточки в карантин: переезд с записью
      состояния возврата, метка «не разобрано», комментарий с претензиями.
      Проверяемо: `backlog-trello.test.mjs` — три запроса, `returnTo`
      записан, карточка из чужой колонки возвращается в очередь
- [x] 3.2 Завести снятие метки у исправленной карточки. Проверяемо:
      метка снимается, комментарии не трогаются
- [x] 3.3 Провести оба действия через исполнение; отсутствие метода
      у файлового бэклога отвечать пропуском с причиной, а не падением.
      Проверяемо: `execute.test.mjs` — с доской действие выполняется,
      без неё пропускается

## 4. Документы

- [x] 4.1 Описать карантин в `manage/README.md`: куда уезжает негодная
      карточка, что с ней делать человеку, как вернуть. Проверяемо: раздел
      не противоречит проверке карточки и таблице переходов
