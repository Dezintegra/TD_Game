# Задачи

Каждый пункт — отдельный коммит, отправляемый сразу.

Эталоны детерминизма правятся тем же коммитом, что и правило: иначе
история баланса не видна в diff.

## 1. Штурмовик (карточка 0276)

- [x] 1.1 Ввести порядок выбора цели доводом `chooseTarget`, поисковые
      функции перевести на `Candidate`, добавить отбор стен и правило
      ближайшей цели для штурмовика в «Бою». Проверяемо:
      `targeting.test.ts` — подошедший юнит перебивает назначенную базу,
      башня в упор перебивает юнита подальше, чужая стена не отвлекает,
      назначенная стена обстреливается, перегородившая путь ломается,
      вне «Боя» действует общий порядок

## 2. Снайпер (карточка 0277)

- [ ] 2.1 Порядок снайпера в «Бою»: генерал выше прочих живых, живые выше
      любых построек, стены не в счёт, назначенная цель живыми перебивается.
      Проверяемо: `targeting.test.ts` — переключение с постройки на юнита,
      с юнита на генерала, возврат к постройкам при отсутствии живых

## 3. Тесла (карточка 0278)

- [ ] 3.1 Порядок Теслы в «Бою»: стреляющие постройки выше прочих, прочие
      выше живых, назначенная цель над всем. Проверяемо:
      `targeting.test.ts` — появление башни во время обстрела юнитов,
      выбор стены при отсутствии башен, обстрел назначенной базы при живых
      противниках рядом
