# Список задач

Порядок шагов — не оформление, а условие осмысленности замера.
Каждый шаг — отдельный коммит, собирающийся и проверяемый сам по себе.

## 1. Противник считает выгоду экономики

- [x] В `packages/sim/src/stats.ts` вынести формулу дохода в
      `incomeWithEffect`, чтобы у неё был один источник истины.
- [x] В `packages/ai/src/value.ts` научить `upgradeGain` оценивать ветку
      добычи энергии: прирост дохода за тик × горизонт планирования.
      Ядерные ветки цели «база» оставить вне сравнения.
- [x] Там же — переписать `hasComparableUpgrade`: сравнимой становится
      всякая фаза с положительным весом хоть у одной цели.
- [x] Переписать раздел «Почему экономика в сравнении не участвует»
      в шапке файла: довод оказался неверен, и это надо объяснить.
- [x] Тесты в `value.test.ts`: прибавка экономики положительна, растёт
      с горизонтом, ядерная ветка в сравнении не участвует.
- [x] Обновить эталон `profile.golden.match.test.ts` тем же коммитом.

## 2. Множитель прокачки получает потолок

- [x] В `packages/shared/src/percent.ts` объявить `MAX_UPGRADE_PPM`
      с обоснованием от предела точности целых чисел.
- [x] В `packages/sim/src/apply.ts` применить `clampPpm` к обоим
      множителям при покупке уровня.
- [x] Тест: множитель не перерастает потолка при большом числе уровней;
      эталон детерминизма при этом не трогается.

## 3. Новая кривая добычи энергии

- [ ] Перенести задуманные числа экономики из `NEUTRAL` в `rules.ts`
      в `balance.ts` постоянными `INCOME_*_BY_DESIGN`; умолчанием
      ключей настройки сделать `null` — «как задумано».
- [ ] Задать новую кривую: прирост прямой, сорок процентов; цена десять
      процентов сложным процентом.
- [ ] Обновить тесты баланса и настройки правил под новые умолчания.
- [ ] Обновить эталон `determinism.golden.match.test.ts` тем же коммитом.

## 4. Прочность базы возвращает длину матча

- [ ] Замерить длину матча при нескольких уровнях `--base-hp`
      на зеркальном `baseline-2026-08`, не меньше шестидесяти матчей
      на уровень.
- [ ] Выбрать уровень, возвращающий медиану к расчётным 720 секундам,
      и записать его в `balance.ts` вместе с обоснованием и оговоркой
      о размере замера.
- [ ] Обновить эталон детерминизма, если прочность базы в него входит.

## 5. Проверка итога

- [ ] Прогнать арену на новой сборке и сравнить с числами до правок:
      боевых улучшений в минуту, доля пустых решений, казна оплота,
      длина матча, доля побед стратега против оплота.
- [ ] Дождаться зелёного CI.
