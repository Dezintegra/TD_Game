## Context

Назначение 0070 ограничено проработкой. Свежая база `734733f`, merge origin/main ответил `Already up to date.`. Сканер чистый: строит план по снимку, затем исполнитель последовательно исполняет действия. В `scan.mjs:334–340` внешний benchmark опрашивается, но отбор сессий с `:568` его не исключает. В `execute.mjs:237–263` чтение свежее, а этап назначения приходит из ранее построенного action: одной свежести хранилища мало.

`backlog-trello.mjs:364–373` уже обновляет byId, описание и колонку исходной карточки после успешного PUT. Эту половину подтверждаем тестами без второго механизма записи. `io.mjs:549` возвращает pending без `links.run`; поэтому запрещать продолжение всех внешних benchmark навсегда нельзя.

## Goals / Non-Goals

Цель: разделить внешний опрос и выдачу сессии одной задаче по свежим оборотам, сохранить запуск без номера и доказать видимость последовательных записей. Границы — proposal; общий контроль устаревших action, конкуренция машин и восстановление исторических карточек не входят.

## Decisions

1. При создании опросов собирать множество taskId реально добавленных `poll-external` либо вывести его из уже созданных actions перед отбором сессий. Проверять его до лимитов и добавления в waitingForSession. Позднее удаление только continue-stage отвергнуто: оно оставляет fail-stage и может зря занять слот. Общий запрет любых двух действий отвергнут: заметка об осиротевшем этапе и последующий запуск допустимы.
2. В ветви внешнего benchmark назначать опрос при наличии `links.run`. Без номера оставить обычный отбор сессии. Это техническое следствие контракта readExternal, а не новый продуктовый выбор. Исключение только для успешного ответа отвергнуто: сканер ещё не знает ответа, а при pending новая сессия тоже не нужна. Опрос CI не менять.
3. После успеха или ошибки прогона штатный applyExternal сохраняет interpret или существующий путь ошибки; следующая сессия определяется новым оборотом. При pending или недоступности связи остаётся повтор опроса без расхода продолжения. Живые процессы эта правка не останавливает. Применение отчётов, удержание бюджета и диагностика задержек сохраняют собственные предохранители.
4. Свежесть Trello проверять реальным адаптером и подставным HTTP-клиентом. Два saveTask разделены readTask того же экземпляра; сверять статус, оба изменённых поля, тело второго PUT и исходные desc/idList. На отказ PUT память не должна показывать новый переход. Перечитывать всю доску после записи отвергнуто: существующее обновление уже даёт нужную видимость без сети. Это не обещание слияния изменений из произвольных ранее сохранённых копий объектов.
5. Дельта ADDED в существующей `pipeline-supervision-resilience` задаёт приоритет назначенного опроса над общими правилами продолжения и остановки. Их безусловные формулировки в `supervise-the-pipeline` и `count-only-real-launches` иначе предписывают противоположные действия одной задаче. При реализации согласовать их по таблице ниже вместе с исправлением сканера. Копировать требования в новую дельту `dev-pipeline-supervisor` отвергнуто: такой основы в основных спецификациях ещё нет, а вторая ADDED-копия разойдётся с первой. Изменение архивируется ПОСЛЕ `supervise-the-pipeline` и `count-only-real-launches`; это порядок сведения спецификаций, не ожидание реализации этих карточек. Требования о живых процессах и эксклюзивности сохраняются.

## Согласование открытых требований при реализации

На этапе design меняются только артефакты `defer-sessions-during-external-polls`. Следующие правки входят в первый коммит implement; они обязательны для согласованного результата, а не отложенная рекомендация архиватору. Каждый путь указан относительно `openspec/changes/`.

| Путь и место | Требуемая оговорка |
| --- | --- |
| `supervise-the-pipeline/specs/dev-pipeline-supervisor/spec.md`, «Срок этапа и снятие поддерева процессов» | Продолжение после timeout и остановка по исчерпании применяются при отсутствии назначенного этой задаче `poll-external` в текущем обороте. Само снятие поддерева и запись timeout остаются обязательными. В сценариях «Этап не уложился в срок» и «Продолжения исчерпаны сроком» явно разделить безусловную обработку процесса и условное продолжение/остановку. |
| Тот же файл, «Ответ сессии берётся из кода возврата и вывода» | В абзаце «Когда отчёта нет, а процесс был» ограничить выбор продолжения/failed отсутствием назначенного опроса. При опросе движение обеспечивает внешний результат; pending оставляет задачу ждать следующего опроса. Словари ответа и отчёта, бюджет, допуск отчёта, журнал и все прежние сценарии сохранить. |
| Тот же файл, «Прерванный этап возобновляется, а не начинается заново» | Способ resume относится к уже допущенному продолжению. В WHEN сценария «Этап прерван и возобновлён» добавить отсутствие назначенного `poll-external`; при назначенном опросе процесса продолжения нет. |
| `count-only-real-launches/specs/dev-pipeline-supervisor/spec.md`, «У несостоявшихся запусков свой предел и своя причина остановки» | Остановку по пределу и сценарий «Предел несостоявшихся запусков исчерпан» ограничить задачами без назначенного опроса в этом обороте. Не менять счётчики, причины, обнуление после удачного порождения и остальные сценарии. |
| Тот же файл, «Сессия не просится, пока места нет» | Сначала исключить задачи с назначенным опросом, затем решить исчерпание пределов и только после этого проверить места. В WHEN «Пределы исчерпаны при занятой машине» добавить отсутствие опроса; добавить парный сценарий с опросом: только poll-external, без continue-stage/fail-stage при занятой машине, счёт продолжений неизменен. |
| `supervise-the-pipeline/design.md`, абзац о timeout и раздел «Продолжатель возобновляет, а не начинает заново» | Указать тот же приоритет опроса перед выбором продолжения/остановки, сохранив снятие процесса по сроку и способ resume для допущенного продолжения. |
| `count-only-real-launches/design.md`, «Решение 5» | В первом пункте отбора исключить id назначенных опросов до пределов; правило остановки до проверки мест оставить для остальных задач. |

В каждой затронутой группе требований явно сослаться на приоритет `External polling defers session selection for the same task`. Исключение действует только в обороте с реально назначенным действием, а не по одному признаку внешнего benchmark. При отсутствии `links.run` сохраняется прежнее продолжение либо остановка по пределу. Удаление номера на следующем свежем снимке возвращает задачу в обычный отбор. При успехе прогона следующий оборот выбирает interpret с его текущими счётчиками; при pending, включая недоступность сервиса, продолжения не тратятся и опрос повторяется. Применение отчётов и независимые предохранители не отменяются.

Не менять чужие proposal/tasks и их отметки выполнения. Для текстов ограничиться названными оговорками, сохранить все остальные требования и сценарии; проверить diff по содержанию. `cap-task-cost` уже связывает проверку с запросом сессии и порядком исчерпанных продолжений; у исключённой задачи нет такого запроса, поэтому отдельной правки этой дельты не требуется.

## Validation design

В scan.test проверить наличие опроса одновременно с отсутствием continue-stage и fail-stage по тому же id, включая достигнутый лимит. Пограничная пара: внешний benchmark с links.run, оборванной по timeout сессией и maxContinuations при занятой машине получает только опрос; та же задача без links.run получает fail-stage даже при занятой машине. При неисчерпанном лимите вариант без номера и со свободным местом получает продолжение. Повторить исключение для maxSpawnFailures и применимого потолка стоимости (без decompose-again); значения лимитов и расхода не изменять. Добавить положительный контроль другой задачи со свободным слотом, локального замера и опроса CI. Исправить прежнее ожидание продолжения внешнего benchmark, если его фикстура уже содержит номер, сохранив отдельный случай без номера.

В execute.test выполнить настоящий результат scan через исполнитель с подставным IO: успех внешнего прогона, pending и недоступность; проверить статус, отсутствие spawnStage и отсутствие добавленного продолжения/журнала выдачи. Повторить при уже исчерпанных продолжениях, включая занятую машину: успех переводит в interpret, pending оставляет benchmark, fail-stage поверх опроса не исполняется. Для успеха снять свежий снимок, просканировать снова и проверить назначение interpret после освобождения места. Изолированные заглушки, сами фильтрующие action, не доказывают исправление.

В backlog-trello.test защитить второй инвариант адаптером, не подставной Map вместо него. Сеть и живые сессии не нужны. Узкие проверки назначены этапу implement; design запускает только OpenSpec CLI.

## Risks / Trade-offs

- Новый этап получает сессию не раньше следующего оборота → это цена свежего выбора; опрос не резервирует вычислительный слот.
- Отсутствие номера могло бы запереть запуск → отдельная ветвь допуска с положительным регрессионным контролем.
- Поздний фильтр оставил бы решение об исчерпании → исключение до лимитов и проверка maxContinuations.
- Соседние правки scan/execute → перед реализацией освежить базу и сохранить их предохранители; не переносить чужие требования в нашу дельту.

## Migration Plan

Миграции данных нет. Доставить правку и тесты обычным PR; новый код начнёт действовать при штатном обновлении супервизора. Откат — откат коммита реализации, без изменения карточек. Исполнитель этой задачи не перезапускает работающий супервизор.

Симуляция, рассинхрон и реконнект игры не затронуты. Эталоны детерминизма и PROTOCOL_VERSION не меняются. Новых зависимостей от Math.random, Date.now или вещественного состояния мира не появляется: пакеты игры не меняются. Арена и замер кадров не нужны.

## Open Questions

Нет: решения выводятся из постановки, журнала и действующего кода.
