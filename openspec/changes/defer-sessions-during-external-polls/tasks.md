## 1. Исправление допуска сессий

- [x] 1.1 (до 2 часов, один коммит) В `supervisor/lib/scan.mjs` назначать опрос внешнего benchmark только с `links.run`, исключить id назначенных опросов до лимитов и waitingForSession. В `supervisor/lib/scan.test.mjs` добавить проверки опроса без continue-stage/fail-stage, достигнутого maxContinuations, положительные контроли другой задачи, внешнего benchmark без номера, локального замера и CI. Сохранить существующие ограничения и исправить только противоречащие новому контракту ожидания. Проверка: `npx vitest run --root supervisor lib/scan.test.mjs`, `npx eslint supervisor/lib/scan.mjs supervisor/lib/scan.test.mjs`, `npx prettier --check supervisor/lib/scan.mjs supervisor/lib/scan.test.mjs`. После отметки пункта, коммита и немедленной отправки открыть черновой PR по правилам implement: функциональная правка уже проверяема самостоятельно.

  В этот же пункт входит согласование пяти требований и связанных сценариев в `openspec/changes/supervise-the-pipeline/specs/dev-pipeline-supervisor/spec.md` и `openspec/changes/count-only-real-launches/specs/dev-pipeline-supervisor/spec.md`, а также двух соответствующих `design.md` по таблице собственного design. Сохранить прочие тексты и все сценарии. Проверить парой: timeout + links.run + maxContinuations + занятая машина дают только опрос; без links.run исчерпание даёт fail-stage даже при занятой машине. Отдельно проверить maxSpawnFailures и применимый потолок стоимости без fail-stage/decompose-again рядом с опросом. В diff явно сверить оговорки в каждом названном требовании и сценарии: вне опроса прежнее поведение сохранено. Выполнить по имени `openspec validate supervise-the-pipeline --strict`, `openspec validate count-only-real-launches --strict` и `openspec validate defer-sessions-during-external-polls --strict`, затем `openspec status --change defer-sessions-during-external-polls`. Это проверки затронутых пунктом документов; проверку всех изменений разом не запускать. При изменении расположения исходных требований после освежения базы согласовать план по правилам этапа, не создавать их вторую копию.

## 2. Сквозные регрессионные проверки

- [x] 2.1 (до 2 часов, один коммит) В `supervisor/lib/execute.test.mjs` исполнить план настоящего scan для успешного внешнего прогона с мёртвой сессией: результат interpret, нет spawnStage и прибавки продолжения, нет журнала выдачи benchmark; свежий следующий оборот допускает interpret. Добавить pending и недоступность внешнего сервиса без запуска/расхода, а также штатный неуспешный результат прогона без старой сессии. В `supervisor/lib/backlog-trello.test.mjs` проверить две записи через один настоящий адаптер с промежуточным readTask: сохраняются interpret, поля обеих записей, desc/idList снимка и правильная колонка второго PUT; отдельный отказ PUT не публикует переход в памяти. Проверка: `npx vitest run --root supervisor lib/scan.test.mjs lib/execute.test.mjs lib/backlog-trello.test.mjs`, `npx eslint supervisor/lib/execute.test.mjs supervisor/lib/backlog-trello.test.mjs`, `npx prettier --check supervisor/lib/execute.test.mjs supervisor/lib/backlog-trello.test.mjs`. Тесты используют подставной IO/HTTP без сети, процессов и доски. Существующее обновление снимка повторно не реализовывать.

  Успех и pending дополнительно исполнить для benchmark с links.run, timeout и исчерпанными продолжениями при занятой машине: нет остановки по пределу поверх опроса и нет расхода продолжения. После успеха проверить сохранение attempts и снять свежий снимок без ручного сброса счёта: следующий оборот назначает fail-stage со stage interpret и прежней причиной исчерпания, без continue-stage, как при занятой машине, так и после освобождения места. Допуск interpret из первого абзаца проверять отдельной фикстурой с неисчерпанными продолжениями, остальными выполненными условиями допуска и свободным местом: после успеха следующий свежий оборот назначает continue-stage со stage interpret. После pending следующий оборот снова назначает опрос без продолжения. Не менять политику счётчиков ради прохождения проверок.

## 3. Правила состава и проверки каждого коммита

Этот раздел — условия двух пунктов выше, не дополнительный шаг. Команды исполняются по одной из назначенного дерева. `.` в командах Git означает это дерево; при другом рабочем каталоге заменить его абсолютным путём назначения. Формы узких проверок сверены с `supervisor/config/stage-settings.json`; полный verify и ожидание CI не входят в задачи исполнителя.

Пункт 1.1 добавляет только `supervisor/lib/scan.mjs`, `supervisor/lib/scan.test.mjs`, `openspec/changes/supervise-the-pipeline/specs/dev-pipeline-supervisor/spec.md`, `openspec/changes/supervise-the-pipeline/design.md`, `openspec/changes/count-only-real-launches/specs/dev-pipeline-supervisor/spec.md`, `openspec/changes/count-only-real-launches/design.md` и отметку собственного пункта в `openspec/changes/defer-sessions-during-external-polls/tasks.md`. Чужие proposal/tasks и отметки выполнения не меняются; в четырёх связанных документах допустимы только оговорки из таблицы design. Пункт 2.1 добавляет только `supervisor/lib/execute.test.mjs`, `supervisor/lib/backlog-trello.test.mjs` и отметку собственного пункта в том же tasks.md. Каждый путь передавать `git -C . add --` явным перечнем; несвязанные изменения внутри этих файлов недопустимы.

Перед коммитом проверить имена `git -C . diff --cached --name-only` и содержание `git -C . diff --cached`. После коммита немедленно выполнить `git -C . push`, затем сверить имена и содержание созданного коммита `git -C . show --stat --oneline HEAD` и `git -C . show --format=fuller HEAD`. Проверить отсутствие хвоста `git -C . log --oneline '@{u}..HEAD'`. Отметка выполненного пункта обязательна и допустима вместе с его рабочими правками; остальные файлы и несвязанные изменения не допускаются.
