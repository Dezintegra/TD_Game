# general-control Specification

## Purpose

Генерал — аватар игрока и единственная единица под прямым управлением.
Спецификация описывает его движение, право строить, гибель и возвращение
в игру.

## Requirements

### Requirement: Генерал существует у каждого игрока

Каждый игрок SHALL иметь ровно одного генерала. Генерал SHALL появляться
на своей базе в начале матча.

#### Scenario: Оба генерала на своих базах в начале матча

- **WHEN** мир создан
- **THEN** у каждого игрока есть генерал, и он находится не далее радиуса расчистки от своей базы

### Requirement: Движение задаётся направлением, а не координатой

Команда движения SHALL передавать направление из девяти возможных
(восемь сторон и остановка), а не целевую точку. Генерал SHALL двигаться
в этом направлении каждый тик, пока направление не сменится.

Диагональное движение SHALL иметь ту же итоговую скорость, что и прямое.

#### Scenario: Генерал движется, пока направление не сброшено

- **WHEN** отдана команда движения на восток и просчитано десять тиков
- **THEN** генерал сместился на восток на десятикратную скорость за тик

#### Scenario: Диагональ не быстрее прямой

- **WHEN** генерал прошёл сто тиков по диагонали и сто тиков по прямой
- **THEN** пройденные расстояния отличаются не более чем на одну сотую клетки

### Requirement: Генерал не проходит сквозь препятствия

Генерал MUST NOT входить в клетку со скалой, стеной, башней или базой.
При упоре в препятствие по диагонали генерал SHALL продолжить движение
вдоль той оси, которая не заблокирована.

#### Scenario: Скала останавливает генерала

- **WHEN** генерал движется в клетку со скалой
- **THEN** его позиция вдоль заблокированной оси не меняется

#### Scenario: Скольжение вдоль стены

- **WHEN** генерал движется по диагонали и заблокирована только одна из двух осей
- **THEN** он смещается вдоль свободной оси

### Requirement: Строительство только в радиусе вокруг генерала

Постройка SHALL размещаться только в клетке, удалённой от генерала
не более чем на радиус строительства. Радиус SHALL повышаться прокачкой.
При мёртвом генерале строительство MUST NOT происходить.

#### Scenario: Клетка вне радиуса

- **WHEN** приходит команда постройки в клетку дальше радиуса строительства
- **THEN** постройка не появляется и энергия не списывается

#### Scenario: Мёртвый генерал не строит

- **WHEN** генерал мёртв и приходит команда постройки
- **THEN** постройка не появляется и энергия не списывается

### Requirement: Гибель и возрождение

Генерал SHALL получать урон от вражеских юнитов, башен и ядерного удара.
При падении здоровья до нуля генерал SHALL исчезать с карты и SHALL
возрождаться на своей базе с полным здоровьем через время возрождения.
Время возрождения SHALL повышаться прокачкой. Базовое значение —
десять секунд.

#### Scenario: Смерть убирает генерала с поля

- **WHEN** здоровье генерала падает до нуля
- **THEN** генерал перестаёт быть целью, не двигается и не строит

#### Scenario: Возрождение на базе

- **WHEN** после гибели генерала прошло время возрождения
- **THEN** генерал снова на карте, рядом со своей базой, с полным здоровьем

#### Scenario: Прокачка сокращает время возрождения

- **WHEN** куплен уровень ветки времени возрождения
- **THEN** следующее возрождение занимает меньше тиков, чем предыдущее

### Requirement: Награда за убийства генералом

Когда добивающий удар нанёс генерал, его владелец SHALL получать энергию
в размере базовой стоимости убитого: для юнита или постройки — её базовой
цены, для вражеского генерала — двадцатикратной базовой стоимости юнита.

#### Scenario: Убийство юнита приносит энергию

- **WHEN** генерал добивает вражеского юнита
- **THEN** энергия владельца выросла на базовую стоимость этого типа юнита

#### Scenario: Убийство чужого генерала приносит двадцатикратную награду

- **WHEN** генерал добивает вражеского генерала
- **THEN** энергия владельца выросла на двадцать базовых стоимостей юнита

#### Scenario: Убийство юнитом награды не даёт

- **WHEN** вражеского юнита добивает не генерал, а юнит или башня
- **THEN** энергия владельца не меняется

