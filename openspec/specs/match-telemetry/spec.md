# match-telemetry Specification

## Purpose
Разбор матчей: запись партии, безголовые прогоны и запросы к тому,
что в них происходило.

Инструмент существует потому, что поведение противника до сих пор
оценивалось глазами, а измерить его было нечем. Запись содержит входы,
а не состояние: мир восстанавливается прогоном из seed и команд, и это
же свойство ловит расхождение — воспроизведение сверяет контрольные
суммы и останавливается, если код разошёлся с записью.

Подробный лог хранит и то, чего не произошло: несостоявшиеся покупки
с причинами и оценки всех рассмотренных рубежей, а не только выбранного.
В мире эти случаи следа не оставляют, и без них видно, что противник
сделал, но не видно, чего он хотел.

Сбор сведений ход матча менять MUST NOT — иначе измерялся бы уже другой
матч.
## Requirements
### Requirement: Сбор сведений не влияет на ход матча

Сбор сведений SHALL быть строго наблюдающим: он читает состояние мира
и решения противника, но не участвует в них.

Инструментация MUST NOT обращаться к генератору случайных чисел
противника, изменять порядок обхода чего-либо и вынуждать противника
считать что-либо дополнительно.

Матч, прогнанный со сбором сведений, и тот же матч без сбора SHALL
давать одинаковую контрольную сумму итогового состояния.

#### Scenario: Лог не сдвигает случайность

- **WHEN** один и тот же матч прогнан с включённым и с выключенным
  сбором сведений
- **THEN** контрольные суммы итоговых состояний совпадают, и совпадает
  число отданных противником команд

#### Scenario: Сбор сведений не появляется в собранном клиенте

- **WHEN** выполняется промышленная сборка клиента
- **THEN** код записи матча в неё не входит, а обработчик приёма записи
  отсутствует в сервере сборки

### Requirement: Запись матча содержит входы, а не состояние

Запись матча SHALL содержать только то, из чего матч воспроизводится:
seed мира, seed и идентификатор профиля каждого противника, версию кода
и команды игрока с номерами тиков.

Состояние мира в запись входить MUST NOT: оно полностью выводится
из перечисленного, а его хранение сделало бы запись на порядки больше
без единого дополнительного сведения.

Запись SHALL содержать контрольные суммы состояния мира, снимаемые
с постоянным шагом, — чтобы воспроизведение можно было проверить.

#### Scenario: Запись мала

- **WHEN** записан матч длиной двенадцать минут
- **THEN** размер записи не превышает нескольких сотен килобайт

#### Scenario: Запись достаточна для воспроизведения

- **WHEN** записанный матч прогоняется заново из записи
- **THEN** контрольные суммы на каждом шаге сверки совпадают
  с записанными

### Requirement: Расхождение воспроизведения обнаруживается и объявляется

Воспроизведение записи SHALL сверять контрольные суммы с записанными.

При расхождении воспроизведение SHALL прекращаться с указанием тика,
на котором суммы разошлись. Продолжать разбор разошедшегося матча
инструмент MUST NOT: он анализировал бы матч, которого не было.

#### Scenario: Изменённый код ловится сверкой

- **WHEN** записанный матч воспроизводится после изменения правил игры
  или устройства противника
- **THEN** сверка обнаруживает расхождение и называет тик, а подробный
  лог не создаётся

### Requirement: Подробный лог содержит и то, чего не произошло

Подробный лог SHALL содержать, помимо совершённых действий:

- все оценённые рубежи каждого решения, а не только выбранный,
  с их выгодой, риском, итоговой оценкой и вероятностью гибели;
- исход каждой попытки потратить энергию, включая «не по карману,
  но коплю» и «покупать нечего»;
- причину, по которой место под постройку не было найдено;
- срыв накопления по исчерпании терпения;
- случай, когда положительной выгоды нет ни у одного рубежа;
- исход каждой отданной команды: применена или отклонена и почему.

Обоснование: состояние мира показывает, что случилось, и не показывает,
чего противник хотел. Все перечисленные события следа в мире
не оставляют и снаружи неотличимы друг от друга.

#### Scenario: Отсутствие места под башню отличимо от нехватки денег

- **WHEN** противник не построил башню на решении
- **THEN** в логе есть запись, различающая «не хватило энергии»,
  «копил на дорогое», «места под башню не нашлось» и «строить было
  не в очереди трат»

#### Scenario: Отклонённая команда видна как отклонённая

- **WHEN** противник отдал команду, которую ядро отклонило
- **THEN** в логе команда помечена отклонённой с указанием причины,
  а не выглядит выполненной

### Requirement: Матчи компьютера против компьютера гоняются пачками

Инструмент SHALL прогонять матчи без браузера и без рендера, задавая
набор seed и профиль каждой стороны.

Матчи SHALL исполняться параллельно: они независимы, и ограничением
служит только число ядер.

Матч SHALL иметь предел длительности. По его достижении матч SHALL
завершаться исходом «ничья по времени» — это полноценный результат,
а не ошибка прогона.

#### Scenario: Пачка матчей завершается без участия человека

- **WHEN** запущен прогон двадцати матчей с заданным набором seed
- **THEN** все двадцать завершаются победой одной из сторон либо
  ничьёй по времени, и по каждому создан лог

#### Scenario: Затянувшийся матч не висит вечно

- **WHEN** ни одна сторона не разрушила базу соперника за предельное
  число тиков
- **THEN** матч завершается, записывается с исходом «ничья по времени»,
  и прогон продолжается со следующего seed

#### Scenario: Два профиля сталкиваются напрямую

- **WHEN** прогон запущен с разными профилями у сторон
- **THEN** в записи каждого матча указано, какой профиль играл за какую
  сторону, и по пачке можно посчитать долю побед каждого

### Requirement: Логи разбираются запросами, а не чтением подряд

Логи SHALL складываться в базу SQLite, пригодную для произвольных
запросов.

Схема SHALL хранить факты, а не выводы: величины решений, а не суждения
о них. Признаки вида «это дрожь» или «это топтание» SHALL вычисляться
запросом при разборе, а не проставляться в момент записи.

Запись каждого матча SHALL содержать версию кода, которой он сыгран,
и признак незакоммиченных изменений в рабочем дереве. Без этого
сравнение матчей, сыгранных разными версиями, невозможно.

Сборка базы из логов SHALL быть повторяемой: при изменении схемы база
пересобирается из тех же логов без повторного прогона матчей.

#### Scenario: Матчи разных версий различимы

- **WHEN** в базе лежат матчи, сыгранные до и после правки противника
- **THEN** их можно разделить запросом по версии кода

#### Scenario: Схема меняется без переигрывания

- **WHEN** в схему добавлен новый столбец
- **THEN** база пересобирается из имеющихся логов, и прогонять матчи
  заново не требуется

#### Scenario: Битая последняя строка не рушит сборку

- **WHEN** прогон был прерван и последняя строка лога записана
  наполовину
- **THEN** сборка базы пропускает её с предупреждением и обрабатывает
  остальные

### Requirement: Приём записи из браузера ограничен машиной разработчика

Обработчик приёма записи SHALL существовать только в сервере разработки
и MUST NOT попадать в собранное приложение.

Обработчик SHALL принимать обращения только с локального адреса,
SHALL составлять имя файла сам из закрытого набора правил и MUST NOT
брать путь к файлу из тела запроса. Размер тела SHALL быть ограничен.

Записи SHALL складываться в каталог `.matchlog/` в корне репозитория,
исключённый из системы контроля версий.

Никакая часть собранных сведений MUST NOT отправляться за пределы
машины разработчика.

#### Scenario: Путь из запроса не используется

- **WHEN** в теле запроса указано имя файла с переходом по каталогам
- **THEN** обработчик его игнорирует и пишет в файл с именем, которое
  составил сам

#### Scenario: Логи не попадают в репозиторий

- **WHEN** после прогона матчей проверяется состояние рабочего дерева
- **THEN** каталог `.matchlog/` в изменениях не значится

### Requirement: Сводка по матчу печатается одной страницей

Инструмент SHALL печатать сводку по матчу или по пачке матчей в объёме,
пригодном для чтения человеком целиком.

Сводка SHALL содержать по меньшей мере: исход и длительность, путь
генерала по долям вероятного пути, ведомость трат по фазам, распределение
причин, по которым покупка не состоялась, и отрыв выбранного рубежа
от следующего по оценке.

Суждений о том, хорошо это или плохо, сводка содержать MUST NOT: она
приводит величины, а выводы делает человек.

#### Scenario: Сводка читается целиком

- **WHEN** напечатана сводка по матчу длиной двенадцать минут
- **THEN** её объём измеряется десятками строк, а не тысячами

