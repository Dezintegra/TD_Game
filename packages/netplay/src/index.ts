/**
 * Ведение сетевого матча с обеих сторон.
 *
 * Пакет знает про время матча и про то, как из потока команд получается
 * мир, и не знает ничего про способ передачи байтов: сообщения уходят
 * через переданную снаружи функцию, время приходит тем же путём.
 * Благодаря этому весь матч целиком прогоняется в тесте без сокетов
 * и без единой настоящей секунды ожидания, а сервер, браузер
 * и компьютерный участник пользуются одним и тем же кодом.
 */
export * from './replay.js';
export * from './host.js';
export * from './guest.js';
