import { defineConfig } from 'vitest/config';

/**
 * Матчевый набор ядра — эталон детерминизма.
 *
 * Прогоняет 1200 тиков (40 игровых секунд) с фиксированным seed
 * и сверяет контрольную сумму с эталоном. Как и быстрый набор,
 * идёт в двух окружениях: второе задаётся флагом
 * `--environment jsdom` в команде пакета. Расхождение сумм между
 * окружениями означает, что в ядро просочилась платформенная
 * зависимость, — ровно то, что мы хотим поймать до продакшна.
 */
export default defineConfig({
  test: {
    name: 'sim:match',
    environment: 'node',
    include: ['src/**/*.match.test.ts'],
  },
});
